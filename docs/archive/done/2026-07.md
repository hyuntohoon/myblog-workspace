# Completed Work — 2026-07

Archived from `plan.md`. Newest first within each section. Format intentionally loose; this is history, not a working spec.

---

## Docs / research

### Lyrics-viewer dependency RFCs (×3): component map + playback research + normalization model — ✅ 2026-07-02 (docs-only, owner-approved close)

Three dependency RFCs carved out of FEAT-lyrics-viewer's original scope (rescope #487), drafted #488, OQ-resolved + accepted #489, Step 1 executed + closed the same day. All docs-only — no code, schema, contract, or infra. Together they unblock FEAT-lyrics-viewer (draft → ready for owner accept).

- **ARCH-frontend-component-map** — published `docs/frontend/component-map.md` (LLM/developer reference) + `docs/frontend/structure.md` (human map), both code-pinned "Verified 2026-07-02"; the RFC's reference section became a pointer (single canonical living copy). Key verdict: **no shared track-row component exists** (≥6 fragmented paths; only play funnels through `requestPlayback`). 5-claim spot re-verify fixed two anchor drifts (`requestPlayback` `:243`→`:242`; slide-over stacks at `--z-panel: 90`, not `--z-overlay`). Includes the Component-map impact template for future RFCs. RFC → `docs/archive/done/rfcs/ARCH-frontend-component-map.md` (Status: **done**).
- **RESEARCH-playback-product-architecture** — decision research: **no custom playback RFC needed**. Code resolved the read path (client-side `GET /v1/me/player`, same sanctioned pattern as `requestPlayback`'s existing `api.spotify.com` call; rule #9 not engaged; `track_id` via live `item.id` → server-side resolve to `Track.id`). **Step 1 live probe PASSED 2026-07-02**: smoke JWT → `/api/playback/spotify-token` 200 → `GET /v1/me/player` 200 `{is_playing: true, progress_ms: 23200, item.id, duration_ms: 182088}` on a live owner Premium session (iPhone); idle = 204 empty (= the viewer's hidden-when-idle state). FEAT-lyrics-viewer Step 3 binds the one-shot position read, track-only on 204/failure. RFC → `docs/archive/done/rfcs/RESEARCH-playback-product-architecture.md` (Status: **done**).
- **ARCH-lyrics-normalization-model** — pinned the normalized payload spec the viewer implements against: `availability`/`source_kind`/`trackable`/`normalizer_version`/`segments[]{i, text, start_ms}` (`text==""` = stanza gap), `normalize_lyrics`/`parse_lrc` pseudocode (synced-only & mixed share one path — segments derive from synced alone; plain-only → `start_ms: null`; plain disagreement = diagnostics only, synced wins; text never silently dropped), + acceptance gate. Read-time derivation, raw sources stay source-of-truth. Prod probes: mixed 91% / plain-only 9% / synced-only 0 (defensive branch); blank stanza markers in 93% of synced rows. RFC → `docs/archive/done/rfcs/ARCH-lyrics-normalization-model.md` (Status: **done**).

PRs: workspace #486 (viewer RFC draft) / #487 (rescope) / #488 (three drafts) / #489 (OQ + accept) / this (Step 1 ×3 + close).

## Feature expansion

### FEAT-mobile-web-app: mobile-first read path + PWA — ✅ 2026-07-05 (all 4 steps prod-smoked; front-only, no infra change)

The public read path (home, `/reviews`, `/review/[slug]`, `/genres`, `/canon`, `/collection`, `/artist/[id]`, `/search`) is now comfortable on a 390px phone and installable to the home screen as a PWA. Pre-RFC state (verified by code audit + live-prod CDP + mobile Lighthouse): header chrome ate ~60% of the first viewport, 10 ad-hoc breakpoint widths + 30+ nowrap clip sites, full MPA reloads, zero PWA surface. Scope per RFC: **PWA is the end state** (no Capacitor/native packaging); logged-in workspace touch redesign deferred to a follow-on RFC (`FEAT-mobile-workspace-touch`, not drafted — OQ4 revisits with real mobile usage).

- **Step 1** (front #234, 2026-07-04) — nav pattern decided at the in-step mockup gate (ws #536): **(B) compact header + hamburger drawer** — bottom edge stays Pocket-tray-only, maximizing freedom for the workspace follow-on (A bottom-tabs / C combo rejected over tray dock conflict + 5-tab density at 390px). Compact 52px bar + drawer + full-width search overlay <768px; no `⌘K` badge on `pointer: coarse`; desktop ≥1024px visually unchanged; logged-in tray coexistence click-checked.
- **Step 2** (front #235) — breakpoint consolidation onto the canonical max-width set **480 · 640 · 767 (below md) · 880 (`--bp-tab`, extended per the RFC's "extend if genuinely needed" clause — the 820/860/880 tablet-landscape cluster was a genuine collapse point) · 1024 · 1280**, documented at the `--bp-*` block in `global.css`; read-path nowrap clip sweep (`.gm-a-toolhint` wrap fixed the `/genres` "CONTAI…" clip); `/genres` mobile force-layout-A via `matchMedia(767)` (stored B/C pref overridden, `.gm-controlbar` hidden). Container queries (search 720 / artist 760) excluded — they size against the container, not the viewport.
- **Step 3** (front #236) — `<ClientRouter />` view transitions; `transition:persist` on all four header roots + the PocketBuckit island. Swap side-effects handled client-side: drawer close + scroll-lock release (after-swap), nav active re-sync (page-load), theme/accent re-apply (after-swap — the swap adopts incoming `<html>` attributes), page module scripts moved to `astro:page-load` inits (review template, albumDetail ×2, drafts).
- **Step 4** (front #237) — `@vite-pwa/astro` (`registerType: 'autoUpdate'`, Workbox `generateSW`): precache = **shell only** (`/`, `/reviews/`, hashed assets — NOT `**/*.html`, which grows unbounded as reviews accumulate); runtime = same-origin `/review/*` NetworkFirst (20 entries / 14d) + `i.scdn.co` covers SWR (200 / 30d); **`/api/*` never cached**. `manifest.webmanifest` dark `#141312` + 192/512/maskable icons + `theme-color` meta; iOS Safari install hint (Safari-on-iOS + not-standalone only, dismiss-persistent). **No infra change needed** (RFC had allowed one cache-behavior touch): the front deploy workflow already uploads all non-`_astro` objects — incl. `sw.js` — with `Cache-Control: no-cache`, and the console CloudFront `handler` passes `.`-URIs straight through (live function body verified via `aws cloudfront get-function`). Rollback path = kill-switch SW deploy (never plain removal). Deps: `vite-plugin-pwa` pinned direct for `virtual:` types (pnpm strict) + `workbox-window`.

Per-step prod smoke quoted as a comment on each front PR. Verification notes (reusable): prod has zero published review/artist pages → `[slug]`/artist verification used a temp mdx fixture in `content/blog/` + a local CORS proxy on :8000 → prod CloudFront; Lighthouse ≥12 has **no PWA/installability category** — installability evidenced via manifest+SW+icons criteria. Open observation: "new deploy picked up within one SW update cycle" self-confirms on the next routine deploy.

PRs: front #234/#235/#236/#237, workspace #535 (RFC accept) / #536 (Step 1a gate) / #539 (steps 2–4 decisions log) + this (close-out). RFC → `docs/archive/done/rfcs/FEAT-mobile-web-app.md` (Status: **done**).

### FEAT-lyrics-engine-sonnet: translation poller engine Amazon Translate → `claude -p` Sonnet — ✅ 2026-07-05 (both steps prod-verified, single session)

Supersedes the 2026-07-04 engine decision in FEAT-lyrics-translation (decisions log row 3 — "Engine swapped Claude → Amazon Translate"): the lyrics-translation poller (`scripts/lyrics_translate_poller.py`) now translates with **one headless `claude -p --model sonnet` subprocess per track** (prompt on stdin, ~300s timeout) instead of context-free per-line Amazon Translate — consistent register across a track, idioms as idioms, elided subjects restored. Trigger: the 2026-07-04 LUX benchmark ran the same headless path with a different prompt shape (의역 instruction + numbered lines + "JSON array only" contract) **15/15 PASS** (5 tracks × opus/sonnet/haiku, 194 non-gap lines, 0 refusals/parse-fails/count-mismatches incl. DE/EN "Berghain" + 75-line "Porcelana") — proving the 07-04 copyright refusal keyed on **prompt shape, not headless-ness**. Sonnet chosen on owner-reviewed quality (Opus converges, Haiku clips nuance) + timing (147s/5 tracks). Workspace `scripts/`+`tools/` only — no contract/schema/infra change; queue schema, claim pattern, fingerprint parity, privacy bar, viewer all untouched.

- **The swap**: benchmark-identical prompt frozen as `PROMPT_HEADER`; validation ported from the bench scorer (fence-strip → JSON array, `{i, ko}` items, `len == N`, `i`-set == `{1..N}`; refusal prose fails extraction → same failure path). **No Amazon fallback** (owner decision): validation failure retries once → `mark_failed` with `engine_validation: <detail>`; transient CLI failure (usage/session-limit, timeout) leaves the claim in place for the 20-min stale-claim auto-retry — "Sonnet said no" (terminal, fail-visible) vs "budget momentarily exhausted" (self-healing). boto3/`translate_lines`/per-line passthrough deleted (per-line Korean passthrough is now the prompt's job); `model='claude.sonnet'`, `TRANSLATOR_VERSION` v1→**v2**.
- **Step 1** poller swap (ws #531) — offline engine-path harness 5/5 (fake CLI: ok/out-of-order/fence/empty-ko, count-mismatch retry→terminal, refusal prose, exit-1 transient), forced-failure check (bogus model alias → claim kept, row untouched), 2-pilot re-request `--drain` (Mundo Nuevo 8 lines 15.0s, Porcelana 75 lines 37.4s → `done|claude.sonnet|v2`, segment/gap/fingerprint parity vs pre-state, authed prod GET attaches all `text_ko`).
- **Step 2** in-place backfill (ws #532) — `tools/lyrics_retranslate_backfill.py` imports the Step-1 poller functions verbatim (no logic fork; per-track commit; UPDATE guarded by `status='done' AND model='amazon.translate'`); pre-state dump `tools/out/retranslate_prestate_20260705-013142.json` (rollback = single UPDATE per row). **11/11 PASS** (denominator = all rows still `amazon.translate` at run start; ~25–65s/track, 0 refusals) → prod **13/13 `done|claude.sonnet|v2`, 0 amazon rows**; authed prod GET spot check (Berghain 43/43 `text_ko`); **owner 3-track viewer spot-audit PASS** (2026-07-05, close-out gate).

PRs: workspace #529 (RFC draft + accept) / #531 / #532 + this close-out. RFC → `docs/archive/done/rfcs/FEAT-lyrics-engine-sonnet.md` (Status: **done**).

### FIX-lyrics-matcher-noise: matcher false-ambiguity source fixes — ✅ 2026-07-04 (all 3 steps prod-verified, single session)

The best-of OQ5 follow-on (owner decision 2026-07-03: "matcher source fixes = separate follow-on RFC"): the conservative lyrics matcher stops manufacturing **false ambiguity** out of LRCLIB crowd-title noise, resolving rows as genuine `exact-title` matches **at the source** instead of parking them for the best-of layer to rescue. All owner OQs adopted as recommended (full comparison pipeline / symmetric both-side strip / ≥90% changed-row precision gate / `step3-v1`); drafted, accepted, executed, merged and prod-verified in one session (explicit owner pre-approval).

- **The fixes** (worker `worker/service/lyrics_matcher.py` + `lyrics_promote.py` via shared helpers): `_strip_noise` — leading track-number prefixes (`01 - Title`), non-rendition parenthetical/bracket segments (`(feat. X)`, `(From "Movie")`) **and trailing non-rendition dash segments** (`- From "Movie"`, `- 2013 Mix` — the Spotify spelling of the same retitle noise), applied symmetrically to catalog + candidate titles; rendition segments (`(Live)`, `- Remastered 2011`) are kept so version gating is unchanged. `exact_base_equal` — noise-stripped **OR** plain (step2-v2) canonical equality, making the new exact set a **strict superset** of the old (no pre-fix match can be lost to an over-eager strip — `Doo Wop Freestyle ('99)`'s parenthetical year IS the title), while the **fuzzy gate stays on plain canonicals** (the strip must never pull a different longer title — `Booty Out the Window` — into fuzzy range). **Group key = base title only** (the artist component split identity-passing alias/spelling variants — the BTS `방탄소년단` class — into false ambiguity). **Version-agreeing representative** (was richness-only `_richest`): agreement → exact-kind → full-canonical pick-stability → richness. `MATCHER_VERSION` `step2-v2` → `step3-v1`; matched evidence records `title_noise_stripped`.
- **Step 1** (worker branch, NOT merged — the matcher is live-wired, so merge = rollout) — 17 new unit tests (308 passed / 34 skipped) + the read-only **old-vs-new replay probe** (`tools/lyrics_matcher_noise_probe.py`: old step2-v2 loaded from `git show origin/main`, both matchers replayed over the SAME re-fetched candidates — code effect isolated from LRCLIB drift; pools: 44 unresolved / 381 best-of / 300 exact-title). **The probe earned its keep**: rounds 1–2 caught three regression classes (asymmetric dash-suffix, artist-key splits, meaningful parentheticals / fuzzy widening) that were fixed at source before any merge; round 3 clean — **exact regression 0 lost, 300/300 (100%) same pick**; old-ambiguous → matched at source 272/397 (68.5%); labelled precision audit **68/68 = 100%** (stratified over the 275 changed rows: prefix/paren/dash 45 + rep-only 23, KR covered) — OQ3 ≥90% gate PASS.
- **Step 2** (worker #64 `c53c48a`, deploy 28692610239, 2026-07-04) — squash merge + prod smoke via manual `lyrics_reassessment` invoke (DB-state evidence): 148 rows touched, matched 10,628→10,632 (+4 = 3 ex-ambiguous + 1 ex-not_found, deltas exact), all new rows `exact-title`/`step3-v1`/non-empty bodies; pre-existing exact-title 8,001 untouched, best-of 2,619/8 unchanged (quoted on worker #64).
- **Step 3** (local one-shot, 2026-07-04) — pool-scoped re-eval of the 41 residual rows reusing `tools/lyrics_bestof_backfill.py` (live path verbatim, pre-run snapshot, prior best-of artifacts preserved): 41/41 evaluated, **matched +20** (source exact-title +16, best-of-ambiguous +4), `consistency_errors` 0. **Close-out: ambiguous residual 18 → 0; review_required 26 → 21** (the 21 have no tier-1/version-agreeing candidate even post-fix — honest conservative residual); corpus `matched` 10,628 → 10,652, `exact-title` 8,001 → 8,021. Week-1 inflow ambiguous-rate vs the old 40:1 baseline left to normal reassessment metrics. Known residual gap (accepted): `ver.` abbreviations (`(KR ver.)`) are not version tokens — duration gate covered all audited cases.

PRs: worker #64, workspace #515 (RFC + probe tool + close-out) + this (archive). RFC → `docs/archive/done/rfcs/FIX-lyrics-matcher-noise.md` (Status: **done**).

### FEAT-lyrics-best-of-promotion: auto-promote the best plausible candidate out of ambiguous/review_required — ✅ 2026-07-03 (all steps prod-verified, closed 2026-07-04)

Successor to **FEAT-lyrics-corpus**: a pure promotion layer that lifts the single best plausible candidate out of the conservative matcher's parked `ambiguous`/`review_required` pool as a distinctly-tagged `matched` row (`match_basis = best-of-ambiguous` / `best-of-review`) — coverage growth with **no manual UI, no `exact-title` change, no schema change, no new public exposure** (track_lyrics stays owner-only, V33 boundary). **v1 = tier-1-only** (exact stripped-base-title + version-token agreement, body-carrying candidates only; gated tiers 2–4 stayed closed on probe evidence — OQ2). The conservative matcher + its evidence are preserved verbatim; every promotion records `evidence.promotion` (`chosen`/`criterion`/`from_status`) — fully reconstructable, reversible. Owner decisions: precision bar ≥90% (OQ1), `review_required` included (OQ3), retroactive delivery = local one-shot (OQ4), matcher source fixes = separate follow-on RFC (OQ5). RFC → `docs/archive/done/rfcs/FEAT-lyrics-best-of-promotion.md` (Status: **done**).

- **Step 0** (ws #499/#502) — RFC draft → review revisions (tier-1-only scoping, body-filter precondition, two mandatory Step-2 contradiction fixes: basis-aware consistency invariant + widened reassessment selection) → owner OQ decisions → accepted.
- **Step 1** (worker #60 `f4dc42d`, ws #503, 2026-07-03) — pure `promote_best` core (`worker/service/lyrics_promote.py`, 14 unit tests incl. the canonical Come Back to Earth 3-candidate → clean-title case) + the read-only risk-quantification probe over the live 2,783-row pool (9.5 min @ 40-concurrency): **tier-1 hit 94.4%**, duration gap 98.0% ≤1 s, ambiguity class 40:1 duplicate-noise vs genuine, review version-agreeing siblings 8/34, gated-tier residual 26 → stay closed; labelled-sample precision **48/48 = 100%**.
- **Step 2** (worker #61 `1e7510f`, ws #505/#506, 2026-07-03) — live integration into the shared `run_eval_batch` (after `decide_match`, before invariant + writer, reusing in-memory candidate bodies — no second LRCLIB fetch); consistency invariant made **basis-aware** (`exact-title` ⇒ version agreement; `best-of-*` ⇒ `evidence.promotion` present); `_BASIS_STRENGTH` reshuffled to integer rungs with both best-of bases at rung 2 (exact supersedes best-of / lateral refusal / never displaces exact, via strict-`>`); reassessment selection widened to re-select `best-of-*` rows with unresolved-first ordering. 13 new tests; worker pytest 275/34skip. Prod smoke: first live reassessment run promoted 32 (exact delta reconciliation, exact-title 7,957 untouched).
- **Step 3** (ws #509, 2026-07-03) — retroactive local one-shot backfill (`tools/lyrics_bestof_backfill.py` — thin chunked driver reusing the worker's live path verbatim: `run_eval_batch` → `promote_best` → basis-aware invariant → `should_replace` → per-row-commit writer; pre-run rollback snapshot; `updated_at`-scoped retry passes; LRCLIB @40). 20-row prod-verified sanity slice, then the full 2,697-row run (16.8 min). **Session results: 2,705/2,757 = 98.1% of the parked pool promoted** — best-of `matched` +2,580 (`best-of-ambiguous` 2,603 + `best-of-review` 8), all-instrumental `no_lyrics` +125, pool-drift fresh `exact-title` +8 (all verified pool members); corpus `matched` share **37% → 49.5%** (10,576/21,366). Guards: `consistency_errors` 0 · zero pre-existing exact-title rows touched · zero empty-lyric / provenance-missing best-of rows · deltas reconcile exactly. **Manual precision audit: stratified 40/40 = 100%** (all 8 best-of-review included). Residual 44: 18 LRCLIB-transient ambiguous (rows untouched → reassessment drains) + 26 review parked with recorded attempt (no version-agreeing sibling — the OQ5 follow-on class; probe predicted both exactly).

### FEAT-lyrics-viewer: authenticated full-screen lyrics viewer overlay — ✅ 2026-07-03 (all 3 steps prod-verified)

The **first consumer** of FEAT-lyrics-corpus: an **authenticated-only full-screen lyrics viewer overlay** (not a route) that renders **project-normalized segments** for the **currently playing track only** — Spotify-mobile focus-one-segment emphasis, manual nav (prev/next + vertical swipe/drag + tap-to-focus + arrows/wheel), **no continuous time-driven progression** (RFC non-goal). Original lyrics only; translation UI/API/authoring out of scope (future RFC). Reads **JWT-gated (authenticated-only)** — stricter than the edge_guard-only `/api/library/*` pattern — and writes/updates/publish recorded as an operator-capability boundary (not implemented). No public route/page/search/cached payload carries lyrics. Three dependency RFCs (component map / playback research / normalization model) all closed docs-only 2026-07-02 before Step 1. RFC → `docs/archive/done/rfcs/FEAT-lyrics-viewer.md` (Status: **done**).

- **Step 1** (backend#98 / ws#491 / front#220, 2026-07-02) — `GET /api/lyrics/{spotify_track_id}` JWT-gated via `require_cognito_token`; server-side resolve `spotify_track_id` → `Track.id` (reverse of `PlaybackService.resolve_uri`; one round trip, no separate resolve endpoint); response conforms to ARCH-lyrics-normalization-model (`availability`/`source_kind`/`trackable`/`normalizer_version`/`segments[]{i,text,start_ms}`). New JWT GET route → matching `infra/apigateway.tf` entry + apply (owner-approved; re-plan No changes). Prod smoke 8/8 (unauth 401 / mixed ok+synced / plain-only ok / no_lyrics / unresolved / missing-row / 404).
- **Step 2** (front#221, 2026-07-02) — full-screen overlay reusing `useDismissable` + `.lf-scrim`; focus-one-segment emphasis + manual nav; availability-aware empty states (`no_lyrics`/instrumental → "가사 없음 (연주곡)"; unresolved/missing → "아직 연결된 가사가 없어요"); isolated debug entry `?lyrics=<id>`. Click-through caught a real-pointer bug: `setPointerCapture` on pointerdown retargeted pointerup/click to the scroll container, killing tap-to-focus for trusted input (JS `.click()` masked it) → fix = defer capture until the 8px drag threshold.
- **Step 3** (front#222, merge `f097356`, 2026-07-03) — **dynamic entry** on `NowPlaying` live branches only (idle/최근재생 branch gets no entry → no recent-history fallback). The tap performs a 1-shot client-side `GET /v1/me/player` (token mint lazy, on the explicit action — same sanctioned direct-`api.spotify.com` pattern as `requestPlayback`; rule #9 not engaged) — `playing` opens with the live `item.id`+`progress_ms` (the now-playing snapshot stores no track id and can be ~1h stale), `idle` (204/paused/non-track) hides the entry ("재생 중 아님"), `unavailable` (token-mint/read failure) keeps a retry-able button. Viewer gains 1-shot position-seeded initial focus (`focusIndexForMs` = last segment whose `start_ms ≤ ms`; plain rows carry no timestamps and are skipped) + a manual refresh `↻` (track change swaps segments, position-only change re-initializes focus once, stopped → "지금 재생 중인 곡이 없어요"); **no auto-advance anywhere**. `playback.api.ts` (`readLivePlayback`) new; `NowPlaying` gains `LyricsEntry` (`onOpenLyrics` prop threaded via `OverviewDash`); `ProfileApp` merges the Step 2 debug entry + Step 3 dynamic entry into one overlay mount (only the dynamic entry enables `canRefresh`). Front-only — no backend/contract/infra change.

  **Prod smoke**: API (unauth 401 + authed 200 ok / 45 synced seg all w/ numeric `start_ms`) + UI via isolatedContext CDP w/ smoke JWT seeded to `localStorage.access_token` (idle-gate: NowPlaying "최근 재생" branch renders **no** `가사` entry — no recent-history fallback; unauth → Cognito; debug-overlay modal "가사 뷰어" + "LYRICS · SYNCED" + 45 seg incl. KR/EN + "간주" stanza gap + tap-to-focus + prev/next, console 0). The **live dynamic-entry path** (tap 가사 on an active Premium session → `/v1/me/player` → load + one-shot position seed → refresh swap / "재생 중 아님") deferred to owner active-session spot-check — no headless method can synthesize an actively-playing Premium session; the three `readLivePlayback()` states map cleanly to the independently-verified gates.

PRs: backend #98, workspace #491, front #220 (api.gen.ts) / #221 (overlay) / #222 (dynamic entry). Deploy: `28601305711` (Deploy Front, success); new bundle `ProfileApp.CiMU2LSE.js` carries all Step 3 markers + `v1/me/player`.

### FEAT-lyrics-corpus: private, correctness-first lyrics corpus — ✅ 2026-07-02 (all steps prod-verified)

A **private, single-owner, correctness-first** lyrics corpus attached to catalog tracks. LRCLIB = only lyric-text source (plain + synced); MusicBrainz = identity/version evidence only; lyrics.ovh excluded. Lyrics are **owner-only** — never in any public API/page/search/shared/edge-cached response (mirrors `artists.aliases` / `album_research`). Album name is never a hard match key; the conservative matcher (`decide_match`) parks remix/live/demo/edit/cover/remaster/rerecording as `ambiguous`/`review_required` rather than mis-merging. RFC → `docs/archive/done/rfcs/FEAT-lyrics-corpus.md` (Status: **done**).

- **Step 0** docs; **Step 1** shared_db `track_lyrics` store + match-status lifecycle + `Track.isrc` (V33/V34, 20.7k tracks); **Step 1b** worker ISRC backfill (myblog_worker #54).
- **Step 2** offline matcher (#478): Phase 1 (N=100 API sample, precision 39/39). Phase 2 matched the **full 20,946-track catalog** via LRCLIB `/api/search` (the 30.68 GiB dump wouldn't fit ~54 GiB disk) — matched 7,955 (38.0%) / no_lyrics 417 / not_found 9,798 / ambiguous 2,743 / review_required 33; consistency PASS, version-conflict false-match 0, precision 64/64 = 100% audited. 91% of matched carry synced (LRC).
- **Step 3** worker incremental collection (worker #57 + ws #481) — `lyrics_incremental` EventBridge job `rate(12 hours)`, alias-fill pattern: recently-added tracks lacking a row → LRCLIB `/api/search` → same matcher → per-row commit + sentinel; transient-skip isolation; bounded to the 120s Lambda. Prod-verified via a seeded 1-track gap.
- **Step 4** periodic reassessment + replacement guard (worker #58 + ws #483) — `lyrics_reassessment` job `rate(1 day)`: re-checks the unresolved pool (~12.5k, stalest-first) → promote-or-refresh; **replacement guard** never downgrades/laterally-swaps a good match (basis ladder isrc>mb-recording>exact-title>fuzzy-title → matched never replaced by the current matcher). Shared eval loop (`lyrics_eval_core.run_eval_batch`) used by Steps 3 & 4. Prod-verified: 30/30 unresolved refreshed, 0 promotions (same-day), 0 matched touched.
- **Infra**: `terraform apply`-ed `worker-lyrics-incremental` + `worker-lyrics-reassessment` EventBridge rules. **No frontend, no public API.** Consumers (viewer/translation/interpretation) are separate later RFCs — see FEAT-lyrics-viewer.

### FEAT-pocket-buckit-viewers: bidirectional DnD + per-drawer Card Viewer — ✅ 2026-07-01

Two front-only, additive extensions to Pocket Buckit, both over the shared `bucketStore` (one source of truth → every island repaints off one optimistic update, so "reflected immediately everywhere" is free). 2 tracks, sequential (Hard rule #4). **No new bucket types, membership rules, API, contract, schema, or infra.**

- **Track A — reverse DnD (front #218, ws #470, 2026-06-30).** The *reverse* of FEAT-my-buckit-artist Step 6: a board My Buckit member dragged onto a Pocket target (tray chip / open drawer) is added to that bucket with the **board's exact semantics**. A symmetric cross-island bridge (`PB_BOARD_DND_START`/`END` board→pocket, `PB_BOARD_DROP` pocket→board) carries the live drag; the board's inline bucket-drop routing was extracted to a board-scope `dropOnBucket(targetBucketId)` that **both** the card `onDrop` and the `PB_BOARD_DROP` listener call (no duplicated routing → General/Artist + artist-expansion semantics provably identical). The Pocket island mirrors the board drag in a `boardDnd` module so chips/drawers preview droppability via a 3-line replica of `canAcceptAlbumDrag`. Existing Pocket→board (Step 6) wiring untouched. **Verified** headless CDP (real Chrome synthetic DragEvents + mock backend, console 0): General drop = cross-bucket **move** (`PUT /reorder` via `ops.insertAlbum`), Artist drop = **source-expansion** (`POST source_album_id`), accept-gate `data-droppable`/`data-drophot` correct, Step 6 intact; prod bundle carries `pb:board-drop`/`pb:board-dnd-start`/`data-drophot` (deploy 28442412334).

- **Track B — per-drawer Card Viewer (front #219, 2026-07-01).** The bucket window opened from a tray chip gains an **ephemeral per-open-drawer** Current ⇄ Card viewer toggle (header button). Card Viewer renders the same `bucket.albums` as a **board-style square-cover grid** — album = cover, artist = square photo + `아티스트` badge, artist tile click → `/artist/[id]` (mirrors the board); album/other tiles are non-navigating (no in-island detail surface). Reads the same provider bucket as the list, so a change via the board, reverse DnD, or edit `−` reflects live in every viewer; two drawers toggle independently. `Cover` gained optional `url`/`square` to show the real cover via the **site-wide global** `.cover`/`.cover img` (no `member.css` dependency); list rows + card tiles share a `dragBind` so the Step 6 drag-out and edit-`−` work in both modes. Tile/grid/toggle styles are self-supplied in `pocket.css` (`.pb-cardgrid` / `.pb-card*` / `.pb-viewtoggle`) because `member.css` (the board's `.bb-tile`/`.lf-cover`) is `/profile`-only and the site-wide drawer must not depend on it. Open-Q 1 resolved: render **all** members in the existing scroll area (no slice cap). **Verified** headless CDP **off-`/profile`** (homepage + mock backend): list⇄card toggle, 2 `<img>` covers + 1 placeholder render with `member.css` NOT linked & `.bb-tile` rule absent (proves self-supply), artist tiles + badge + click→`/artist/ar1`, two drawers with independent per-window mode, edit-`−` live reflection (card grid 3→2 + tray chip badge 3→2); console clean. Prod bundle `PocketBuckit.BqMp8rYZ.js` + CSS `Cc5GMNRB.css` carry all Track B markers (deploy 28479488342).

**Caveat (both tracks):** a *trusted native HTML5 drag gesture* can't be synthesized headlessly (same as Step 6 / Track A) — the drag routing is exercised via synthetic events + the verbatim reused payload; the real gesture is an owner spot-check on prod.

PRs: front #218 (A) / #219 (B), workspace #470 (A) / #471 / this. RFC: `docs/archive/done/rfcs/FEAT-pocket-buckit-viewers.md` (Status: done) + memory `reference-pocket-buckit-execution-plan`.

### FEAT-nowplaying-live-sync: /profile now-playing one-shot live read + 동기화 button — ✅ 2026-07-03

The `/profile` now-playing card no longer shows only the ~1h-stale worker snapshot (hourly cron). Front-only, no contract/infra change:

- **Entry auto-read** — `useNowPlaying` fires the snapshot fetch and **one** client-side `GET /v1/me/player` in parallel; a decisive live result (`playing`/`idle`) wins regardless of arrival order, `unavailable` silently keeps the snapshot (zero degradation on token/network failure).
- **동기화 button** (`SyncControl`, single-flight) beside the sync note on all variants incl. the idle box; success stamps "동기화 방금". Fixes the 가사 entry being hidden by a stale-idle snapshot (entry visibility is snapshot-gated).
- `readLivePlayback()` additively returns `track`/`artist`/`album`/`albumCoverUrl` from the same response — LyricsEntry callers untouched; its tap still does its own one-shot read.
- Sanction: dated 2026-07-03 amendment in `RESEARCH-playback-product-architecture.md` — a single one-shot read on `/profile` entry (owner-only authed page) joins the explicit-action reads; token mint stays async/lazy, never polled, rule #9 not engaged.

Verified pre-merge: `pnpm lint` 0/0 + `astro check` 0 errors + mocked-CDP click-through (stale-fallback / 동기화-click live flip / idle flip + 가사 hidden / entry auto-read). Prod smoke 2026-07-03 (deploy run 28631003668): `/profile/` 200, deployed chunk `ProfileApp.OJAtaBoE.js` carries `albumCoverUrl` + `v1/me/player` + 11× 동기화 strings. **Open:** real-Premium live-path spot-check by owner (same deferral shape as FEAT-lyrics-viewer Step 3).

PRs: front #223 (merge `8b8a6c5`), workspace #495 (sanction amendment + plan row) / this close-out.

### FEAT-lyrics-translation: Korean translation for designated lyrics in the viewer — ✅ 2026-07-04

Owner-designated tracks get a segment-aligned Korean translation in the lyrics viewer, fully GUI-driven end to end: viewer 번역 요청 button → `track_lyrics_translations` request row (shared_db V35) → local launchd poller (60s, genre-heal V25 claim pattern) translates per-line via **Amazon Translate** (auto→ko, INFORMAL; engine swapped from `claude -p` after a consistent headless copyright refusal — RFC decisions log) → additive `translation`/`text_ko` on `GET /api/lyrics/{id}` → 원문+번역 toggle. No new AWS infra, no new secret, no MT/LLM in any user-facing endpoint; translations carry the corpus's owner-only privacy bar.

- **Step 1** shared_db #51 (`v0.27.0`): V35 `track_lyrics_translations`, prod-applied.
- **Step 2** backend #99 / ws #520 / front #229: JWT `POST /api/lyrics/{id}/translation-request` + additive read extension; apigateway route terraform-applied; contract merged + `api.gen.ts` regen. Prod smoke 28/0.
- **Step 3** ws #523/#524: `lyrics_translate_poller.py` + 60s plist live via launchd; per-line passthrough for already-Korean/untranslatable lines; **pilot gate PASSED** — LUX all-13-matched / 590 lines owner-audited 13/13 (denominator 13 owner-accepted), ES/IT/DE/EN/Latin mixed.
- **Step 4** front #230: viewer 번역 toggle (interleaved dimmed `text_ko`, focus/nav unit unchanged) + request-button lifecycle (none/stale→요청, failed→재요청, requested→passive 요청됨) + Hangul-ratio ≥50% hide on Korean-dominant tracks + `manual` origin badge. Pre-merge CDP click-through on an isolated worktree; prod smoke: deployed-bundle marker + real-browser toggle/nav on Berghain + full suite 28/0 (deploy run 28701828463).

PRs: shared_db #51, backend #99, front #229 / #230, workspace #520 / #521 / #522 / #523 / #524 / this close-out. RFC: `docs/archive/done/rfcs/FEAT-lyrics-translation.md` (Status: done) + memory `project-feat-lyrics-translation`.

### FEAT-lyrics-auto-progression: Clock-based auto-advancing lyrics + Spotify-style redesign — ✅ 2026-07-04

Repealed FEAT-lyrics-viewer's manual-only non-goal: the viewer now **auto-advances** synced lyrics with a client-side clock estimate (~250ms `setInterval`, paused on `document.hidden`) seeded from the existing one-shot `readLivePlayback()` position — no polling, no SDK coupling, no new endpoint — plus a **Spotify-style visual redesign** (always-dark viewer, album-cover blur backdrop `.lyv-bg`/`.lyv-bg-overlay` from a real `albumCoverUrl` prop, large sans-serif 3-level focus/near/far typography, minimal chrome). Live auto/manual toggle; manual nav self-corrects the estimate against the jumped line's `start_ms`; `↻` re-sync re-anchors; plain-only tracks degrade to manual-only. Front-only — no backend/contract/schema/infra change; the lyrics privacy boundary (JWT-gated, owner-only) is untouched.

- **Steps 1+2** shipped in a single PR per owner decision — front **#231** (squash `4200877`; Step 1 engine `c487933`, Step 2 redesign `5bb34b4`, rebased clean over #230). Visual direction = owner-approved design preview "A".
- Pre-merge: `pnpm lint` + `astro check` + `pnpm build` 0 errors. Post-merge: deploy run `28704710047` success; bundle markers live (`collection.D4iALjeu.css` `.lyv-bg` ×4, `ProfileApp.CtLz5xxK.js` `initialAlbumCoverUrl`); **prod smoke 28/0** quoted on #231.

PRs: front #231, workspace #526 / #527 / this close-out. RFC: `docs/archive/done/rfcs/FEAT-lyrics-auto-progression.md` (Status: done).
