# Completed Work — 2026-07

Archived from `plan.md`. Newest first within each section. Format intentionally loose; this is history, not a working spec.

---

## Docs / research

### Lyrics-viewer dependency RFCs (×3): component map + playback research + normalization model — ✅ 2026-07-02 (docs-only, owner-approved close)

Three dependency RFCs carved out of FEAT-lyrics-viewer's original scope (rescope #487), drafted #488, OQ-resolved + accepted #489, Step 1 executed + closed the same day. All docs-only — no code, schema, contract, or infra. Together they unblock FEAT-lyrics-viewer (draft → ready for owner accept).

- **ARCH-frontend-component-map** — published `docs/frontend/component-map.md` (LLM/developer reference) + `docs/frontend/structure.md` (human map), both code-pinned "Verified 2026-07-02"; the RFC's reference section became a pointer (single canonical living copy). Key verdict: **no shared track-row component exists** (≥6 fragmented paths; only play funnels through `requestPlayback`). 5-claim spot re-verify fixed two anchor drifts (`requestPlayback` `:243`→`:242`; slide-over stacks at `--z-panel: 90`, not `--z-overlay`). Includes the Component-map impact template for future RFCs. RFC → `docs/rfcs/ARCH-frontend-component-map.md` (Status: **done**).
- **RESEARCH-playback-product-architecture** — decision research: **no custom playback RFC needed**. Code resolved the read path (client-side `GET /v1/me/player`, same sanctioned pattern as `requestPlayback`'s existing `api.spotify.com` call; rule #9 not engaged; `track_id` via live `item.id` → server-side resolve to `Track.id`). **Step 1 live probe PASSED 2026-07-02**: smoke JWT → `/api/playback/spotify-token` 200 → `GET /v1/me/player` 200 `{is_playing: true, progress_ms: 23200, item.id, duration_ms: 182088}` on a live owner Premium session (iPhone); idle = 204 empty (= the viewer's hidden-when-idle state). FEAT-lyrics-viewer Step 3 binds the one-shot position read, track-only on 204/failure. RFC → `docs/rfcs/RESEARCH-playback-product-architecture.md` (Status: **done**).
- **ARCH-lyrics-normalization-model** — pinned the normalized payload spec the viewer implements against: `availability`/`source_kind`/`trackable`/`normalizer_version`/`segments[]{i, text, start_ms}` (`text==""` = stanza gap), `normalize_lyrics`/`parse_lrc` pseudocode (synced-only & mixed share one path — segments derive from synced alone; plain-only → `start_ms: null`; plain disagreement = diagnostics only, synced wins; text never silently dropped), + acceptance gate. Read-time derivation, raw sources stay source-of-truth. Prod probes: mixed 91% / plain-only 9% / synced-only 0 (defensive branch); blank stanza markers in 93% of synced rows. RFC → `docs/rfcs/ARCH-lyrics-normalization-model.md` (Status: **done**).

PRs: workspace #486 (viewer RFC draft) / #487 (rescope) / #488 (three drafts) / #489 (OQ + accept) / this (Step 1 ×3 + close).

## Feature expansion

### FEAT-lyrics-viewer: authenticated full-screen lyrics viewer overlay — ✅ 2026-07-03 (all 3 steps prod-verified)

The **first consumer** of FEAT-lyrics-corpus: an **authenticated-only full-screen lyrics viewer overlay** (not a route) that renders **project-normalized segments** for the **currently playing track only** — Spotify-mobile focus-one-segment emphasis, manual nav (prev/next + vertical swipe/drag + tap-to-focus + arrows/wheel), **no continuous time-driven progression** (RFC non-goal). Original lyrics only; translation UI/API/authoring out of scope (future RFC). Reads **JWT-gated (authenticated-only)** — stricter than the edge_guard-only `/api/library/*` pattern — and writes/updates/publish recorded as an operator-capability boundary (not implemented). No public route/page/search/cached payload carries lyrics. Three dependency RFCs (component map / playback research / normalization model) all closed docs-only 2026-07-02 before Step 1. RFC → `docs/rfcs/FEAT-lyrics-viewer.md` (Status: **done**).

- **Step 1** (backend#98 / ws#491 / front#220, 2026-07-02) — `GET /api/lyrics/{spotify_track_id}` JWT-gated via `require_cognito_token`; server-side resolve `spotify_track_id` → `Track.id` (reverse of `PlaybackService.resolve_uri`; one round trip, no separate resolve endpoint); response conforms to ARCH-lyrics-normalization-model (`availability`/`source_kind`/`trackable`/`normalizer_version`/`segments[]{i,text,start_ms}`). New JWT GET route → matching `infra/apigateway.tf` entry + apply (owner-approved; re-plan No changes). Prod smoke 8/8 (unauth 401 / mixed ok+synced / plain-only ok / no_lyrics / unresolved / missing-row / 404).
- **Step 2** (front#221, 2026-07-02) — full-screen overlay reusing `useDismissable` + `.lf-scrim`; focus-one-segment emphasis + manual nav; availability-aware empty states (`no_lyrics`/instrumental → "가사 없음 (연주곡)"; unresolved/missing → "아직 연결된 가사가 없어요"); isolated debug entry `?lyrics=<id>`. Click-through caught a real-pointer bug: `setPointerCapture` on pointerdown retargeted pointerup/click to the scroll container, killing tap-to-focus for trusted input (JS `.click()` masked it) → fix = defer capture until the 8px drag threshold.
- **Step 3** (front#222, merge `f097356`, 2026-07-03) — **dynamic entry** on `NowPlaying` live branches only (idle/최근재생 branch gets no entry → no recent-history fallback). The tap performs a 1-shot client-side `GET /v1/me/player` (token mint lazy, on the explicit action — same sanctioned direct-`api.spotify.com` pattern as `requestPlayback`; rule #9 not engaged) — `playing` opens with the live `item.id`+`progress_ms` (the now-playing snapshot stores no track id and can be ~1h stale), `idle` (204/paused/non-track) hides the entry ("재생 중 아님"), `unavailable` (token-mint/read failure) keeps a retry-able button. Viewer gains 1-shot position-seeded initial focus (`focusIndexForMs` = last segment whose `start_ms ≤ ms`; plain rows carry no timestamps and are skipped) + a manual refresh `↻` (track change swaps segments, position-only change re-initializes focus once, stopped → "지금 재생 중인 곡이 없어요"); **no auto-advance anywhere**. `playback.api.ts` (`readLivePlayback`) new; `NowPlaying` gains `LyricsEntry` (`onOpenLyrics` prop threaded via `OverviewDash`); `ProfileApp` merges the Step 2 debug entry + Step 3 dynamic entry into one overlay mount (only the dynamic entry enables `canRefresh`). Front-only — no backend/contract/infra change.

  **Prod smoke**: API (unauth 401 + authed 200 ok / 45 synced seg all w/ numeric `start_ms`) + UI via isolatedContext CDP w/ smoke JWT seeded to `localStorage.access_token` (idle-gate: NowPlaying "최근 재생" branch renders **no** `가사` entry — no recent-history fallback; unauth → Cognito; debug-overlay modal "가사 뷰어" + "LYRICS · SYNCED" + 45 seg incl. KR/EN + "간주" stanza gap + tap-to-focus + prev/next, console 0). The **live dynamic-entry path** (tap 가사 on an active Premium session → `/v1/me/player` → load + one-shot position seed → refresh swap / "재생 중 아님") deferred to owner active-session spot-check — no headless method can synthesize an actively-playing Premium session; the three `readLivePlayback()` states map cleanly to the independently-verified gates.

PRs: backend #98, workspace #491, front #220 (api.gen.ts) / #221 (overlay) / #222 (dynamic entry). Deploy: `28601305711` (Deploy Front, success); new bundle `ProfileApp.CiMU2LSE.js` carries all Step 3 markers + `v1/me/player`.

### FEAT-lyrics-corpus: private, correctness-first lyrics corpus — ✅ 2026-07-02 (all steps prod-verified)

A **private, single-owner, correctness-first** lyrics corpus attached to catalog tracks. LRCLIB = only lyric-text source (plain + synced); MusicBrainz = identity/version evidence only; lyrics.ovh excluded. Lyrics are **owner-only** — never in any public API/page/search/shared/edge-cached response (mirrors `artists.aliases` / `album_research`). Album name is never a hard match key; the conservative matcher (`decide_match`) parks remix/live/demo/edit/cover/remaster/rerecording as `ambiguous`/`review_required` rather than mis-merging. RFC → `docs/rfcs/FEAT-lyrics-corpus.md` (Status: **done**).

- **Step 0** docs; **Step 1** shared_db `track_lyrics` store + match-status lifecycle + `Track.isrc` (V33/V34, 20.7k tracks); **Step 1b** worker ISRC backfill (myblog_worker #54).
- **Step 2** offline matcher (#478): Phase 1 (N=100 API sample, precision 39/39). Phase 2 matched the **full 20,946-track catalog** via LRCLIB `/api/search` (the 30.68 GiB dump wouldn't fit ~54 GiB disk) — matched 7,955 (38.0%) / no_lyrics 417 / not_found 9,798 / ambiguous 2,743 / review_required 33; consistency PASS, version-conflict false-match 0, precision 64/64 = 100% audited. 91% of matched carry synced (LRC).
- **Step 3** worker incremental collection (worker #57 + ws #481) — `lyrics_incremental` EventBridge job `rate(12 hours)`, alias-fill pattern: recently-added tracks lacking a row → LRCLIB `/api/search` → same matcher → per-row commit + sentinel; transient-skip isolation; bounded to the 120s Lambda. Prod-verified via a seeded 1-track gap.
- **Step 4** periodic reassessment + replacement guard (worker #58 + ws #483) — `lyrics_reassessment` job `rate(1 day)`: re-checks the unresolved pool (~12.5k, stalest-first) → promote-or-refresh; **replacement guard** never downgrades/laterally-swaps a good match (basis ladder isrc>mb-recording>exact-title>fuzzy-title → matched never replaced by the current matcher). Shared eval loop (`lyrics_eval_core.run_eval_batch`) used by Steps 3 & 4. Prod-verified: 30/30 unresolved refreshed, 0 promotions (same-day), 0 matched touched.
- **Infra**: `terraform apply`-ed `worker-lyrics-incremental` + `worker-lyrics-reassessment` EventBridge rules. **No frontend, no public API.** Consumers (viewer/translation/interpretation) are separate later RFCs — see FEAT-lyrics-viewer.

### FEAT-pocket-buckit-viewers: bidirectional DnD + per-drawer Card Viewer — ✅ 2026-07-01

Two front-only, additive extensions to Pocket Buckit, both over the shared `bucketStore` (one source of truth → every island repaints off one optimistic update, so "reflected immediately everywhere" is free). 2 tracks, sequential (Hard rule #4). **No new bucket types, membership rules, API, contract, schema, or infra.**

- **Track A — reverse DnD (front #218, ws #470, 2026-06-30).** The *reverse* of FEAT-my-buckit-artist Step 6: a board My Buckit member dragged onto a Pocket target (tray chip / open drawer) is added to that bucket with the **board's exact semantics**. A symmetric cross-island bridge (`PB_BOARD_DND_START`/`END` board→pocket, `PB_BOARD_DROP` pocket→board) carries the live drag; the board's inline bucket-drop routing was extracted to a board-scope `dropOnBucket(targetBucketId)` that **both** the card `onDrop` and the `PB_BOARD_DROP` listener call (no duplicated routing → General/Artist + artist-expansion semantics provably identical). The Pocket island mirrors the board drag in a `boardDnd` module so chips/drawers preview droppability via a 3-line replica of `canAcceptAlbumDrag`. Existing Pocket→board (Step 6) wiring untouched. **Verified** headless CDP (real Chrome synthetic DragEvents + mock backend, console 0): General drop = cross-bucket **move** (`PUT /reorder` via `ops.insertAlbum`), Artist drop = **source-expansion** (`POST source_album_id`), accept-gate `data-droppable`/`data-drophot` correct, Step 6 intact; prod bundle carries `pb:board-drop`/`pb:board-dnd-start`/`data-drophot` (deploy 28442412334).

- **Track B — per-drawer Card Viewer (front #219, 2026-07-01).** The bucket window opened from a tray chip gains an **ephemeral per-open-drawer** Current ⇄ Card viewer toggle (header button). Card Viewer renders the same `bucket.albums` as a **board-style square-cover grid** — album = cover, artist = square photo + `아티스트` badge, artist tile click → `/artist/[id]` (mirrors the board); album/other tiles are non-navigating (no in-island detail surface). Reads the same provider bucket as the list, so a change via the board, reverse DnD, or edit `−` reflects live in every viewer; two drawers toggle independently. `Cover` gained optional `url`/`square` to show the real cover via the **site-wide global** `.cover`/`.cover img` (no `member.css` dependency); list rows + card tiles share a `dragBind` so the Step 6 drag-out and edit-`−` work in both modes. Tile/grid/toggle styles are self-supplied in `pocket.css` (`.pb-cardgrid` / `.pb-card*` / `.pb-viewtoggle`) because `member.css` (the board's `.bb-tile`/`.lf-cover`) is `/profile`-only and the site-wide drawer must not depend on it. Open-Q 1 resolved: render **all** members in the existing scroll area (no slice cap). **Verified** headless CDP **off-`/profile`** (homepage + mock backend): list⇄card toggle, 2 `<img>` covers + 1 placeholder render with `member.css` NOT linked & `.bb-tile` rule absent (proves self-supply), artist tiles + badge + click→`/artist/ar1`, two drawers with independent per-window mode, edit-`−` live reflection (card grid 3→2 + tray chip badge 3→2); console clean. Prod bundle `PocketBuckit.BqMp8rYZ.js` + CSS `Cc5GMNRB.css` carry all Track B markers (deploy 28479488342).

**Caveat (both tracks):** a *trusted native HTML5 drag gesture* can't be synthesized headlessly (same as Step 6 / Track A) — the drag routing is exercised via synthetic events + the verbatim reused payload; the real gesture is an owner spot-check on prod.

PRs: front #218 (A) / #219 (B), workspace #470 (A) / #471 / this. RFC: `docs/rfcs/FEAT-pocket-buckit-viewers.md` (Status: done) + memory `reference-pocket-buckit-execution-plan`.
