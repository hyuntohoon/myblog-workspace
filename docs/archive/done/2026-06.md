# Completed Work — 2026-06

Archived from `plan.md`. Newest first within each section. Format intentionally loose; this is history, not a working spec.

---

## Infra & cost

### CHORE-secrets-ssm-migration: Secrets Manager → SSM Parameter Store — ✅ 2026-06-22

Cut the AWS Secrets Manager bill **~$3.20/mo → ~$0/mo** (Secrets Manager fully emptied) by moving all app secrets to **SSM Parameter Store Standard SecureString** (free storage, same KMS-at-rest + per-resource IAM). Root cause was storage (per-secret $0.40 × 8, grown 3→8 since late May); API calls were negligible. Consolidation couldn't reach <$1 without breaking least-privilege (SM IAM scopes per-secret-ARN), so SSM was the only secure path. RFC: `docs/rfcs/CHORE-secrets-ssm-migration.md` (full history + decisions + owner runbook). Design = **dual-source loader** (`SECRETS_PARAM` SSM-preferred → `SECRETS_ARN` SM fallback) → code deploys were safe no-ops, cutover a reversible Terraform env flip. PRs: backend #85, music #46, worker #50 (+ test fix #51), infra #414/#416/#418/#420; docs #413/#415/#417/#419. **Prod-validated**: smoke 28/0 (CRUD + edge_guard) SSM-only, worker invoke 200, **spotify token write-back to the SSM param confirmed** (timestamp advanced), zero SSM-fallback logs, final `terraform plan` clean. `claude_aws_manager` self-granted ssm/kms (inline `claude-ssm-myblog` — iam:PutUserPolicy is allowed; **policy must stay** or the local pollers/scripts that now read SSM break). All 8 SM secrets deleted (30-day recovery window) — incl. the empty/dormant `myblog/anthropic` (no value set, researchWorkerLambda 0 invocations/30d); when FEAT-ai-editorial-critique ships, provision anthropic in **SSM** (RFC Step 6), not SM. `test-db`+`neon-api` had zero readers (GHA secret is the live test-db source). `git log` authoritative.

---

## Feature expansion

### FEAT-pocket-buckit-workspace: tray multi-drawer workspace + edit mode + cached store; Step C deferred — ✅ 2026-06-24

Follow-on to the shipped Pocket Buckit (the membership track) — an *interaction-shell + read-efficiency* layer, **no new item kinds / no scale change**. Built as gated front-only steps; the cross-repo read-model step (C) was **deferred** after a prod-evidence review.

- **Step A (front #208)** — the tray became a real **multi-drawer workspace**: several movable mini-drawers open at once (per-drawer position/z/focus, brought-to-front on re-click, never duplicated, cascade spawn, per-bucket persisted+clamped positions, `pbDrawerIn` connected-to-tray open anim), plus an explicit **edit/arrange mode** gating tray reorder + bucket-delete + drawer item-removal (separate from drawer-open; normal-mode drag never reorders).
- **Step B (front #209 + #210)** — a module-level, **user-scoped (Cognito `sub`) sessionStorage-backed observable `bucketStore`** that the Pocket provider + `BucketBoard` + `LikedBoard` all subscribe to (one source of truth, cross-island/tab sync, logout-clear). **SWR**: seed instantly from cache, revalidate in the **background** only when stale (30s `staleTime`) or after a mutation — replacing the MPA's full-tree refetch-on-every-nav. (`bucketStore.ensureFresh()` still calls the full `listBuckets()` → B cut *frequency + perceived latency*, not payload size.)
- **Step C (cross-repo `GET /api/buckets/summary` flat read-model + lazy `GET /api/buckets/{id}`) — DEFERRED, RFC closed.** Decided from a prod-evidence dossier + an adversarial judge panel (judge 0.74; implement advocate 0.42, conceding "no concrete user-visible harm right now"). Step B already removed C's *only* named harm (heavy per-nav `GET /api/buckets` → perceived latency); the residual payload C would shrink is **sub-perceptual** — prod = **7 buckets, all flat roots / zero nesting, 96 items / 76 albums, max 46 in one bucket**; full `GET /api/buckets` ≈ **45–55 KB / ~10–13 KB gzip** (only ~7 KB content text), **≤1 fetch/30s, single owner** — and C's tree-reconstruction premise (`parent_id`+`position` depth/path) is **moot at zero nesting**. Fails the **necessity gate** vs a 4-repo permanent-contract cost. C stays additive + fully designed in the RFC → **revivable hot** (no redesign) when **any** of: (a) buckets nest in prod (`parent_id IS NOT NULL`, depth ≥ 2); (b) full `GET /api/buckets` > ~150 KB / ~40 KB gzip (≈3× today); (c) a *measured* cold-load/first-paint regression after cache eviction or logout/relogin.

PRs: front **#208 / #209 / #210** (all merged + `Deploy Front (S3+CF)` green 2026-06-24; deployed bundle verified to ship the markers — `pbDrawerIn`/`openDrawers` in `PocketBuckit.*.js`, `ensureFresh`/`pb:cache:buckets` in `bucketStore.*.js`). A/B were front-only + additive (no contract/infra). RFC: `docs/rfcs/FEAT-pocket-buckit-workspace.md` (Status: closed — A+B shipped, C deferred + revival trigger) + memory `project-pocket-buckit-workspace-cdeferred`. `git log` authoritative.

### FEAT-analysis-explore: 분석 버킷 임포트 source made *explorable* (time-range + drill-down + clock) — ✅ 2026-06-23

Follow-on to FEAT-listening-history-import + FEAT-listening-live-merge: turned the lifetime-**aggregate** 임포트(평생+라이브) source into an **explorable** one — filter by time period, drill into any artist/album/track, and see *when* you listen — all re-aggregating the already-owned `_unified_events` union (per-stream `event_ts`, import + live) with **no new ingestion / ML / schema / index** (the `ts`/`played_at` indexes already existed). Real Stats.fm features (Plus-gated there; free here — we own the data). RFC: `docs/rfcs/FEAT-analysis-explore.md` (Status accepted→done at close time on explicit owner approval, rule #5). **Single-pass** build (owner-authorized rule #4 escape — collapsed the 6-session cadence into one coordinated contract → backend → front pass). Prod-shipped + smoked **2026-06-23**:

- **Time-range** — an optional half-open `[from, to)` on `event_ts`, threaded into BOTH union legs via `_unified_events(frm, to)` → `_stream_totals` / `_stream_album_weights` / all 6 stream-history methods. Default (none) = prior lifetime+live behavior. `as_of` = `max(SSH.ts)` stays **GLOBAL** (unfiltered) so the import/live boundary is invariant under the range (a range ≤ as_of is import-only/exact; a range incl. now still pulls the live tail). Front: 전체/올해/작년/이번달 presets + **data-derived** 연도·시대 dropdowns (populated from the lifetime `per_year`, shown only when past years / multiple decades exist), each mapping a KST wall-clock boundary → a UTC instant (KST = UTC+9, no DST). Retrospective stays lifetime.
- **Item drill-down** — `GET /api/library/stream-history/item?type=artist|album|track&id=&metric=&from=&to=` → count, time_ms, first/last listen, KST per-year, and (artist only) top tracks/albums. Entity key = the same grouping key the panels use: uri (track) / artist_name (artist) / catalog album_id (album); a non-catalog album id → 404. Front: a private `lf-slideover` (OverviewDash pattern — the public `/artist/[id]` has no per-user data) opened from any chart-item click. `DistChart` gained an optional `onSelect` (`data-chart-click` + keyboard) — the 좋아요 charts pass none, so they're unchanged.
- **Listening clock** — `GET /api/library/stream-history/clock?metric=&from=&to=` → KST hour×weekday cells (`extract(dow/hour from timezone('Asia/Seoul', event_ts))`), non-empty cells only. Front: a 7×24 heatmap (월→일 rows), coloured by the active metric, peak slot called out.

Additive contract (+2 paths, +3 schemas `Backend_StreamItemDetailResponse`/`StreamClockResponse`/`StreamClockCell`, +`from`/`to` params on the 6 existing endpoints); all edge_guard reads, **DB-only (rule #9)**. PRs: **workspace #437 → backend #91 → front #201** (contract-first so the backend notify is a no-op + the front deploy gate sees the spec on `main`). Verify: backend 295 pytest + new half-open-boundary / clock self-consistency / `as_of`-invariant integration tests; pyright 0; **read-only prod invariants** all hold (partition `[None,T)+[T,None)==lifetime 3252`, drill-down counts == panel values, clock cell-sum == totals); front lint/astro/build 0; **full real-browser CDP click-through** (local backend → prod DB) — period 전체 3,252 → 이번달 948 (correct KST `from=`), 작년 empty-state, track/artist/album drill-downs, 168-cell clock (peak 월요일 18시), console 0. Post-deploy **prod smoke**: new endpoints serve via CloudFront (clock self-consistent, range/drill-down correct, 404/422 guards), deployed front bundle carries the new UI (`시간대`/`대표 트랙`/`data-chart-click`). A 4-dimension adversarial review (SQL / contract+security / frontend / cross-repo) returned **zero findings**. NB all current import data is calendar-**2026**, so the 연도/시대 dropdowns are data-gated. Custom date-picker + writing-screen evidence + "intensity" metric remain deferred (non-goals). `git log` authoritative.

### FEAT-listening-live-merge: 분석 버킷 임포트 source = lifetime + live (union the recently-played poller) — ✅ 2026-06-23

Follow-on to FEAT-listening-history-import: folded the live recently-played poller into the 분석 버킷 import source so it reads **lifetime + live** as one continuous signal, and **removed the standalone 재생 toggle**. The original import RFC deliberately kept the two grains separate ("never silently union — double-count"); this did the union **safely via a time boundary**, not per-row dedup. RFC: `docs/rfcs/FEAT-listening-live-merge.md` (Status accepted→done at archive time on explicit owner approval, rule #5). 2 steps, prod-verified **2026-06-23**:

- **Step 1** — backend `as_of`-boundary union. Shared `_unified_events()` subquery in `library_service.py`: import (`spotify_stream_history`, `ts ≤ as_of`, real `ms_played`) `UNION ALL` live (`spotify_track_play_events`, `played_at > as_of` where `as_of = max(ts)` over the import; **count exact**, **time estimated** = `coalesce(tracks.duration_sec,0) × 1000` per play; title/album-artist reconstructed from catalog). All 6 stream-history methods + totals + album-weights aggregate over it. Additive contract field `live_streams` on StreamRank / StreamAlbumRank / Retrospective responses (>0 ⇒ time includes estimate). No schema change (tables existed). backend #90 / workspace #433 / front #199.
- **Step 2 (FINAL)** — front toggle collapse. `LikedAnalysis`: source toggle `[임포트(평생+라이브), 좋아요]` — dropped the `'played'` source + its `getPlayed{Genre,Artist}Distribution` calls (the play-events client wrappers + backend endpoints LEFT in place — RFC Open Q3). `ImportAnalysis`: hero marks the time total estimated (`≈` / "시간 청취 (추정)") + captions the live extension ("평생 + 라이브 · {as_of}까지 임포트 · 이후 N회 라이브"; metric caption adds "· 라이브 시간 추정" on the time metric) when `live_streams > 0`. Front-only, no contract change (`generate:types` = 0 drift). front #200 / workspace #434.

**Prod-verified**: live tail union shifted totals **3,215 → 3,252 streams (+37 live)**; retrospective 2026 +37 **and on-this-day 0→9** (live plays at 06-22 UTC = 06-23 KST land on "today" — emergent win). Estimated-time honesty = a single "추정" marker (not a split bar — RFC Open Q1 lean). Pre-merge CDP click-through (local backend → prod DB) confirmed the toggle has no 재생, hero `≈175 시간 청취 (추정)` / `3,252 회 재생` / `이후 37회 라이브`, 좋아요 source intact, 0 console errors; post-merge prod smoke greps the deployed `ProfileApp.*.js` positive for 임포트(평생+라이브) / 시간 청취 (추정) / 라이브 시간 추정 / 평생 + 라이브. `git log` authoritative.

### FEAT-listening-history-import: `/profile` 분석 버킷 — 임포트(평생) lifetime listening analytics — ✅ 2026-06-23

Imported the owner's Spotify **Extended Streaming History** (GDPR export — the per-stream lifetime ledger, the ONLY source of true play counts + listening time; no Spotify API exposes either) into prod Postgres and surfaced it in the 분석 버킷 as the new **임포트(평생)** primary "favorite" source (좋아요=intent / 재생=recent behavior demoted to secondary lenses). RFC: `docs/rfcs/FEAT-listening-history-import.md` (Status flipped in-progress→done at archive time on explicit owner approval, rule #5; full per-step Decisions log there). 6 steps, one per session, all shipped + prod-verified **2026-06-23**:

- **Step 1** — shared_db V27 `spotify_stream_history` + `stream_import_runs` (BIGINT `ms_played`, dedup `(ts, spotify_track_uri)`, indexes, no IP/UA), prod-applied + tag `v0.24.0` (#44).
- **Step 2** — `myblog_shared_db/scripts/import_streaming_history.py` ($0 local; SSM creds; parse Audio+Video → idempotent `ON CONFLICT DO NOTHING` → `spotify:track:`-prefix-stripped catalog resolution → Spotify `GET /tracks` URI→album lookup → key-sorted ≤20/msg catalog-sync enqueue; ip/platform/conn_country never read). **Prod import**: 4523 rows (4520 music / 3 podcast), 186h lifetime, 80.6% album-resolved; 486 catalog-absent albums enqueued (#45).
- **Step 3** — `--resolve-only` re-run after the SQS catalog wave drained: 861 rows newly resolved → **album coverage 80.6% → 99.7%** (4506/4520; 14-row tail = Spotify-lookup failures); release_date 99.96%, genre 92.7%. **Coverage GATE = PASS** → the gated panels ship. `--resolve-only` stays a recurring owner action.
- **Step 4** — ungated `GET /api/library/stream-history/top-{tracks,artists}?metric=count|time` — SQL `GROUP BY count(*)`/`sum(ms_played)` over denormalized cols (coverage-independent); `StreamRankResponse.unit` (count↔time axis); shared_db pin v0.23.0→v0.24.0 (#88).
- **Step 5** — gated `top-albums` / `genre-distribution` / `era-distribution` / `retrospective` — aggregate by `album_id` in SQL then resolve in Python; era buckets `release_date` by decade (chronological); retrospective per-year + "on this day" via `ts AT TIME ZONE 'Asia/Seoul'` (today = SQL `now()` KST); residual 미분류 carried as `unclassified`/`unresolved` (#89).
- **Step 6** — front `ImportAnalysis` view: lifetime hero (time+plays + `as_of` horizon), count↔time toggle, 트랙/아티스트/앨범(covers)/장르/연대(decade histogram)/회고(per-year + on-this-day). Extends the existing editorial system, not a new look. The pre-merge browser click-through (real local backend → prod DB) **caught + fixed a unit bug** (time-mode gate captions showed raw ms → made metric-aware) that lint + astro-check both passed (#198).

**Prod-verified**: lifetime 172.4h / 3,215 in-scope streams (≥30s, not skipped); top track Burning slow 48×, top artist Kid Milli 274×; genre Hip-Hop 1575 / 미분류 132; era 2020s 2517 plays·129h; count↔time rankings diverge (Radiohead climbs by time — long-track bias, research §3.2). Endpoints live via CloudFront; deployed front bundle greps positive for the import-source strings. PRs: shared_db #44/#45, backend #88/#89, front #196/#197/#198, workspace #425/#426/#428/#429/#430. **Future follow-on (separate RFC)**: multiuser self-serve GDPR upload (S3 presigned PUT → worker async; entangles with FEAT-public-bucket-multiuser per-user scoping). `git log` authoritative.

### FEAT-genre-artist-distribution: `/profile` 분석 버킷 — 좋아요/재생 장르·아티스트 분포 + 분류하기 — ✅ 2026-06-21

A dual-source analysis surface on `/profile`: the owner's Spotify 좋아요(saved) tracks + actual 재생(play) history, each broken down by 장르 / 아티스트 through one shared, source-agnostic chart. Genre resolves `track_genres`(override) → `album_genres`(inherit) → 미분류, with an `uncatalogued/ungenred` breakdown and a 분류하기 button that enqueues catalog-absent albums for catalog sync. RFC: `docs/archive/done/rfcs/FEAT-genre-artist-distribution.md` (closed; full completion notes + decisions there). Status flipped accepted→done at archive time on explicit owner approval (rule #5). Authoritative history: `git log` (shared_db #39 `v0.22.0`; worker #47; backend #79; ws contract #399; front #182). **Prod-verified**: 1,017 saved tracks synced; 71% of *likes* uncatalogued vs 4% of *plays* — the two sources tell different stories. **Post-merge (same day)**: fixed a classify **404** (missing apigateway POST route — GET rides the catch-all but POSTs need an explicit entry), an SQS message-size/worker-timeout (**chunked `send_album_sync` to 20/msg**, backend #80), and an artist-upsert **deadlock livelock** under burst concurrency (**sorted by `spotify_id`**, worker #48); **applied** the EventBridge crons; **미분류 1,017 → 0**. The 분류하기 button later **merged with the `장르 채우기` on-demand trigger** (see `FEAT-genre-autoheal` below). The by-artist/by-genre *grouping* the owner originally meant is the **평론 버킷**'s existing group toggle (`FEAT-bucket-organize`), not this surface.

### FEAT-genre-autoheal: on-demand 장르 채우기 (web button → local poller → existing backfill) — ✅ 2026-06-21

Gives the daily genre backfill a **web trigger** without leaving the laptop. The 분석 버킷 `분류하기` button (`POST /api/library/saved-tracks/fill-genres`) inserts a `genre_backfill_requests` row (shared_db **V25**); a new 5-min local poller (`scripts/genre_heal_poller.py` + `com.myblog.genre-heal-poller.plist`, mirrors `research-poller`) claims it (single-flight via `FOR UPDATE SKIP LOCKED` + the `claimed_at` gate) and runs the **EXISTING** `backfill_genres.py --incremental --label --execute` (S1 mapping → S2 iTunes → S3 LLM). So `claude -p` (S3, Max subscription) **stays local** — the cloud only enqueues (pull, not push; rule #9). Same pipeline as the daily run; **no new genre logic**. Also rescheduled the daily genre-backfill launchd **09:30 → 04:00**. This is the *on-demand* variant of `FEAT-genre-autoheal`; the *cloud iTunes-only* variant stays deferred (the S3 LLM can't run in Lambda). **Prod-verified e2e**: button → 202 → request #1 → poller claim → backfill → the 264 cataloged-but-ungenred saved-track albums all got genres (**장르 대기 264 → 0**). PRs: shared_db V25 #41 + #40 (04:00); backend #81; ws #405; front #185 → **#186 unified `분류하기` + `장르 채우기` into one button** (catalog-sync + genre-backfill in one click). Memory: `reference-sqs-fanout-burst-pitfalls`, `reference-apigateway-post-route-required`.

### FIX-uiux-audit-2026-06-21: prod UI/UX audit fix batch — #418 hydration + fake-stats removal + MED/LOW polish — ✅ 2026-06-21

A `myblog_front`-only batch acting on a same-day prod UI/UX audit (chrome-devtools MCP serial capture of 10 pages × 3 viewports → 3 parallel analysis agents → live-DOM adversarial verification → an 8-group fix-map workflow → one commit per group). 18 findings; no contract/infra/backend. Authoritative history: `git log` (front #181). **2 real bugs**: (1) `/profile` React **#418 hydration mismatch** — `ProfileApp` mounted `client:load` while `layout`/`density`/`theme`/dashboard-`rows` `useState` initializers read `localStorage`/`document` (absent at SSR) so the server tree diverged from the client → switched the island to `client:only="react"` (auth-gated page, no SSR/SEO value); (2) the `/profile` 톱 아티스트 / 장르 분포 / 감상 활동 widgets shipped **hardcoded fabricated seed data** (Vault Engine 412…) to real users → removed from the dashboard registry + default board + add-menu, 통계 tab → 준비 중 placeholder (real aggregation is FEAT-genre-artist-distribution, shipped the same day). **MED/LOW polish**: `/search` first-class on-page input (rendered in a stable position so it never remounts / drops focus / breaks Korean IME) + header-dropdown suppression so the two search surfaces stop overlapping; genres scroll-fade affordance; low-count grids (`auto-fit` + a `--few` modifier) so a lone item stops sitting in a wide void; `/write` mobile toolbar ≥44px tap targets + hide `⌘K` on coarse pointer + editor field labels; loading skeletons (collection + bucket board); `/home` hero/list de-dup + redundant-counter removal; query-aware nav active-state; star-aria rounding; credit-line contrast; **zero "form field id/name" a11y warnings site-wide**; sidebar relabel 들은 앨범→평론한 앨범. **Prod smoke green** (PR #181 comment): live-verified #418 gone (profile console empty), no 샘플 data, `/search` input + IME-safe focus + no overlay + `?q=` sync, `/reviews` footer removed + nav correct, `/home` de-dup + 3-cell, 0 form-field warnings. **Audit note** — three "missing render" findings were disproved as **capture-timing races** (the 1440 fullPage shot fired while the page's data API was still `pending`): `/review/lit/` tracklist, `/collection/` grid, `/genres/` outline all render fine — logged as a reusable lesson (memory `reference-fullpage-screenshot-capture-race`), not "fixed".

### FEAT-editor-buckit: pre-writing editorial commissioning desk (idea report → nightly memo→skeleton → in-app draft) — ✅ 2026-06-18

A `$0` `claude -p` pipeline that turns the owner's review-bucket signal into *what to write next*, shipped in three stages, all prod-live. RFC: `docs/archive/done/rfcs/FEAT-editor-buckit.md` (closed). Status flipped in-progress→done at archive time on explicit owner approval (rule #5). Authoritative history: `git log` (ws #378, #382–#390; shared_db #37; backend #78; front #177/#178).

- **Stage 0 — idea report** (#378): `scripts/editor_buckit.py` — read-only prod bucket export + live WebSearch → dated Markdown deck of catalog-anchored "commission cards" (`docs/editorial/ideas/`, gitignored). Proved out (5 cards, refused to pad).
- **Stage 1 — nightly memo→skeleton** (#382–#387): the "오늘 밤 키우기" (`review_bucket_items.prep_tonight`) checkbox gates an unattended 03:00 `launchd` job (`scripts/buckit_nightly.py` + `com.myblog.buckit-nightly.plist`) that turns each checked memo into an **inventory-shaped** Korean section skeleton (`docs/buckit/<date>/`, gitignored). shared_db V22 `prep_tonight` (#37) + set-only backend item-PATCH (#78) + `MemoWindow` 2-col memo overlay (front #177). **Audit-hardened**: claude is a pure text transformer with NO tools (the agentic-write path was both broken in headless `-p` and a secret-exfil hole); the script does all I/O. Nightly enabled.
- **Stage 2 — in-app draft delivery** (#388–#390): the skeleton also lands as a `status='draft'` post in `/write`'s 임시 저장함 inbox. **Step 6** = `DraftsInbox.tsx` frontend inbox (front #178). **Step 7** = `buckit_nightly.py` mints a `$0` smoke JWT → `POST /api/posts {status:'draft'}` (provenance in `body_mdx`, **not** `Post.extra`; **no** `album_ids`; no contract change) → grow-once stamps `post_id` + clears `prep_tonight`. A 5-lens adversarial re-audit (unsafe→fixed: durable post_id dedup, Korean-`untitled`-slug 409 fix, transport-error catch, collision-safe pairing) + a **root-caused Neon pgbouncer read-only leak fix** (session `SET default_transaction_read_only` poisoned the `-pooler` → backend INSERT 500 → txn-scoped `conn.read_only`; same fix applied to `editor_buckit.py`; see `reference-neon-pooler-readonly-leak`). Verified live: real opus run (94s, `$0`) → 9/9, zero residue.

Owner constraints honored throughout: `$0` only (Max-subscription `claude -p`, no API key / paid Lambda), single-user (no `user_id`), never publishes prose, grounds on **buckets** not the empty review corpus.

> **Follow-up — 2026-06-19 (nightly output contract revised):** Stage 1's nightly output was changed from an **inventory-shaped section skeleton** to a **complete Korean review draft** (prose) — `Checked Memo → Full Review Draft`. The owner now wakes to a finished draft to edit + publish (still `status='draft'`, never auto-published); a too-thin memo yields a short `초안 생성 보류` note instead of a fabricated review. This reverses the Stage-1 "AI does facts, the editor does judgment / don't choose the center" design recorded above. The **no-tool safety architecture is unchanged** (the script does all I/O; `claude -p` has no tools). Live contract: `docs/editorial/buckit-nightly.md` + `buckit-nightly-run.md` (v0.2), inlined into the prompt by `scripts/buckit_nightly.py`. *(Docs/prompt change only — no code-logic change; the next 03:00 run produces the new draft shape.)*

### FEAT-ux-polish-2026-06-16: owner-driven 16-item cross-repo UX sweep (search / research notes / profile / identity) — ✅ 2026-06-16

An owner-driven 16-item UX polish pass across `myblog_front` / `myblog_backend` / `myblog_music`, batching small same-day fixes rather than one structural change. No RFC; git log authoritative. Authoritative history: `git log` (music #45, ws #374, front #170/#171/#172/#173/#174/#175, be #76/#77). **Verification gap (preserved):** the auth-gated UX (palette auto-search, bucket add-modal, now-playing cover, review-edit, research status chips) shipped + lint/build-passed + recall-validated, but was **not yet click-through-verified in a logged-in prod session at archive time** — treat that subset as ship-confirmed, not behavior-confirmed.

- **Search** — unified-search ranking blends relevance + popularity (music #45; the recall gate was validated vs the prod catalog = **0 regression**), plus writer-palette compact layout + auto-search, bucket add-modal scroll-lock + sizing, and header search-box width (front #170).
- **Research notes** — readable notes: status chips + fixed hierarchy + collapsible sections (front #171); wider `/write` reading column in split view (front #174); a rich bucket research-note **reading modal** built on a shared `NoteBody` component (front #175).
- **Profile** — now-playing album **cover** + recent-tracks dedup (be #76 / ws #374 contract / front #172); edit-my-reviews affordance + Library-tab removal (front #170).
- **Identity (FEAT-bucket-identity Direction A)** — Pitchfork-style footer + the **LOWFREQ persona removed** (`김저음` / `@lowfreq` / "혼자 쓰는…" → `buckit`; front #170); bucket naming unified on `버킷` (front #173) + the `is_done` 409 message aligned to `평론 완료` (be #77). Directions B/C carried forward in the open `docs/rfcs/FEAT-bucket-identity.md` RFC (B = `NEXT`, C deferred).
- **Verified already-correct (no change)** — Item 11 (recent-album accumulation): the durable `spotify_play_events` log already accumulates correctly, so no fix was needed.
- **Deferred out of this sweep** — Item 4 (full home redesign) was split off and shipped separately as **FEAT-home-redesign-v2** (front #176, also 2026-06-16); bucket Directions B/C remain in the `FEAT-bucket-identity` RFC.

### FEAT-home-redesign-v2: full home redesign as Variant B "Refined Editorial" (reader-first, fully public) — ✅ 2026-06-16

The full home redesign (UX-sweep item 4), implemented in `myblog_front` from the Claude Design handoff as **Variant B "Refined Editorial"** — the owner's chosen direction. Reader-first and fully public: the only writer-facing affordance is the header 글쓰기. Supersedes the home UI from FEAT-home-redesign. No RFC (follow-on to FEAT-home-redesign; `git log` authoritative — the predecessor's brainstorm RFC is archived at `docs/archive/done/rfcs/FEAT-home-redesign.md`). Authoritative history: `git log` (ws #376; front #176).

- **Layout** — BNM **feature hero** (big cover + serif italic title + pull quote + stars) → **최신 평론** type-forward list (hairline dividers) → 장르 → 숫자. New `.bk-*` CSS in `global.css`.
- **Removed** — the old dual-lane WriterStrip + drag/reorder module system; deleted the now-orphaned `BnmHero` / `LatestReviews`.
- **Verification** — build + headless screenshot (front #176). The DONE + LIVE state was recorded workspace-side in ws #376 (commit `bc2eafc`, 2026-06-16).
- **Deferred** — the mock's bucket-curation rows (이번 주 / 추천 버킷) are **not** brought to the home. They are reframed as flexible PUBLIC collections (genre / artist / playlist 모음) surfaced on dedicated surfaces (`/collection`, genre/artist pages), per the owner's "keep main clear/general" caveat; tracked as **FEAT-bucket-identity Direction D** (added in ws #376), not started.

### FIX-research-cover-fanout: batched research-status endpoint replaces the per-cover note GET fan-out — ✅ 2026-06-16

Fixed the bucket board firing one `GET /api/research/albums/{id}` per cover on mount (album-count concurrent requests, each pulling a full note just to render a status dot) → hitting the ~10 account Lambda-concurrency ceiling → **503s** → covers/notes intermittently rendering "조사 안 됨" until a refresh re-raced. The fix collapses the fan-out into one batched call. No RFC; `git log` authoritative. Authoritative history: `git log` (be #75 / ws #373 / front #169).

- **Backend (be #75)** — new batched `GET /api/research/status?album_ids=…` → `{statuses:{id:status}}` resolving a whole board's cover dots in one query; rides the GET `/api/{proxy+}` catch-all (no new apigw route). Unresearched albums are simply absent from the map. ws #373 merged the `openapi.json` contract (`Backend_ResearchStatusMapResponse`).
- **Front (front #169)** — dropped the per-cover GET: cover dots now render from the bucket-payload `research_status` seed + this one batched call, with a single batched poll while any album is queued/running; the full note GET fires **only on panel-open**. Also stopped the store from caching a transient failure as a permanent null.
- A Lambda-concurrency raise (account quota) was an optional band-aid, not needed once the fan-out was gone — not applied.

### FEAT-genre-subgenres: tier-1 editorial sub-genre layer — tagging, definitions + read-only genre map — ✅ 2026-06-15

A tier-1 editorial sub-genre layer that links a *review* to precise sub-genres (critical authority + owner-authored definitions) and seeds the edge-graph substrate for later related-review recommendation. Distinct from the machine `album_genres` store: tier-1 is human/editorial, attached at review time, stored per-post in `post_genres`. Supersedes the tier-1 carve-out deferred from FEAT-genre-system. RFC: `docs/archive/done/rfcs/FEAT-genre-subgenres.md` (closed; Status done, all steps prod-live + archived). Status flipped in-progress→done at archive time on explicit owner approval (rule #5). Authoritative history: `git log` (ws #362/#364/#368; shared_db V20/#35→v0.20.0; backend #73/#74; front #162/#163/#164).

- **Step 1 — schema + seed** (shared_db V20, be #73): `genre_edges` + `post_genres` tables (plain `V{N}__*.sql`, no Alembic; `post_genres` keyed by `post_id` FK to `posts.id` per the post-children convention, `genre_edges` ON DELETE RESTRICT). Seeded 211 tier-1 genres under the 12 tier-0 parents + 40 edges (33 `influenced_by` / 7 `related`). Dry-run → owner seed review → executed against Neon prod **before** merge (`reference-shared-db-cross-repo-rollout`); verified on prod (tier1=211, tier0=12, post_genres=0). Step-1 trims: dropped generic "Soundtrack" node, folded House sub-forms, dropped Djent/Drumstep, K-Pop = idol-pop + ballad (palette 220→211).
- **Step 1b — backend tree/edges API + ORM** (shared_db #35→v0.20.0, be #73, ws #364): ORM models, `GET /api/genres/tree` returns tier-1 nodes + edges, editorial tagging via the post create/update payload `genre_ids` → `post_genres` (no separate publish-payload field). `/api/genres/tree` = 12+211+40 live.
- **Step 2 — writer picker** (front #163): `GenrePicker` in the `/write` SettingsPanel — flattens the 211 tier-1 (+12 tier-0) under their roots → removable selected pills + type-to-filter search (flat results w/ parent breadcrumb) + parent-grouped browse list with ✓ on-state. `WriterApp.genreIds` rides the post create/update payload `genre_ids` → `post_genres`.
- **Step 3 — review-page tags + definitions** (be #74, front #163): the publish service derives `subgenres: [{slug,label}]` from `post_genres` (`derive_subgenres`, DB-not-request, shared by publish + restore) and bakes it into the MDX frontmatter (top-level, after `tags`); `content.config.ts` + `review/[slug].astro` render distinct bordered chips in the hero, separated from the machine tier-0 `musicReview.genres` line. **No contract change** (subgenres derived, not a request field). On-demand definitions deferred to authoring time (mostly empty today, Claude-Code-driven).
- **Step 4 — `/genres` ego-view + taxonomy viz** (front #164): A/B/C layout switcher — **A** Outliner, **B** Miller columns, **C** horizontal dendrogram (pan+zoom) — each sharing one per-node ego-view (definition + 상위/영향/관련 diagram). **Read-only** (owner-confirmed 2026-06-15; the old inline-edit path dropped — definitions are Claude-Code-authored). Review-volume-independent, so kept here rather than deferred.
- **Prod smoke**: `/genres` renders 12+211+40 live; `genre_ids` tagging round-trips (`post_genres` persisted, replace-set on update); rendered chips + UI confirmed in deployed bundles. Verified via CDP click-through + prod e2e + screenshot of the rendered chips.
- **Deferred → FEAT-genre-recommendation** (`docs/rfcs/FEAT-genre-recommendation.md`, **Frozen**): old Steps 5–6 (related-review recommendation + owner edge-authoring UI) split out, gated on real review volume — nothing to rank until reviews accrue. This RFC produced the recommendation's data (edge graph + per-review tags) but not the recommendation block.

### FEAT-genre-deepen: deepen the genre taxonomy to 4 tiers + a dense edge graph — ✅ 2026-06-15

Owner-directed ("촘촘하게 / 훨씬 더 깊이") reversal of the `FEAT-genre-subgenres` single-parent **2-tier** decision: the taxonomy was deepened to **4 tiers / 1,230 genres** (was 223) and the relationship graph densified from a sparse 40 edges (0 nodes with 2+ edges) to a real influence/related web. Containment stays single-parent (a clean tree); cross-cutting links live in `genre_edges`. Everything additive — the existing 12 tier-0 + 211 tier-1 rows + the 40 V20 edges are untouched. Recommendation stays Frozen. RFC: `docs/archive/done/rfcs/FEAT-genre-deepen.md` (closed). Authoritative history: `git log` (ws #370/#371; shared_db #36; front #165).

- **Result (prod-live)** — **1,230 genres**: 13 top-level (added **Blues** as the 13th, the root ancestor of rock/soul/R&B), 246 tier-1, 780 tier-2, 179 tier-3, 12 tier-4. **2,064 edges** (1,434 influenced_by / 590 related); **874 nodes now carry 2+ relationships** (was 0).
- **Research** — a 13-agent workflow: one taxonomy expert per top-level branch deepened it to tier 2/3/4 (RYM/Wikipedia/AllMusic/Discogs-grounded, real genres only) and proposed intra/inter-branch edges; a synthesis pass added the cross-branch edge web + flagged issues.
- **Cleanup** — merged 6 duplicate concepts; added the **Blues** branch (Delta/Chicago/Electric, Jump, Boogie-Woogie, British, Blues-Rock, R&B-classic→Doo-Wop …) + Kwaito/Calypso/Soca/Rare-Groove; reparented Disco (→ Electronic, was buried under dance-pop) / Riddim / Trip-Hop-UK; dropped dangling edges. ~45 cross-branch slug collisions deduped first-wins (acceptable).
- **Migration (shared_db V21, #36)** — data-only, no DDL; tier-ordered `INSERT … ON CONFLICT DO NOTHING`. Validated against prod in a rolled-back tx (1230/13/2064, all joins resolve) before the real apply (psql `BEGIN…COMMIT`); prod = 1230/13/2064. CI schema-parity pass (data-only).
- **Frontend N-tier (front #165)** — `gm-model.buildDoc` recurses the full forest; TreeA expands at any depth; TreeB drills (col-2 re-roots) + breadcrumb; TreeC is a recursive depth-as-column dendrogram; the `/write` `GenrePicker` browse is a collapsible tree (roots collapsed → lazy-expand) so 1.2k nodes never paint at once. Verified via headless CDP on a 4-tier mock; lint 0 / astro check 0 / build.
- **Prod smoke** — live `/genres` renders "13개 최상위 장르"; expanding Electronic → House → Deep House surfaces **Balearic House (tier-3)** at depth-3. Deep nodes show 0 albums (nothing tagged yet — populated organically). Frontend ships before the V21 apply by design (renders whatever depth the API has; no degraded window).
- **Deferred / caveats** — the related-review recommendation (`FEAT-genre-recommendation`, old subgenres Steps 5–6) stays **Frozen**, gated on review volume, unchanged here. Definitions remain on-demand / Claude-authored; most are empty.

### FEAT-genre-autoheal: daily launchd genre backfill self-heals new albums (`--label`) — ✅ 2026-06-15

Fixed the daily genre backfill so newly synced albums no longer accumulate genre-less. The launchd job ran `shared_db/scripts/backfill_genres` as `--incremental --execute` (no `--label`), which took the cached-decisions branch: it **replayed a frozen `decisions.json`** every day (06-13 & 06-14 logs identical — `982 albums marked`) and never re-labeled albums synced since, so every new album stayed bare. No RFC; `git log` authoritative. Authoritative history: `git log` (workspace #353, #355) + shared_db #33. Memory: `reference-genre-backfill-stale-decisions-replay`.

- **Local fix (DONE + LIVE)** — the plist now runs `--incremental --label --execute` (shared_db #33), so each run rebuilds decisions from the current marker-less catalog and then executes, instead of replaying the cached `decisions.json`. The installed copy was reloaded + kickstart-verified (re-labeled the one marker-less album → **prod 1122/1122 marked, 0 bare**). The local daily job now self-heals — laptop-dependent (fires 09:30 when awake; launchd catches up after sleep).
- **Scope** — tracking + contract V19 landed in ws #353; the local self-heal fix + cloud-deferral reframe in ws #355. Docs-only in the workspace; the labeling-mode change itself is shared_db #33 (service repo). Never formally accepted → ran from a plan.md row straight to the prod-verified local fix.
- **DEFERRED (NOT done) — full laptop-independence via a cloud iTunes-baseline heal.** Owner-deferred, **not scoped/accepted**, hung behind the daily album-ingest batch (worker EventBridge) while the local launchd LLM pass stays the low→high / K-Pop-arbitration upgrade. **Blocker**: the S3 LLM step shells `claude -p` on the Max-subscription token (`~/.claude/.credentials.json`), absent in Lambda → a cloud pass would be iTunes-only and must use a DISTINCT partial marker so it never excludes the LLM pass.

### FEAT-global-search: unified 전역 검색 (평론 first-class) replacing Pagefind — ✅ 2026-06-15

Unified all music search across the site under one 전역 검색 design with 평론 (reviews) as a first-class result type, and retired Pagefind end-to-end. No RFC; `git log` authoritative. Shipped in two front PRs over four steps. Authoritative history: `git log` (front #159 = Step 1, front #160 = steps ②③④; workspace docs commits ws #359 / #360).

- **Step 1 — public `/search` page** (front #159): standalone public search surface with 평론 as a first-class result type. Done + prod-live (ws #359).
- **Step ② — header instant-dropdown** (front #160): a `HeaderSearch` instant-dropdown island (DB-only public search + a build-time 평론 index `reviewIndex.ts`) that replaces Pagefind **end-to-end** — Pagefind deps/script/CSS, the `data-pagefind*` attributes, and the deploy step all removed, and `/pagefind/*` purged to 404.
- **Step ③ — writer `CommandPalette` re-skin** (front #160): re-skinned onto the shared `ResultRow`.
- **Step ④ — bucket `AddAlbumModal` re-skin** (front #160): re-skinned onto the same shared `ResultRow`. The shared row supports `action: navigate|button|static` plus `is-current`/`is-present` states; interactive Spotify rows are **no longer dimmed**, and the bucket "담김" (already-added) action is disabled from existing ids.
- **Prod smoke** (front #160 / ws #360): header dropdown live with real data — for "저스디스" returned 평론 1 / 아티스트 3 / 앨범 3 / 트랙 4, with artist→album recall firing.

### FEAT-artist-page: public `/artist/[id]` critic's-lens hub (+ review/search link-in) — ✅ 2026-06-15

Public per-artist hub page at `/artist/[id]` — a "critic's lens" view built from the Claude Design handoff — plus the link-in that makes it reachable from review bylines and global search. No RFC; `git log` authoritative. Authoritative history: `git log` (ws #356/#357/#358 for the hub + plan/contract, ws #359 for the byline link-in; service-repo PRs music #43 / front #157 / front #158 live in their own repos and are not in the workspace log).

- **Hub page (music #43 / front #157 / ws #356/#357)** — new music `GET /api/music/artists/ids` build-enumeration endpoint feeds a static `getStaticPaths` over **805 catalog artists**; each page is a static shell with an `ArtistHub` runtime island (hero + discography + top-tracks) and a build-time review overlay pulled from the blog collection (LIT review surfaced when the artist has one). Contract for `artists/ids` shipped with the plan row (ws #356). Prod smoke: 저스디스 hub renders a real hero + LIT review + discography + top-tracks; **805 pages served**; a non-catalog UUID 404s.
- **Link-in (front #158 / ws #359)** — review byline now links the artist name through to the hub (front #158); search→artist navigation went live via `FEAT-global-search` ② (the header instant-dropdown / artist→album recall path), so the hub is reachable from both reviews and global search.

### FEAT-track-play-history: durable TRACK-level append-only play log — ✅ 2026-06-15

A durable, append-only TRACK-level listening log (`spotify_track_play_events`, V19), the track-grained counterpart to the existing album-level `spotify_play_events`. The worker records every played track from the recently-played payload, SAVEPOINT-isolated inside the listening-sync transaction so a track-log write failure can never abort album sync. No RFC; `git log` authoritative. Authoritative history: `git log` (ws #353, #354; shared_db #32, worker #46).

- **Schema (shared_db #32, V19)** — `spotify_track_play_events` append-only table: `spotify_track_id` + resolved `track_id` (→ `tracks`, `ON DELETE SET NULL`) + `album_id` (→ `albums`, `ON DELETE SET NULL`) + `played_at` + `source` + `recorded_at`, `UNIQUE (spotify_track_id, played_at)` for idempotent re-ingest, plus a `played_at` index. V19 prod-applied to Neon before dependents merged.
- **Worker (worker #46)** — the listening-sync path also appends each played track to the new log, **SAVEPOINT-isolated within the same sync transaction** so the track-log INSERT is contained: a failure there rolls back only to the savepoint and never breaks album-level sync.
- **Workspace (ws #353/#354)** — plan.md tracking + `docs/contracts/schema.sql` regenerated to V19 (#353); done+live flip and in-flight row drop (#354).
- **Prod verification** — V19 applied to prod, prod smoke **50/50 rows** with `track_id` + `album_id` resolved.

### FEAT-public-bucket-multiuser (Scope A): public read-only bucket viewer — ✅ 2026-06-15

Scope A of the public-bucket RFC, shipped end-to-end + prod-verified (A1–A5). Opt-in per-bucket `is_public` lets the owner publish curated review buckets to a no-auth `/collection` viewer; the previously-unauthenticated `GET /api/buckets` board was closed behind a JWT. The RFC stays open in `docs/rfcs/` for **Scope B** (multi-user accounts — not started). Authoritative history: `git log` (shared_db #30/#31, be #69–#72, ws #346–#351, front #154–#156).

- **A1** shared_db V18 (#30) — `review_buckets.is_public BOOLEAN NOT NULL DEFAULT false`; applied to Neon prod, 5 existing buckets default private.
- **A2 + A5** backend (be #69) — `GET /api/buckets/public` (whitelisted, flat; `is_public=true` + `kind='review'`, spotify_library excluded) + `PATCH /api/buckets/{id}` accepts `is_public` (refuses spotify_library → 400); `GET /api/buckets` now JWT-gated (closed the pre-existing unauthed board exposure). Prod smoke: public=200, board=401.
- **A3** front (#155) — `/profile` per-bucket 🌐 공개 toggle in `BucketCard` (optimistic + `PATCH`, mirrors `setColor`; hidden for spotify_library). Verified via headless CDP + prod real-auth e2e.
- **A4** front (#156) — read-only `/collection` viewer: slim `AlbumGrid` (no DnD/edit) client-fetching the public endpoint, graceful empty state, sitemap-included, header+footer link. Verified via CDP (mocked + real endpoint) + prod e2e (publish throwaway → renders → delete → empty).
- **Cleanup** — shared_db `v0.19.0` tag (#31) + backend repin to the tag (be #71) resolving the be #70 SHA hotfix; backend full-suite **test-ordering pollution fixed** (be #72, 13 failed → 217 passed). Rollout incident logged in the RFC (be #69 shipped `is_public` on pin v0.18.1 → 500 → hotfix #70).

### FEAT-home-redesign: editorial two-lane home (reader module stack + auth-gated writer strip) + `/canon` — ✅ 2026-06-14

Replaced the bare `ReviewsIndex` home (`/`) with a magazine-style editorial front page: a public reader lane (personalizable drag-reorder / hide-restore module stack, localStorage) plus a thin auth-gated writer strip pinned under the header, all rendering honestly on real data only and degrading gracefully at near-zero content (posts were reset to 0 in STAB-5). Brainstorm RFC (one feedback round, never formally accepted — ran from draft to ship); status flipped draft→done at archive time on explicit owner approval (rule #5). RFC: `docs/archive/done/rfcs/FEAT-home-redesign.md` (closed). Authoritative history: `git log` (front #151 + #153; ws #343/#344/#346). Front-only — no backend/API/contract/infra change.

- **Editorial home, steps 1–5 + `/canon`** (front **#151**, `746591c`) — `home/EditorialHome.tsx` (auth-gated writer strip + reader module stack behind a 홈 편집 toggle, drag/keyboard reorder, hide/restore, empty state), `home/BnmHero.tsx` (Best New Music hero, 30-day rolling window, self-hides empty, stars-only), `home/LatestReviews.tsx`, `home/BrowseGenres.tsx` (genre teaser **kept separate** from the full `/genres` Outliner, single source `/api/genres/tree`), real "By the numbers" honest counts replacing the placeholder, an editorial `components/footer.astro` rewrite, and the `/canon` route (`pages/canon.astro` + `components/canon/CanonPage.tsx` — computed ★4.0+ canon hall, no ranking/numbers, year-end placeholder; owner decision D5). Reviewer pass 0 HIGH / 3 MED (all fixed). Auto-deployed, **prod smoke PASSED** on `https://www.ratemymusic.blog/` and `/canon`.
- **`/blog → /review` prefix migration, Step 1** (front **#153** / ws **#344**) — **extracted 2026-06-14 to its own RFC** `docs/rfcs/FEAT-blog-to-review-migration.md` (still open). Read page moved to `/review/[slug]` (singular — D9, avoids colliding with the surviving `/reviews` browser), `/blog/category` removed, old `/blog/*` URLs kept resolving via static no-404 redirect stubs (static S3-website model → no request-time 301), internal-link + sitemap sweep. **Prod smoke PASSED, zero 404s** (`/blog/lit/`, `/blog/`, `/blog/category/…` all HTTP 200 and forward correctly; `/review/lit/` renders). **DEFERRED — Step 2** (optional true HTTP 301 via a console-managed CloudFront Function, owner-applied) remains open under that RFC.
- **SUPERSEDED:** the home UI shipped here was **later replaced by FEAT-home-redesign-v2** (Variant B from the Claude Design handoff, **front #176**; recorded ws #376). Of this feature, only the Step-1 `/blog → /review` migration remains live; the editorial-home modules above were the prior generation.
- Decisions baked into the build: genre teaser + full `/genres` page kept separate; BNM = 30-day rolling (daily-rebuild EventBridge cron noted as a follow-up); stars only, never a numeric score to readers. No schema/contract/front-data-model change.

### FEAT-genre-confidence-arbitration: single-source `album_genres` low→high corroboration — ✅ 2026-06-14

Fixed the single-source `album_genres` lows that surfaced as "장르 없음": Spotify's Nov-2024 artist-`genres` deprecation killed the S1 mapping voter, so most Western albums fell to iTunes-only → 1 vote → `low`. Executed as **surgical prod data writes** (dry-run-then-execute, `low→high` only, never an override) plus a durability bake into the canonical pipeline. **Never formally accepted** — ran straight from a draft spike to prod fixes; dropped from the Backlog as functionally complete per the plan.md drop-when-done convention. Authoritative history: `git log` (workspace #333–#337) + shared_db #29.

- **Phase 1 (re-run, no code)** — re-corroborated 103 stale albums (artist `genres` repopulated after the 2026-06-12 backfill) against current `artists.genres`, the canonical `ON CONFLICT low→high` rule: **102 albums / 146 rows upgraded** (catalog low-only 338→236; e.g. NOWITZKI→Hip-Hop, SOS→R&B-Soul).
- **Phase 2 (catalog-wide corroborator)** — `claude -p` single-best-label over all 225 genuine single-source albums (artist `genres` empty): **135 agreed → upgraded to high**, **90 disagreed → left at iTunes-low** (corroborator-only, no override — safe for unverifiable/ambiguous artists), 0 fails. Plus a manual correction of the famous-wrong handful (Blonde + channel ORANGE → `R&B-Soul:high`, source `llm`; wrong iTunes `Pop` kept low, high wins via labels_map). Net: **crate low-only 12→0, catalog low-only 338→91**, 960 albums carry a high genre.
- **Durability (shared_db #29, v0.18.2)** — single-source corroborator baked into the canonical `shared_db/scripts/backfill_genres.combine_album`: a new XOR (mapping/iTunes, no K-Pop) mode raises the lone-source label low→high **iff the LLM's PRIMARY pick (`genres[0]`) agrees**; never overrides or adds labels. `need_llm` extended to single_source; `ALBUM_PROMPT` now requires `genres[0]`=primary (guards against an LLM secondary cementing a wrong high). 7 new unit tests, full suite 52 green. Scripts-only (wheel `src/` unchanged → no service pin bump). **Future album syncs now self-correct** instead of regressing to single-source lows; the 91 residual lows are non-crate obscure/ambiguous albums left at iTunes-low by design.
- No schema/contract/front change (`album_genres.confidence` already existed; front already low-fallbacks). Built on `FEAT-genre-system` (DONE) + the `claude -p` `$0` poller path from `FEAT-album-research-notes`.

### FEAT-genre-system: tier-0 genre vocabulary, automated labeling + public genre map — ✅ 2026-06-13

Tier-0 of the genre system: a canonical 2-tier genre vocabulary, automated album labeling, and a public `/genres` page. RFC: `docs/archive/done/rfcs/FEAT-genre-system.md` (closed). Steps 0–7 all shipped + prod-verified 2026-06-13. Only the Outliner view was productionized; ego-view / columns / diagram = tier-1, deferred to **FEAT-genre-subgenres** (Frozen). Never formally accepted→done by hook; status flipped draft→done at archive time on explicit owner approval (rule #5, 2026-06-14).

- **Vocabulary + schema** — `genres` table (2-tier `parent_id` tree) + `album_genres` (album↔genre, `confidence` low/high, `source`). Canonical labeling via `shared_db/scripts/backfill_genres.combine_album` (iTunes + Spotify-artist + K-Pop voters, low→high corroboration).
- **Backend (be #66)** — `GET /api/genres/tree` with per-node `album_count`; owner inline create/edit `POST /api/genres` (JWT).
- **Front (front #145, ws #325/#326)** — public `/genres` Outliner page: real tree, owner inline edit, `album_count` share-bars, dark mode.
- **Prod smoke green**: tree 200 / 12 nodes with `album_count` Σ1,564; `/genres` renders real canon.

### FEAT-album-research-notes: auto AI research notes for bucketed albums — ✅ 2026-06-11

Per-bucket opt-in auto AI research notes for albums (scope modes, web-search-grounded, restart/refine, writer-GUI display). RFC: `docs/archive/done/rfcs/FEAT-album-research-notes.md` (closed). All 5 steps shipped + prod-verified. **Execution backend pivoted 2026-06-11** (owner, subscription-cost not API): primary executor = a **$0 local poller → headless `claude -p`** on the Max subscription; the paid `researchSQS`/`researchWorkerLambda` path (built + prod-applied Steps 1–2) retained **dormant** as the cloud-autonomous upgrade (inert while the backend never feeds the queue + the key stays unset).

- **Step 1** shared_db v0.16.0 / Neon V16 (#25) — `album_research` table + bucket `research_mode`/`research_selected`; applied to Neon test + prod.
- **Step 2** infra (#304/#306) — researchSQS + DLQ, researchWorkerLambda (900s), `myblog/anthropic` secret, research-worker IAM; concurrency via SQS ESM `maximum_concurrency=2` (account reserved-floor is 10). Retained dormant after the pivot.
- **Step 3** $0 local poller (#310) — `scripts/research_poller.py`: poll `queued` → claim → `claude -p` w/ WebSearch/WebFetch → write `result_md`; launchd-automated, prod-verified.
- **Step 4** backend — INSERT-queued-row triggers (no SQS send) + routes + API GW POST route + api.gen.ts regen.
- **Step 5** front (#138) — writer GUI: BucketBoard mode control + per-item checkbox + cover badge/slide-over, `/write` margin rail, shared `ResearchNote` + no-dep markdown renderer.
- **Follow-up fix** `fix/research-cover-badge-status` (ws #313 / be #62 / front #143) — seed `research_status` per album into bucket GET so a done album's "조사 완료" badge paints on first load. Prod smoke: all items carry `research_status`.
- Optional `'all'`-bucket → poller → *rendered*-note visual e2e left to the owner's subscription session.

### FEAT-album-catalog-ingest: scheduled known-artist album ingest (curated, lean catalog) — ✅ 2026-06-10

A source-agnostic worker job that grows the music catalog with **only review-relevant albums** — full-length releases by already-cataloged artists that clear a popularity gate ("famous, or planned-use" — owner's curation bar). A daily EventBridge rule fires `blogWorkerLambda` with `{"job":"album_ingest"}`; the worker rotates through gate-passing catalog artists, sweeps their discographies via `GET /artists/{id}/albums?include_groups=album`, filters/dedups, and feeds novel IDs through the **existing** ingest pipeline (`blogSQS` → `sync_albums_batch` upsert). Not a display feature (new-release surfacing is the separate Frozen FEAT-new-release-feed); no DB migration, no provider change — the cap is curation policy, not a storage wall. RFC: `docs/archive/done/rfcs/FEAT-album-catalog-ingest.md` (closed). Steps 1–3 merged + deployed + prod-verified same day. Authoritative history: `git log` (ws #297/#298 — plus #299 plan-drop, #300 RFC-archive; worker #42/#43 in `myblog_worker`).

- **Step 1 — worker 429/5xx retry** (worker #42): `Retry-After`-aware bounded-backoff retry on the client-credentials `spotify_client.py` (`get_albums`/`get_album`/`get_artists` + the new artist-albums call), mirroring `spotify_user_client` policy (retryable `{429,500,502,503,504}`, max 3 tries). Hardens the existing reactive ingest too so bulk ingest can't leak messages to the DLQ; independently mergeable.
- **Step 2 — `album_ingest` job** (worker #43): new `discover_new_album_ids(session)` is the only Spotify-coupled seam (swap source without touching the pipeline). Eligible artists `popularity >= ARTIST_POP_MIN` (60 → 305 artists today), `SWEEP_ARTISTS_PER_TICK` (30) per tick on a stable cursor (alias-fill pattern) → discography fetch, full-lengths only → keep `release_date >= INGEST_SINCE` (deploy date → **no back-catalog backfill**; reactive candidates path remains the on-demand route for older albums) → dedup via `get_existing_spotify_ids()` → multi-get → drop `ALBUM_POP_MIN < 20` → enqueue ≤ `MAX_ENQUEUE_PER_TICK` (60) at ≤20 IDs/msg (candidates contract). Cap-check against `MAX_CATALOG_ALBUMS` (5,000) short-circuits + warns. Single `WARNING` summary line (prod `LOG_LEVEL=WARNING`). Included a drive-by fix of a nonexistent `spotify.get_album` reference in the DRY_RUN single path. `browse/new-releases` evaluated and **rejected** as a source (live probe: stale 2023…2025-07 only, zero 2026, 73% junk singles).
- **Step 3 — infra EventBridge rule + IAM** (ws #298): `rate(1 day)` rule → `blogWorkerLambda` with constant input `{"job":"album_ingest"}` + matching `aws_lambda_permission` + worker role gains `sqs:SendMessage` (+ `GetQueueUrl`) on `blogSQS`. `terraform apply`: 3 added, no drift; rule ENABLED.
- **RFC accept** (ws #297): owner-accepted mode A new-releases-only with the resolved gate defaults (`ARTIST_POP_MIN=60`, `ALBUM_POP_MIN=20`, sweep 30/day, cap 5,000); status → accepted. plan.md row dropped (ws #299); RFC moved to archive (ws #300).
- **Prod verification (2026-06-10):** one-shot manual invoke `eligible=305 swept=30 discovered=351 fresh=0 novel=0 enqueued=0` — the new-releases-only `INGEST_SINCE` gate correctly rejects back-catalog **as designed** (`enqueued=0` is the expected outcome, not a failure); albums 936→936, DLQ 0→0, ~10 s/tick. All steps prod-live; feature complete.
- **Deferred / caveats:** expected steady-state volume ~150 albums/yr (305 artists × ~0.5 full-length/yr), zero initial burst; a day-0 album below `ALBUM_POP_MIN` self-heals on the next sweep cycle (~10 d), accepted trade-off = up to one sweep-cycle ingest delay. EPs excluded (Spotify tags most as `album_type=single`; editor can still add any EP via search — revisit only if missed EPs become a felt gap). Source-longevity risk: `/artists/{id}/albums` is not on the Feb-2026 removal list and probed fresh, but the app is Dev Mode; if it degrades, the `discover_new_album_ids` seam swaps to `/search`-driven or editor-curated lists with no pipeline change. Backfill later = relax `INGEST_SINCE` (config-only).

### FEAT-spotify-library-sync: explicit two-way Spotify saved-albums Library mirror bucket — ✅ 2026-06-08

A new special `kind='spotify_library'` bucket in the `/profile` BucketBoard that **explicitly** two-way mirrors the owner's Spotify saved-albums Library (`GET/PUT/DELETE /me/albums`, album-level only — no playlists): add an album + press 동기화 to save it to Spotify; remove + sync to delete it **only if MyBlog added it** (a pre-existing saved album is never deleted). All Spotify I/O runs in the worker (hard rule #9); the backend enqueues + serves cached state, the frontend polls. This is the deferred **D11 follow-up** to `FEAT-member-dashboard` (Spotify write actions), implementing the original 6-point spec's reqs **4/5/6** (reqs 1/2/3 already shipped in `FEAT-member-dashboard-realdata` #287, out of scope). RFC: `docs/archive/done/rfcs/FEAT-spotify-library-sync.md` (closed). Authoritative history: `git log` (ws #288–#291; archived via #292; service-repo PRs shared_db #24, worker #41, backend #60, front #116/#117 live in their own repos).

- **Step 0 — token re-bootstrap (owner precondition)** — owner re-ran `scripts/spotify_bootstrap_token.py --write` to mint a refresh token carrying `user-library-read` + `user-library-modify` (the prior secret was read-only: `user-read-recently-played`/`user-read-currently-playing`). Longest lead, done first; until done every PUT/DELETE 403s missing-scope and rows mark `needs_attention`.
- **Step 1 — shared_db V15** (shared_db #24, v0.15.0) — `V15__spotify_library_sync.sql` (additive, idempotent): two PG enums (`spotify_library_source` myblog_added/preexisting, `spotify_library_state` pending/synced/failed/needs_attention) + `review_buckets.kind` column (default `'review'`, partial-unique single-`spotify_library` index) + a dedicated `spotify_library_albums` **side table** (keyed on `album_id`) carrying immutable `source` + `state` + `in_bucket`/`in_spotify`, separate from `review_bucket_items` so the pre-existing-source fact survives a bucket-remove (req 5). Applied to Neon prod before merge (rule #3). OQ1 resolved: this feature takes V15, genre re-bumps to V16.
- **Step 2 — worker reconcile** (worker #41) — `library_sync_service.py` (raw-SQL style, no ORM pin beyond the applied migration) + new `spotify_user_client` methods (`get_saved_albums`/`check_saved_albums`/`save_albums`/`remove_albums`, chunk ≤ 20) + `{"job":"spotify_library_sync"}` dispatch in `handler.py`. One idempotent pass: read Library → map to catalog (unknown ids enqueue album-sync + skip) → first-touch `contains`-check source stamp → ADD MyBlog-added, REMOVE only `source=myblog_added` dropped from bucket (**never** `preexisting`), PULL pre-existing Library albums into the bucket → re-`contains` verify → `last_synced_at`. Per-album failure never aborts the pass.
- **Step 3 — backend enqueue + state** (backend #60, contract #289) — lazy `get_or_create` of the special bucket; `POST /api/buckets/spotify-library/sync` (202, Cognito-gated, server-debounced) via new `SqsClient.send_library_sync()` + matching `apigateway.tf` route; `GET /api/buckets/spotify-library/state` (edge_guard GET, polled) returns per-album source/state + `last_synced_at` + `needs_reauth`. `openapi.json` regenerated → workspace merge → front `api.gen.ts`.
- **Step 4 — frontend bucket UI** (front #116/#117) — the new bucket renders just below the recently-listened strip (filtered out of the normal tree by `kind`), reusing AlbumChip/BucketCard DnD + `copyAlbum` (add/remove = **bucket intent only, no Spotify call**), a 동기화 button polling `.../state` until `last_synced_at` advances, per-album source/state badges, and a needs-reauth banner.
- **Activation gates (workspace)** — #289 merged contract + apigateway route + bootstrap scopes; #290 (Gate 3) flipped `SPOTIFY_LIBRARY_WRITES_ENABLED=true` + UI banner mirror; #291 plan DONE. **Safety**: the first prod reconcile ran under `DRY_RUN` (logs intended PUT/DELETE sets without executing) for owner CloudWatch review before any real write — the primary guard for the first feature that mutates the real account.
- **Prod verification (2026-06-08):** 동기화 PULLed 3 saved-library albums (`preexisting`/`synced`), `writes_enabled=true`; add-album + 동기화 saves to Spotify; pre-existing albums never deleted (immutable `source` side table).
- **Deferred / non-goals:** auto/scheduled reconcile (sync is explicit by design — no EventBridge rule; unattended writes to the real account are high blast-radius; documented as a future flag); playlists (album-level Library only); multi-user scoping (single-owner, no `user_id` → `FEAT-multi-user-accounts`). OQ3 (un-catalogable Library album loop) left as a UI note "일부 앨범은 다음 동기화에 반영됩니다" now, a miss-counter cap later; OQ4 (large-Library 720s SQS visibility window) mitigated by chunk + self-re-enqueue, threshold deferred to real-Library scale.

### FEAT-member-dashboard-realdata: `/profile` drafts + durable listening — ✅ 2026-06-08

Made `/profile` a real personal dashboard: swapped the remaining sample/cache-only surfaces to durable data and surfaced draft reviews. RFC accepted + shipped same day, all 4 steps end-to-end, prod-verified. RFC: `docs/archive/done/rfcs/FEAT-member-dashboard-realdata.md` (closed; decisions D-A/D-B/D-C + verification there).

- **Goal 5 / concept separation (the organizing idea):** raw cumulative log `spotify_play_events` → **들은 앨범(누적)** archive; rolling cache `spotify_recent_albums` → **최근 들은 앨범**; new rolling cache `spotify_recent_tracks` → **최근 재생 트랙**; singleton `spotify_now_playing` → **재생 중 / 최근 재생**; `posts.status` → **초안 / 평론**.
- **Step 1** shared_db #23 — `V14__spotify_recent_tracks` (track-level rolling cache, denormalized + nullable album FK), bump v0.14.0, tag pushed; **V14 applied to Neon prod** before dependents merged (`reference-shared-db-cross-repo-rollout`).
- **Step 2** worker #40 — `sync_recent_albums` also caches the track window from the same recently-played payload (no extra Spotify call/scope); ON CONFLICT dedup + prune to 50. 158 pass incl. +2 real-engine (idempotent, prune-to-window). Raw `text()` SQL → no ORM pin bump.
- **Step 3** backend #59 + contract #286 — `GET /api/library/recent-tracks` + `GET /api/library/listened-albums` (the archive aggregated from `spotify_play_events`, **D-A: no rollup table**), + `album_cover_url` on `PostListItem` for draft cards. 67/67 relevant green; openapi regenerated.
- **Step 4** front #115 — drafts in 평론 tab (runtime `GET /api/posts?status=draft`, dashed 초안 card → `?id=`), real 최근 재생 트랙 + 들은 앨범(누적) widgets, now-playing 최근 재생 idle fallback; dropped the `RECENT_TRACKS` sample. Built in an isolated worktree off `origin/main` (a concurrent `chore/copy-cleanup` session had merged by then).
- **Prod verification:** backend curl (listened-albums → channel ORANGE ×27 from 220 real `spotify_play_events`; 50 recent-tracks; "LIT" draft carries `album_cover_url`) + a CDP browser click-through of the auth-gated `/profile` (real smoke JWT) confirming all 5 surfaces render real prod data (front PR #115 comment).
- Foundation for `FEAT-ai-editorial-critique`; shares `spotify_play_events` with `FEAT-genre-artist-distribution` (distinct surface). Genre-taxonomy's soft-reserved V14 bumped to V15.

---

## Stabilization (STAB-1..7)

### STAB-1: System stabilization & hardening program (pre-expansion) — ✅ 2026-06-06

- Umbrella RFC from the 2026-06-05 architecture review re-verified against code: 47 real issues across auth boundary / worker-SQS-EventBridge / shared_db-schema drift / repo hygiene / S3-CloudFront / search-observability / side-effecting GET / IAM, plus the final **category → section + review-tags** model decision. Investigation/sequencing only — no code/Terraform/data change in STAB-1 itself; fixes spawned the scoped follow-ups STAB-2..7. Closed once all seven shipped + prod-live → **product-expansion freeze lifted**.
- RFC: `docs/archive/done/rfcs/STAB-1-stabilization.md` (closed). Per-RFC bodies in `docs/archive/done/rfcs/STAB-*.md`; full decisions/verification there + `git log`.
- **STAB-2** — P0 auth boundary, 5 steps (#255–#267): fail-closed app guard → edge_guard JWT verify (500→403) → `POST /api/categories` gate → music `/candidates` auth → `$default`-stage invoke-domain throttle floor. Closed AUTH-3/5/9 + P6-2 (raw-invoke `Bearer x` bypass, unauthed draft exposure, backend fail-open, unauthed music candidates).
- **STAB-3** — blogSQS visibility 30→720s (≥ worker Lambda timeout, kills redelivery-during-processing), applied #256.
- **STAB-4** — schema.sql backfill from prod pg_dump + architecture.md / sqs-doc drift fix (docs-only), #254.
- **STAB-5** — category → section rename (single-FK, in-place V13) + review tags (reuse empty `tags`/`post_tags`), 6 steps (#269–#276): no public create API; prod junk reset (13 posts / 11 MDX / 3 categories all junk → lossless reset to 0 posts / 4 seed sections). shared_db V13 / v0.13.0.
- **STAB-6** — repo hygiene (untrack bundle.zip / worker transcripts / allure-results / `.DS_Store` / `.idea` / `.vscode`) + S3 remote tfstate backend (was local-only with plaintext Cognito secret), #268 (S3-only — DynamoDB lock blocked by IAM, see memory).
- **STAB-7** — necessity-gated IAM least-privilege: removed P7-5 account-wide SQS-consume + P7-1 dead RDS grant, documented out-of-IaC resources, applied #257 (Step 4 / OQ2 observability deferred).

---

## Bug Fixes

### BUG-21: `/profile` 평론 버킷 color reset to default is a no-op — ✅ 2026-06-05 (backend #53)

- `PATCH /api/buckets/{id}` with `{"color": null}` returned 200 but never cleared the color — a bucket color could be set but never reset to the default ink.
- Root cause: `bucket_service.update_bucket` guarded on `if color is not None:`, so the route's `exclude_unset` signal for an explicit clear (`color=None`) was indistinguishable from an omitted field. Fix: sentinel default (`_UNSET`) to tell "color omitted" (rename keeps color) apart from "color cleared" (`None`/`""` → default). Schema/contract unchanged.
- Found via a live prod CDP click-through of the board with a smoke JWT (color set / cover reorder / cross-bucket move / album trash→restore all passed end-to-end; only the color *reset* path was broken).
- Tests: route-contract (`color:null` forwards `color=None`) + real-engine set→clear regression. prod smoke green (`PATCH {color:null}` → 200, GET → null); smoke account `우선순위` color reset.

---

## Feature Work

### FEAT-music-search-recall: writer-facing music search recall re-assessment — ✅ 2026-06-05

- Scope: `myblog_front` + `myblog_music` + `myblog_shared_db` + workspace contract/infra. Goal: lift `/api/music/search/unified` recall to Hit@5 ≥ 0.9 / Hit@1 ≥ 0.6 on a versioned 30-query fixture (multi-token / KO / common-word / typo / alias). **Goal met at Step 4 — Hit@5 = Hit@1 = 1.000 (from 0.600 baseline), prod-verified + flag live.**
- RFC: `docs/archive/done/rfcs/FEAT-music-search-recall.md` (closed). Full Decisions log + OQ resolutions live there.
- **Steps 1–2** — front IME composition fix (front #90) + music versioned 30-query fixture & recall gate (music #35, baseline **0.600**); workspace #235.
- **Step 3 (probe)** — empirical extension decision on the live Neon PG17 engine: pg_bigm / pg_search **not installable** on Neon; **pg_trgm does work on Korean** (syllable-level trigrams, `similarity('방탄','방탄소년단')=0.286`). Inverted the brainstorm's pg_bigm premise (music #230 probe).
- **Step 4** — shared_db V12 `pg_trgm` + GIN indexes + flag (shared_db #20 / v0.11.0 / music #36) → infra flag-on (workspace #236); workspace #237. **0.600 → 1.000.** Gate target met; remaining steps became optional/follow-on.
- **Step 5 — DEFERRED 06-05** (workspace #238): gate already 1.000 + no album/track-alias fixture cases → zero gate benefit; "no new API call" premise false (albums/tracks lack `musicbrainz_id` → ~5,600 MB re-matches, ≈10× the step text); no observed miss signal.
- **Step 6** — music query decomposition (music #37 `e74f977`; single-file, gate held 1.000, precision lift); workspace #239. prod-verified.
- **Step 7** — music `?explain=1` additive `debug` field (music #38 `dfec791`) → contract #240 → front #91 `d64d93f`; workspace #241. prod-verified.
- RFC **accepted 06-05** (owner approval, workspace #242 — also relaxed hard rule #5 to an approval-gated convention). Brainstorm survived a 3-agent critique (architect / reviewer / general-purpose) before draft. **All steps resolved: 1–4/6/7 done, 5 deferred — feature complete + prod-live.**
- Out of scope (tracked): Spotify auto-fallback in `/unified` (rule #9 — would need a separate `/unified_with_fallback` route, Open Question 1); a `search_misses` event table (deferred, V10 `spotify_play_events` precedent — decide on a real miss signal).

### FEAT-member-dashboard: 회원 대시보드 `/profile` (auth-gated 5탭, vertical-slice 멀티스텝) — ✅ 2026-06-04

- Scope: `myblog_shared_db` + `myblog_backend` + `myblog_worker` + workspace contract/infra + `myblog_front`. Cross-repo from Step 2. `/profile` (개요·평론·평론 버킷·라이브러리·통계), built in vertical slices on real data where it existed.
- RFC: `docs/archive/done/rfcs/FEAT-member-dashboard.md` (closed). Full decisions log D1–D31 lives there.
- **Step 1** — frontend shell (front #72). Real reviews + derived stats, sample-labeled placeholders behind one adapter (`src/lib/member.ts`), nested bucket board on localStorage.
- **Step 2** — library tab: to-listen + reviewed (shared_db v0.7.0 / Neon V8, backend, front). New `album_to_listen_items` table + reviewed derived view from `post_albums` (D18); backend `GET/POST/DELETE/PUT /api/library/to-listen` + `GET /api/library/reviewed`, JWT routes. prod e2e (to-listen CRUD+reorder via smoke JWT; reviewed real data).
- **Step 3** — listening history + now-playing + Spotify 연동 (shared_db #17 / v0.8.0 / Neon V9, worker #33, backend #48, front #76, workspace #208). `spotify_recent_albums` + `spotify_now_playing`; worker 1h cron + manual-refresh SQS (rule #9); backend `recently-listened|now-playing|spotify-connection` + `refresh-recent`. Refresh token bootstrapped out-of-band; in-app PKCE deferred (D27). prod smoke green.
- **Follow-ups D28–D31** (multi-agent review PR #210): D28 now-playing drops progress/duration vanity fields (workspace #211 / front #77 / backend #49); D31 server-side debounce + poll-until-advance (worker #34 / workspace #213 / backend #50 / front #78); D29 append-only `spotify_play_events` durable listen history (shared_db #18 / v0.9.0 / Neon V10, worker #35); D30 Spotify token write-back + `needs_reauth` (worker #36 / workspace #216 contract / #217 infra / backend #51 / front #79). Rollout: retry/backoff + symmetric isolation (worker #37), real-album panel (front #80), cron FailedInvocations alarm (workspace #220). All prod smoke green.
- **Step 5** — nested bucket tree + move (shared_db v0.10.0 / Neon V11, backend #52, workspace #223/#224, front #81). `review_buckets.parent_id` → backend nested `GET /api/buckets` + `PUT /api/buckets/{id}/move` (app-layer cycle prevention) + D22 hard-delete cleanup; front DnD board on the API (`src/lib/buckets.ts`), `AddAlbumModal` → `components/member/`, `/reviews/queue` retired. prod smoke green: backend e2e 18/18 + integration 20/20 real-engine; front `/reviews/queue`→404.
- Carve-outs (still draft, Backlog): **FEAT-genre-artist-distribution** (Step 4 — genre/artist 통계, gated on FEAT-genre-taxonomy) + **FEAT-multi-user-accounts** (Step 6 — multi-user/social).
- **NOT retired** (deferred, intentional): flat `PUT /api/buckets/reorder` — the new member `BucketBoard.tsx` still uses it to persist cross-bucket *album* moves (only bucket *reparenting* moved to `/{id}/move`). The RFC's "retire one release after the move route" is gated on first adding a nested-tree-aware item-move route + migrating the front; until then the endpoint stays. (Confirmed by the 2026-06-04 cleanup audit.)

### FEAT-review-bucket-board: 평론 버킷 보드 (review queue kanban + inline review drawer) — ✅ 2026-06-03

- Scope: `myblog_shared_db` + `myblog_backend` + workspace contract + `myblog_front`. Cross-repo, backend tables from the start (no localStorage MVP). All 5 steps shipped end-to-end.
- RFC: `docs/archive/done/rfcs/FEAT-review-bucket-board.md` (closed).
- **Step 1** — shared_db V6 migration + models (shared_db #13). `review_buckets` + `review_bucket_items` tables; V6 applied to Neon main (prod) + test branch **before** the PR merged (per `reference-shared-db-cross-repo-rollout`).
- **Step 2** — backend buckets API (backend #44 → infra #194). `GET/POST/PATCH/DELETE /api/buckets`, item add/update/delete, `PUT /api/buckets/reorder`; all mutations `require_cognito_token`. Single-user model — no ownership checks (buckets are global). Infra #194 added the matching API Gateway JWT routes. Recommendation signals: recency + popularity only.
- **Step 3** — workspace contract merge (#195). Merged `openapi.json` regenerated; front types derived.
- **Step 4** — frontend board UI (front #70). `/reviews/queue` kanban: user-created buckets, intra/inter-bucket drag-and-drop priority, search→add album, click→`AlbumDetailPanel`, optimistic reorder with persist.
- **Step 5** — frontend inline review drawer (front #71). `ReviewDrawer` launched from the detail panel; draft in `localStorage` per item (`bucket-draft:{id}`, isolated from the writer's `lowfreq-draft`); publish reuses `savePost` → `publishToGit`; on success item → `status=published` + `post_id` and the board moves it into the `is_done` ("작성 완료") bucket. `WriterApp` gains `?album=` prefill for "전체 편집기로 열기".
  - Decisions (2026-06-03): `is_done` bucket created on first publish + item moves there; inline draft is front-only per-item localStorage (backend has no `drafting` status flow).
  - Prod smoke: deployed-bundle grep (`qb-drawer-*`, `bucket-draft:`) + a live inline-publish e2e replicating `ReviewDrawer.onPublish()` against prod (smoke-user JWT, isolated throwaway bucket, full cleanup) — 16/16 pass: `POST /api/posts` → `POST /api/publish` (MDX committed) → bucket item `PATCH` linked → `DELETE ?hard=true` removed row + content MDX. Client-side `is_done` move not exercised against the real bucket (Step-4 reorder logic, verified by code read). See front PR #71 comment.
- Deferred (in the archived RFC, not this round): Spotify recently-played / top-tracks recommendation signal — rides the Frozen `FEAT-spotify-personalize-light` when that ships.
