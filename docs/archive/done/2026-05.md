# Completed Work — 2026-05

Archived from `plan.md`. Newest first within each section. Format intentionally loose; this is history, not a working spec.

---

## Feature Work

### FEAT-write-ux-bundle: 추천 트랙 UI (F) + 앨범 상세 확장 (D) — ✅ 2026-05-29

- Scope: `myblog_front` + `myblog_backend` (`post_recommended_tracks` 백엔드는 #31 에서 완성).
- 회의록 `~/.claude/plans/soft-hatching-raccoon.md` 의 F + D 항목. plan row 는 머지 후 드롭됐으나 done.md 기록 누락 → 2026-05-30 청소 패스에서 소급 기록 (git log 가 진실원).
- **PR-1c (F, 추천 트랙 UI)**: front #41 (SHA: 36dc783) — `RecommendedTracksBlock.tsx` 신규 + `WriterApp.tsx` 와이어링, 선택 순서=position, optional note, 최대 5곡. 백엔드 `feat(posts): support recommended_tracks on update + detail load` = backend #31 (SHA: 4b7138b).
- **PR-2 (D, 앨범 상세 확장)**: front #42 (SHA: 84cb909) — 앨범 헤더 label + 트랙별 feat 아티스트. music 스키마 `TrackOut.duration_sec/feat_artist_names`, `AlbumOut.label` 노출.
- Verification: pnpm lint + astro check + dev 작성→발행→`post_recommended_tracks` row 확인.

### BUG-19: writer/global 음악 검색 cross-expansion + per-bucket offset — ✅ 2026-05-30

- Scope: `myblog_music` + `myblog_front`.
- music #28 (SHA: a35dc15): unified search 1-hop cross-expansion + per-bucket offsets (앨범/아티스트/트랙 버킷별 독립 페이징).
- front #43 (SHA: afc5946): writer + global 검색에 feat 아티스트 리스트 렌더. front #44 (SHA: a9ddff2): per-bucket "더 보기" load-more UI.
- RFC: `docs/archive/done/rfcs/BUG-19-writer-music-search-context.md` (closed).

### N (CHORE-ops): scripts/health.sh 운영 가시성 스크립트 — ✅ 2026-05-29

- Scope: workspace `scripts/health.sh` (workspace #126, SHA: 527005d — health.sh 추가 + 완료 plan row 드롭 동반).
- prod DATABASE_URL 로 실행: aliases 빈배열 비율 / `musicbrainz_id` sentinel 비율 / `post_metrics` 마지막 갱신 시각 3개 SQL. BUG-14 잔존행 수렴 확인 등에 재사용.

### FEAT-search-grid-fix: 검색 결과 그리드 — wrap + 가로 스크롤 제거 — ✅ 2026-05-29

- Scope: `myblog_front` (`src/components/search-bar.astro` CSS only).
- RFC: `docs/archive/done/rfcs/FEAT-search-grid-fix.md`. Step 1 만 진행, Step 2 (worker cover index) deferred — `object-fit:cover` 가 시각 일관성 잡음.
- 진단: 이미지 aspect 1/1 + object-fit:cover 는 이미 적용돼 있었음. 진짜 버그는 `.results-row { display:flex; overflow-x:auto; flex:0 0 135px }` → 결과 가로 스크롤 발생. `.results-wrap` 도 `overflow-x:auto` 보조.
- 변경: `.results-row` → `grid; grid-template-columns:repeat(auto-fill, minmax(115px, 1fr)); gap:1rem`. `.results-wrap` overflow 제거. `.card` 의 고정폭 제거.
- Verification (Playwright headless × 3 viewports): 375 → 2cols, 768 → 5cols, 1440 → 9cols. `.results-wrap` 가로 스크롤 없음, 카드 aspect 1:1, console error 0.
- Deploy 회복: PR-3(#38) + PR-39 Deploy 가 `api.gen.ts is out of sync` 로 연속 실패 → hotfix PR-40 으로 회복 (workspace contract 변경 시 frontend 도 `pnpm generate:types` 필요 — [[feedback-frontend-api-gen-sync]]).
- PR: `hyuntohoon/myblog_front#39` (CSS), `#40` (api.gen.ts hotfix).

### FEAT-writer-cleanup: Writer dead 필드 제거 + archive/hard 삭제 플로우 — ✅ 2026-05-29

- Scope: `myblog_backend` + `myblog_front` + workspace `infra/apigateway.tf` + `docs/contracts/openapi.json`.
- RFC: `docs/archive/done/rfcs/FEAT-writer-cleanup.md`.
- 3-PR 시퀀스 + 1 infra 동반 PR:
  - **PR-1** (`hyuntohoon/myblog_backend#30`): DELETE `/api/posts/{id}` → 기본 soft archive (status='archived', 200), `?hard=true` → CASCADE 삭제 (204), PATCH `/api/posts/{id}/restore`, GET `?include_archived=true`. `PostService.archive/restore` + `PostRepository.set_status`. 29 pytest 통과.
  - **PR-1B** (`hyuntohoon/myblog-workspace#116`): `aws_apigatewayv2_route.posts_restore_patch` (Cognito JWT) + `docs/contracts/openapi.json` 머지 재생성. terraform apply 는 human-only (하드 룰 #6).
  - **PR-2** (`hyuntohoon/myblog_front#37`): WriterApp dead 필드 (bestNew/tags/genre/author/authorRole) 제거. SettingsPanel/PreviewView/SubjectBlock 정리. `DraftPersist` 축소 + 구버전 localStorage 키 silent ignore. 23+ / 192- 라인.
  - **PR-3** (`hyuntohoon/myblog_front#38`): `/drafts` → "내 글" 관리 페이지. 탭 (초안 ↔ 보관함), 행 액션 (편집/보관/복구/완전 삭제), confirm 모달, toast.
- Verification:
  - PR-1: pytest 29/29, openapi.json 재생성 (+77 lines).
  - PR-2: pnpm lint clean, astro check 0/0, Playwright e2e (dead 필드 부재 + console error 0).
  - PR-3: pnpm lint clean, astro check 0/0, Playwright e2e (탭/보관/복구/완전삭제/모달 흐름 + console error 0).
  - Prod smoke 27/27 PASS (DELETE 두 분기 + PATCH /restore 두 분기 라이브 검증). PATCH /restore route 는 terraform apply 완료 (route id `31j0uzr`); 2026-05-30 smoke 재실행에서 archive→restore→re-archive→hard 흐름 4 케이스 모두 PASS.
- Rollback: 각 PR 독립 revert. 보관행은 status='archived' 로 남아 PATCH/SQL UPDATE 로 복구 가능. 스키마 변경 없음 — enum 활용.

### BUG-20: prod SEO meta points to localhost (canonical/og/sitemap broken) — ✅ 2026-05-30

- Scope: `myblog_front` only.
- 진단: prod `www.ratemymusic.blog` 의 `<link rel="canonical">`, `<meta og:url>`, `/sitemap-index.xml`, `/sitemap-0.xml` 가 모두 `http://localhost:4321/...` 로 나가고 있었음. `/robots.txt` 404. 원인은 `src/constants.ts` 의 `SITE.url = 'http://localhost:4321'` 하드코딩 — 빌드 시 `@astrojs/sitemap` 과 `seo.astro` 가 그대로 사용. 동시에 `og:type` 가 모든 페이지에서 `"website"` 하드코딩 (블로그 포스트는 `"article"` 이어야 함), sitemap 에 `drafts/write/admin/test` 사적 경로 포함.
- 발견 경위: CHORE-seo-audit Step 0 으로 시작했으나 조사 결과 missing essentials 가 아니라 active correctness bug 였음 → BUG-20 으로 격상, CHORE 행 흡수.
- 변경 (myblog_front #45, SHA: 6e0a8ad):
  - `src/constants.ts`: `SITE.url` → `https://www.ratemymusic.blog`
  - `astro.config.ts`: `sitemap({ filter: page => !/\/(?:drafts|write|admin|test)\//.test(page) })`
  - `public/robots.txt` 신규: `Allow: /` + `Disallow: /admin/ /drafts/ /write/` + sitemap 위치
  - `src/components/seo.astro`: `type` prop 추가 (default `'website'`)
  - `src/pages/blog/[slug].astro`: `<Seo type="article">`
- Verification:
  - Local: `pnpm lint` clean, `pnpm exec astro check` 0/0/0, `astro build` 후 `dist/sitemap-0.xml` (16→12 entries, 전부 prod URL), `dist/robots.txt` 포함, sample post canonical/og:type 확인.
  - Prod smoke 머지+deploy 후: `/sitemap-index.xml` → `https://www.ratemymusic.blog/sitemap-0.xml`, `/sitemap-0.xml` 12 entries 전부 prod URL + `drafts/write/admin/test` 제외, `/robots.txt` 200, sample post `/blog/untitled/` canonical & og:url → prod, og:type → `article`. 4/4 PASS.
- Follow-up backlog (P3, 별도 트리거 시 행 추가):
  - default OG image (`/og-image.png` 부재 — `defaultOgImage` 참조하지만 파일 없음)
  - JSON-LD (Article / BlogPosting structured data)
  - RSS feed (`seo.astro` 가 `/rss.xml` 참조하지만 미생성 — 생성 or alternate link 제거)
  - 더미 blog entries (`blog/2`, `blog/2211`, `blog/aaadsaaaaaaaaa`, ...) 정리
- Rollback: 단순 revert (5-file 변경, DB/schema/infra 무관).
- PR: `hyuntohoon/myblog_front#45` (코드), `hyuntohoon/myblog-workspace#157` (plan row 등록).

### BUG-14: MusicBrainz hangul alias coverage — Lucene `query=` 전환 + mini-reset — ✅ 2026-05-30

- Scope: `myblog_worker` + workspace (RFC + SQL + plan)
- 진단: prod artists 877행 중 hangul-name 236행에서 mbid not_found 비율 62% (비-hangul 27% 대비 2.3배). 원인은 `musicbrainzngs.search_artists(artist=name)` 가 MB 의 primary/sort name 인덱스만 조회 — 한글 alias 인덱스를 미커버. MB API 직접 호출 sample 5/5 가 `alias:<name>` 으로 score=100 hit.
- **Step 1** (myblog_worker #29, SHA: bb50bcb): `artist=` → `query=_escape_lucene(name)` 전환 + `_escape_lucene` helper. Unit 테스트로 escape + 호출 인자 mock 검증. live MB 통합 테스트는 `MB_LIVE_TEST=1` opt-in (기존 `TEST_DB_URL` skipif 패턴 따름).
- **Step 2** (workspace #155): `scripts/bug14-step2-mini-reset.sql` — `musicbrainz_id='not_found' AND name ~ '[가-힣]'` 142행을 NULL 로 reset, 백업 CSV 동시 export (`docs/archive/done/2026-05/bug14-step2-reset-backup-20260530-160352.csv`). EventBridge alias_fill (rate(15 min)) 가 NULL 행만 재시도하므로 sentinel 잔존 행은 노이즈 없음.
- **Step 3 (종료 판단)**: Step 2 reset 26분 후 측정 — 처리된 20행 중 17 resolved / 3 sentinel = **시도 성공률 85%** (≥80% RFC 기준 부합). EventBridge 가 1 사이클당 ~20행 처리하므로 잔존 122 NULL 은 후속 사이클에서 동일 성공률로 흡수될 가능성 높음. "시도하면 풀린다" 가 입증돼 종료 — 122행 잔존은 EventBridge 자연 처리에 위임. backlog 방향 (1) latinized fallback / (3) 매핑 테이블 는 잔존 패턴이 일관되게 남으면 별도 RFC 로 재진입.
- Verification:
  - Step 1: worker pytest 통과 + post-merge prod EventBridge 1-2 사이클 정상 (cross-check / tiebreaker 거절 로그 폭증 없음).
  - Step 2: pre-reset NULL=0 sentinel=142, post-reset NULL=142 sentinel=0 (mini-reset 즉시 측정).
  - Step 3 측정 (2026-05-30 16:30 KST): NULL=122 sentinel=3 resolved=111 (기준점 16:04: NULL=142 sentinel=0 resolved=94). Δresolved=+17, Δsentinel=+3 = 처리 20행 중 17 회복.
- Rollback: Step 1 PR revert (코드 1줄+helper). Step 2 는 백업 CSV 로 복구 가능 (`UPDATE ... SET musicbrainz_id = 'not_found' WHERE id IN (...)`).
- PRs: myblog_worker #29 (SHA: bb50bcb), workspace #154 (RFC open Q 해소), workspace #155 (Step 2 SQL).
- RFC: `docs/archive/done/rfcs/BUG-14-mb-hangul-alias.md` (Status: closed).
- Backup CSV: `docs/archive/done/2026-05/bug14-step2-reset-backup-20260530-160352.csv` — 142행 원본 ID/mbid 매핑 (rollback 자료).
- 교훈: ≥80% 기준 평가 시 *분모* 정의 명확화 필요 — "처리 시도 성공률" 과 "절대 흡수율" 은 EventBridge throughput 에 따라 크게 갈림. 본 건은 시도 성공률 기준으로 종료. ([[feedback-alias-search-availability]] sentinel-on-reject 게이트는 그대로 작동 — Step 1 변경은 후보 union 만 확장)

### BUG-18: alias_fill fetch-side MBID uniqueness pre-check — ✅ 2026-05-29

- Scope: `myblog_worker` (+ workspace RFC)
- 진단: BUG-17 (per-row UPDATE) + BUG-15 (cross-check) 머지 후에도 alias_fill 매 사이클 같은 3행 (`j-hope` / `Kim Tae Hoon` / `dj friz`) 이 `ORDER BY spotify_id` head 자리만 차지하며 같은 false-match MBID 를 받아옴 → BUG-13 partial UNIQUE 에서 UPDATE rollback → row 가 NULL 상태로 남아 다음 사이클에 동일 반복. cross-check 가 못 거른 이유: 해당 3행 `genres` 가 `[]` 또는 한국어 localized (`_COUNTRY_HINTS` 영문 needle 과 불일치, [[project-prod-artists-genres-korean]]).
- 변경 (myblog_worker #22, SHA: 1d87cf2):
  - `worker/clients/musicbrainz_client.py:fetch_artist_mbid_and_aliases` 에 `is_mbid_taken: Optional[Callable[[str], bool]] = None` 추가. 후보 루프 cross-check 통과 직후 pre-check → 이미 점유면 INFO 로그 + `continue` (get_artist_by_id 호출도 절약).
  - `worker/service/sync_service.py:generate_and_save_aliases` 안에 `_is_mbid_taken` closure (`session.execute(SELECT 1)` — BUG-17 cached-conn 회귀 방지). `skipped_precheck` 카운터 + summary INFO ("ok / sentinel / collision / total").
- Verification:
  - Unit: 단위 6 신규 케이스 (musicbrainz_client 3 + sync_service 3) — legacy callback=None / 1st reject → 2nd 채택 / 모든 후보 reject → sentinel / plumbing 가드.
  - **실엔진 통합 테스트 신규** ([[feedback-sa-session-lifecycle-mock-blind]] 의무): `tests/integration/test_alias_fill_session_lifecycle.py` — row1 pre-check 거절→2nd 채택 / row2 모든 후보 거절→sentinel / row3 clean 시나리오. row2 commit 후 row3 pre-check SELECT 가 실엔진에서 깨끗하게 동작하는지로 BUG-17 cached-conn 회귀 차단.
  - Local: pytest 68 passed, 1 skipped (TEST_DB_URL 미설정 시 collection-time skip).
- Step 1 머지 후 CI 실패 → hotfix (myblog_worker #23, SHA: 1f40783): Neon test branch artists 테이블에 `musicbrainz_id` 컬럼 부재 (BUG-13 partial UNIQUE 마이그레이션이 test branch 에 미적용된 schema drift). 통합 테스트 engine fixture 에 information_schema 확인 추가 — 컬럼 부재 시 module 전체 `pytest.skip`. prod 무관 (deploy job 이 needs:test 로 막혀 prod 배포 안 됨, prod schema 정상). 근본 해결 (test branch schema sync) 은 별도 후행.
- Rollback: PR #22 revert (코드 callback 패스/closure 추가 + 카운터). pre-check 자체는 default `None` 분기로 backwards compat.
- PRs: myblog_worker #22 (코드), #23 (CI skip 가드), workspace #122 (RFC draft + plan row), #123 (feedback round), #127 (plan Active 등록), #128 (Status accepted).
- RFC: `docs/archive/done/rfcs/BUG-18-mbid-uniqueness-pre-check.md` (Status: closed).
- 교훈 ([[feedback-sa-session-lifecycle-mock-blind]] 강화): session.commit / connection 경계 바꿀 땐 실엔진 통합 테스트 1개 필수. BUG-17 PR #20 → prod 사망 → hotfix PR #21 의 직접 후속 — 본 PR 은 같은 함수를 다시 만지면서 통합 테스트로 가드.

### BUG-17: alias_fill `executemany` 배치가 첫 UniqueViolation 에서 통째로 롤백 — ✅ 2026-05-28~29

- Scope: `myblog_worker` (+ workspace RFC)
- 진단: `worker/service/sync_service.py:generate_and_save_aliases` 의 단일 `executemany` UPDATE 가 한 행의 `psycopg.errors.UniqueViolation` (BUG-13 partial UNIQUE on `musicbrainz_id`) 으로 10행 배치 전체 롤백. CloudWatch ≥95 ERROR/24h, alias_fill 13/754 NOT NULL 에서 진척 0. 근본 원인은 MB false-match 가 이미 다른 artist 에 박힌 MBID 를 반환 (예: Kim Gwang-Jin → MBID already on Kim Chang-Wan).
- 변경 (myblog_worker #20, SHA: 0f98381):
  - per-row UPDATE + `IntegrityError` 캐치. 행마다 자체 tx 커밋 / collision 행만 rollback + WARNING + skip (sentinel 강제 박지 않음 — BUG-15 widening 후 재시도 여지 보존).
  - SELECT 에 `ORDER BY spotify_id` 추가 — 동일 collision 행이 영구적으로 head 점유하지 않도록 alphabet-late 행은 큐 꼬리로 이동.
  - 신규 INFO 1-line cycle summary ("MB lookup done: %d ok, %d skipped (UNIQUE collision), of %d looked up").
- **Hotfix (myblog_worker #21, SHA: 43dd002)**: PR #20 prod 머지 직후 alias_fill 가 첫 per-row UPDATE 에서 `"This Connection is closed"` 로 사망 (CloudWatch 2026-05-28T16:36:32Z). 원인: refactor 가 `conn = session.connection()` 을 한 번 캐싱한 뒤 SELECT 후 `session.commit()` 호출 → Neon connection pooler 가 connection 회수 → 후속 per-row UPDATE 의 `conn` 핸들이 stale. 해결: SELECT/UPDATE 모두 `session.execute()` 로 전환 (매 호출 session 통해 connection 재해결 → post-commit 호출이 fresh tx 획득). Unit 테스트의 mock target `session.connection().execute` → `session.execute` 갱신, 3 pass 유지.
- 교훈 ([[feedback-sa-session-lifecycle-mock-blind]] 신규 메모리): pure-mock 테스트는 SQLAlchemy session lifecycle 의 connection-pool 의미 못 잡음. `generate_and_save_aliases` 에 실엔진 통합 테스트 부재 — BUG-18 이 이를 정식 추가하며 PR #22 의 의무 게이트가 됨.
- Verification:
  - PR #20: pytest 3 신규 케이스 (1-row collision / all-success / empty-SELECT) — mock 만.
  - PR #21: 동일 3 mock 테스트 pass (mock target 갱신).
  - Prod: hotfix 머지 후 alias_fill 정상 사이클 재개 — CloudWatch 의 connection-closed ERROR 가라앉음. workspace #119 에서 plan row drop 시 prod-verified 명시.
- Rollback: PR #20 + #21 묶음 revert. 양쪽 모두 단일 함수 내 변경, 외부 영향 없음.
- PRs: myblog_worker #20 (코드), myblog_worker #21 (hotfix), workspace #115 (RFC draft + plan row), workspace #119 (plan row drop).
- RFC: `docs/archive/done/rfcs/BUG-17-alias-fill-batch-failure.md` (Status: closed).

### BUG-12: Writer page filter buttons — instant client-side apply — ✅ 2026-05-28

- Scope: `myblog_front`
- BUG-11 회귀. 필터 버튼이 `setFilter` 만 호출하고 끝나 화면 결과 불변이던 문제.
- 백엔드 호출 type 을 `'album,artist,track'` 으로 고정 (상수 `BACKEND_TYPES`). `filterToType` 제거.
- `visibleResults = useMemo(filter==='all' ? results : results.filter(r => r.kind === filter))` → 그리드/카운트 모두 visibleResults 기준.
- 빈 필터 안내 ("현재 필터에 해당하는 결과가 없습니다") + 힌트 문구 `"DB·싱크 공통 적용"` → `"즉시 적용"`.
- **Pre-merge: Headless Playwright e2e 11 PASS / 0 FAIL** (chromium headless + dev server + prod API CORS 프록시) — 4 필터 토글 즉시 반영(album=8, track=6, artist=1), 전체 필터 인터리브 first3=[앨범,아티스트,트랙], 콘솔 에러 0.
- Post-merge prod smoke 22/22 PASS (회귀 가드).
- PRs: myblog_front #35, workspace #96 (RFC + plan row), workspace #97 (plan drop).
- RFC: `docs/archive/done/rfcs/BUG-12-writer-filter-instant-apply.md`.

### BUG-11: Writer page music search — album/track/artist filter not working correctly — ✅ 2026-05-28

- Scope: `myblog_front`
- 글쓰기 페이지 음악 검색의 4가지 버그를 한 PR로 묶어 수정.
- (A) Spotify 싱크가 `data.albums` 만 매핑하고 artists/tracks 무시 → `runSpotifySync` 응답 매핑을 `runDBSearch` 와 동일 구조로 확장.
- (B) 필터 힌트 `"싱크 시 적용"` 부정확 → `"DB·싱크 공통 적용"` (BUG-12 에서 다시 `"즉시 적용"` 으로 갱신).
- (C) Spotify track 클릭 시 nested `album.spotify_id` 미보존 → `TrackSearchResult` 에 `album_spotify_id` 필드 추가, `album_id` 도 nullable. `onPick(track)` 에서 source 별 라우팅: `spotify` 면 `/albums/by-spotify/{album_spotify_id}`, 그 외는 `/albums/{album_id}`.
- (D) "전체" 필터 결과 타입별 편향 → round-robin `interleave(albums, artists, tracks)`.
- Pre-merge: `pnpm lint` + `pnpm exec astro check` 통과. Post-merge: prod smoke 22/22 PASS (`search?type=album|artist|track` + `/albums/by-spotify/{id}` 회귀 가드).
- 후속 회귀: 필터 버튼 onClick 이 `setFilter` 만 호출하고 화면 재반영 로직 없음 → BUG-12 로 별도 후속.
- 교훈 (메모리에 저장): UI 인터랙션 변경은 API smoke 만으로 부족, 브라우저(또는 headless) 클릭 확인 필수.
- PRs: myblog_front #34, workspace #94 (RFC + plan row), workspace #95 (plan drop).
- RFC: `docs/archive/done/rfcs/BUG-11-write-page-search-filter.md`.

### PR-metrics-real: DB-backed `POST /api/metrics/batch` — ✅ 2026-05-28

- Scope: `myblog_backend`, `infra`
- `SqlMetricsRepository` reads `likes` / `comments_count` from `post_metrics` joined to `posts.slug`; `InMemoryMetricsRepository` retained as test fallback. `app/di.py` wires the SQL repo (`_metrics_repo: MetricsRepository = SqlMetricsRepository()`).
- `aws_apigatewayv2_route.metrics_batch_post` (`POST /api/metrics/batch`, no auth) integrated to backend Lambda. Live route id `jwmmbz8`.
- Cognito `admin_client` 상시 drift (5개 OAuth 필드)를 라이브 상태에 맞춰 Terraform 코드 정리 후에야 apply 진행 — 2026-05-27 BUG-1 incident 재발 방지 위해 별도 PR로 분리.
- Verification: post-reconcile `terraform plan` = `1 to add, 0 to change, 0 to destroy`. `terraform apply` = `Resources: 1 added, 0 changed, 0 destroyed`. Prod smoke `POST /api/metrics/batch {"slugs":["432432432432","non-existent-slug-xyz"]}` → `HTTP 200 {"data":{"432432432432":{"likes":0,"comments":0},"non-existent-slug-xyz":{"likes":0,"comments":0}}}` (0/0 = DB row, mock 아님).
- PRs: myblog_backend #25, workspace #77 (Active row), #78 (route IaC), #80 (Active row update), #81 (cognito reconcile), #82 (plan note), #83 (this archive entry).

### PR-12: Post CRUD — drafts 목록/삭제/수정 엔드포인트 신설 — ✅ 2026-05-27

- Scope: `myblog_backend`, `myblog_front`
- `GET /api/posts?status=` — 포스트 목록 (status 필터, Cognito)
- `GET /api/posts/{id}` — 포스트 상세 (category, album_ids, artist_ids 포함)
- `PUT /api/posts/{id}` — 포스트 업데이트
- `DELETE /api/posts/{id}` — 포스트 삭제
- PostRepository/Service: get_by_id, list_by_status, update, delete_by_id
- `drafts.astro`: `{ posts: [...] }` 파싱 수정, 삭제 동작
- `WriterApp`: "Save Draft" → DB 저장, `?id=` 파라미터로 기존 초안 로드
- Verification: `pytest tests/ → 14 passed`; `pnpm lint → pass`; `astro check → 0 errors`
- PRs: myblog_backend #20 (SHA: d61bbe0), myblog_front #17 (SHA: 06ea2f8), workspace #41 (SHA: 52c8d2d)

### PR-11: Rating scale 통일 (0–5, 0.5 step) — ✅ 2026-05-27

- Scope: `myblog_backend`, `myblog_front`
- `rating_scale` 필드를 `WritePostRequest` / `UpdatePostRequest`에서 제거; 내부 `rating_scale=5` 하드코딩
- 프론트: `api.gen.ts`, `WriterApp`, write 스크립트에서 `rating_scale` 제거; 0–5 범위 validation 적용
- workspace `docs/contracts/openapi.json` 재동기화
- Verification: `pytest tests/ → 6 passed`; `pnpm lint → pass`; `astro check → 0 errors`
- PRs: myblog_backend #19 (SHA: 1af813f), myblog_front #16 (SHA: 4b632c0)

### BUG-1: API Gateway missing PUT/DELETE routes for /api/posts — ✅ 2026-05-27

- Scope: `infra/`
- `PUT /api/posts/{id}` and `DELETE /api/posts/{id}` returned 404 — routes existed in FastAPI but not in API Gateway
- Added `aws_apigatewayv2_route.posts_put` and `.posts_delete` with JWT authorizer (consistent with `POST /api/posts`)
- Also applied Cognito `logout_urls` drift: root `/` → `/admin`
- Verification: POST 200, PUT 200, DELETE 204 confirmed against production; full create→publish→delete E2E tested
- PR: workspace #43 (SHA: 85c30cf)

---

## Cross-Repo Refactor & Bug Fixes

### ARCH-12: Contract-first workflow with strict OpenAPI enforcement — ✅ 2026-05-27

- RFC: `docs/done/rfcs/ARCH-12-contract-first-openapi.md`
- PRs: myblog_backend #17+#18, myblog_music #19+#20, myblog_front #14+#15, workspace #36
- Outcome: Single merged `docs/contracts/openapi.json`; CI fails on spec drift in backend/music; CI fails on type drift in frontend; cross-repo trigger via `repository_dispatch`
- Post-merge fixes: `client_payload` JSON object 전달 방식 수정, frontend `_workspace/` ESLint ignore 추가

### BUG: Cognito OAuth 로그인 불가 — ✅ 2026-05-27

- 원인 1: `AllowedOAuthFlowsUserPoolClient: false` (OAuth 자체 비활성화)
- 원인 2: callback URL trailing slash 불일치 (`/admin/callback` vs `/admin/callback/`)
- 수정: AWS CLI로 App Client `68ccmcanfbvla9qbovnb9b18bt` OAuth 활성화 + callback URL 수정

### ARCH-11: Absorb `myblog_publish` into `myblog_backend` — ✅ 2026-05-27

- Step 1: `POST /api/publish` added to `myblog_backend` with Cognito JWT auth — myblog_backend PR #14 (SHA: 5d33bd2)
- Step 2: Frontend cutover — `publishToGit()` now hits `myblog_backend` via `apiFetch` — myblog_front PR #13 (SHA: 440615e)
- Step 3: `publisher-github` Lambda decommissioned; `myblog/publish` secret deleted; API GW route rerouted to backend integration
- Step 4: Docs, ADR 0006, RFC archived — workspace PR #33
- Post-verification fixes:
  - `myblog/backend` secret was malformed JSON (`{KEY: value}` → `{"KEY": "value"}`); `EDGE_SECRET`/`DATABASE_URL` never loaded in prod
  - `edge_guard` blocked API Gateway requests (no `x-origin-verify`); fixed to allow `Authorization: Bearer` requests — myblog_backend PR #15
  - Lambda env vars (`ENV=prod`, `GITHUB_REPO_OWNER`, `GITHUB_REPO_NAME`, `GITHUB_REPO_BRANCH`, `CONTENT_DIR`) missing; recorded in Terraform — workspace PR #34
- Verification: `POST /api/publish` → HTTP 200 `{"ok":true,"slug":"arch-11-smoke-test","path":"content/blog/2026-05-27--arch-11-smoke-test/index.mdx"}`; CloudWatch confirmed invocation on `ratemymusic-api`
- ADR: `docs/decisions/0006-publish-absorbed-into-backend.md`
- RFC: `docs/done/rfcs/ARCH-11-absorb-publish-into-backend.md`

### PR-13: Replace Gemini alias generation with MusicBrainz — ✅ 2026-05-27 (SHA: 4931bc1)

- Scope: `myblog_worker`, `myblog_shared_db`
- `artists.musicbrainz_id TEXT UNIQUE` 컬럼 추가 (Neon 마이그레이션 적용 완료)
- `worker/clients/musicbrainz_client.py` 신규: 이름 검색 → MBID → aliases fetch (1 req/sec, score ≥ 90)
- `generate_and_save_aliases`: Gemini → MusicBrainz 교체, `WHERE musicbrainz_id IS NULL` 필터, 미매치 시 `'not_found'` sentinel
- `GEMINI_API_KEY` 제거, `requests` → `musicbrainzngs>=0.7`
- PRs: myblog_shared_db #3, myblog_worker #16

### BUG-8: musicApi `/candidates` 500 — SQS producer IAM missing — ✅ 2026-05-25

- Scope: `infra/iam_roles.tf`
- Cause: IAC-2 removed `AmazonSQSFullAccess`, taking producer permissions with it. `AWSLambdaSQSQueueExecutionRole` is consumer-only (no `GetQueueUrl`).
- Fix: Added `sqs:GetQueueUrl`, `sqs:SendMessage`, `sqs:SendMessageBatch` inline policy to `LambdaRDSControlRole`.
- Verification: `terraform apply` clean; site sync button returns 200.
- PR: infra #22

### COGNITO-1: Activate JWT validation on `/candidates` — ✅ 2026-05-25

- Scope: `infra/` + `myblog_front`
- Steps: (1) `COGNITO_USER_POOL_ID` added to `musicApi` Lambda env via Terraform; (2) `searchBarDb.client.ts` replaced `getJSON(url)` with auth-header fetch.
- Verification: Logged-in sync button → 200; logged-out → 401.
- PRs: infra #18, myblog_front #7

### PR-1: typo, dead code, import cleanup — ✅ 2026-05-21

- DEBT-2, DEBT-3, DESIGN-2
- Renamed `cadidate_search_service.py` → `candidate_search_service.py`; moved class-body imports to module top; deleted unused `singleflight.py`.

### PR-2: Runtime bug fixes (schema, NameError, model mismatch) — ✅ 2026-05-21 (SHA: fae9397)

- BUG-1, BUG-2, BUG-3 (model only)
- Removed duplicate `)` in `db/schema.sql`; deleted `get_primary_artist_map` from `album_service`; added `unique=True` to `Track.spotify_id`.
- Also: removed duplicate `get_by_spotify_id` definition in `album_repo.py`; removed stale comment in `album_service.get_album_detail_by_spotify`.

### PR-3: Production print/timing cleanup — ✅ 2026-05-21 (SHA: f4a5308)

- DEBT-1, DEBT-4
- Replaced `print()` with `logging` in `album_repo`, `sqs_client`, `candidate_search_service`. Removed timing and forced connection acquisition.

### PR-4: `/candidates` Cognito auth — ✅ 2026-05-23

- Cognito JWT validation via JWKS. Bypassed when `COGNITO_USER_POOL_ID` unset (dev).

### PR-5: CI unit test gate — ✅ 2026-05-23

- INFRA-1 (test gate scope)
- Unit test job gates deploy in `myblog_music`. Integration test (`tests/test_candidates_localstack.py`) marked and excluded from CI gate.
- Follow-up (2026-05-25): exit-5 workaround removed; 8 unit tests confirmed passing — myblog_music PR #14.

### PR-6: Tracks `spotify_id` UNIQUE migration — ✅ 2026-05-25

- BUG-3 migration part
- Script at `db/migrations/001_tracks_spotify_id_unique.sql`. Executed against Neon: 0 duplicates, `uq_tracks_spotify_id` applied `CONCURRENTLY` (zero-downtime).

### PR-7: Worker — SQS error handling + AttributeError + logging — ✅ 2026-05-23 (SHA: a156cfb)

- SEC-3, BUG-4, DEBT-7
- Exceptions re-raised so SQS retries and DLQ fires. Replaced `sync_album_by_spotify` with `sync_albums_batch([album_id], market)`. `print()` → `logging` across `sync_service.py`.

### PR-8: Publish — auth + CORS hardening — ✅ 2026-05-23 (SHA: 7866864)

- SEC-1, SEC-2, REFACTOR-3
- `FRONT_ORIGIN` env replaces `allow_origins=["*"]`; `edge_guard` middleware checks `x-origin-verify`; `load_dotenv` gated on `APP_ENV=dev`.

### PR-9: Backend config dedup + print removal — ✅ 2026-05-23 (SHA: f16ecd7)

- REFACTOR-1, REFACTOR-2, DEBT-6
- Single `Settings` in `app/core/config.py`; `_load_secrets_once()` folded into `get_settings()` via `@lru_cache`; no `print` in `get_settings`.

### PR-10: Frontend API path + rating schema — ✅ 2026-05-23 (SHA: 4a618f2)

- BUG-5, BUG-6, BUG-7, REFACTOR-5
- `/categories` → `/api/categories`; rating max comment corrected; `rating_scale` added to `publishToGit` payload; `apiFetch` typed.

### DEBT-5: `AlbumOut.external_url` missing — ✅ 2026-05-25

- `ext_refs['spotify_url']` → `AlbumOut.external_url` in DB-read paths. myblog_music PR #11, 3 unit tests.

---

### ARCH-6: Shared DB models package — ✅ 2026-05-26 (myblog_shared_db SHA: 7523e92)

- New private repo `hyuntohoon/myblog_shared_db` (`myblog-shared-db` Python package, v0.1.0).
- Canonical SQLAlchemy 2.0 DeclarativeBase models in `src/myblog_shared_db/models.py`; Core Table objects in `tables.py`.
- `myblog_backend/app/models/` (6 files) deleted; `myblog_music/app/domain/models.py` deleted; `myblog_worker/worker/infra/tables.py` deleted (was orphaned).
- All 3 consumers import from `myblog_shared_db` package; `requirements.txt` + CI git-auth steps added to each.
- Schema parity CI in `myblog_shared_db`: stack-based paren parser validates modeled columns against `docs/contracts/schema.sql`.
- ADR 0005 written. All 6 consumer CLAUDE.md files updated. RFC moved to `docs/done/rfcs/`.
- RFC: `docs/done/rfcs/ARCH-6-shared-db-package.md`

---

## Documentation & Architecture

### DOC-1: SQS contract document — ✅ 2026-05-23

- `docs/contracts/sqs-album-sync.md` written, covering the `{album_ids, market}` message format between `myblog_music` and `myblog_worker`.

### DOC-2: Per-repo CLAUDE.md — ✅ 2026-05-23

- Each of the 5 service repos now has a CLAUDE.md covering responsibility, local dev setup, invariants, test commands.

### ARCH-1: Schema single source of truth — ✅ 2026-05-23

- Canonical DDL at `docs/contracts/schema.sql`. ADR 0003 records the decision. Worker upsert fixed to include `ext_refs` in `ON CONFLICT DO UPDATE` (resolves ARCH-5). `aliases` column added to music schema.

### ARCH-2: Spotify client duplication — ✅ 2026-05-25

- Accepted as deliberate duplication. Cross-reference comments added in both files. ADR 0004.

---

## Infrastructure

### P1-E: Secrets Manager migration — ✅ 2026-05-24

- 4 secrets created (`myblog/backend`, `myblog/worker`, `myblog/music`, `myblog/publish`).
- Terraform `infra/secrets.tf` applied — 4 IAM policies.
- All 4 service repos updated: `SECRETS_ARN` pattern, `get_settings()` + `lru_cache`. Plaintext secrets removed from all Lambda env (`DATABASE_URL`, `SPOTIFY_CLIENT_ID/SECRET`, `EDGE_SECRET`, `GITHUB_TOKEN`).
- Follow-up (2026-05-25): `myblog_worker` `DATABASE_URL` missing check + `infra/db.py` lazy init — myblog_worker PR #9, 7 unit tests.

### IAC-1 (core): SAM → Terraform full migration — ✅ 2026-05-25

- `Apply complete! 21 imported, 12 added, 9 changed, 0 destroyed.`
- Imported: 4 Lambdas, 1 event source, 12 API GW resources, 4 log groups
- Added: 9 CloudWatch alarms, 3 consolidated Lambda invoke permissions
- Changed: log retention 0→14d ×4, event source `+ReportBatchItemFailures`, Lambda timeouts (worker 10→120, api/publish 3→15, music 10→15)
- Zero destroy/replace — no downtime.
- Decisions: Lambda config imported and corrected in place; EventBridge included (Gemini); Terraform state kept local in `infra/terraform.tfstate`.

### CICD-1: Frontend CI/CD fix — ✅ 2026-05-25 (myblog_front PR #6)

- pnpm version aligned to v10 (was v9 reading v10 lockfile — silently regenerating).
- Split into `check` (lint + astro check) → `deploy` (build + S3 sync); `deploy` requires `check`.
- S3 `Cache-Control` corrected per path: `_astro/**` immutable, `*.html` no-cache, `pagefind/**` 1h.
- `actions/cache@v4` for pnpm store keyed on `pnpm-lock.yaml`.

### CICD-2: code-review workflow + pyright CI — ✅ 2026-05-25

- `code-review.yml` added in `myblog_front` (PRs #8, #9). Non-interactive Claude Code; skips if `ANTHROPIC_API_KEY` absent.
- pyright baseline added in `myblog_backend` (PR #10) and `myblog_music` (PR #15). `reportAttributeAccessIssue` and `reportMissingImports` off (SQLAlchemy ORM + boto3 false positives); real bug categories kept as warnings.
- `/write` 404 fixed via CloudFront function: `uri + '.html'` → `uri + '/index.html'` (Astro directory format). LIVE published + cache invalidated.

---

## Frontend Redesign

### FRONT-1~5: Pitchfork-style frontend redesign — ✅ 2026-05-25

All phases merged to main. Design language: Lowfreq Magazine editorial (Newsreader italic serif, cream palette, Pitchfork-style rating layout).

| Phase | Content | PR |
|-------|---------|----|
| Phase 0 | Design system (global.css, Newsreader font, color tokens) | front #6 |
| Phase 1 | Navigation (header.astro, sticky + editorial) | front #6 |
| Phase 2 | Main page (hero-review, best-new-music, review-grid) | front #8 |
| Phase 3 | Review detail (review-hero.astro, [slug].astro) | front #8 |
| Phase 4 | Write page (write.astro 2-panel layout) | front #8 |
| Phase 5 | BNM badge + drafts.astro | front #8 |

ESLint cleanup: front PR #9 — 84 errors across Phase 2-5 components (HTML comments in JSX, no-alert, unused vars).

### FEAT-W2: writer search — split DB search / Spotify sync buttons — ✅ 2026-05-25 (front PR #11)

- Restored 2-button UX lost in FEAT-W1 redesign.
- **Search** button → `/api/music/search/unified` (DB-only, public, Enter key)
- **Spotify sync** button → `/api/music/search/candidates` (`apiFetch` + Cognito, 3s cooldown)
- Result tiles show `DB` / `SPOTIFY` source badge.
- `AlbumSearchResult.source?: 'db' | 'spotify'` added.
- Debounce auto-search removed in favor of explicit clicks.

---

## Architecture Review Items (from 2026-05-22 review)

### Frontend ↔ backend type contract — ✅ 2026-05-25

- `PostPayload` now derived from generated `WritePostRequest`. `pnpm generate:types` regenerates from `docs/contracts/openapi-backend.json`. myblog_front PR #5.

### Gemini alias generation decoupled from sync — ✅ 2026-05-25

- Removed from SQS sync path. Runs on EventBridge `rate(15 min)`. LIMIT reduced 20→10 to fit 120s timeout. myblog_worker PR #10.
