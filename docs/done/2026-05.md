# Completed Work — 2026-05

Archived from `plan.md`. Newest first within each section. Format intentionally loose; this is history, not a working spec.

---

## Cross-Repo Refactor & Bug Fixes

### BUG-8: musicApi `/candidates` 500 — SQS producer IAM missing — ✅ 2026-05-25

- Scope: `infra/iam_roles.tf`
- Cause: IAC-2 removed `AmazonSQSFullAccess`, taking producer permissions with it. `AWSLambdaSQSQueueExecutionRole` is consumer-only (no `GetQueueUrl`).
- Fix: Added `sqs:GetQueueUrl`, `sqs:SendMessage`, `sqs:SendMessageBatch` inline policy to `LambdaRDSControlRole`.
- Verification: `terraform apply` clean; site sync button returns 200.
- PR: infra #22

### COGNITO-1: Activate JWT validation on `/candidates` — ✅ 2026-05-25

- Scope: `infra/` + `myblog_front`
- Steps: (1) `COGNITO_USER_POOL_ID` added to `musicApi` Lambda env via Terraform; (2) `searchBarDb.client.ts` replaced `getJSON(url)` with auth-header fetch.
- Verification: Logged-in sync button → 200; logged-out → 401.
- PRs: infra #18, myblog_front #7

### PR-1: typo, dead code, import cleanup — ✅ 2026-05-21

- DEBT-2, DEBT-3, DESIGN-2
- Renamed `cadidate_search_service.py` → `candidate_search_service.py`; moved class-body imports to module top; deleted unused `singleflight.py`.

### PR-2: Runtime bug fixes (schema, NameError, model mismatch) — ✅ 2026-05-21 (SHA: fae9397)

- BUG-1, BUG-2, BUG-3 (model only)
- Removed duplicate `)` in `db/schema.sql`; deleted `get_primary_artist_map` from `album_service`; added `unique=True` to `Track.spotify_id`.
- Also: removed duplicate `get_by_spotify_id` definition in `album_repo.py`; removed stale comment in `album_service.get_album_detail_by_spotify`.

### PR-3: Production print/timing cleanup — ✅ 2026-05-21 (SHA: f4a5308)

- DEBT-1, DEBT-4
- Replaced `print()` with `logging` in `album_repo`, `sqs_client`, `candidate_search_service`. Removed timing and forced connection acquisition.

### PR-4: `/candidates` Cognito auth — ✅ 2026-05-23

- Cognito JWT validation via JWKS. Bypassed when `COGNITO_USER_POOL_ID` unset (dev).

### PR-5: CI unit test gate — ✅ 2026-05-23

- INFRA-1 (test gate scope)
- Unit test job gates deploy in `myblog_music`. Integration test (`tests/test_candidates_localstack.py`) marked and excluded from CI gate.
- Follow-up (2026-05-25): exit-5 workaround removed; 8 unit tests confirmed passing — myblog_music PR #14.

### PR-6: Tracks `spotify_id` UNIQUE migration — ✅ 2026-05-25

- BUG-3 migration part
- Script at `db/migrations/001_tracks_spotify_id_unique.sql`. Executed against Neon: 0 duplicates, `uq_tracks_spotify_id` applied `CONCURRENTLY` (zero-downtime).

### PR-7: Worker — SQS error handling + AttributeError + logging — ✅ 2026-05-23 (SHA: a156cfb)

- SEC-3, BUG-4, DEBT-7
- Exceptions re-raised so SQS retries and DLQ fires. Replaced `sync_album_by_spotify` with `sync_albums_batch([album_id], market)`. `print()` → `logging` across `sync_service.py`.

### PR-8: Publish — auth + CORS hardening — ✅ 2026-05-23 (SHA: 7866864)

- SEC-1, SEC-2, REFACTOR-3
- `FRONT_ORIGIN` env replaces `allow_origins=["*"]`; `edge_guard` middleware checks `x-origin-verify`; `load_dotenv` gated on `APP_ENV=dev`.

### PR-9: Backend config dedup + print removal — ✅ 2026-05-23 (SHA: f16ecd7)

- REFACTOR-1, REFACTOR-2, DEBT-6
- Single `Settings` in `app/core/config.py`; `_load_secrets_once()` folded into `get_settings()` via `@lru_cache`; no `print` in `get_settings`.

### PR-10: Frontend API path + rating schema — ✅ 2026-05-23 (SHA: 4a618f2)

- BUG-5, BUG-6, BUG-7, REFACTOR-5
- `/categories` → `/api/categories`; rating max comment corrected; `rating_scale` added to `publishToGit` payload; `apiFetch` typed.

### DEBT-5: `AlbumOut.external_url` missing — ✅ 2026-05-25

- `ext_refs['spotify_url']` → `AlbumOut.external_url` in DB-read paths. myblog_music PR #11, 3 unit tests.

---

### ARCH-6: Shared DB models package — ✅ 2026-05-26 (myblog_shared_db SHA: 7523e92)

- New private repo `hyuntohoon/myblog_shared_db` (`myblog-shared-db` Python package, v0.1.0).
- Canonical SQLAlchemy 2.0 DeclarativeBase models in `src/myblog_shared_db/models.py`; Core Table objects in `tables.py`.
- `myblog_backend/app/models/` (6 files) deleted; `myblog_music/app/domain/models.py` deleted; `myblog_worker/worker/infra/tables.py` deleted (was orphaned).
- All 3 consumers import from `myblog_shared_db` package; `requirements.txt` + CI git-auth steps added to each.
- Schema parity CI in `myblog_shared_db`: stack-based paren parser validates modeled columns against `docs/contracts/schema.sql`.
- ADR 0005 written. All 6 consumer CLAUDE.md files updated. RFC moved to `docs/done/rfcs/`.
- RFC: `docs/done/rfcs/ARCH-6-shared-db-package.md`

---

## Documentation & Architecture

### DOC-1: SQS contract document — ✅ 2026-05-23

- `docs/contracts/sqs-album-sync.md` written, covering the `{album_ids, market}` message format between `myblog_music` and `myblog_worker`.

### DOC-2: Per-repo CLAUDE.md — ✅ 2026-05-23

- Each of the 5 service repos now has a CLAUDE.md covering responsibility, local dev setup, invariants, test commands.

### ARCH-1: Schema single source of truth — ✅ 2026-05-23

- Canonical DDL at `docs/contracts/schema.sql`. ADR 0003 records the decision. Worker upsert fixed to include `ext_refs` in `ON CONFLICT DO UPDATE` (resolves ARCH-5). `aliases` column added to music schema.

### ARCH-2: Spotify client duplication — ✅ 2026-05-25

- Accepted as deliberate duplication. Cross-reference comments added in both files. ADR 0004.

---

## Infrastructure

### P1-E: Secrets Manager migration — ✅ 2026-05-24

- 4 secrets created (`myblog/backend`, `myblog/worker`, `myblog/music`, `myblog/publish`).
- Terraform `infra/secrets.tf` applied — 4 IAM policies.
- All 4 service repos updated: `SECRETS_ARN` pattern, `get_settings()` + `lru_cache`. Plaintext secrets removed from all Lambda env (`DATABASE_URL`, `SPOTIFY_CLIENT_ID/SECRET`, `EDGE_SECRET`, `GITHUB_TOKEN`).
- Follow-up (2026-05-25): `myblog_worker` `DATABASE_URL` missing check + `infra/db.py` lazy init — myblog_worker PR #9, 7 unit tests.

### IAC-1 (core): SAM → Terraform full migration — ✅ 2026-05-25

- `Apply complete! 21 imported, 12 added, 9 changed, 0 destroyed.`
- Imported: 4 Lambdas, 1 event source, 12 API GW resources, 4 log groups
- Added: 9 CloudWatch alarms, 3 consolidated Lambda invoke permissions
- Changed: log retention 0→14d ×4, event source `+ReportBatchItemFailures`, Lambda timeouts (worker 10→120, api/publish 3→15, music 10→15)
- Zero destroy/replace — no downtime.
- Decisions: Lambda config imported and corrected in place; EventBridge included (Gemini); Terraform state kept local in `infra/terraform.tfstate`.

### CICD-1: Frontend CI/CD fix — ✅ 2026-05-25 (myblog_front PR #6)

- pnpm version aligned to v10 (was v9 reading v10 lockfile — silently regenerating).
- Split into `check` (lint + astro check) → `deploy` (build + S3 sync); `deploy` requires `check`.
- S3 `Cache-Control` corrected per path: `_astro/**` immutable, `*.html` no-cache, `pagefind/**` 1h.
- `actions/cache@v4` for pnpm store keyed on `pnpm-lock.yaml`.

### CICD-2: code-review workflow + pyright CI — ✅ 2026-05-25

- `code-review.yml` added in `myblog_front` (PRs #8, #9). Non-interactive Claude Code; skips if `ANTHROPIC_API_KEY` absent.
- pyright baseline added in `myblog_backend` (PR #10) and `myblog_music` (PR #15). `reportAttributeAccessIssue` and `reportMissingImports` off (SQLAlchemy ORM + boto3 false positives); real bug categories kept as warnings.
- `/write` 404 fixed via CloudFront function: `uri + '.html'` → `uri + '/index.html'` (Astro directory format). LIVE published + cache invalidated.

---

## Frontend Redesign

### FRONT-1~5: Pitchfork-style frontend redesign — ✅ 2026-05-25

All phases merged to main. Design language: Lowfreq Magazine editorial (Newsreader italic serif, cream palette, Pitchfork-style rating layout).

| Phase | Content | PR |
|-------|---------|----|
| Phase 0 | Design system (global.css, Newsreader font, color tokens) | front #6 |
| Phase 1 | Navigation (header.astro, sticky + editorial) | front #6 |
| Phase 2 | Main page (hero-review, best-new-music, review-grid) | front #8 |
| Phase 3 | Review detail (review-hero.astro, [slug].astro) | front #8 |
| Phase 4 | Write page (write.astro 2-panel layout) | front #8 |
| Phase 5 | BNM badge + drafts.astro | front #8 |

ESLint cleanup: front PR #9 — 84 errors across Phase 2-5 components (HTML comments in JSX, no-alert, unused vars).

### FEAT-W2: writer search — split DB search / Spotify sync buttons — ✅ 2026-05-25 (front PR #11)

- Restored 2-button UX lost in FEAT-W1 redesign.
- **Search** button → `/api/music/search/unified` (DB-only, public, Enter key)
- **Spotify sync** button → `/api/music/search/candidates` (`apiFetch` + Cognito, 3s cooldown)
- Result tiles show `DB` / `SPOTIFY` source badge.
- `AlbumSearchResult.source?: 'db' | 'spotify'` added.
- Debounce auto-search removed in favor of explicit clicks.

---

## Architecture Review Items (from 2026-05-22 review)

### Frontend ↔ backend type contract — ✅ 2026-05-25

- `PostPayload` now derived from generated `WritePostRequest`. `pnpm generate:types` regenerates from `docs/contracts/openapi-backend.json`. myblog_front PR #5.

### Gemini alias generation decoupled from sync — ✅ 2026-05-25

- Removed from SQS sync path. Runs on EventBridge `rate(15 min)`. LIMIT reduced 20→10 to fit 120s timeout. myblog_worker PR #10.
