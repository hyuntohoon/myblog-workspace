# Plan

Active workspace tracker for cross-repo work. Each row carries `Scope / Order (if multi-repo) / Verification (local + prod smoke) / Rollback / Status`; RFC-backed entries collapse to a one-line pointer at `docs/rfcs/<id>-<slug>.md`. When a row finishes, drop it — `git log` and `docs/archive/done/YYYY-MM.md` are authoritative for history.

---

## Active

- **FEAT-lyrics-sync-precision** (**Steps 1+2 SHIPPED + prod 검증 2026-08-01**, front #325 `0f5748b` / deploy 30647516837; **Step 3 측정 후 NO-GO**; 남은 것 = 오너 실기기 청취 1건) — 가사 뷰어의 고정 `SYNC_LEAD_MS = 300`을 없앤다. 그 300은 레이턴시 보정이 아니라 **250 ms 틱 그레인(+125) + `.lyv-line` opacity 0.35 s 램프(+175)를 눈대중으로 상쇄하던 값**이라, 두 원인을 구조적으로 제거하고 ~80 ms만 "취향"으로 명시해 남긴다(Step 1: 다음 경계 `setTimeout` 스케줄링 + fast-attack/slow-release 분리). Step 2는 앵커가 조용히 어긋나는 실제 원인 3가지 — 뷰어에 pause 개념 자체가 없음, 탭 복귀 시 무검증, `MYBLOG_PLAYBACK_CHANGED` 미구독 — 을 **이벤트 기반 재앵커**로 닫는다(**단서 2026-08-01**: 세 번째 원인은 구독으로 닫혔지만 `sendPlayerCommand`가 그 이벤트를 발행하지 않아 **transport 명령에 대해서는 지금도 안 닫혀 있다**. 그래서 `'command'` 잔차 시리즈는 `sendConnectPlay`만 측정한 값이다 — 그 수치로 판단하기 전에 `FEAT-lyrics-viewer-playback` OQ4를 먼저 볼 것. ~~**부분 해소 2026-08-01**: Step 1이 `refresh('command')`를 부르므로 ⏭/⏮ 잔차는 이제 찍힌다~~ — **철회 2026-08-02 (코드 확인)**: `logResidual`은 `refresh`의 *같은 곡* 분기에서만 호출되므로 곡이 바뀌는 명령은 잔차를 남기지 않는다. **해소 2026-08-03 — front #342 `a53d0a4` / deploy 30772039596, prod smoke 19/0**: OQ4가 "발행한다"로 닫히면서 이 채널이 실제로 살아났다. 다만 그때 같이 확인된 것 — **기록 지점은 옮기지 않았고 옮길 이유도 없다**. 잔차는 예측 위치 − 실제 위치인데 곡이 바뀌면 예측 자체가 사라지므로 ⏭/⏮는 *잴 게 없는 것*이지 놓치는 게 아니다. 비어 있던 절반은 ⏸/▶였고(같은 곡이라 잴 수 있는데 읽기가 아예 없었다) 발행이 정확히 그 절반을 채운다. **읽을 때 주의**: `estimateMs`는 일시정지를 모르고 벽시계로 앵커를 늙히므로 멈춘 플레이어에서 잰 표본에는 왕복 시간이 통째로 섞인다 — 실측 두 개가 **37,084 ms(`readState: 'paused'`)와 89 ms(`readState: 'playing'`)**였다. 그래서 `logResidual`에 `readState`를 같이 남긴다. **라벨 없이는 이 시리즈를 읽으면 안 된다**). Step 3은 `AudioContext.outputLatency`가 실제 출력 경로를 반영하는지 **측정 우선 go/no-go**(무의미하면 스텝 폐기 후 보고). **폴링 없음 — D28 유지**(오너 2026-08-01: 재생은 1.0배속이고 4분 곡 시계 오차 ~10 ms라 시간이 아니라 *사건*만 앵커를 무효화한다 → 적응형 폴링 제안 철회). **저장·학습 오프셋 전면 폐기**(오너: 기록값은 그 순간의 네트워크·상황을 박제한다) → 서버 저장과 그에 딸린 멀티유저 전파 문제까지 같이 소멸. 프론트 단독, 마이그레이션·라우트·계약 변경 0. 측정 부산물: LRC `[offset:]` 태그는 prod synced 11,019행 중 **5행** → 사문. **실측 결과** (CDP, 실제 `/members/?me` 대시보드 + 재생 위치만 스텁): 줄 전환이 매 경계마다 `start_ms` **65 ms 전, 편차 0** (80 대비 65는 rAF 샘플러가 1프레임 먹은 것) — 기존 인터벌은 구조상 0–250 ms 산포였다. 불연속 3종 전부 통과(뒤로 seek→line 1 복귀 / 곡 중간 일시정지 7초 유지→경계 2개 지나도 line 2 고정 / 재개→line 3, 멈춘 동안 굴러간 시계가 아니라 실제 위치에서 이어짐 — **첫 시도는 마지막 줄에서 얼어 "안 움직임"이 자명참이라 재측정함**). **RFC에서 벗어난 결정 1건**: `idle`을 일시정지로 간주하려던 계획을 버리고 `readLivePlayback`에 위치를 실은 `paused` 상태를 신설 — `idle`엔 위치가 없어 추정치로 얼려야 했고 그건 이 RFC의 목적과 정반대. 대신 공유 타입 변경이라 소비자 3곳 전수 스윕했고, 거기서 `NowPlayingLyricsEntry`가 새 상태를 `확인 실패` 분기로 흘리던 **실제 잠복 버그**를 잡음(타입체커는 못 잡는 종류). **기록된 비용**: 뷰어가 열려 있으면 재생 명령 1건당 읽기가 2회(NowPlaying의 기존 1:1 확인 읽기 + 뷰어) — 둘 다 사용자 행동과 1:1이라 폭주는 아니고, `FEAT-lyrics-viewer-playback`이 뷰어에 자체 버튼을 주면 거기서 합치는 게 자연스러움. **Step 3 NO-GO**: `outputLatency` 16 ms로 측정됐으나 실격 사유는 수치가 아니라 구조 — Spotify SDK는 우리 `AudioContext`를 통해 재생하지 않고, Connect 원격 재생이면 소리가 아예 이 기기에 없음. 오디오 출력 지연은 이제 "미해결 과제"가 아니라 **기록된 수용 한계**. → `docs/rfcs/FEAT-lyrics-sync-precision.md`.
- **FEAT-multi-user-accounts** (in-progress since 2026-07-07) — multi-user platform: Google+Kakao signup, RYM-style public album reviews, per-user buckets, Last.fm/Spotify member listening, `LLMEngine`. **Phases 0–4 + P3b/3c + library user-scope + 07-14 surface-audit remediation + profile-merge PR1–3 ALL SHIPPED & prod-verified** (shipped-phase digest → `docs/archive/done/2026-07.md`; full phase log + decisions → RFC) — **but read "Phase 4 SHIPPED" narrowly**: what landed is the owner-central `LLMEngine` scaffolding (interface + CLI engine + API-engine skeleton + usage metering) and **no caller is wired to the API engine**, so a user-facing AI request would be the product's first (correction recorded 2026-07-30 in `FEAT-album-review-authoring` §"문서·UI 충돌" #7). Phase 1's review behaviour (rating, one-per-user-per-album, edit/delete, album average+count, profile list) is **shipped and unchanged**; the rating-**writing** increment on top of it (unified writing entry, AI-condensed one-liner, editorial-candidate mark, profile rating stats) is tracked separately as **FEAT-album-review-authoring** in `## Active`, and that RFC also fixes the 평론/평가 wording this RFC left ambiguous (owner mapping 2026-07-31: 평론 = review, 평가 = rating). **Remaining (all owner-only): launch gates G1/G2** (Google brand verification + Kakao 프로덕션 심사 + OAuth secret reissue; G1 = ≥10 reviews by ≥5 non-owner users in 4wk after public launch; **owner deferred 2026-07-20, no timeline — keep listed, don't re-prompt**), owner live-login returnTo observation, and the deferred canonical-link decision (runtime `/members/?u=` vs static `/members/[handle]` — necessity-gated while prod `/api/members` is empty). → `docs/rfcs/FEAT-multi-user-accounts.md` (Status: in-progress).

- **FEAT-album-review-authoring** (**in-progress** — Steps 확정 + **Step 1 SHIPPED + prod 검증 2026-07-31** + **Step 2 SHIPPED + prod 검증 2026-08-02**(ws #775 · backend #144 · front #331; 평가 이력 개수·평균·별점분포·정렬 3축 + "평론 쓸 것" 목록 + 죽은 `computeStats` 삭제. 새 읽기 `GET /api/me/review-candidates` 하나 — 인증 GET이라 **인프라 변경 0**. prod smoke 19/0, 신규 라우트 authed 200 / 익명 403, 배포된 화면에서 비공개 두 가지(자기 전용 안내·후보 목록)가 방문자에게 새지 않음 확인). **다음 = Step 3(AI 증류 오너 시범)**, 단 OQ3 잔여(기본 프롬프트 문구)가 열려 있다. ⚠️ **Step 2는 전제 미성립 상태로 착수했다**: Step 1 배포 이틀 뒤 prod `album_reviews`가 **여전히 0행**(rating·한줄평·후보 표시 전부 0)인 반면 버킷은 89→**102앨범**으로 증가 — 담기는 늘고 남기기는 0인 진단 그대로다. 오너 지시로 **예시 시드 데이터로 만들고 검증**했고(prod에는 아무것도 쓰지 않음), 그래서 **빈 상태가 1급 설계 대상**이었다. Step 1의 관측(오너 rating 행이 0을 벗어나는지)은 **Step 2로 닫히지 않는다** — Step 2가 그 값을 보여주는 화면이다. Step 2 이후에도 0이면 Step 3 전에 Step 1 입구를 실제 화면에서 점검하는 것이 순서) + **입구 점검 완료 2026-08-02**(그 순서를 실행 — prod 쓰기 0건. **빈 상태의 안내 링크 2개가 둘 다 회원 디렉터리로 가고 있었다**: `href="?tab=bucket"`이 쿼리를 통째로 갈아치워 `?u=`가 소실. 평가가 0이라 그 빈 상태가 곧 화면이었으므로 **제품 안에서 입구로 가는 경로가 전부 막혀 있었다** → front `fix/album-review-entrance-links`로 수정. 함께: 60자가 모바일 2줄 상자에서 잘리던 것 → 숫자 유지, 상자 3줄. 앨범 화면 입구 자체는 정상. **종결 2026-08-02**: 버킷 보드 표시 버튼이 hover 전까지 투명하던 건 의도된 설계였으나(주석 근거 = "~90개가 다 켜지면 소음"), 임시 버킷으로 두 상태를 찍어 보인 뒤 **오너가 항상 보이게 바꾸기로 결정** — 소음 비용은 가정이고 발견 비용은 매일 지불되고 있었다(표시 이틀 0건). "항상 켜짐"이 아니라 **조용히 상존**(평소 0.45 · 호버 1 · 표시되면 1+액센트) + `:focus-visible` 추가 → front #338. ⚠️ **Step 3 착수 조건 추가** — 수정 배포 후의 값을 다시 보고 간다. 그 전의 "0이 계속된다"는 **입구가 막힌 상태에서 잰 값**이라 Step 3의 근거가 못 된다) + **재관측 게이트 판정 보류 2026-08-03**(1차 재측정 08-02 14:39Z는 `album_reviews` 0행이지만 **판정 불가**: 수정이 살아 있던 시간이 **59분**, 그 사이 제품 사용 흔적 0건이라 두 가설을 못 가른다. 게다가 "rating 0인가"만 보는 게이트는 오너가 제품을 안 열기만 해도 영원히 0이라 **Step 3을 자동 정당화**한다 → 진단 원문("담기는 늘고 남기기는 0")에서 분모를 가져와 **통과 조건 = `aebabe7`(08-02 13:40Z) 이후 담기 10건 이상인데도 rating 0**으로 확정. 담기가 10건 미만이면 판정하지 않는다. OQ3 문구 작업도 게이트 뒤로 미룸. **오너 숙제 없음** — 평소대로 쓰면 이틀이면 분모가 찬다) + **2차 재측정 2026-08-02 23:15Z = 미달, 판정하지 않음**(분모 규칙의 첫 적용. `aebabe7` 이후 담기 **0건**, 수정이 살아 있던 시간 9시간 35분, 마지막 담기는 08-01 16:28Z로 1차와 동일. `album_reviews`도 여전히 0행. 새 관측 하나 — 그 창 안에 스포티파이 청취 4곡(최근 23:07Z)이 있어 **계정은 활동 중이었다**. 단 그 표는 `/me/player/recently-played` 폴링이라 "우리 사이트를 열었다"의 증거는 아니다. 야간 grow의 분모 오염 없음(`rec_reason`은 점수 0이면 NULL이라 표지로 못 쓰므로 행을 직접 확인). **다음 재측정은 분모부터** — 담기 <10이면 그 자리에서 끝내고, 간격을 좁혀 다시 재는 것도 무의미하다) + **"이틀이면 찬다"는 틀렸다, 실측 2026-08-02**(분모 10의 근거였던 08-01+07-31 이틀 20건이 **최근에서 가장 몰린 구간**이었다. 전 기간(06-05~08-02, 140건 전부 오너) 롤링 창으로 재보면 10건에 닿는 비율이 **2일 13.8% · 4일 30.4% · 7일 43.4% · 10일 56.0% · 14일 69.6%**. 담기 있는 날은 59일 중 24일(41%)이고 공백은 1일 11회·2일 6회·3일 2회·4일 1회·**8일 3회** — 며칠 0건은 기본값이지 이상 징후가 아니다. **문턱 10이 아니라 기대 시간이 틀렸다** → **다음 재측정은 2026-08-12 이후**, 그 전엔 세 번째 null만 나온다) — 앨범 **평가**(공개 별점 + 한줄평)를 쓰는 경험을 채운다. 지금 제품에서 **일반 사용자에게는 쓰기 입구가 아예 없다**(헤더 글쓰기가 오너 신호로 숨겨져 있음) — 이 행은 그 한 칸을 메우는 작업이고, 디스커버리가 아니라 이미 있는 핵심 흐름(듣기 → 담기 → 남기기)의 마지막 단계라서 2026-07-23 "안전망·품질 우선" 결정과 충돌하지 않는다. 평가 볼륨에 얼어 있는 **FEAT-genre-recommendation**·**FEAT-ai-editorial-critique**보다 **앞** — 그 둘의 전제("평가가 쌓인다")를 만드는 쪽이다.
  - **이미 출시된 것 = 이 행의 작업이 아님** (FEAT-multi-user Phase 1, 2026-07-08): 0.5단위 별점, 글 없이 별점만, 1인 1앨범 1평가, 수정·삭제, 앨범 평균·평가 개수, 프로필의 최신순 "평가한 앨범" 목록, 일일 생성 상한. 규칙은 하나도 바꾸지 않는다.
  - **새 범위 (사용자에게 보이는 능력 단위)**: (C1) 제품 전체에서 **하나의 쓰기 입구** — 헤더·앨범 화면·홈 앨범 카드·Buckit 앨범 카드; 카드/앨범 화면에서 들어오면 앨범이 이미 선택된 상태, 헤더에서 들어오면 종류와 앨범을 고름; 일반 사용자는 평가, 편집 권한 사용자는 **평론/평가 선택**, 권한 없는 사용자에게 평론 항목을 노출하지 않음. (C3) 정리되지 않은 **자유 감상 → AI가 한줄평으로 정리**(제품 기본 프롬프트, 결과 편집 가능, **발행은 사용자가 명시적으로 — 자동 발행 경로 없음**). (C4) 자유 감상 **원문은 작성자에게만** 보이게 남고, 평가를 삭제하면 원문도 함께 삭제. (C5) 프로필에서 **평가한 앨범 수 · 평균 별점 · 별점 분포 · 평가 목록 · 정렬/개별 보기** — 평점만 남긴 항목도 전부 포함. 기존 프로필 목록은 재사용하고 평균·분포·정렬만 새로 얹는다. (C6, 07-31 추가) **평론 후보 표시** — "이건 나중에 평론으로 쓰겠다"를 앨범에 남긴다. **비공개**(남에게 예고로 안 보임), **듣기 전에도 가능**(평가가 아니라 앨범 상태에 붙음), **버킷 화면·앨범 화면 양쪽에서 수정 가능**. 이걸로 평가가 평론의 대체재가 아니라 **평론 후보를 고르는 도구**가 된다.
  - **용어 고정 (오너 매칭 2026-07-31)**: **평론 = review**(편집 권한 사용자의 장문 편집물), **평가 = rating**(사용자의 공개 별점 + 한줄평). **한국어 "리뷰"는 쓰지 않는다.** 입구는 공유하되 권한·기대치·발행 동작은 계속 분리. 지금 화면 문구가 둘을 섞어 쓰고 있어(편집 지면 빈 상태가 "아직 리뷰가 없습니다", 프로필 탭 이름 **평론**이 비편집 사용자에게는 자기 평가 목록을 보여줌, 같은 대상을 "평가"로도 부름) 화면 문구 한정으로 정리 — **URL·API·테이블 이름은 그대로 둔다** (`album_reviews`·`/api/reviews/*`는 사용자 평가인데 이름이 review라 매칭과 반대다 — 이 RFC의 파일명·plan-id도 같은 이유로 유지). **식별자 개명은 범위 실측 후 "지금 안 함"으로 결정**(2026-07-31): 내부 이름은 싸지만 **API 경로 변경이 비싸다**(openapi+프런트 타입 재생성 + API GW 라우트 3개 + apply + 구/신 병행); 파급은 backend·front·shared_db·infra로 좁다(**music·worker는 0건**). **절충 = 착수할 때 내부 이름만 정리**하고 API 경로·테이블 이름은 유지. 기회비용은 기록해 둠 — 평가 0행·클라이언트 1개인 지금이 가장 싼 시점이긴 하다.
  - **범위 밖**: 트랙/아티스트 평가, AI 자동 발행, 장르·아티스트 통계, 댓글·반응·팔로우, 편집 리서치 노트 파이프라인 변경, 추천·발견 확장, 평론 편집기 자체, 다중 저자.
  - **오너 결정 2026-07-30**: (1) **범위는 전체 한 묶음** — C1·C3·C4·C5를 하나의 기능으로 본다(초안 권고였던 "C1+C5 먼저, AI 뒤"는 채택 안 됨). (2) **AI 도움은 오너 계정에서 먼저 시범** 후 일반 개방 — 그전까지 일반 사용자에게 AI 항목은 안 보이고 직접 쓰기만 보인다. 되돌리기 지점이 코드가 아니라 "연다는 결정"에 있다는 점이 이 게이트의 요지. 편집 권한과는 별개의 축. (3) **평가 형식 = 한줄평, 평가 전체에 적용** — 직접 쓰기·AI 정리 모두 별점 + 한 줄로 수렴(이동진 영화 한줄평 준거; 기존 긴 코멘트는 유효). (4) **AI 정리의 성격 = 증류** — "재료는 작성자가 준다": 자유 감상 안에 있는 것만으로 응축, 재료 밖 사실·평가·비유 보탬 금지(RFC 하드 룰 2-1).
  - **미결 (RFC Open questions에 남김 — 확정 작업으로 취급하지 않음)**: 기본 프롬프트의 구체 문구(OQ3 잔여 — 오너 시범에서 다듬음, Step 3), 일반 개방 시점의 사용 한도·대기 표시(OQ4 잔여, Step 5), 입구의 배치·형태(OQ9, Step 4). **종결됨**: 한줄평 60자(OQ1, Step 1에서 확정) · 용어 평가=rating/평론=review(OQ2) · **정렬 = 최신순+별점순+이름순(OQ7, 오너 2026-08-02)** · **신규 사용자 통계 접근 = 현행 유지(OQ5, 오너 2026-08-02)**.
  - **진단 교체 (2026-07-31, prod 실측)**: 초안은 "쓰기 입구가 없어서"라고 진단했지만 **오너는 입구가 있는데도 앨범 89개를 담아두고 평가가 0**이었다(`album_reviews` 0행 · 버킷 항목 117행/89앨범 = 28행 중복 · `status` 120개 전부 `candidate` · 노트 80 · 초안 5 → 발행 0). 입력은 쌓이는데 산출만 0인 패턴이 세 군데서 반복된다. **실제 원인(오너 확인)** = (1) 평가가 평론만큼 무겁게 느껴짐, (2) 앨범을 버킷 사이로 옮기며 정리하는 순간에 별점·한줄평을 남길 자리가 없어 **정리와 평가가 끊겨 있음**. 따라서 목표는 입구를 여는 것이 아니라 **평가를 가볍게 만들고 정리 동작과 잇는 것** — 한줄평이 (1)의 처방, C6이 (2)의 처방이다. **오늘 값이 나오는 조각은 C6 + 한줄평**이고, C1은 공개 이후에만 측정되며, C5는 평가가 쌓인 뒤라야 채워진다(초안이 C5를 "오늘 값"으로 적은 것은 틀렸고 RFC에서 정정함). **관측 지점 = 오너의 `album_reviews`가 0에서 벗어나는지.** 버킷 멤버십은 보조 지표로 쓰지 않는다 — 버킷 이름은 오너가 붙인 이름표라 무엇의 증거도 아니다(`Done Listen`에 있어도 실제 청취 기록은 아님; 이전 판이 이걸 "다 들은 것"으로 읽은 것은 오류).
  - **관통 개념 + 데이터 원칙 (2026-07-31)**: **사용자-앨범 상태는 하나** — 별점·한줄평·평론 후보가 한 상태의 측면이고 평가는 그중 공개되는 부분(버킷 이름 `To Review`/`Done Listen`은 오너가 손으로 만든 우회일 뿐 제품 개념이 아님). **리서치 노트는 앨범 것, 조사 요청은 사람 것** — 노트 본문은 앨범당 하나만 존재하고 사용자별로 복제되지 않는다. 저장 구조는 착수 때 결정. **버킷 구조 개편도, 버킷 이름에 기능을 붙이는 일도 범위 밖**(오너 지시 2026-07-31) — 오너가 지은 이름은 이름표로만 두고, 제품이 그 이름을 읽어 동작을 바꾸거나 사실의 근거로 쓰지 않는다. 상태를 버킷 화면에서도 고칠 수 있게 하는 것까지다. **"들었다"는 상태에 넣지 않는다**(OQ10 종결): 청취 기록 자동 수집은 이미 연동(Last.fm·Spotify) 쪽에 있고 지금 건드릴 것이 아니다. 이 RFC의 상태 = 별점·한줄평·평론 후보.
  - **Steps 확정 (오너 2026-07-31)** — 사용자 향 능력 단위 5단계, 한 세션 1단계(하드 룰 4): **1** 가볍게 남기고 평론 쓸 것 체크(한줄평 형식 + C6) · **2** 쌓인 것 보기(평론 쓸 것 목록 + 프로필 평가 이력 C5) · **3** AI 증류 오너 시범(C3+C4, **이 단계 끝이 개방 게이트**) · **4** 통합 입구 + 권한 정리(C1 + 감사 E-5) · **5** AI 일반 개방(조건부). "한 묶음"은 유지되지만 한 PR이라는 뜻은 아니다.
  - **Step 1 SHIPPED + prod 검증 2026-07-31** (shared_db #71 · backend #143 · front #324 · ws #759; V50 prod 적용 완료) — 오너 결정 3개가 여기서 굳었다: **한줄평 상한 60자**(실제 화면 폭 기준 — 오버레이 한 줄 ≈ 35자, 모바일 ≈ 25자), **"상태는 하나"를 기존 `album_reviews` 행 확장으로 구현**(`rating` NULL 허용 + `review_candidate` 열, V50 — 새 테이블 없음), **내부 이름 정리는 별도 커밋**(경로 이름을 딴 파일·배포 환경변수·JSON 필드는 제외). **API 경로·API Gateway·terraform 변경 0** — 쓰기는 기존 PUT을 부분 갱신으로 재사용, 내 상태 읽기는 `GET /api/{proxy+}` 캐치올. **이 단계의 유일한 실질 위험 = 공개 읽기의 `rating IS NOT NULL` 누락**(비공개 상태가 공개 목록에 새는 경로)이라 DB CHECK 3개 + 라우트 테스트 + 공개 읽기별 실엔진 회귀로 막았고, **prod에서 직접 확인**했다(마크만 있는 상태 → 공개 count 0·누출 없음 · 61자 422 · 별점 저장/삭제가 마크를 안 지움 · 마지막 측면 제거 시 204; 테스트 데이터 정리 완료). prod 스모크 19/0, 통합 93 passed·DB사유 스킵 0. **관측 시작**: 오너의 `album_reviews` 중 rating 있는 행이 0을 벗어나는지.
  - **의존**: 사용자 요청 AI 호출은 **제품 최초**다(공용 LLM 계층은 뼈대만, 호출자 0). 일반 사용자에게 쓰기 입구를 여는 것은 감사 **E-5**(`/write`·`/drafts`가 로그인만 확인)와 같은 범위에서 정리돼야 한다. 선행 RFC의 공개 게이트 G1을 대체하지 않는다. `DATA-*`·가사·리서치 노트 계열과 겹치는 범위 없음. → `docs/rfcs/FEAT-album-review-authoring.md` (Status: in-progress).

- **DATA-release-noise** (P1 — from FEAT-release-calendar OQ4 + 2026-07-23 감사 P-7) — **Step (b) read-side filter SHIPPED + prod-smoked 2026-07-24** (myblog_music #60). Fresh 07-24 재측정으로 감사치 확인·악화 발견: 홈 "새 앨범" 30일 후보 풀 94건 중 54건(57%)·`/releases/` 7월 173건 중 ~36건(~21%)이 클래식 컴필("065 Piano Essentials"류, 죽은 작곡가 귀속); 노이즈 100% Spotify `album_ingest` 경로 유입. 오너 선택 = (b). 공용 술어 `is_compilation_noise`(아티스트 ≥10 ∪ 순수컴필 레이블 ∪ 컴필 제목패턴, `settings` 튜닝, read-side·가역) → feed_service + release_calendar_service. 오탐 안전: 진짜 클래식 연주 6건(모차르트 레퀴엠 n=8/DG, Argerich n=3, Orff n=8/Pentatone 등) 전부 통과(순진한 n≥6은 3건 오탐). **Prod 실측: 홈 컴필 7/12→0/12, `/releases/` ~36→1**(잔여 1 = MB-announced "The Art of Itzhak Perlman"/Vivaldi 귀속, 확정앨범 없어 제목신호만 적용 → 오너 A 결정으로 향후 (a)에서 흡수). `no_upc` sentinel 재점검 ✓ 건강(805건 전부 watchlist pop≥50, 매 사이클 무료 재확인, iTunes-소스 스킵은 설계대로 — 노이즈 요인 아님). **남은 것**: (a) ingest-side 필터/소스신뢰가중 + (c) exact-dup dedup — (b) prod 결과로 근거 확보됨, 착수 여부 ~07-27 오너 결정. Deps: PERF-home-feed-latency(원인 공유 — (a) 시 N+1도 흡수).

- **FIX-lyrics-primary-artist + FIX-isrc-backfill** (2026-07-26~29 — `DATA-catalog-noise-and-lyrics-coverage` "Related defects" #1·#2) — **코드·데이터·스케줄 전부 종료**(worker #80/#81/#82/#90, backend #142, music #64, ws #698). 결과만: LRCLIB 검색 아티스트 정확도 **45.3% → 96.8%**(1,793곡 ground truth), `tracks.isrc` **0% → 99.9%**(26,594/26,619), 백로그 소진 후 주 1회로 하향. 상세 이력 → `git log` + `docs/archive/done/2026-07.md`. **남은 것은 관찰 2건 + 후속 근거 2건뿐:**
  - **관찰 (a)** 주간 ISRC 슬롯이 신규 유입분을 실제로 집어가는지 1회 확인(일 21:00 UTC).
  - **관찰 (b)** `not_found` 회수율 — 검색어가 실제로 바뀌는 행이 **5,412 / 14,034 = 38.6%**. 회수가 크면 `DATA-catalog-noise` Step 3(가사 풀 위생)의 기대값을 다시 계산해야 한다. (n=40 수동 실행에서 5건 회수 = 12.5%, 기존 1.54% 대비 — 단 n=40은 비율 추정치가 아니다.)
  - **후속 근거 (1) 규칙 없이 고치는 방법이 존재한다.** `decide_match`가 이미 크레딧 *전체* + aliases로 검증하므로, 대표를 맞히는 대신 **크레딧마다 LRCLIB를 한 번씩 조회해 합집합**을 취하면 구성상 100%가 되고 잔여 오차 ~3%가 사라진다. 이번에 안 한 이유는 비용뿐 (LRCLIB 실효 상한 ~2.5 req/s, multi-credit 11,016곡 → 요청량 2배+). 풀 위생 이후 재평가.
  - **후속 근거 (2) ISRC가 카탈로그 중복을 증명했다** — `DATA-catalog-noise` (c) exact-dup dedup에 직결. 중복 ISRC **1,982개 / 묶인 4,571행 / 제거 가능 2,589행(카탈로그의 9.7%)**, 최대 중복도 8. 전부 2026-06~07 생성된 클래식 벌크 컴필(바흐 BWV 992 한 녹음이 7개 컴필에 중복 적재). **지금까지 dedup에 쓸 신뢰 키가 없었는데 exact-match 키가 생겼다.** 같은 뿌리로 **중복 행의 `album_artists`가 서로 달라** 어떤 대표-아티스트 규칙이든 정확도 상한이 여기서 막힌다.

_2026-07-18 UI/UX planning session (areas 1–6, 9, 10 investigated; owner decisions folded into the rows below — all since promoted to in-progress/shipped; areas 7·8·11·12 deferred to a follow-up measurement/research session):_

- **FEAT-member-player** (in-progress — **모든 스텝·후보가 배포 완료**; 남은 것은 오너 실기기 확인 몇 건뿐) — '지금 듣는 음악'을 실제 플레이어 바로 재구축(D1 능력 계층, D28 무폴링 유지). Steps 1–4(스코프·재동의 / 멤버별 토큰 민팅 / hairline-LCD 바 + Connect 원격 + 시계추정 폴백 / 기기 안내) 2026-07-19~20, **Step 5 통합 재생 사다리 + 기기 선택기 + Media Session + 네비게이션 지속 2026-08-02**(front #334 `9df4c7a`), **Step 5b `/write` 셸 합류**(#335 `8e82d74`), **Step 6 전체**(6a/6b/6d #302·#303, 6c/6e #336), **Step 7 전체**(7a #302, 7b/7c #337), 진입점 결함 수정(#340), transport 이벤트 공표(#342). **오너 콜드스타트 확인 2026-08-03** — 이 스텝이 존재한 이유였던 '재생 시작이 안 된다'가 실사용에서 해소됨. **남은 오너 확인 4건(아무것도 안 막음)**: 나머지 5개 재생 표면 개별 확인 · 실제 두 번째 기기로 양방향 전환 · OS 미디어 키 · `/write` 오디오 귀로 확인. **Status를 done으로 올릴지는 오너 판단**(하드 룰 5). 이 RFC의 산출물을 `FEAT-playback-bucket-player`가 그대로 소비한다. → `docs/rfcs/FEAT-member-player.md`.

- **FEAT-playback-bucket-player** (**accepted** — 오너 승격 2026-08-03; **Steps 2·3·4 SHIPPED 2026-08-03**, shared_db #72 V51 · backend #149 · ws #811 + front #344; 다음 = Step 5 front) — 재생 권한이 있는 사용자마다 **시스템 소유 재생 버킷 1개**: 자동 생성, 삭제·복제 불가, Pocket 안에서 위치는 자유. 트랙/앨범만 받고(앨범은 **앨범 순서대로** 트랙으로 펼침) 아티스트는 거부, **버킷의 항목 순서가 곧 대기열**이며 끝난 곡은 자동 제거·현재 곡 제거 시 다음으로 진행. 재생 자체는 **새로 만들지 않는다** — `FEAT-member-player` Step 5에서 **이미 배포된** `play(intent)` 사다리·기기 선택기·Media Session·네비게이션 지속을 그대로 쓴다(2026-08-02 배포, 08-03 오너 콜드스타트 확인). 대기열 재생은 우리 목록의 꼬리를 `play({kind:'uris'})`로 다시 쏘는 방식이라 **Spotify 자체 대기열에는 쓰지 않는다**(6c `queueTrack`은 "다음에 듣기" 전용으로 유지) — 역방향 동기화 금지 규칙이 만들 수밖에 없는 불일치를 애초에 안 만든다. UI는 **한 세션 두 형태**(Pocket 안 미니 플레이어 / 밖으로 꺼내면 확장 플레이어, 둘 다 가사·트랙·앨범 정보 진입 제공). **탭 1개만 재생 소유**(양도 + 소유 탭 사망 복구 설계 포함, 상수는 추측이 아니라 실측). 스키마는 기존 관용구 그대로 — `type='playback'`(닫힌 enum 2→3), `kind='playback_queue'`, `UNIQUE(user_id) WHERE kind='playback_queue'`(V40 사용자별 부분 유니크와 동일). **오늘 없는 것 = 시스템 버킷 삭제 방어**(`delete_bucket`에 가드가 아예 없어 `spotify_library`·`to_listen`도 지금 지워진다 → 같은 PR에서 패턴 수정). **오너 결정 2026-08-03 4건**: (1) 두 RFC 다 승격, 재생 버킷 먼저. (2) **멤버도 두 rung 다** — `streaming` 스코프를 연다(권고를 넘어선 선택이라 비용을 적어둠: 스코프·재동의는 `FEAT-member-player` 소관이고, 연동된 멤버는 전원 재동의하며 — 지금은 1계정, 이미 `streaming` 보유 — prod에 스포티파이 계정이 1개뿐이라 **멤버 경로는 검증 안 된 채 나간다**). (3) 앨범 펼침은 **곡 수와 무관하게 확인창 없음 + 실행취소**(D8 1:N 확인 규칙을 이 버킷에 한해 뒤집음). (4) **가사를 멤버에게 개방** — `FEAT-lyrics-annotations` §6.9 O2(backend #139·front #319로 배포됨)를 되돌리는 것이고, 가드 변경은 가사 쪽 소관. **주석도 함께 개방**(OQ4b, 오너 2026-08-03 — 권고는 본문만이었음) → 가사 여는 작업의 범위는 라우트 + `annotations` 필드 **둘 다**이고, 같은 PR에서 `models.py:315-318`의 "운영자 전용 연구 데이터" 주석 수정과 front #319의 403 화면 삭제까지 해야 한다(안 지우면 도달 불가 상태로 썩는다). 그 스텝에서 한 번 볼 것: 주석은 Genius 유래 콘텐츠라 출처 표기 의무가 더 넓은 대상에게 걸린다. **남은 미결 = 확장 플레이어 형태 1건**(Step 6에서 나란히 비교 후 결정). **Step 2 완료 2026-08-03** — shared_db #72 `V51__playback_bucket.sql`: `ck_review_buckets_type` 3값 확장 + `idx_review_buckets_single_playback`(`UNIQUE (user_id) WHERE kind='playback_queue'`). **백필 없음**(0행 생성/변경). `BEGIN…ROLLBACK` 드라이런에서 같은 사용자의 두 번째 큐가 인덱스에 걸려 거부되는 것까지 확인한 뒤 prod 적용, 새 커넥션으로 재확인(`indisunique=t indisvalid=t`), **Neon 테스트 브랜치에도 적용**해 Step 3 통합 테스트가 드리프트에서 시작하지 않게 했다. `review_bucket_items`는 손대지 않았다 — `item_type='playback'`은 V28부터 있고 V30이 **일부러** 고유 인덱스를 안 걸어 중복을 허용하므로 큐에 그대로 맞는다. 곁가지로 정본 미러의 버전 주석 2곳이 V47/V48에 멈춰 있던 것(본문은 이미 V50)을 바로잡음 — CI가 구조적으로 못 보는 드리프트. **Step 3 완료 2026-08-03**(backend #149) — backend: `get_or_create_playback_bucket`(버킷 트리 읽기에서 지연 생성) + 앨범→트랙 펼침(**앨범 순서**) + playback 버킷 타입 게이트. **패턴 수정 2건을 같은 PR에서 스윕**: `delete_bucket`에 가드가 아예 없어 `spotify_library`·`to_listen`도 지워지던 것 → 3종 모두 409, 그리고 발행 가드(`is_public`)도 `spotify_library`만 막던 동일한 종별 드리프트 → 둘 다 `SYSTEM_BUCKET_KINDS` 하나를 본다. **계획과 달랐던 것 2건**(코드 대조로 발견): (1) **새 authed POST가 아니다** — 형제인 `expand_artist_source`가 기존 `POST /api/buckets/{id}/items`에 `source_*`로 올라타므로 앨범 펼침도 그렇게 했다 → **`infra/apigateway.tf` 변경 없음, `terraform apply` 없음**(오너 결정). (2) **disc 컬럼이 없다** — `track_no ASC NULLS LAST`로 정렬하고, 그 결과 **멀티디스크 앨범은 디스크가 섞인다**(스포티파이 `track_number`가 디스크마다 1로 돌아가기 때문). 제품의 모든 앨범-트랙 읽기가 공유하는 기존 한계라 여기서 고치지 않고 기록만 했다. 검증: pytest **784 passed / 1 skipped**(그 1건은 무관한 기존 데이터 의존 skip), `TEST_DB_URL`을 넣고 돌려 통합 스위트가 실제로 실행됨 · pyright 0 errors · **순서 테스트 대조 확인**(ORDER BY를 빼면 두 테스트가 실패 — 목이 아니라 실제 SQL을 검증). **Step 4 완료 2026-08-03**(ws #811 계약 병합 + front #344 타입 재생성, 같은 세션 연속) — 병합 결과가 Step 3의 정정을 독립적으로 확인해줬다: **경로 변경 0**(앨범 펼침이 정말 새 라우트였다면 여기서 새 path가 나타났어야 하고 `infra/apigateway.tf`가 낡은 상태였을 것이다), 늘어난 것은 스키마 2개(`Backend_TrackExpansion`·`Backend_TrackExpansionResponse`) · `DELETE /api/buckets/{id}`의 **409** 응답(삭제 가드가 이제 계약이다) · `POST /api/buckets/{id}/items` 유니온의 **세 번째** 멤버(Step 5의 프런트가 분기할 지점)뿐 — 총 +39줄, 삭제 0. front 타입 재생성을 같은 세션에 붙인 것은 Step 5를 앞당긴 게 아니라 front 배포 게이트가 `api.gen.ts`를 워크스페이스 main과 diff하기 때문(`feedback-frontend-api-gen-sync`); Step 5의 나머지는 손대지 않았다. **Step 5 완료 2026-08-03**(front #345) — 재생 버킷이 Buckit 보드에서 자기 정체를 갖는다: `crMeta`가 kind로 `재생 대기열` 태그를 붙이고(이전엔 평범한 `담음` 크레이트였다), 트레이 칩이 `재생 대기열에 추가`/`action:'queue'`/`ordered`를 처음으로 쓰며, 드롭은 트랙→1행·앨범→앨범 순서 N행·아티스트→거부로 갈린다. 대기열은 `bucketStore` 위의 **순수 투영**이고 두 번째 스토어를 만들지 않았다. **재생 호출 0건 — 의도된 제외**. prod 데이터로 실브라우저 검증: *OK Computer* 드롭 → **12곡이 정확한 앨범 순서**(Airbag→…→The Tourist), 중복 트랙 드롭 12→13행(같은 곡 2번, 409 아님), 아티스트는 드래그오버 거부 + **쓰기 0건**이며 트레이 칩은 `opacity 0.38` + 이유 문구로 **자리를 지킨 채** 흐려지고 모든 칩의 `x/y/width`가 드래그 전/중/후 동일(무재배열 불변식을 추정이 아니라 실측), 삭제는 ⋯ 메뉴에서 아예 빠지고 휴지통은 이유를 답하며 **낙관적 깜빡임 없음**(Step 3 이후 이월돼 있던 관측 해소), 위치 이동은 13행을 유지한 채 서버에 persist. **계획에 없던 발견 2건**: (1) `canAcceptAlbumDrag`에 **같은 리포 쌍둥이**가 있었다(`pocketBuckit/boardDnd.ts`의 `boardDragAccepts` — 트레이는 두 번째 React 루트라 보드의 `dnd`를 못 읽는다) → 같은 PR에서 둘 다 고치고 테이블 기반 테스트로 케이스별로 묶어뒀다. (2) **Step 3 삭제 가드가 우회 가능**했다 → **같은 날 수정 완료**(backend #150 `69fb3e0` + front #346 `3537d66`): `delete_bucket`이 대상의 kind만 보던 것을 **서브트리 검사**로 바꿨다(`_system_bucket_in_subtree` — 시스템 버킷 ≤3개에서 위로 걸어 올라가므로 기존 `_walk_ancestors` 재사용, 두 번째 트리 순회를 안 만든다). **이동은 그대로 자유** — 파괴적인 행위는 중첩이 아니라 삭제라서 가드를 move가 아닌 delete에 걸었다. prod 재검증: 직접 삭제 409 · **중첩 후 부모 삭제도 409**(이전엔 204) + `move it out first` 안내 · 큐가 **같은 id로 생존**(이전엔 새 id·0항목) · 큐를 빼내면 부모 삭제 204로 정상 복귀(과잉 차단 아님). 통합 테스트 8건 추가, 가드를 꺼서 **정확히 우회 6건만 실패하고 정상동작 5건은 통과**하는 대조까지 확인. **Step 6 완료 2026-08-03**(front #347 + ws #814) — **OQ2가 비교로 닫혔고, 비교가 답을 바꿨다**: 세 안을 실측 크기로 나란히 그리고 실제 서비스 4곳(Genius·Apple Music 웹·SoundCloud·Bandcamp; Spotify 웹·YouTube Music은 로그인 벽이라 **미확인**으로 남김)을 브라우저로 재보니, **본문 위에 카드를 띄우는 서비스가 하나도 없었고**(→ 떠 있는 형태는 기본이 아니라 *뜯어낸 뒤 상태*로 강등) **폰에서 재생·큐·가사를 오버레이 하나에 담은 곳도 없었다**(Apple은 390에서 큐 패널을 아예 없앤다). 오너가 고른 것은 세 안 중 하나가 아니라 **네 번째 — 도킹된 사이드 패널이 뜯어져 플로팅이 되는 형태**(도킹이 기본, 레이아웃이 폭을 양보해 **덮지 않고 밀어냄**; 390은 도크 없이 탭 시트). 물리는 **이미 배포된 `DockableLyricsSheet`에서 추출**(복사 아님, 341→138줄, `AlbumDetail` 무변경). 세션 스토어는 트리가 안 가진 것만 갖고 **큐는 Step 5 투영 그대로**. 실패 문구는 전부 기존 문장 재사용(transport는 `PlayerCommandOutcome`에 문장이 없어 `NowPlaying`의 것을 그대로). **RFC가 놓친 감사 발견 1건**: 꼬리가 URI가 아니다 — `TrackBrief`에 Spotify id가 없고 리졸브는 한 곡에 한 요청이라 40곡이면 재생 한 번에 40요청 → 프런트 단독 리졸버 seam(메모이즈·중복제거·선반입)으로 정상 상태 0요청, **진짜 해결(페이로드 필드 1개, cross-repo)은 RFC 후속으로 기록**. 검증: vitest **265**(247→265, 스텁이 Spotify 비동기 적용을 1,200ms로 모델링하고 `LAG-1`에서 중간 상태 확인) · lint · astro check 0 errors · **재생경로 게이트를 베이스라인 대비로**(origin/main 7건 → 브랜치 7건, **추가 0**; 절대 카운트로 돌리면 거짓 녹색이었다 — main이 이미 7건이었고 위임한 codex가 리터럴을 상수 뒤로 세탁해 통과시켰던 것을 되돌림) · 실브라우저 실측: 밀어내기(shell 1280→900, dock 0→380px, 패널이 shell 끝에서 정확히 시작) · 뜯기(30px에서 900→889.5px = 저항 0.35× 그대로, 70px 넘으면 float + dock 0px) · 되붙이기(shell 900 복귀) · **3번 행 재생 시 `uris` 10개 = 꼬리 전체, `context_uri` 없음, Spotify 호출 1건** · 390은 `is-mobile` 전체 시트 + 탭 4개 + **뜯기 핸들러 미부착**. prod 스모크 **19/0**, 번들 마커 확인. **Step 6b 완료 2026-08-03~04 — 오너가 Step 6을 써보고 낸 지적 3건이 모두 같은 원인이었다**: 세션이 `currentItemId`(버킷 행 id)를 기준으로 삼아 **자기가 큐에서 시작한 재생만** 알 수 있었고, 제품의 다른 ▶ 는 전부 세션을 우회했다. **전반**(front **#348** `f4e2faa`) — 세션이 `readLivePlayback`으로 라이브를 흡수한다(마운트 1회 + `MYBLOG_PLAYBACK_CHANGED` 1회, **폴링 없음 — D28**): 큐 행과 매칭되면 그 행을, 아니면 `external`로 잡아 "〈기기〉에서 재생 중 · 대기열 밖"을 보여주며 **transport는 양쪽 다 동작**한다. `paused`는 idle로 접지 않고 흡수, `unavailable`은 침묵이 아니라 실패, 앵커는 `readAtMs`. **후반**(front **#349**) — **▶ 가 큐를 갈아끼운다**: 앨범 ▶ → 큐가 그 앨범 트랙들로 교체되고 0번부터 재생, 트랙 ▶ → 그 트랙으로 교체. 호출부 3곳(`AlbumOverlay`·`BucketBoard` 액션시트·`albumDetail.client.ts`)이 전부 `play()` 직행을 그만두고 세션의 단일 진입점 `replaceQueueAndPlay`로 들어간다. **대량 삭제 API가 없어 교체는 N+1 요청**이라, 순서가 곧 정확성이다 — **쓰기 먼저, 삭제 나중**(삭제를 먼저 하면 실패 창이 *빈 큐* 위에 열려 재생할 것도 되돌릴 것도 없어진다). 변위된 행은 낙관적으로 걷어내 패널이 즉시 맞고, 실제 DELETE는 순차로(서버가 `position`을 다시 매기는 중에 N개를 동시에 쏘는 건 아무도 안 재본 레이스) 재생이 시작된 뒤에 돈다. 삭제가 실패하면 낙관적 화면을 거짓말로 두지 않고 강제 재조회 후 문구로 알린다. **파괴적이므로 Undo**(기존 idiom — 라벨 + 되돌리기, 확인창 없음; OQ3가 고른 트레이드). Undo는 행만 되살리고 **소리는 끊지 않는다** — 되돌릴 대상은 잃어버린 큐지, 방금 시작한 재생이 아니다. 곁가지로 확정삭제 트리 워커가 `session.ts`와 `PlaybackPanel.tsx`에 **한 글자도 안 틀리고 두 벌** 있던 것을 `queue.ts` 한 곳으로 합쳤다. 검증: vitest **281**(270→281) · lint · astro check 0 errors · **대조 실험** — 순서를 삭제-먼저로 뒤집으면 정확히 4건이 깨진다(반쯤 지워진 큐 불변식 + 실패보존 2건 포함)라 자기충족 테스트가 아니다 · **grep 게이트 2종을 베이스라인 대비로**(`api.spotify.com` 7→7 **추가 0**; 세션 우회 `play({…})` 7→4 — 줄어든 3건이 정확히 이번 호출부) · **실브라우저 실측**(prod 데이터, 스텁이 Spotify 적용 지연 900ms 모델링): 앨범 ▶ → 큐가 *OK Computer* 12곡에서 *Kid A* 11곡으로, 보드 시트 ▶ → *Swimming* 13곡으로 **앨범 순서 그대로** 교체되고 **250ms 간격 전수 샘플링에서 큐 길이가 11 아니면 13뿐 — 중간값이 한 번도 없다**(반쯤 지워진 상태 부재를 추정이 아니라 측정), 매 전송이 `uris` 꼬리 전체 + `context_uri` 없음 + Spotify 호출 1건, 되돌리기로 이전 12곡이 **순서까지 정확히** 복구됨. **⚠️ 이번에 발견한 선행 결함(내 변경 아님, #348 소관)**: 우리가 쏜 play/pause 직후 `MYBLOG_PLAYBACK_CHANGED`가 즉시 `adoptLive()`를 부르는데 이 읽기가 **Spotify의 ack→적용 창 안에** 떨어져 방금 세운 상태를 낡은 답으로 덮는다 — 재생을 시작했는데 패널은 ▶(정지)로 보이고, 일시정지했는데 Ⅱ로 되돌아간다. 다음 이벤트에서 스스로 낫는 표시 전용 결함. **손대지 않았다** — 고치려면 "우리가 일으킨 읽기를 무시"하거나 적용 창 상수가 필요한데, 이 RFC가 Step 7에 걸어둔 "상수는 추측하지 말고 실측" 규율이 여기에도 걸린다. **오너 결정 대기**. **트랙 ▶ 는 배선했지만 휴면 상태로 나간다** — 유일한 표면이 `/review/[slug]`인데 prod에 평론 페이지가 **0개**라(6c 다음에 듣기와 같은 처지) 실브라우저로 누를 수가 없다. **Step 7 완료 2026-08-04**(front #350 `40f27a6`) — 탭이 여러 개여도 **재생을 소유하는 탭은 하나**고, 나머지는 같은 세션을 읽기전용으로 비추며 "이 탭에서 재생하기"로 넘겨받고, 소유 탭이 죽으면 남은 탭이 주워간다. **측정이 설계를 바꿨다** — 순진한 타임아웃은 애초에 성립하지 않는다: 숨은 탭의 1초 인터벌은 **60.0초에 한 번**으로 떨어지고(그것도 5분이 아니라 **숨은 지 ~65초** 만에), 게다가 **크롬이 최전면 앱이 아니면 활성 탭까지 전부 `hidden`**이라 스로틀링은 예외가 아니라 재생 탭의 평상시 상태다. 안전하려면 임계값이 60초를 넘어야 하는데, `SIGKILL` 후 리스가 **42.6초 낡은 채로 디스크에 남는** 것까지 재고 나면 그건 크래시 후 1분 넘게 재생을 못 줍는다는 뜻 — 정반대 실패다. 그래서 **하트비트 나이를 '물어볼 자격'으로 강등**하고 생존은 채널 챌린지/응답으로 증명한다(스로틀된 탭이 **≤2ms, 48/48** 응답 — 메시지 핸들러는 타이머 태스크가 아니라서 스로틀을 안 받는다). `HEARTBEAT_MS=5s · STALE_MS=15s · CHALLENGE_MS=1s`. 명시적 양도(`claim()` 무조건)와 죽은 소유자 회수(챌린지 무응답 시에만)는 **다른 경로**로 갈라 뒀다. 앵커는 `performance.now()` 원점이 문서마다 달라 **에포크로 변환해 실어 보내고 받는 쪽에서 복원**한다(그대로 보내면 미러의 진행선이 두 탭의 나이 차만큼 틀린다). **소유권은 SDK 기기와 transport만 가른다 — 큐는 어느 탭에서든 그대로 읽고 고친다**(rung 1에서는 미러의 조작을 소유 탭으로 **전달**해 둘 다 리모컨으로 쓴다). **#348·#349가 중간에 main에 들어와 리베이스**했고, 리스 말고도 3가지를 붙여야 했다(각각 없으면 깨지는 테스트 동반 확인): `external` 현재재생을 전선에 실어야 하고(안 실으면 미러가 '재생 없음'을 자신 있게 잘못 보여준다), `adoptLive()`는 **소유자 전용**이며(두 탭이 각자 흡수하면 서로의 상태를 조금씩 낡은 읽기로 덮어쓴다), `replaceQueueAndPlay`는 전달이 아니라 **리스를 가져간다**(▶는 이 탭을 SDK 기기로 띄울 수 있고 Undo는 누른 탭의 것이다). **위임 구현 리뷰에서 결함 1건 발견·수정**: `gate()`가 `ownerRung==='remote'`일 때만 전달해서, 큐가 비면 `advance()`가 rung을 null로 되돌리는 탓에 **한 탭이 재생하고 멈춘 상태에서 다른 탭의 드롭이 어느 탭에서도 재생되지 않고 조용히 사라졌다** — 전달은 두 번째 기기를 만들지 않으므로 rung과 무관하게 전달한다. 검증: vitest **293**(281→293) · lint · astro check 0 errors · build 2210쪽 · **grep 게이트를 베이스라인 대비로**(`api.spotify.com` 9→9 **추가 0**, 새 fetch 0) · **실브라우저 2탭 실측**(prod 데이터): rung 1에서 B가 ⏸를 눌러 **Spotify 호출 0건**인데 A가 `PUT /pause`를 쏘고 둘 다 ▶로 뒤집힘 · 양방향 양도(B가 시작한 곡을 A가 비추다가 넘겨받음) · **스로틀된 소유자는 리스가 39초(임계의 2.6배) 낡도록 안 빼앗김**(순진한 타임아웃이면 15초에 빼앗긴다) · **죽은 소유자는 15.9초 만에 회수**(예측 ≈16초) · rung 2 미러는 transport 3개가 disabled인데 **큐 행은 전부 클릭 가능**. **Step 8 preflight audit 완료 2026-08-04**(front #352, squash `96cf773`) — Step 8 진행 전 오너가 요청한 배포된 플레이어 전수 조사. 기록만 하지 말고 실제로 고치라는 지시였고, 6건을 원인까지 확인 후 수정했다: (1) 우리 자신의 transport가 자기 adoption과 경합해 방금 세운 상태를 낡은 읽기가 덮던 것 — `localWriteSeq` 가드로 해결(적용 창 상수 추측 아님), 이 RFC 6b가 "오너 결정 대기"로 남겨뒀던 항목. (2) 자연 종료가 UI·큐에 전혀 반영 안 됨 — `onCompleted()` 호출부가 앱 전체에 **0곳**이었다(확인된 사실, 추정 아님); rung 2는 SDK `player_state_changed` 푸시 이벤트를, rung 1(Connect 리모컨 — "오너가 실제 쓰는 건 전부 이 경로")은 트랙당 1회 예약 확인(폴링 아님, D28 유지)을 새로 연결해 둘 다 기존 `adoptLive()`로 흘려보낸다. (3) 가사·트랙 정보 버튼이 모든 마운트 지점에서 리터럴 no-op(`NOOP_PLAYBACK_ENTRY`)에 연결돼 있었다 — 가사는 `ent:open-album`을 본뜬 새 앱 전역 이벤트(`ent:open-live-lyrics`)로 실제 라이브 가사 뷰어를 연다(코드에 이미 적혀 있던 "앨범처럼 앱 전역 이벤트가 생기기 전까지" 그 이벤트); **트랙 정보는 이번 세션에서 오너 지시로 그대로 둠**(트랙 전용 목적지가 제품에 아직 없고, 그건 `ARCH-entity-interaction-v2`의 범위이자 바로 이 Step 8의 게이트 대상이라 새로 만드는 건 조사 범위 밖). (4) 앨범 정보가 대기열 밖 재생(외부 시작)에서 조용히 실패 — `ExternalNowPlaying`이 Spotify 앨범 id를 안 실어 날랐던 것 확인, 기존 `by-spotify` 카탈로그 조회로 연결. (5) 모바일 재생 시트의 탭 4개(대기열/가사/트랙/앨범)가 `role="tab"`/`aria-selected`를 달고도 본문은 항상 대기열만 보여줬다(확인된 사실) — 대기열만 진짜 탭으로 남기고 나머지 3개는 정직한 액션 버튼으로 교체. **검증**: `pnpm lint`·`astro check` 0 errors·`pnpm test` 447/447(신규 4건 — write-seq 레이스 가드, 자연 종료 감지 매치/불일치 양쪽, rung 2 이벤트가 "스스로는 절대 안 울림"까지); 로컬 dev 서버 + 스텁 백엔드·Spotify로 데스크톱·390px 모바일 실브라우저 CDP — 가사 버튼 실클릭이 실제 라이브 가사 뷰어를 여는 것, 외부 재생 상태에서 앨범 정보가 실제 앨범 오버레이를 여는 것, 모바일 시트가 진짜 탭 1개 + 정직한 버튼 3개로 보이는 것까지 화면으로 확인. **손대지 않은 것, 숨기지 않고 기록**: `NowPlaying.tsx`는 `lib/playback/session.ts`와 완전히 독립된 두 번째 라이브 재생 추적기다(서로 상태를 공유한 적이 없음) — 같은 부류의 레이스가 있는지 따로 확인했더니 `NowPlaying`은 이미 자기만의 다른 방식(자기 명령 진행 중엔 확인읽기 자체를 안 함)으로 고쳐져 있어 새로 깨뜨리진 않았지만, 두 추적기를 하나로 합치는 건 이번 조사 범위보다 큰 별도 작업으로 남겨뒀다; rung 1 예약 확인의 1500ms 버퍼는 1차 추정치이고 Step 7 리스 상수처럼 실측되지 않았다. **Step 8의 게이트 자체도 이 작업과 별개로 이미 풀렸다**: `ARCH-entity-interaction-v2` Step 1–4가 오늘(2026-08-04) 전부 배포됐다(front #351, 병행 세션) — Step 8이 요구한 건 Step 1–3뿐이라 조건은 충족. **Step 8 착수는 오너 판단**(하드 룰 4 — 이 세션은 사전 점검이었지 RFC 스텝이 아니었음). → `docs/rfcs/FEAT-playback-bucket-player.md`.
- **ARCH-entity-interaction-domain-audit** (**draft**, 작성 2026-08-05) — `ARCH-entity-interaction-v2`가 non-goal로 비운 영역(리뷰/메모 커맨드, 재생 상태 소유권, 가사 호스트, 버킷 담기 플로우, 모달 인프라, 전역 이벤트)의 독립 5-에이전트 감사. **BUG-20(front #355, 세션 내 즉시 수정, RFC 스텝 아님)**: `LikedBoard`가 sync 전용 `spotify_library` 버킷 배제 규칙을 빠뜨려 실사용 데이터 오염 경로였던 것 + `trashAlbum`의 temp id 서버 전달 레이스 — 둘 다 수정+테스트+prod 배포+스모크 완료(`docs/archive/done/2026-08.md`). 오너가 감사 결론 전체를 승인(2026-08-05, "다 추천으로 ㄱ") — Status는 표준 규칙대로 명시적 승격 전까지 draft. **Step 1 완료 2026-08-05**(문서 전용, `component-map.md` 범도메인 레지스트리 3종 — 이벤트/상태소유자/모달 — 추가 + 사실 오류 2건 수정: `AddToBucketMenu` "manual ESC" 주장 근거 없음·`NowPlaying`/`LikedBoard` "링크 불가" 주장은 `ARCH-entity-interaction-v2` Step 1이 이미 08-04에 stale로 잡았으나 문서는 안 고쳐져 있었음. `ARCH-entity-interaction-v2` Step 6과의 겹침은 병합이 아니라 명시적 이관으로 처리 — E1 앨범/트랙/아티스트 정규 정의는 여전히 그 RFC의 Step 6 몫, 여기서 옮겨 적지 않음. **Step 2 완료 2026-08-05**(front #356, ESLint `no-restricted-syntax` G3+G5 — 이벤트 리터럴/`spotify_library` 리터럴 금지, `PB_ADD_TRACK_EVENT`를 `lib/pocketBuckit/events.ts`로 이관해 위반 해소, prod 스모크 19/19). **Step 4 완료 2026-08-05**(front #357, ad hoc ESC 전용 다이얼로그 7개를 `useDismissable`로 이관 — `RecentAlbumsModal`/`RecentTracksModal`/`TrashDrawer`/`ItemDetailSlideover`/`CommandPalette`/`DraftsInbox`/`gm-shared`의 `Peek`; `ActionSheet`는 RFC가 명시한 예외로 유지. `useDismissable.test.ts` 신설(기존 0건) + 마이그레이션 컴포넌트마다 인터랙션 테스트. **실브라우저 CDP 검증이 실제 버그를 잡음**: `Peek`은 `GenreMap.tsx`가 마운트 해제 없이 `nodeId=null`로 한 번 마운트했다가 나중에 값을 바꾸는 구조라, 다른 6개와 같은 `useDismissable(true, ...)`를 그대로 쓰면 마운트 이펙트가 `ref.current`가 아직 `null`인 시점에 딱 한 번만 돌아 ESC는 되지만(onClose를 매 렌더 갱신되는 ref로 읽어서) autoFocus·Tab-trap이 조용히 죽어 있었다 — G4가 막으려던 바로 그 회귀. `open`을 `!!node`로 고쳐 수정, 회귀 테스트 추가, 배포 후 prod(`www.ratemymusic.blog/genres/`)에서 실 데이터로 재확인. OQ3 해소(TrashDrawer 비-포털 마운트는 의도 아님 — 마이그레이션함, 포털 자체는 Step 4 범위 밖이라 그대로). lint 0·astro check 0·vitest 36/36 파일·478/478. **OQ1 해소 2026-08-05**(오너 결정: Step 3은 이 RFC 소관 유지 — `FEAT-member-player`는 열린 슬롯 없음, `FEAT-playback-bucket-player`는 자기 Step 8 감사에서 이미 범위 밖으로 명시). **Step 3을 3a/3b/3c로 분할 2026-08-05**: 코드 대조 결과 원래 한 문단 추정치보다 두 자릿수 더 컸음(`NowPlaying.tsx`/`LyricsViewer.tsx` 합쳐 ~3,500줄, `session.ts`와 구조적으로 안 겹치는 독립 상태). 3a = 이름 붙은 버그만(`controlBusyRef`가 자기 명령 대기 중 진짜 외부 변경까지 씹는 것 — `session.ts`의 `localWriteSeq` 패턴 이식, front-only 소규모), 3b = `NowPlaying`이 재생 트랙 식별만 `playbackSession`에서 가져오도록, 3c = `LyricsViewer` 동기화 앵커 이관(`FEAT-lyrics-sync-precision`이 튜닝한 서브-100ms 로직이라 최고위험 · 3b 이후로 게이트). **외부 리뷰 후속 완료 2026-08-05**(9개 가설 검증, 코드 대조 — Step 1/2/4는 상태 그대로 정확했음 확인): component-map.md 경로 오기 정정(파일은 그대로 workspace, RFC의 `myblog_front/` 접두만 오류) + front 코드 주석 2곳도 저장소 명시(front #359); ESLint G3/G5 교차면제 버그 확인·분리(front #358); PR 템플릿 신설(front #358); **spotify_library가 백엔드에 검증이 아예 없던 것 확인·수정**(`add_item`/`expand_artist_source`/`expand_album_tracks`/`reorder` 4곳, backend #151); G2가 실제로 막아야 할 미수정 사례 2건 발견·수정(`insertAlbum` temp-id 가드, `releaseShared.tsx` 이종-네임스페이스 id 유입 — 둘 다 front #358; G2 자체는 여전히 CI 게이트 없음, "Partially" 유지); `ARCH-entity-interaction-v2` E6 문구 정정(코드는 항상 데이터 기반이었음, 문장만 화면-맥락으로 오독 유발). **Step 3a 완료 2026-08-05**(front #360): `NowPlaying.tsx`의 `controlBusyRef`가 자기 명령 대기 중 진짜 외부 재생-변경 이벤트까지 씹던 레이스를, `session.ts`의 `localWriteSeq` 패턴(컴포넌트 스코프 `useRef` 카운터 — 더 최신 로컬 authoritative write가 없으면 읽기를 적용, 있으면 스스로 폐기)으로 교체. `controlBusyRef` 자체는 그대로(재진입 가드 역할 유지), 리스너 안에서만 교체. `useNowPlaying` 훅을 테스트 가능하게 export하고 `NowPlaying.test.ts` 신설 — 인플라이트 명령 중 진짜 외부 이벤트가 반영되는 케이스(수정 전 코드로 되돌려 실패 확인 후 복구) + 자기 커맨드의 echo는 여전히 폐기되는 케이스. 실기기 검증 불필요(RFC가 순수 가드 로직 변경으로 명시). lint 0·astro check 0·vitest 38/38 파일·482/482. prod 스모크 19/19. 3b/3c(재생 상태 실제 병합)는 미착수, 새 세션. **Step 3b 완료 2026-08-05**(front #361): `useNowPlaying`이 `playbackSession`을 `useSyncExternalStore`로 구독(`PlaybackPanel.tsx`와 같은 패턴) — 재생 트랙 식별(`trackId`)+앵커만 세션에서 가져오고, tier/mode/like/device/reconnect는 그대로 카드 자체 로컬. `controlBusyRef`로 게이팅(자기 컨트롤 콜 진행 중인 세션 업데이트는 무시 — Step 3a와 같은 레이스가 컴포넌트를 건너서 재발). **실브라우저 검증(로컬 dev + 스텁 백엔드 + 인페이지 Spotify 스텁, ack→apply 지연 모델링)이 배포 전 실버그 2건을 잡음**: (1) 카드의 실제 렌더 게이트는 `moment`가 아니라 `np`(title/artist/cover)라 첫 구현은 `moment`만 수렴하고 카드는 계속 IdleBox였음 — 새 트랙 수렴 시 `np`도 최소한으로 시딩하도록 수정; (2) 이 카드 자신의 `onPlaybackChanged` 읽기가 Spotify ack→apply 지연과 경합해 stale 'idle'로 착지하면서, 세션 구독이 방금 맞게 세팅한 상태를 무조건 지워버리던 것 — idle 분기 진입 전에 `playbackSession`의 최신 상태를 다시 확인하도록 수정(세션이 "재생 중"이라고 하면 stale idle 읽기가 못 덮어씀). 락스크린 Media Session은 간접 검증만(발행 이펙트 자체는 무변경, 입력값이 올바르게 채워짐만 확인 — rung-2 SDK 실기기 클릭테스트는 이번 하네스 범위 밖, 과대주장 안 함). lint 0·astro check 0·vitest 38/38 파일·484/484(신규 2건: 크로스-트래커 수렴, 컨트롤콜 중 지연). prod 스모크 19/19. 3c(`LyricsViewer` 동기화 앵커)는 이 Step이 prod 안정 확인된 뒤로 계속 게이트, 새 세션. **Step 3c 완료 2026-08-06**(front #374), Step 3(3a/3b/3c) 전체 종결: 세션 시작 시 코드 대조로 `refresh()`가 anchor를 identity/jump·skip 확인/번역/커버·메타와 한 함수에서 다루는 걸 확인, 오너에게 두 설계안 제시 후 **보수적(세션 우선, 폴백 유지)** 안 채택 — `playbackSession.currentSpotifyTrackId()`(신규, `rowForSpotifyTrack`의 역방향 대응) 로 같은 트랙임을 확인할 때만 세션의 anchor/playing/durationMs를 추가로 받아들이고(additive, `refresh()`는 무변경), 확인이 안 되면 뷰어 자체 읽기가 계속 유일한 소스. 이 뷰어의 트랜스포트는 `playbackSession`을 거치지 않고 `sendPlayerCommand`를 직접 부르므로, 세션의 재수용이 이 컴포넌트 자신이 낸 명령의 ack→apply 지연과 경합할 수 있어 `refresh()`가 이미 쓰던 `awaitingTrack`/`awaitingChangeFrom`/`awaitingPlayState` 가드를 그대로 재사용(BUG-28 erratum이 확인한 것과 같은 3-way 팬아웃). CDP 검증(스텁 백엔드 + 인페이지 Spotify 스텁, ack→apply 지연 모델링)으로 세션 채택과 가드 둘 다 실동작 확인 — 세션 갱신 시 포커스 줄·재생 상태가 정확히 따라가고, 뷰어 자신의 ⏸ 직후 3개의 상충하는 stale 읽기(뷰어 자체 refresh·session.adoptLive·NowPlaying 리스너)가 동시에 도착해도 가드 창 동안 뒤집히지 않음 확인. lint 0·astro check 0·vitest 47/47 파일·510/510(신규 4건, `currentSpotifyTrackId`). prod 스모크 19/19 — PR #374 코멘트. 다음 = Step 5(memo 이름 충돌, `FEAT-album-review-authoring` 게이트 2026-08-12+) — 새 세션. → `docs/rfcs/ARCH-entity-interaction-domain-audit.md`
- **ARCH-entity-interaction-v2** (**accepted** — 오너 승격 2026-08-03; **Step 1 전수 감사 완료 2026-08-04**(ws #818) · **Step 2 정규 정의 + 드롭 매트릭스 완료 2026-08-04**(ws #819) · **Step 3 단일 드래그 페이로드 + 어댑터 배포 2026-08-04**(front **#351** `eeea113`) · **Step 4 제스처 실측 완료 2026-08-04**(ws 문서 전용, 배포 없음); **OQ3 오너 확정 2026-08-04**(권고 기본값 그대로 — HeaderSearch A8/B5·라이터 추천 트랙 ★ B9·라이터 작품 검색 C9만 제외); **Step 5·6 완료 2026-08-06 — RFC 6단계 전부 종결** (free wins 2026-08-04 배포 시작, front #353 `46962eb`) · **TrackRow play/add/drag 슬롯 개방 + memo-trow 흡수 배포 2026-08-05**(front #354) · **E7 앨범 id 투영 + 배선 배포 2026-08-05**(front #362) · **보드 거부 시각 상태 렌더 배포 2026-08-05**(front #363 `325b2b4`) · **E1 Rule 0 G5 동기 id 획득 배포 2026-08-05**(front #364 `0ea8d233`) · **TrackRow add grant(멤버 앨범 상세 트랙리스트) 배포 2026-08-05**(front #365) · **TrackRow drag grant(LikedBoard, G2 종결) + 백엔드 track_id resolve 수정 배포 2026-08-05**(front #366 + backend #152) · **TrackRow play grant(멤버 모달 + 공개 AlbumOverlay) 배포 2026-08-06**(front #367) · **TrackRow drag grant(AlbumDetail 모달 2곳, 스크림 pointer-events 통과 수정) 배포 2026-08-06**(front #368) — **Step 5 원래 스코프(표면 3그룹 × play/add/drag) 전부 grant 완료**; **RFC 6단계 전부 완료(2026-08-06)** — 다음 없음, Status 승격(accepted → 그 다음)만 오너 결정 대기 — **Step 6 결과(component-map.md 재작성, front repo 아님·워크스페이스 문서 전용)**: E1 정규 정의(앨범/트랙/아티스트 3개 표+ Rule 0 id-형태 표)를 처음으로 component-map.md에 옮겨 적었다(Step 2가 "아직 하루 된 정의를 또 옮기면 두 번째로 낡은 사본이 생긴다"며 Step 6로 미뤄뒀던 바로 그 일). "Track-click behavior" 절을 표면별 play/add/drag grant 실측 표로 다시 썼다 — 2026-08-05에 적힌 "아직 아무 표면도 안 받았다"는 문장이 딱 하루 만에 거짓이 된 것(이 RFC의 결정 로그가 이미 두 번 지적한 것과 같은 패턴 — 줄번호 드리프트, 그리고 component-map.md 자신의 예전 "NowPlaying/LikedBoard 링크 안 됨" 주장이 그걸 찾아낸 RFC보다 오래 살아남은 것). **이번 스텝이 목표로 안 삼았는데 재검증하다 걸린 것**: play 경로 서술을 고치려고 코드를 다시 보니 문서가 계속 인용해온 `requestPlayback`이라는 함수가 아예 없었다 — `FEAT-playback-bucket-player` 도중 `play()`로 이름이 바뀌었는데 문서는 갱신된 적이 없었다. "정확히 2개 파일에서 import"라는 카운트도 양방향으로 틀렸다: `scripts/albumDetail.client.ts`는 이제 그걸 아예 안 쓰고(`playbackSession.replaceQueueAndPlay`로 대체 — 이번 세션 play/drag grant들이 쓰는 바로 그 호출), `play()` 자신은 지금 정확히 4개 파일에서 직접 import된다(2개가 아니라). impact template에 이 RFC(Step 1-6) 채운 항목도 추가, 문서 상단 Verified 스탬프를 `bcb6544` 기준 2026-08-06로 재기록. 검증: 문서 전용 — `BoardAlbum`/`OpenAlbumDetail` 필드 존재, `routeAlbumDrop` export, 표면별 정확한 `actions={{...}}` 배선(LikedBoard/AlbumDetailView/AlbumOverlay/MemoWindow), `play()` import 사이트 4곳 — 전부 이번 세션에 `bcb6544` 기준 grep으로 재확인. **RFC 자신의 마지막 스텝**이라 이걸로 6단계 전부 종결. — **Step 5 결과(9번째 슬라이스, TrackRow drag grant on AlbumDetail modals, front #368)**: Step 5의 마지막 남은 항목 — `AlbumDetail`의 두 모달(`StandardModal`/`MemoWindow`)에 `drag`를 grant했다. 구조 문제(스크림 `z-index:90`이 트레이 `~72`보다 위)는 오너가 고른 옵션대로 "드래그 중에만 스크림 pointer-events를 끄기"로 풀었다 — `PB_DND_START_EVENT`/`PB_DND_END_EVENT`(이미 모든 grant된 TrackRow가 쏘는 그 이벤트)를 구독해 그 창에서만 스크림의 `pointer-events`를 `none`으로. **실브라우저 디버깅이 잡은 함정**: 스크림 서브트리 전체(모달 카드까지 포함)를 `pointer-events:none`으로 내리면 네이티브 드래그 세션이 조용히 죽었다 — `dragstart`는 뜨지만 `dragover`/`drop`이 문서 전체에서 단 한 번도 안 뜸(대조군으로 모달 없는 짧은 드래그는 정상 동작 확인). 원인은 드래그 중인 행 자신이 `pointer-events:none`을 상속해 브라우저의 드래그-소스 히트테스트가 깨진 것으로 추정 — 모달 카드(`.lf-modal-card`/`.memo-modal`)에 `pointer-events:auto`를 명시해 카드 안쪽은 항상 정상, 카드 바깥 배경만 드래그 중 통과되게 고치자 실 트러스티드 CDP 드래그로 `dragstart`→`start`→`board-start`→`dragover`(칩)→`drop`→`board-drop`까지 전부 발화하고 실제 `POST /api/buckets/{id}/items` **201** 확인. 검증: lint·astro check 0 errors·vitest 46개 파일 503개 통과(+4, `AlbumDetailView.dragTrack.test.tsx`·`AlbumDetail.dragTrack.test.tsx` — 페이로드 모양 + 스크림 pointer-events 토글/복원, 두 모달 다); 로컬 CORS+JWT 프록시로 prod 데이터를 물려 실제 버킷 앨범(Troye Sivan *Bloom*)에서 모달 안 트랙을 재생 대기열 칩으로 드래그 → 실제 큐 추가 확인, 테스트 중 앨범 전체 드래그(대조군)로 우연히 늘어난 행까지 포함해 매번 원래 13곡·순서 그대로 복구(잔여물 0). 배포 후: `scripts/smoke.sh prod` 19/19 PASS, S3 `_astro/**` 번들 타임스탬프가 배포 완료 시각과 일치. → PR #368 코멘트에 스모크 결과 기록. — **Step 5 결과(8번째 슬라이스, TrackRow play grant, front #367)**: RFC §Step5가 남긴 마지막 슬롯 `play`를 grant했다 — 6번째 슬라이스가 "새 재생 프리미티브가 필요해 범위 밖"이라 적어둔 전제가 틀렸음을 착수 전 재감사로 발견: `playbackSession.replaceQueueAndPlay({kind:'track', trackId, title})`는 이미 존재하고 테스트(`session.test.ts:605`)도 있고 바닐라 평론 페이지의 트랙별 ▶(`albumDetail.client.ts:147`)에서 이미 프로덕션에 쓰이고 있었다 — 아무도 그걸 React `TrackRow`의 `play` 슬롯에 안 붙였을 뿐. `AlbumDetailView`의 공유 `Tracklist`에 `onAddTrack`과 같은 모양의 `onPlayTrack` prop을 신설해 `actions.play`에 연결. `add`와 달리 **양쪽 호스트 모두에** grant했다 — 멤버 `AlbumDetail` 모달(StandardModal, 이 모달엔 지금까지 재생 자체가 없었다 — 토스트는 `AlbumOverlay`에서 그대로 포팅)과 공개 `AlbumOverlay`(`isLoggedIn() && !target.unresolved`로, 기존 앨범 단위 ▶과 같은 게이트) — `add`가 공개를 뺀 이유("무명 방문자는 담을 버킷이 없다")가 `play`엔 안 통한다: `AlbumOverlay`는 이미 로그인 방문자에게 앨범 단위 ▶을 주고 있어 트랙 단위를 뺄 의미론적 근거가 없다. 검증: lint·astro check 0 errors·vitest 44개 파일 499개 통과(+3, `AlbumDetailView.playTrack.test.tsx`·`AlbumDetail.playTrack.test.tsx`); 로컬 CORS+JWT 프록시로 prod 데이터를 물려 CDP로 실제 버킷 앨범(Troye Sivan *Bloom*)에서 두 표면 모두 트랙 ▶ 클릭 → 큐가 그 한 곡으로 교체(`POST` 새 행 + 이전 13행 순차 `DELETE`) → 스모크 계정에 연결된 Spotify 기기가 없어 재생 자체는 토큰 실패로 "Spotify를 연동하면…" 토스트 + 되돌리기(기존 앨범 ▶과 같은 실패 모드) 확인, 매번 원래 13곡·순서 그대로 복구(잔여물 0). 첫 공개-오버레이 시도에서 큐가 13→14로 늘고 삭제가 안 된 이상 현상을 만났는데, 이는 코드 버그가 아니라 두 테스트 사이 복구를 `bucketStore`를 거치지 않은 raw fetch로 했다가 앱의 인메모리 캐시가 DB와 어긋난 테스트 아티팩트였다(하드 리로드 후 재시도로 정상 확인). 배포 후: `scripts/smoke.sh prod` 19/19 PASS, S3 `_astro/**` 번들 타임스탬프가 배포 완료 시각과 일치. → PR #367 코멘트에 스모크 결과 기록. — **Step 5 결과(7번째 슬라이스, TrackRow drag grant on LikedBoard + G2 종결, front #366 + backend #152)**: 6번째 슬라이스가 미룬 drag를 재검토 — 착수 전 재실측으로 `LikedBoard`→`PocketTray`는 사실 스크림/탭 배타성 문제가 없음을 확인했다(`PocketTray`는 `layout.astro`에 `client:only`+`transition:persist`로 전역 마운트돼 탭과 무관하게 항상 떠 있음; 스크림/탭 배타성은 `AlbumDetail`·memo-trow·`LikedBoard`→`BucketBoard`에만 적용되는 문제였다). 오너 확인 후 `LikedBoard`의 TrackRow에 `actions.drag`를 `{origin:{kind:'external',copies:true}}`(버킷 멤버십이 아님)로 grant. 실 트러스티드 CDP 드래그(재생 대기열 칩)로 브릿지는 정확히 발화했으나 백엔드가 500 — `_add_typed_item`이 `track_id`를 내부 `Track.id` PK로만 조회했는데 `LikedBoard`(`Backend_SavedTrackItem`)엔 `spotify_track_id`뿐이라 UUID 파싱 실패가 처리 안 된 예외로 샜다(**G2가 지목한 바로 그 공백**). `bucket_service.py`에서 PK 우선 조회 후 `Track.spotify_id` 폴백으로 수정(backend #152, front보다 먼저 머지·배포해 아직 못 지원하는 백엔드에 grant가 나가지 않게 함). 검증: front lint·astro check 0 errors·vitest 496개 통과(+1, `LikedBoard.drag.test.tsx`); backend pytest 801개 통과·1 skip(무관)·0 fail(+2, spotify_id 폴백 성공 케이스·미상 id는 500이 아니라 404로 유지되는 케이스); 배포 후 prod에 직접 `POST`(스포티파이 트랙 id)로 **201**·해석된 내부 track id 확인·`DELETE` 204 정리(잔여물 0) — PR #152·#366 코멘트에 기록. `AlbumDetail`/memo-trow 모달의 drag는 여전히 미부여(스크림이 트레이를 덮는 문제 그대로, 다음 슬라이스 후보). — **Step 5 결과(6번째 슬라이스, TrackRow add grant, front #365)**: RFC §Step5가 남긴 마지막 항목("표면별 play/add/drag grant") 중 `add`만 이번 슬라이스 스코프로 grant했다 — 멤버 `AlbumDetail` 모달(정보/편집 모드)의 트랙리스트(`AlbumDetailView`, 공개 `AlbumOverlay`와 공유하는 본문)에 `onAddTrack` prop을 `onOpenLyrics`와 같은 host-gating 모양으로 신설: 공개 오버레이는 계속 생략(무명 방문자는 담을 버킷이 없다는 A11과 같은 논리), 멤버 모달만 공급. 실제 담기 흐름은 `member/AlbumDetail.tsx`의 `StandardModal`에 `ReviewTrackAdder`와 동일한 pending-intent 패턴(같은 `AddToBucketMenu`, 같은 `seq` 갱신)으로 배선하되 윈도 이벤트 우회 없이 평범한 콜백으로 — 모달과 트랙리스트가 같은 React 트리라서 필요 없음. **drag는 이번 슬라이스에서 안 뺐다 — 못 뺐다**: 세션 시작 시 오늘 코드의 TrackRow 소비처 3곳(`LikedBoard`/`AlbumDetailView`/memo-trow)을 전수 실측한 결과 어느 표면도 살아있는 드롭 타깃과 동시에 화면에 없음을 확인 — `AlbumDetail`/memo-trow 둘 다 `.scrim`(`z-index: var(--z-panel)=90`)이 `PocketTray`(`--z-pocket:70`+2≈72)를 덮고, `LikedBoard`는 `StatsTab`에서만 렌더돼 `BucketBoard`(`bucket` 탭)와 동시에 안 뜬다. 강행하면 착지할 곳 없는 죽은 드래그가 되므로, 오너에게 옵션 3개(add만 grant / 이번 세션에서 z-index까지 고쳐 drag 완주 / 착지 없이 dragstart만 검증)를 제시해 "add만, drag는 다음 슬라이스로"를 확정받았다. 검증: lint·astro check 0 errors·vitest 41개 파일 495개 통과(+3, 신규 `AlbumDetailView.addTrack.test.tsx`/`AlbumDetail.addTrack.test.tsx` — DB id로 `onAddTrack` 호출, `onAddTrack` 생략 시 버튼 없음, `AddToBucketMenu`가 클릭한 트랙 id/제목으로 마운트); 로컬 CORS+JWT 프록시로 prod 데이터를 물려 CDP로 실제 버킷 앨범(Troye Sivan *Bloom*)에서 트랙 ➕ 클릭 → 버킷 선택 시트(정확한 트랙명) → 재생 큐 모드 선택 → 실제 `POST /api/buckets/{id}/items` **201** → 버킷 카운트 13→14 확인 → page-context `DELETE`로 즉시 삭제(13으로 복귀, 잔여물 0); 390×844 모바일에서도 시트가 잘림 없이 렌더되고 탭이 먹히는 것을 스크린샷으로 확인. grep 게이트: `draggable`/`dataTransfer` 매치는 `origin/main` 대비 **증가분 0**(이번 슬라이스는 drag를 안 건드림). 배포 후: `scripts/smoke.sh prod` 19/19 PASS, S3 `_astro/**` 번들 타임스탬프가 배포 완료 시각과 일치. → PR #365 코멘트에 스모크 결과 기록. — **Step 5 결과(다섯 번째 슬라이스, E1 Rule 0 G5, front #364)**: `NowPlaying.tsx`의 `ArtistNames`가 컴포넌트 인스턴스마다 독립적으로 `resolveDbArtistId`(프로미스)를 재호출해 렌더 상태를 채우던 것을, `spotifyCatalog.ts`에 추가한 동기 getter `getResolvedDbArtistId`를 거치도록 바꿨다 — Rule 0가 "비동기 재조회는 내비게이션엔 합법, 드래그엔 불법(드래그 시작 시점에 id를 동기로 쥐고 있거나 드래그 불가여야)"이라 명시한 그대로, 미래에 어떤 표면이든 이 컴포넌트에 드래그를 grant할 때 읽어야 할 "유일하게 정당한 동기 id 읽기"를 만들었다. 게이트/판정 로직은 그대로(미해석 시 평문, 해석되면 링크) — 렌더 결과는 이전과 동일하고 id 확보 경로만 바뀌었다. 아직 어떤 표면도 여기서 드래그를 grant하지 않아 이번 슬라이스로 사용자가 보는 변화는 없다. 검증: lint·astro check 0 errors·vitest 39개 파일 492개 통과(+3, 신규 `ArtistNames.test.tsx` — 미해석시 평문→해석 후 링크, 다른 인스턴스가 이미 해석한 id로 새 마운트가 동기 시드되는 것, artists 없을 때 평문 폴백); 로컬 dev의 더미 인증 토큰(`isLocalEnv()` 우회)으로는 prod Spotify 세션을 못 흉내내 실 아티스트 링크 클릭스루는 못 했음 — prod 스모크(19/19 PASS) + 배포 후 라이브 번들(`SelfDashboard.D2DvxXkY.js`, 배포 직후 타임스탬프) 확인으로 대체. → PR #364 코멘트에 스모크 결과 기록. — **Step 5 결과(4번째 슬라이스, board reject visual, front #363)**: `BucketCard`는 *수락*일 때만 하이라이트를 켰고 거부 칸은 아무 시각 상태도 없었다(E3 §E3 line 540이 지목한 Step 5 의무) — 트레이가 이미 지키는 계약(`pocket.css` `[data-dropreject]`: 자리 유지·흐림·이유 표시)을 보드에도 그렸다. `boardDnd.ts`에 순수 함수 `memberAcceptsLabel(bucket)`을 추가 — `canAcceptAlbumDrag`가 이미 답하는 세 분기를 그대로 읽어 "이 타깃이 뭘 받는지"만 이름 붙인다(게이트 자체는 손대지 않음 — General은 라벨이 없다, 거부할 일이 없어서). `BucketCard`는 `dropTarget`과 나란한 `dropReject` 상태로 호버 중인 거부 대상+이유를 들고 opacity 0.4 + `title`로 그리고, `TrashDock`도 자신의 거부 분기(복사 소스·기존 라이브러리 앨범·비앨범 행)로 같은 처리를 받는다. 검증: lint·astro check 0 errors·vitest 38개 파일 489개 통과(+5, `memberAcceptsLabel`을 `canAcceptAlbumDrag`의 모든 거부 분기에 대해 고정); 로컬 CORS+JWT 프록시로 prod 데이터를 물려 실 CDP 네이티브 드래그(합성 DragEvent가 아니라 트러스티드 제스처 — 합성 dispatchEvent는 이 React 트리에서 안 먹힘, 세션에서 직접 확인) — 아티스트 멤버→재생 대기열: 거부(버킷 카운트 불변), 같은 멤버→General: 수락(이동 확인, 회귀 없음). 테스트용 General/Artist 버킷은 검증 직후 삭제(잔여물 0). 배포 후 라이브 번들(`SelfDashboard.*.js`/`PocketBuckit.*.js`)에서 거부 문자열(`만 받아요`/`data-dropreject`) 확인 + prod `/members/` 콘솔 에러 0. → PR #363 코멘트에 스모크 결과 기록. — **Step 5 결과(3번째 슬라이스 / E7, front #362)**: `buildReviewCards`가 이미 읽던 `d.albumIds`/`d.artistIds`를 방출되는 `ReviewCard`/`ReviewHit`에 실제로 투영(G1 종결) — 계약·백엔드·마이그레이션 없음, 프런트 전용. 각 카드의 커버를 `openAlbum()` 오버레이 버튼으로 열되(앨범 id가 있을 때만) 나머지(제목 등)는 그대로 평론 링크로 남기는 커버-버튼/텍스트-링크 분리 패턴으로 4개 표면(`CanonPage`, `ReviewsIndex` 5개 카드 변형 전부, `EditorialHome` 히어로+최신 평론 행, `SearchPage` 평론 페싯 — 마지막은 같은 파일의 기존 `AlbumCard`/`.gs-albcard-open` 패턴 재사용) 배선. `artistIds`는 투영만 하고 이번 슬라이스에서 링크는 안 걸었다 — `ReviewCard.artist`가 여러 아티스트를 join한 표시 문자열이라 `artistIds[0]`를 안전하게 매핑할 방법이 없음. `HeaderSearch`의 인스턴트 드롭다운 평론 행도 범위 밖으로 남겨뒀다 — 행당 단일 액션 키보드 모델(`flatAction`)이 두 번째 인터랙티브 영역과 안 맞음. 검증: lint·astro check(0 errors)·vitest 38개 파일 484개 통과; 로컬 fixture MDX 3건(앨범 id 있음/없음 섞어) + CDP로 실브라우저 클릭 확인 — 홈 히어로/최신 행, `/canon`, `/reviews` 그리드+리스트, `/search` 평론 페싯 전부 커버 버튼은 오버레이만 열고(URL 불변) 텍스트는 평론으로 정상 이동, id 없는 카드는 버튼 자체가 안 뜸, DOM 중첩 경고 0; fixture는 커밋 안 하고 검증 직후 삭제(잔여물 0). prod 스모크: 배포 성공(health smoke green) + 4개 페이지 200 확인 — **prod 발행 평론 0이라 이 변경은 오늘 아무것도 바꾸지 않는다**(RFC 자신의 명시적 전제, Step 5 커버리지 집계에서 "배포된 기능"으로 과대 서술 금지). — **Step 5 결과(부분, front #353)**: RFC §H가 지목한 "계약 변경 없는 free win" 2건만 이번 세션 스코프로 배선했다 — **A11 `/collection`**(앨범 48장, id는 있는데 클릭 가능 요소 0 → 카드를 버튼으로 감싸 `openAlbum()` 연결, A1/A7/A9와 같은 "오버레이만, 드래그 없음" 패턴 — 로그아웃 방문자에게 드롭 대상이 없어 드래그는 의도적으로 안 붙임)과 **C8 `ReleaseRadar`**(candidate·tracked 아티스트 행이 `artist_id`는 있는데 `artistHref` 미사용 → `SearchPage.tsx` 패턴대로 링크화; candidate 행은 체크박스 `<label>` 안에 있어 `stopPropagation()`으로 토글과 분리). 드래그 페이로드는 안 건드려 grep 게이트가 공짜로 통과. front #343(audit-p2, 다른 세션)이 같은 두 파일 중 `CollectionView.tsx`를 건드리고 있어 겹치는 줄을 피해 배선했다 — 머지 후 diff로 무충돌 확인. 검증: lint·astro check 0 errors·vitest 447/447; 로컬 CORS+JWT 프록시로 prod 데이터를 물려 CDP 클릭 확인(데스크톱+390px) — `/collection` "Pure Heroine" 클릭 → 실제 오버레이, `/radar` candidate·tracked 링크 클릭 → 실제 `/artist/{id}/` 이동, 체크박스 안 토글됨; 배포 후 라이브 번들에서 두 마커("앨범 보기"/"아티스트 보기") 확인 + 로그아웃 prod에서 재확인. 테스트용 tracked-artist 1건 + playback-queue 트랙 9건은 스모크 계정에 추가했다가 검증 직후 전량 삭제(잔여물 0). — **Step 5 결과(2번째 슬라이스, front #354)**: `shared/TrackRow.tsx`의 예약 슬롯 `play`/`add`를 실제 콜백 버튼으로 열고, `drag`(E2 `DragPayload`) 슬롯을 신설 — granting 시 네이티브 `draggable` + 기존 보드⇄트레이 브릿지(`PB_DND_*`/`PB_BOARD_DND_*`) 양쪽에 페이로드를 디스패치해, 다음에 어떤 표면이 grant하든 추가 배선 없이 실제로 동작한다(둘 중 하나만 재사용했다면 트레이가 수락 미리보기만 하고 드롭은 조용히 no-op — E3가 지적한 라이브러리 버킷과 같은 버그 클래스라 양쪽 리스너를 먼저 추적한 뒤 재사용). 이번 슬라이스에서 세 슬롯을 실제로 grant하는 표면은 아직 없음 — 표면별 배선은 남은 Step 5 몫. 동시에 **`memo-trow` 쌍둥이를 흡수**(E4): `member/AlbumDetail.tsx`의 `MemoWindow`가 자체 트랙 행을 그리던 것을 그만두고 `shared/TrackRow.tsx`를 새 `openLyrics` 액션(행 전체가 가사 버튼 — `role="button"`+키보드 지원, 기존 `open`은 앨범 열기 전용이라 재사용 불가)으로 렌더링하게 바꿨고, 죽은 `.memo-trow*` CSS를 지웠다. 검증: vitest 27개 파일·465개 통과(신규 `TrackRow.test.tsx` 9건 포함)·lint·astro check 0 errors; 로컬 CORS+JWT 프록시로 prod 데이터를 물려 CDP로 실제 버킷 앨범(Troye Sivan *Bloom*)의 메모창을 열어 10개 행 전부 TrackRow 기반 가사 버튼으로 렌더되고 클릭 시 올바른 제목/아티스트/앨범/커버로 가사 시트가 열리는 것을 확인, 테스트용 버킷은 검증 직후 삭제(잔여물 0). — **Step 4 결과**(문서 전용): raw CDP `Input.dispatchTouchEvent`(`resize_page`도 페이지-JS `DragEvent` 스텁도 아님)로 헤드리스 크롬을 직접 몰아, 실 프로덕션 표면(`BucketBoard` 세로 리스트·최근 들은 앨범 가로 스트립·`PocketTray` 포인터 재정렬 레일)에 실 카탈로그 앨범(임시 버킷 5개, 검증 후 전량 삭제 — prod 잔여물 0)을 채워 놓고 측정했다. **핵심 결과**: 네이티브 `draggable`은 터치에서 0–60px 전 구간 **`dragstart`를 단 한 번도 내지 않는다** — 브라우저 자신의 `pointercancel`이 스크롤-대-탭 판정을 앱보다 먼저 가로채므로, Step 5가 이식할 "드래그 임계값"이란 게 애초에 존재하지 않는다(Step 1의 Are.na 실측과 정확히 같은 결론을 자사 표면에서 재확인). 케밥(⋯) 탭-대체 경로는 0px에서 매번 액션시트를 연다 — 신뢰 확인. `PocketTray`의 유일한 실제 터치 포인터 드래그(`Math.hypot<6` 임계값 + `touchAction:none`)는 이 하네스에서 **완주를 확인하지 못했다**(21프레임 폴링 내내 `data-reordering`이 한 번도 `true`가 안 됨) — "깨졌다"가 아니라 **미확인**으로 기록, 실기기 확인 전엔 Step 5가 이 패턴을 다른 표면에 복제하지 않는다. **OQ4 결론**: 롱프레스 드래그는 이제 근거가 전무(Are.na 포기 + 네이티브 드래그 터치 완전 불참, 2건 모두 반대 증거)하고, 케밥/`AddToBucketMenu` 탭-대체가 소거법으로 채택된다. 측정 표 전체 → RFC §Step 4. — **Step 3 결과**: E2의 단일 `DragPayload`(`lib/entityDrag.ts`)와 양방향 어댑터를 넣고 **모든 드래그 소스가 새 페이로드를 만들고 모든 규칙 호출부가 경계에서 어댑트**하게 했다. `boardDnd.ts`의 라우팅은 **한 줄도 다시 쓰지 않았다**(검증된 자산). `DndItem.kind:'album'` → `'member'`(E2가 지시한 개명 — 실제 의미는 "아무 타입의 멤버"). **어떤 표면도 드래그를 새로 얻지 않았다.** 곁가지로 **섬 간 배선 2개가 두 번째 드리프트 쌍이었던 것**을 없앴다 — `PB_DND_START`·`PB_BOARD_DND_START`가 각각 `DndItem`의 수기 복제본을 실어 나르던 것을 페이로드 자체로 바꿨다. **코드가 명세를 3번 정정했고 2개는 같은 부류다**: (1) `origin`은 2종이 아니라 **3종** — `spotify_library` 행은 *진짜 멤버십인데도 복사되어* 나간다(버킷이 동기화 소유), `internal`로 합치면 복사가 이동이 된다. (2) `external`엔 **`copies` 비트**가 필요하다 — 멤버십 없이 앨범 id만 든 소스는 General 버킷에서 **아무 일도 안 하는데**, "멤버십이 아니면 copy"로 추론하면 그 no-op이 조용히 `copyAlbum` **쓰기**가 된다. **둘 다 리뷰가 아니라 왕복 테스트가 잡았다.** (3) `EntityRef`에 **`bucket`**이 필요하다(버킷 노드도 드래그된다). **어댑터는 주장이 아니라 측정이다** — 페이로드 27종 코퍼스(테스트의 모든 페이로드 + 실제 드래그 소스 4종)를 `fromDndItem`→`toDndItem`으로 왕복시켜, 4개 수락 게이트 대상마다 **같은 수락 답 + 같은 `ops` 호출을 같은 순서로** 요구한다. vitest **293 → 439**(기준선은 가정하지 않고 `origin/main`에서 실측). **Step 2가 넘긴 라이브러리 항목은 포함했고, 범위는 Step 2 설명보다 좁다** — 게이트가 `type`만 보던 것을 `kind`도 보게 해서 트랙/아티스트 행이 **수락처럼 하이라이트되던 것**을 없앴다(`ops` 변화 0, E3 드리프트 쌍 의무대로 **두 파일 모두**). 다만 코드와 대조해 보니 **Step 2의 서술이 너무 좁았다**: 보드에는 **어떤 칸에도 거부 시각 상태가 없다**(`BucketCard`는 *수락*일 때만 하이라이트를 켠다). E3의 거부 계약을 지키는 건 **트레이뿐**이라, 보드에 흐림+이유를 그리는 것은 라이브러리 한 칸짜리 패치가 아니라 **매트릭스 전체의 Step 5 의무**다. **라이브러리 수정은 대조군과 함께 측정**했다 — 스모크 계정은 Spotify를 동기화한 적이 없어 `kind='spotify_library'` 버킷이 없다. 그래서 **진짜 서버 행 하나를 전송 중에 재라벨**했고(행·id·모든 쓰기는 진짜, 클라이언트만 라이브러리로 본다), 같은 페이지·같은 행에서 HMR로 라이브러리 분기를 빼 `origin/main`을 재현했다: **아티스트 행이 origin/main에선 하이라이트되고 Step 3에선 안 된다**(쓰기 0). 서버 쪽 라이브러리 의미(sync·`source` 플래그)는 **미검증으로 명시**. **일부러 안 한 것 1건**: 수락 게이트 쌍둥이는 **해소 가능하다** — E3는 "공유 컨텍스트 없음"을 근거로 들지만 그건 드래그 *상태* 얘기지 *규칙*은 두 파일이 임포트할 수 있는 순수 함수다. 검증된 규칙 모듈을 "아무것도 안 바뀜을 증명하는 단계"에서 구조 변경하는 건 그 단계의 계약과 싸우는 것이라 **Step 5/6로 넘겼다**. 실브라우저 CDP 전수(prod 데이터, 합성 DragEvent가 실제 DOM의 React 핸들러를 구동 — 핸들러는 `dataTransfer`가 아니라 모듈 전역 페이로드를 읽는다): 이동 · 최근-들은 복사(**스트립 20 그대로** = 복사지 이동 아님) · 라이브러리 복사아웃(`POST /items`, **라이브러리 행 잔존**) · 아티스트 확장(원본 앨범 잔존, "Swimming"→아티스트 "Mac Miller") · 휴지통(드래그 중 도크 마운트→`DELETE`→언마운트) · 버킷-속-버킷(서버에서 중첩 확인) · **순환 가드**(드래그오버에서 거부, 쓰기 0) · **거부 드래그가 트레이를 재배열하지 않음**(아티스트→큐 칩: 하이라이트 없음 + `data-dropreject` + "재생 대기열 — 트랙 · 앨범만 받아요" + 쓰기 0, 같은 드래그가 General 칩에는 **수락** = 무차별 거부가 아님, **모든 칩의 x/y/width가 드래그 전·중·후 동일**) · 역방향 DnD(보드 멤버→트레이 칩) · 정방향(트레이 서랍 항목→보드 버킷). **prod 잔여물 0**(테스트용 버킷 전부 생성 후 삭제). lint · astro check 0 errors · build 2210쪽. → `docs/rfcs/ARCH-entity-interaction-v2.md`. — **Step 2 결과**(문서 전용, 배포 없음): 앨범·트랙·아티스트의 **정규 정의(E1)**, **드롭 매트릭스(E3)**, **앨범 상세 결정(E6)**, **앨범 id 투영 결정(E7)**을 RFC에 못 박았다. **오너 결정 2건 종결** — (1) **OQ1: `/album/[id]` 라우트 없음**(2026-07-08 결정 *유지*, 다만 이번엔 "나중 RFC로 미룸"이 아니라 **닫음**). 대신 **앨범 id를 공개 평론 카드 페이로드에 싣는다**. (2) **OQ2: 앨범 상세 호스트 2개 유지**(2026-07-18 오너 결정 8 *재확인*) — 실익 있는 항목은 `memo-trow`/`TrackRow` **쌍둥이 해소**(Step 5). **문서를 쓰면서 코드가 3가지를 정정했다**: ① **Step 1이 OQ1 비용을 과대평가했다** — 앨범 id는 이미 `publish_service.py`가 프런트매터에 쓰고, `content.config.ts`가 검증하고, `buildReviewCards`가 **이미 읽고 있다**(평론 여부 판정 필터). 빠진 건 방출되는 `ReviewCard`뿐이라, 계약·백엔드·마이그레이션 **전부 불필요**한 프런트 투영 한 곳이다(막힌 동작이라는 결론 자체는 그대로). ② **드롭 대상이 5개가 아니라 6개** — 스포티파이 라이브러리 버킷이 자기 규칙(앨범 전용·복사로 들어감·앨범 id 없으면 거부)을 갖는데 RFC 목록에 없었다. ③ **그 거부가 지금 눈에 안 보인다** — 라이브러리는 `kind`로 식별되는데 수락 게이트는 `type`만 보므로 트랙/아티스트를 끌면 **수락처럼 하이라이트되고** 드롭하면 조용히 아무 일도 안 일어난다(결과는 맞고 *시각 계약*이 깨진 유일한 칸) → **Step 3에 배정**. E1/E3의 모든 칸은 `origin/main` `40f27a6`의 **심볼**을 인용하고 작성 후 전수 재출력해 재확인했다(줄번호는 하루 만에 밀린다는 Step 1 교훈 적용). — **Step 1 결과**: `origin/main` `40f27a6` 기준으로 앨범 27 · 트랙 9 · 아티스트 9개 표면을 전수 재측정해 id/열기/동작/드래그 매트릭스를 RFC에 넣었다(모든 행이 `path:line` 인용, 표를 쓴 뒤 그 줄을 다시 출력해 재검증). **재측정이 즉시 자기 값을 증명했다** — 이 RFC 자신의 *Current state*에 적힌 드래그 인용 5개 중 4개가 **하루 만에 줄이 밀려 있었다**(`:569`→`:580`, `:965`→`:981`, `:435`→`:444`, `:589`→`:641`). 결론(드래그 가능한 표면은 2곳)은 그대로라 Step 8 게이트의 전제는 유지된다. **페이로드 공백 6건은 "빠진 필드"가 아니라 "막힌 동작"으로 기록하고 하나도 고치지 않았다**(이 RFC의 non-goal): 가장 큰 것은 **빌드타임 `ReviewCard`에 앨범·아티스트 id가 아예 없다**는 것이라 공개 편집 표면(canon·평론 인덱스·홈 평론 행·검색 평론 카드)은 라우트든 오버레이든 **어떤 방법으로도 앨범을 지목할 수 없다** → OQ1이 "라우트를 만들까"와 "프런트매터에 id를 넣을까" 둘로 갈라졌고 후자가 더 싸고 어느 쪽이든 선행조건이다. 종류가 다른 1건도 따로 표시: 발매 이벤트는 DB id 해석 실패 시 **스포티파이 id를 `albumId` 자리에 그대로 넣는다**(부재가 아니라 id 네임스페이스 혼동). **일부러 공백으로 안 적은 2건** — `/collection`(prod에서 앨범 48장, id 있는데 클릭 가능 요소 **0**)과 `ReleaseRadar`(`artist_id`를 들고도 허브 링크 없음, `grep -c artistHref` = **0**)는 id가 이미 있으니 계약 변경이 아니라 Step 5 배선이다. **실브라우저 스팟체크 6건**(prod, 로그아웃): `/collection` 48 figure·클릭 0·draggable 0 · `/search` 앨범 20·트랙 20행이 전부 "**앨범** 상세 보기"·담기 0 · 공개 오버레이는 닫기+평가 유도뿐(가사 0) · `/artist` 디스코·주요 트랙 모두 앨범만 열기 · `/releases` 아티스트 링크 52 + 앨범 열기 **0**(예정 발매는 앨범 id 자체가 없다) · 홈 타일 16 + **`a[href^="/review/"]` = 0**. 그 마지막 0이 범위를 규정한다 — **제품에서 유일한 공개 담기 표면(평론 히어로)과 유일한 트랙 담기 표면(평론 인라인 트랙리스트)이 지금 아무도 도달할 수 없다**. **외부 비교는 반복이 아니라 확장** — 앞선 분석들이 다루지 않은 *드래그 의미*를 가장 가까운 유사 제품 Are.na에서 실측: 로그아웃 상태에서 카드 **12/12가 `draggable="true"`**이고 드래그 노드가 이동 링크를 감싼 컨테이너(클릭 대 드래그를 네이티브 DnD가 가른다 — `BucketBoard.tsx:617-618`이 이미 문서화한 그 방식)이며 이름 있는 비드래그 짝 "Connect"가 공존하는데, **390×844 터치에서는 draggable이 12→0으로 사라지고 카드와 Connect는 남는다**(카드 수를 같은 패스에서 세어 렌더 실패가 아님을 확인). 즉 가장 가까운 유사 제품은 터치에서 롱프레스 드래그를 만들지 않고 **드래그를 아예 포기**한다 → OQ4의 권장 기본값을 뒷받침하고, 롱프레스는 이제 "반증이 없어서"가 아니라 **존재할 적극적 이유**가 필요해졌다. **멤버 쪽 브라우저 확인은 Step 3으로 의도적 유예**(드래그 노드가 DOM에 있으려면 멤버 보드에 항목이 있어야 하고, Step 3이 이미 보드+트레이 CDP 드래그 매트릭스를 의무화한다) — §F에 그렇게 적어 뒀지 전수 커버리지인 척하지 않았다. 미측정으로 남긴 것: 스포티파이 웹·유튜브 뮤직(로그인 벽 — Step 6과 같은 경계), Finder·Yoink(네이티브 macOS, 계측 경로 없음). — 앨범·트랙·아티스트마다 **정규 id / 정규 URL / 열기 동작 / 표시 데이터 / 가능한 동작 / 드래그 페이로드**를 한 곳에 못 박고, 모든 표면이 거기 맞추거나 이유를 적은 예외로 남는다. 계기: 지금 제품에서 **드래그 가능한 표면은 단 2곳**(`BucketBoard` 멤버 칩·버킷 노드, `PocketTray` 멤버)이라 재생 버킷을 Buckit 밖에서 채울 방법이 없다. 이동 vs 복사/추가 구분은 **그대로 보존**하되 `DragPayload.origin`으로 구조화하고, 드롭 대상마다 **직접추가 / 변환 / 거부**를 엔티티별로 선언(거부는 "자리를 지킨 채 흐릿하게" — 트레이가 드래그 중 재배열되지 않는 기존 불변식). `boardDnd.ts`의 이동·확장·거부 규칙은 **검증된 자산이라 다시 쓰지 않고** 어댑터로 감싼다. 클릭·드래그·스크롤 충돌과 모바일 대체 경로는 **먼저 프로토타입으로 실측**하고 결정. 재검토 대상 3건은 각각 "그 사이 무엇이 바뀌었는지"와 함께 명시 — 앨범 상세 2계통(이미 읽기 본문은 공유됨 → 권장 기본값 **현행 유지**), 앨범 정규 URL 유무(`/album/[id]` 라우트 없음 결정이 "나중 RFC"로 미뤄둔 그 질문), `TrackRow`의 예약 슬롯 `play`/`add`(이번 범위가 그 승인에 해당 → 개방). ⚠️ `component-map.md`는 **실측 stale**(NowPlaying·LikedBoard 아티스트 링크가 "불가"로 적혀 있으나 #293·#299에서 이미 배포됨) → Step 1은 옮겨 적지 않고 전수 재측정. **Erratum 2026-08-06**(BUG-24, 후속 감사): Step 5 종결 헤더의 "TrackRow play/add/drag 슬롯 전부 3그룹에 걸쳐 grant 완료" 주장은 표면-그룹 단위론 맞지만 슬롯-짝 단위론 틀렸다 — `MemoWindow`(9번째 슬라이스, front #368)는 `drag`만 받고 짝이 되는 `add`(탭 대체)를 못 받아 Rule #14(드래그 단독 의존 금지) 위반; `component-map.md` 자신의 "Ownership by domain" 표는 이미 정확히 적어뒀었다(`openLyrics?+drag`). RFC 재개는 아님 — 수정은 `ARCH-album-card-contract-and-composition` Stage 7 몫, BUG-24로 별도 추적. → `docs/rfcs/ARCH-entity-interaction-v2.md`.

- **ARCH-album-card-contract-and-composition** (**accepted** — owner promotion 2026-08-06; **Stages 1–4 SHIPPED + prod verified 2026-08-06** (Stage 1: front #372 `14ccaef`, deploy `31074197178`; Stage 2: front #373 `386539f`, deploy `31075214832`; Stage 3: front #377 `c32949a`, deploy `31079317610`; Stage 4: front #379 `e1268a7`, deploy `31082278000`; prod smoke 19/19 each); **next = Stage 5 migrate the remaining Home surfaces**) — `ARCH-entity-interaction-v2` 종결 이후 오너 지시로 돌린 2026-08-06 근거기반 감사(7개 병렬 조사, 두 RFC의 자기 완료주장이 아니라 `origin/main`을 직접 대조 — workspace `b914194`/front `bcb6544`/backend `9fa9c0f`)에서 나온 앨범-카드 아키텍처 RFC. 지금 앨범 표현이 최소 9곳에서 각자 구현돼 있고(Home 타일 3종은 CSS 접두사만 다른 사실상 동일 컴포넌트, 커버 폴백만 4종 독립 구현), `BucketBoard.AlbumChip`은 유일하게 도메인 상태(itemId/temp:*)를 프레젠테이션 컴포넌트에 박아 넣은 곳이자 BUG-21이 실제로 사는 곳이다. `TrackRow`의 `{lyrics?, open?, openLyrics?, play?, add?, drag?}` 캐퍼빌리티 패턴을 앨범 레벨로 한 단계 끌어올려 캐노니컬 `AlbumCard` + 어댑터(Home/Bucket/Memo/에디토리얼)로 재구성 — 상속·`variant` 문자열·God-props 전부 명시적으로 기각. Stage 2에서 캐노니컬 display model/capability type과 아직 아무 표면도 import하지 않는 null-rendering shim을 배포했고, `drag`는 타입 수준에서 `add` 탭 대체와 짝지었다. Stage 4에서 `NewReleasesCard`가 첫 라이브 소비자로 전환됐고, 기존 렌더러는 Stage 9 parity fixture로 남겼다. Stage 6(Bucket 어댑터)은 **BUG-21 수정 선행 필수**, Stage 7(Memo 어댑터)이 BUG-24 수정을 흡수. 발행 리뷰 문서(`ReviewCard`)는 앨범이 아니라는 경계는 새로 만드는 게 아니라 이미 배포된 E7(front #362)의 확장. → `docs/rfcs/ARCH-album-card-contract-and-composition.md`.

- **BUG-24** (신규 2026-08-06, 감사에서 확인) — `MemoWindow`의 `TrackRow`가 `drag`만 grant받고 짝이 되는 `add`(탭 대체)를 못 받음(front #368, 9번째 슬라이스). `ARCH-entity-interaction-v2`의 binding Rule #14(터치에서 드래그 단독 의존 금지, CDP 실측: 네이티브 `draggable`은 터치에서 `dragstart` 0회) 위반 — 다른 모든 grant는 짝을 맞췄는데(`StandardModal`은 `add`+`drag` 둘 다) `MemoWindow`만 빠짐. 터치 사용자는 `MemoWindow` 트랙리스트에서 트랙을 다른 버킷으로 옮길 방법이 아예 없다. `component-map.md`의 "Ownership by domain" 표는 이미 정확(`openLyrics?+drag`, add 없음) — 틀린 건 Step 5 종결 헤더의 집계 서술뿐, 해당 RFC에 erratum 기록함. 수정은 `ARCH-album-card-contract-and-composition` Stage 7이 어댑터 경계에서 흡수(개별 패치가 아니라 캐퍼빌리티 짝 강제 규칙으로).


- **CHORE-claude-review-ci-skip-status** (신규 2026-08-06, 감사에서 확인 — 앨범-카드 RFC와 무관, 독립 CI 위생 항목) — `myblog_front/.github/workflows/code-review.yml`의 `keycheck` 스텝이 `ANTHROPIC_API_KEY` 부재 시 실제 리뷰 스텝 2개를 **스텝 레벨**로 스킵하는데, GitHub Actions는 스텝 스킵을 잡 레벨 "Skipped"로 표시하지 않아 PR Checks 탭에서 "리뷰가 실제로 돌아서 이슈 없음"과 "리뷰가 아예 안 돌았음"이 똑같이 초록으로 보인다. **지금 실제로 살아있는 문제**(`gh secret list --repo hyuntohoon/myblog_front`에 `ANTHROPIC_API_KEY` 없음 — 모든 PR이 리뷰 없이 초록). `6b8551b`부터 의도된 설계(키 없다고 PR을 막지 않으려는 선택)라 회귀는 아님. 권장: `keycheck`를 별도 잡으로 분리하고 `code-review` 잡을 **잡 레벨** `if:`로 게이팅 — 키 부재 시 회색 "Skipped"로 시각적으로 구분(실패 처리는 안 함, 원래 설계 의도 유지). branch protection이 아예 없어(`gh api .../branches/main/protection` 404) 이 초록/스킵 구분은 GitHub 강제가 아니라 사람이 보는 신뢰 신호일 뿐이라는 점도 기록.

**2026-08-06 재생/모달/보안 감사 (플레이북 진입점부터 큐 정체성·모달·멀티유저 인가까지, 앨범-카드 RFC와 겹치지 않는 나머지 재현 가능 항목).** front 최신 HEAD `cc16ab49`(로컬 워킹트리는 이보다 2커밋 뒤인 `bcb6544`, BUG-21/22 반영 안 된 상태라 인용은 전부 pinned SHA 기준) / backend `9fa9c0f` / music `fa2e70d` / worker `c1e42478` / shared_db `76ebbc80` / workspace `f9faf48` 기준. 이전 세션의 가설을 코드 대조로 재검증 — BUG-21(temp-id 레이스)·BUG-22(`playbackSession` 컨버전스 드롭)는 이미 배포 완료(front #369/#370), BUG-23·BUG-24·`CHORE-openalbumdetail-id-pairing-guard`·`CHORE-claude-review-ci-skip-status`는 위 항목들로 이미 추적 중이라 재조사 대상에서 제외. 8개 병렬 조사의 신규 확정 결과만 아래 행으로 분리.

- **BUG-25** (신규 2026-08-06, 감사에서 확인 — **보안, 최우선순위**) — `GET /api/buckets/spotify-library/state`(`myblog_backend/app/api/routes/buckets.py:309-313`)에 인증 의존성이 전혀 없다. 시그니처엔 `Depends(require_owner)`도 `Depends(require_cognito_token)`도 없고, 형제 라우트 `POST /spotify-library/sync`(`:344-348`)만 `provisioned_owner_id`로 게이팅돼 있다. `infra/apigateway.tf`에도 이 GET 전용 라우트가 없어 인증 없는 catch-all `GET /api/{proxy+}`를 타고, 유일한 방어선은 `edge_guard`의 CDN-오리진 공유 비밀(`x-origin-verify`)뿐인데 이건 로그인 여부와 무관하게 CloudFront를 통과하는 모든 요청에 자동으로 붙는다 — **로그인 없이도 응답을 받는다.** 서비스 레이어(`bucket_service.py:1504-1517`)도 `user_id` 필터가 전혀 없이 전역 조회(같은 클래스의 `get_or_create_spotify_library_bucket`은 몇 줄 위에서 `user_id` 필터를 올바르게 쓰는데 이 읽기 경로만 빠짐 — 일관성 결여). `SpotifyLibraryAlbum` 테이블 자체가 `user_id` 컬럼 없이 `album_id` 전역 유니크라 스키마 차원에서도 사용자별 구분이 불가능(`myblog_shared_db/src/myblog_shared_db/models.py:885-916`) — `review_buckets`가 멀티유저 전환 때 받은 `user_id`+부분 유니크(V40)를 이 테이블만 못 받았다. 오늘은 Spotify 동기화 쓰기가 오너 전용이라 실제 노출 대상이 "오너 1명의 라이브러리"로 제한되지만(2번째 실사용자가 없어 크로스유저 유출 자체는 아직 관측 불가), **분류는 익명 접근 노출**이다 — 인증 자체가 없다. 회귀 테스트(두 실사용자 실DB 픽스처, 기존 `tests/integration/test_bucket_service_db.py` 패턴 재사용) + 전체 증거 → `inv-7-spotify-library-auth.md`(세션 스크래치패드). 수정 순서: (1) 라우트에 회원 인증 의존성 추가 + `infra/apigateway.tf` Cognito 라우트 신설, (2) 서비스 함수 3개(`get_spotify_library_state`/`list_spotify_library_albums`/`library_last_synced_at`)에 `user_id` 매개변수+필터, (3) `myblog_shared_db` 마이그레이션으로 `spotify_library_albums.user_id` 추가 + 유니크를 `(album_id, user_id)`로. 정방향 마이그레이션+인프라 apply 필요 — 오너 go-ahead 후 착수.
**BUG-26 fixed + deployed 2026-08-06** (front #378 `0504637`, deploy 31079444240, prod smoke 19/0) — `rowForSpotifyTrack()`은 이제 앵커(`currentItemId`)를 받아, 매치가 여럿이면(중복 트랙) 앵커가 그중 하나면 그 행을 유지하고(연속성, 매 read마다 첫 매치로 재추측하는 플래핑 방지), 그래도 여럿이면 `ambiguous`를 반환 — 이건 큐(중복 공존) 자체의 성질이라 같은 행이 연속 채택돼도 계속 유지된다. `adoptLive()`의 삭제 게이트는 이제 **둘 다** 요구: 직전 행의 실제 경과 위치가 자기 길이 끝에 도달했는지(`BOUNDARY_BUFFER_MS` 허용치, 경계-체크 스케줄러가 이미 쓰는 상수 재사용) **그리고** `!ambiguous`. `playFrom()`(index 기반 확정 재생)은 ambiguity 플래그를 리셋 — 추측이 아니라 확실한 식별이므로. 결정형 회귀 테스트 4건 신설(지연 프라미스/fake timer로 실제 비동기 갭 모델링) — 진짜 완주는 여전히 삭제됨 / 완주 전 트랙 변경은 삭제 안 됨(b) / 모호 매치는 다음 전환에서도 삭제 안 됨(a+b 결합) / 확정 앵커된 중복 occurrence는 반복 read에도 안 flapping(a). 기존에 과도하게 넓은 삭제를 정답으로 박제했던 테스트(`session.test.ts:574-611`)는 진짜 완주(179초 경과) 케이스로 재작성. lint 0·astro check 0·vitest 49/49 파일·520/520. `inv-2-queue-identity-deletion.md`(plan.md 행이 참조한 세션 스크래치패드 증거 파일)는 실제로 존재하지 않았음 — plan.md 행 자체의 file:line 증거로 충분해 재현 안 함, 기록만. `FEAT-playback-bucket-player.md`의 해당 erratum에 해소 표시.
**BUG-27 + BUG-29 fixed + deployed 2026-08-06** (front #375 `69f7ac6`, deploy 31076299689, prod smoke 19/0) — `next()`/`previous()`에 `externalAdvance()` 신설(`togglePlay()`의 기존 raw-command 폴백 패턴 그대로 `sendPlayerCommand({kind:'next'|'previous'})`), `onDropped()` 가드에 `current.external !== null` 추가. `session.test.ts`에 3개 테스트 추가(external 중 next/previous → `sendPlayerCommand` 호출 확인 2건, external 중 드롭 → `mocks.play` 미호출 확인 1건) — 전부 수정 전엔 실패, 수정 후 통과 확인. lint 0·astro check 0·vitest 49/49 파일·516/516. `FEAT-playback-bucket-player.md`의 두 erratum(“transport works either way”/T2 드롭 규칙) 항목에 각각 해소 표시.
**BUG-28 + CHORE-nowplaying-trackid-namespace fixed + deployed 2026-08-06** (front #376 `061edfb`, deploy 31077498900, prod smoke 19/0) — `applyLive()`의 재생-중 브랜치에 `playbackSession.currentSpotifyTrackId()`와 어긋나면 스킵하는 신선도 가드 추가(idle-클리어 브랜치의 기존 패턴과 동형); Step 3b 컨버전스 이펙트는 `row?.trackId` 직접 참조 대신 `currentSpotifyTrackId()`(Step 3c가 신설한 캐시-온리 역방향 조회)를 사용하도록 교체 — 필드 이름 변경(`spotifyTrackId`/`catalogTrackId`)은 후속으로 미룸, 동작 수정만. `NowPlaying.test.ts`에 신규 회귀 테스트(BUG-28, 가드 제거 시 실패 확인) + 기존 Step 3b 테스트 3개를 올바른 해석값 기대로 수정 + `afterEach`에 세션 목 상태 리셋 추가(가드가 `currentSpotifyTrackId()`를 읽게 되면서 잠재해 있던 테스트 간 목값 누수가 실제로 드러남). lint 0·astro check 0·vitest 49/49 파일·517/517. **미수정, 기록만**: `MYBLOG_PLAYBACK_CHANGED` 한 이벤트가 `session.ts`/`NowPlaying`/`LyricsViewer` 3곳에서 각자 `readLivePlayback()`을 부르는 fan-out은 여전히 무 single-flight — `ARCH-entity-interaction-domain-audit`의 "single-flight/centrally cached" 목표는 미실현 상태 유지(개별 트래커는 각자 건전, 통합만 안 됨).
- **CHORE-lyrics-member-guard-reopen** (신규 2026-08-06, 감사에서 확인 — 완료주장 정정 동반) — 오너가 2026-08-03(`FEAT-playback-bucket-player` 오너 결정 4)에 "가사를 멤버에게 개방"을 결정하고 그 항목이 "SHIPPED"로 기록됐지만, 백엔드 `GET /api/lyrics/{spotify_track_id}`(`myblog_backend/app/api/routes/lyrics.py:51-56`)는 지금도 `Depends(require_owner)`다 — 코드가 결정을 못 따라갔다. 프런트 이벤트 체인(`ENT_OPEN_LIVE_LYRICS`)은 정상 배포돼 있으므로(가사 진입점 자체는 아래 참고 — `/members/<본인>` 한정으로 의도된 설계, 문제 없음), 비오너 멤버가 로그인해서 가사 버튼을 눌러도 뷰어는 뜨지만 데이터 fetch가 403으로 막힌다. 오늘은 prod 실사용자가 오너 1명뿐이라 미관측이지만 멀티유저 온보딩이 진행되는 순간 확실히 걸림. 수정은 가사 스트림 PR 소관(guard 변경) — 이 세션은 fix 착수 안 함, plan 항목만 신설. → `inv-1-entry-points.md` 가설 1.
- **DATA-multidisc-track-order** (신규 2026-08-06, 감사에서 확인 — 확정 구조적 결함, 이미 자체 문서화된 채 방치) — `tracks` 테이블에 `disc_no`/`disc_number` 컬럼이 아예 없다(ORM `myblog_shared_db/src/myblog_shared_db/models.py:273-320`, 캐노니컬 스키마 `tests/canonical_schema.sql:265-279`, 51개 마이그레이션 전수 grep 0건 — 있다가 빠진 적도 없이 처음부터 없음). `myblog_music`(`track_repo.py:163`)·`myblog_worker`(`sync_service.py:174`) 둘 다 Spotify의 `track_number`만 읽고 `disc_number`는 아예 참조하지 않는다(향후 스키마 수정 시 둘 다 트윈-드리프트 스윕 필요). 앨범 트랙리스트를 읽는 모든 경로(`myblog_music/track_repo.py:23`, `myblog_backend/bucket_service.py:1044-1050` — `FEAT-playback-bucket-player`의 "앨범→트랙 앨범 순서" 큐 확장 경로)가 `track_no ASC NULLS LAST`만으로 정렬한다. Spotify는 디스크마다 `track_number`를 1부터 다시 매기므로 디스크1 트랙3·디스크2 트랙3이 같은 순위로 충돌해 `created_at`/`id` 삽입 순서로 임의 정렬된다. **`bucket_service.py:1018-1022`의 독스트링이 이 한계를 이미 스스로 적어놨다** — 새 발견이 아니라 알고 미룬 부채. 프런트는 재정렬을 전혀 안 하고(전수 grep 0건) 백엔드 순서를 그대로 신뢰 — 이게 옳은 태도다(`track_no` 단독으론 프런트가 디스크 충돌을 해소할 신호가 없음). 실 다중디스크 앨범을 어느 리포 픽스처에서도 못 찾아 라이브 데이터 스팟체크 필요 — 확인용 SQL → `inv-8-multidisc-ordering.md` §5. 데이터 모델 마이그레이션으로 관리(카드/재생-상태 리팩터와 무관): (1) `myblog_shared_db`에 `tracks.disc_no INTEGER` 마이그레이션 + prod 적용(머지 전), (2) 백필은 Spotify 재조회 필요(로컬 계산 불가) + `myblog_music`/`myblog_worker` 트윈 수정으로 `disc_number` 수집 시작, (3) `myblog_music`/`myblog_backend` 모든 `ORDER BY`를 `(disc_no, track_no)`로, (4) OpenAPI 재수출 → 계약 병합 → 프런트 타입 재생성(프런트 로직 변경 불필요).
- **ARCH-overlay-modal-isolation** (신규 RFC, draft, 작성 2026-08-06) — `ARCH-entity-interaction-domain-audit` Step 4가 이미 닫은 ESC/포커스트랩(`useDismissable`)과 별개로, 배경 스크롤 락(`useScrollLock`)과 `AddToBucketMenu`의 모달 시맨틱스 전면을 감사했다. 메커니즘(`useScrollLock.ts`) 자체는 잘 설계돼 있음(모듈-레벨 refcount, 언마운트 시 확실한 해제) — 문제는 **입양**: 스크림 있는 `role="dialog"` 오버레이 27개 인벤토리 중 16개가 이 훅을 아예 안 부른다(같은 파일 `BucketBoard.tsx` 안에서도 연구노트 모달만 걸고 휴지통·삭제확인은 빠짐). 여기에 `AddToBucketMenu`가 `role="dialog" aria-modal="true"`를 선언하면서 포커스이동·트랩·복원·ESC·스크롤락이 전부 없고, `AlbumDetail`의 `StandardModal` 위에 중첩됐을 때 ESC를 누르면 `useDismissable`의 `openStack`에 등록이 안 돼 있어서 **아래 앨범 모달이 대신 닫히는** 결함까지 겹친다. iOS 터치 스크롤 완화(`position:fixed` 기법)·`overscroll-behavior`도 전체 코드에서 0건. 5개 서로 다른 기능 영역(멤버 보드/포켓 버킷/오버뷰 대시보드/라이터 앱/장르맵)에 걸친 구조적 격차라 개별 패치가 아니라 공통 오버레이 계약 RFC로 관리 — 상세 → `docs/rfcs/ARCH-overlay-modal-isolation.md`.
- **참고 (신규 항목 아님, 상태만 기록)** — 항목 1(가사 진입점)은 대부분 **의도된 설계**로 재확인(사이트 전역 버튼 + `/members/<본인>` 단일 실동작 경로 — 위 `CHORE-lyrics-member-guard-reopen`이 그 경로의 백엔드 잔여 갭만 분리); 항목 2(트랙정보 NOOP)는 **이미 RFC에 의도적 보류로 기록됨**(`ARCH-entity-interaction-v2` 캐노니컬 트랙 스코프 대기, 새 항목 아님); 항목 12(MemoWindow 터치 대체)는 BUG-24로 이미 추적 중, `ARCH-album-card-contract-and-composition` Stage 7이 흡수; 항목 15(temp-id 레이스)는 BUG-21로 이미 배포 완료(front #369); 항목 16(앨범-상세 식별자 계약)은 `CHORE-openalbumdetail-id-pairing-guard`로 이미 추적 중; 항목 17(Claude Review CI)은 `CHORE-claude-review-ci-skip-status`로 이미 추적 중.

**Steps 6+7 closed out 2026-08-02.** 6c (다음에 듣기) could not ride 6b's surfaces as the RFC assumed — the queue endpoint takes a track or episode uri, never an album — so it is track-only, and it **ships dormant**: its only surface is `/review/[slug]` and prod has **zero** review pages (index, sitemap, empty `content/blog`, and a real build emitting 0 all agree). The reachable alternative was refused on purpose: adding an action to the live overlay's shared `TrackRow` is a documented owner product decision, not a refactor. 6e (셔플·반복·볼륨) is live and costs **zero** extra requests — it reads the one-shot body the bar already fetches — and it carries the rule that **a volume 403 is a device limit, not a capability loss** (otherwise a speaker with no volume API would hide play/pause). 7b+7c first had to fix the rule Step 7 exists to enforce: the capability matrix was **already duplicated and drifted** across two components, so it moved to `@lib/playerCapabilityMatrix` and all surfaces now render from it; `/help/player` and the bar's "왜 컨트롤이 없나요?" popover are joined end to end, verified through a real 403 degrade. **Both prod-verified** (deploys 30749023560 / 30749644584): 6e's markers incl. `unsupported-on-device` are in the live dashboard chunk, `/help/player/` serves all nine rows with the Premium-vs-연동 distinction intact, and 7b's copy + help link resolve to one shared `playerCapabilityMatrix.*.js` chunk.

**✅ 오너 확인 2026-08-03 — 핵심 게이트 종결.** 오너가 실기기에서 유휴 바 진입점(`▶ 이어듣기`, front #340)을 눌러 확인 완료. 이 단계가 존재한 이유였던 **콜드 스타트에서 실제로 소리가 나는가**가 닫혔고, 사다리의 rung1→rung2 인계와 인페이지 기기가 테스트가 아니라 실사용에서 동작함이 확인됐다. `재생 시작이 안 된다`는 해결됐다. **남은 오너 항목(아무것도 막지 않음)**: 나머지 5개 재생 표면 개별 확인, 진짜 두 번째 기기와의 양방향 전환, OS 미디어 키, `/write` 소리 연속성(구조·CDP로만 확인됨). 앞선 세션 요약이 이 확인으로 "대부분"이 닫힌다고 한 것은 과장이었다 — 가장 중요한 하나가 닫힌 것이다.

~~**Blocking unknown (OQ4)**~~ — **CLOSED by prod measurement 2026-08-02 (CDP, real ClientRouter + real SDK script, nothing written to prod): the iframe does NOT survive, and `data-astro-transition-persist` does not save it.** Refuted twice — a runtime-injected node has no counterpart in the incoming document and is dropped outright, and even inside a genuinely persisted island (host survived as the *same node object*) the iframe still reloaded. Root cause is not Astro-specific: `swapBodyElement` re-inserts, and re-insertion destroys an iframe's browsing context, so Astro's node-identity guarantee is simply not the guarantee audio needs. **Working fix measured**: an `astro:before-swap` override that mutates `<body>` in place — real SDK iframe, 3 hops, **0 reloads / 0 detaches / same node**. So Step 5 = ladder rewrite **plus** a swap-strategy change in `layout.astro` whose blast radius is every navigation on the site (needs its own regression pass over the persisted islands). **Corollary**: persistence is a **rung-2-only** cost — rung 1 plays on another device, so it is global for free. Also measured 2026-08-02 and recorded in the RFC: we are in **Extended Quota Mode** (6/6 Dev-Mode-removed capabilities answer 200 ⇒ one app covers unlimited users, no second app registration ever needed) and that grant is **irreplaceable** (individuals cannot apply since 2025-05-15) while already-shipped catalog/library/좋아요 features depend on it; and we were **not** grandfathered into the 2024-11-27 deprecations (Audio Features / Recommendations / Related Artists all dead here too — never plan on derived mood or "similar artists"). → `docs/rfcs/FEAT-member-player.md`.

_Shipped history lives in `git log` + `docs/archive/done/` (per-month digests + archived RFCs) — plan.md carries open work only._

---

## 2026-07-23 감사 파생 작업 (남은 것 = P2뿐)

> `docs/reviews/2026-07-23-current-state-audit.md`에서 검증·수락된 항목만. 근본 원인은 "발행 평론 0" (콘텐츠 생성이 구속 제약) — 아래는 콘텐츠와 무관하게 오늘 실害이 있거나 안전망 공백인 것 위주.
>
> **오너 결정 2026-07-23:** (1) 우선순위 = **안전망·품질 먼저** → OPS·REFACTOR RFC가 활성 우선, discovery는 P2 유지. (2) 제품 정체성 = **비평지 프레이밍 유지**(첫화면 재구성 안 함 → 감사 P-2/P-4는 오너 결정으로 종결, 액션 없음). (3) RFC 2건 **둘 다 accepted**. (4) 문서 불일치 **전체 정리 승인**(architecture.md·infra/README·RFC 상태 3건 — 이 세션에서 반영 완료).

**P2 — 실사용자 증거 후 (조기 착수 비권장)**

> P0·P1은 전부 종료돼 행을 내렸다(2026-08-03 정리). 이력 → `git log` + `docs/archive/done/2026-07.md`.

- **CHORE-dep-reproducibility** — `myblog_backend`·`myblog_music`. lockfile 도입 + `fastapi`/`boto3`/`SQLAlchemy` 핀 + backend 비태그 SHA→태그(감사 C-10). 트리거: 핀-드리프트 배포 500 재발. 현재 additive 컨벤션 하 수용. **2026-07-26 audit adds evidence, not a new row**: `SQLAlchemy==2.*` and `psycopg[binary]==3.*` are open major-range pins, so each deploy re-resolves them — and querying a CVE database with the literal string `2.*` returns three CRITICAL SQLAlchemy 0.x/1.x advisories that do not apply (real installed version 2.0.50). The wildcards actively produce false security signal, which is a second argument for this row beyond reproducibility. Dependency *notification* is separate → OPS-safety-net-drift Step 4.
- **DISCOVERY-flow (가설, 기각-현시점)** — `signup→import→Buckit→publish→discovery`를 핵심으로 투자. 감사 §3: 발행 0·타 사용자 0·공개 컬렉션은 오너 자기것 1개·`/members/` 공백 → discovery 명목상. 실사용자+콘텐츠 증거 나올 때 재평가. **핵심을 무엇으로 볼지는 오너 전략 결정**.

---

## 2026-07-26 감사 파생 작업 (남은 것 = P2 일부 + 결정 완료 항목)

> Source: `docs/reviews/AUDIT-2026-07-26-system-audit.md` (merged ws #704, squash `9d4711c`). 21 findings; **all evidence stays in the report** — these rows are pointers, not summaries.
> **D-1 (the P0) was implemented and RESOLVED 2026-07-27** — dedicated nightly-agent identity + server-side grow, live e2e-verified; digest in `docs/archive/done/2026-07.md`, successor row in `## Later`. Everything else is unimplemented, and the report's §7 owner decisions remain open except the D-1 identity choice (option (b), dedicated service identity).
> ⚠️ **Read §9 first.** The report's own re-review corrected **14 of its claims** and added 5 findings. Acting on a pre-§9 statement will mislead you — D-1's root cause and DEP-2's applicability were both overturned.

_(P0 resolved — D-1/FIX-nightly-draft-delivery closed 2026-07-27; digest → `docs/archive/done/2026-07.md`; the trigger-gated Phase B successor row lives in `## Later`)_

**P2 — 남은 batch 항목**

> P1과 batch의 A-3·A-5·D-3·DEP-3은 전부 종료돼 행을 내렸다(2026-08-03 정리). 이력 → `git log` + `docs/archive/done/2026-08.md`.

> **E-5 is not in this batch** — it belongs to **FEAT-album-review-authoring Step 4** ("통합 입구 + 권한 정리(C1 + 감사 E-5)"), which is rule-4 gated with OQ9 (입구의 배치·형태) still open. Fixing it here would either duplicate that step or pre-commit its design (owner call 2026-08-02). Left listed there, removed from here so the batch is not double-counted.

- **D-2** `LLMTransientError` is never retried (`cli_engine.py:131-146` retries only `LLMValidationError`) → one blip costs a whole night, no alert · **E-2** `/collection/` bylines a raw Cognito-sub fragment (`@user-0468fd3c`) as a display name on a public page · **E-4** 8 homepage `button.nrl-open` fail WCAG 2.5.3 "Label in Name" (visible text not contained in `aria-label`) · **E-6** `.lf-artist-link` is emitted site-wide by the `AlbumOverlay` island but styled only in `member/layout.css` (loads on `/collection/` + `/settings/` only) → no hover affordance anywhere else; **4th recurrence** of the documented `member.css` trap, live-verified · **DEP-2** `@astrojs/rss` 4.0.13 → 4.0.19, **hygiene only** — applicability overturned in re-review (the advisory's `source.title`/`enclosure.type` are never populated and the feed emits zero items).

**Decided, no action**

- **PUB-1** — the workspace and 4 service repos are **PUBLIC**; merging the audit published reproduction detail for still-unfixed defects. Owner chose to merge as-is (2026-07-26). Severity was overstated in the report: `package.json`/`pnpm-lock.yaml` are already public, so §8's version list added nothing an attacker could not derive. Residual exposure was A-3's URL and the E-5 surfaces; **A-3 is fixed as of 2026-08-02**, so what remains published is the E-5 surfaces, now tracked under FEAT-album-review-authoring Step 4.

**Not covered by this audit** (so the clean verdicts aren't read as broader than they are) — `infra/` beyond apigateway/eventbridge/lambda/monitoring (IAM scope, S3/CloudFront policies, KMS, Cognito), all six CI workflow files, `tools/` and the non-buckit `scripts/`, frontend server-side code, PWA/service-worker config, escaping of member-supplied content at render sites, load/concurrency testing, DB-level data quality, Terraform state, and call-path tracing for the Python CVEs (`starlette` is the one in the request path). → audit §9.4.

---

## Backlog

> Scope-ready drafts/deferred work — each still needs an owner go (+ RFC accept where draft) before promotion to Active.

- **CHORE-research-prompt-port-not-swap** (measured 2026-07-30; **legs 1–6 SHIPPED same day**, ws #751 — remaining: the three measurements in §7 below, none of which block) — **do not adopt `album_research_v4.md`; port from it.** The "v2 covers 1 of 6 gaps, v4 covers 4" argument in `docs/editorial/research-note-requirements.md` was a section count, never an output measurement. Measured out of band (no `PROMPT_VERSION` change, no DB write; album block, Genius block, engine, model, tools held identical, base prompt the only variable): scored against **Pitchfork's *RENAISSANCE* review** (Shepherd, 9.0, 2022-08-01 — a Pitchfork #1 AOTY inside the last 10 years that is also in our catalogue; neither note cited pitchfork.com, so the yardstick held), **v2 12/22, v4 12/22 — a tie with almost no overlap in which twelve.** v4 alone found the Uncle Jonny dedication (Exclaim! + W + Tina Knowles quote, death date correctly `[미확인]`), Ts Madison, Homecoming; v2 alone found Vjuan Allure (root of the vogue-fem "Ha" lineage), Princess Loko, A. G. Cook. Different axes, not different quality — v4 on biography/primary statements, v2 on genealogy/credits — so a swap trades one for the other. **v4 also carries a regression**: v2's rule 36 bans "others' critical verdicts", v4's rule 32 drops that clause, and on the second album (*ONYX*) v4 read the published IZM review and imported its *readings* as inputs (20 refs, incl. a `PPAP` verdict cited as `확인 1건`) — a statistical rebuild from 38 coded reviews can see what reviews contain, never what a note must avoid. Cost: v4 = 1.5–3× input tokens, 1.25–1.7× wall clock (the 03:00 draft agent shares the subscription). **Work = 3 legs**: (1) port v4's biography/primary-statement pressure into v2, (2) keep v2's others'-verdicts ban, (3) add the **liner-notes source + sample-chain-depth** rule *neither* prompt has — both notes missed the same seven items — "The Ha Dance" (1991) · Masters at Work · Trigger Man · dembow/Shabba · Kelman Duran · Ivy Park · **liner notes as a source at all** — of which five are one continuous chain (*Pure/Honey* → MikeQ → Vjuan Allure's rework of "The Ha Dance"), the review's best passage, so the miss is *depth*. Leg 3 is the largest win and is independent of the v2/v4 question. **Not required unless someone still wants the swap**: the `PROMPT_VERSION` filter in `research_service.py:39,58` would hide all existing notes (fix = newest-row-per-album; the requirements doc calls this "tracked separately" but it was never tracked until this row), and `[확인: Genius]` appears **0× in v4 vs 3× in v2**, so switching today would drop the wrong-song defence shipped 2026-07-30 (ws #747). Evidence + denominators in `docs/editorial/research-note-requirements.md` §"Consequences for the prompt versions"; **the 22-item coding sheet, the four raw notes and the blocks as fed are checked in** at `docs/reviews/measurements/2026-07-30-research-prompt-v2-vs-v4/` (the earlier version of this row quoted 12/22 without recording what was counted). **SHIPPED 2026-07-30 (ws #751)**: CATALOG block built from our own DB and injected after the Genius block (artist genre tags · other albums we hold · **catalog-internal credit crossings** — the R6-shaped fact computable nowhere else · label neighbours · owner bucket signals), declaring itself a *library, not a discography* so its absence is never evidence; **Genius confirmed-absence overclaim corrected** — the rule called an absent relationship on a matched track a confirmed absence, but Genius records only what was registered/cleared, so it now reads "등재된 관계 없음" and mandates searching uncredited samples independently (the old wording switched off the search for exactly what brat hid two of), and the relationship list declares its own registered/one-hop limit; **depth rule** (chain the 1–3 most decisive samples to the root, per-hop citation duty, record where a chain breaks, one line per source on what that source was about — the 1–3 cap is the wall-clock brake); **no-verdicts extended to never OPEN criticism of the album under study**, with the ONYX incident inline; liner notes named as a source with a press-quote degradation path and a 앨범소개-is-distributor-copy warning; appositive duty; **body/appendix split with a per-item use-line** (notes grew 6.1k → 14.7–19.7k chars and the nightly drafter re-reads them whole); two genre sections replaced by 계보와 비교 좌표; v4 ports (자료 상황 · 제작 의도·발언 · 아티스트와 전작); owner-check list with the KOMCA no-crawl handoff; and every incident-born hard rule now carries its incident inline so a future rewriter knows what they are deleting. `PROMPT_VERSION` stays `v2` on purpose — the read path filters on it and bumping hides all 80 stored notes. Held back deliberately: dissolving 사운드로 알 수 없는 사실 (structural reading, no measurement). Verified: scripts suite 48 passed / 0 skipped incl. the executed-DB test; both blocks rendered read-only against prod ONYX — **the CATALOG block surfaces 화기엄금 (2019)**, the prior album a published review used for its central producer-mapping argument and which v2 could not reach without reading that review. **Remaining = measurement only**: (1) re-run smoke on RENAISSANCE against the checked-in 22-item sheet + ONYX with `grep -ci izm` = 0, recording score, wall clock, output length and **per-hop citation reality** (fabricated hops are the depth rule's top risk; pass = recover ≥3 of items 5/6/8/22 at ≤1.3× v2 wall clock) — note this is grading our own exam, since the rules were born from these two albums' failures; (2) generalisation on one album not used in rule design; (3) the **`[확인]` audit** — never run once, no false-`[확인]` rate exists at any denominator, so measure before redesigning trust. **The highest-information measurement remains outside all of these**: the owner writing ONE review from ONE note, marking used/unused — 80 notes have produced 0 completed reviews, so every number here is a proxy for an outcome never observed.

- **FEAT-ai-editorial-critique** (draft) — **AI editorial critic, Phase 0 thin slice** (the product's defining feature — see 2026-06-06 whole-project review, memory `project-ai-editorial-greenfield`). LLM service layer + `POST /api/editorial/critique` that runs `docs/editorial/review-critique-method.md` over a draft + its already-linked album DB facts (`post_albums`, no schema change), returning grounded Korean feedback + `[ ]`/`[source?]` markers + §4 checklist — reconstruction/feedback only, never rewrites. UI-less; proven by curl + a golden-draft eval (critic must not hallucinate) before any frontend. Backend-only, 2 steps (Step 1 local + golden eval → Step 2 infra JWT route + contract). RFC → `docs/rfcs/FEAT-ai-editorial-critique.md`. **Caveats baked in: token-ceiling cost guard + manual-fire only ($0-bias owner); fact-check is narrow-not-deterministic (most honesty-line facts live outside the DB → `[source?]`); prompt single-source vs drift (OQ1).** Fact-check pass, `/write` sidebar, persistence, planner, publish-gate = later RFCs (non-goals). Status draft — needs owner accept before Active. **Open thread (from FEAT-editor-buckit, 2026-06-19):** on promotion, fold in the explicit 2-stage editorial pipeline (nightly full-draft → critique pass) + the research-grounded, tool-agnostic critique method §3.5 (`docs/editorial/review-critique-method.md` v2.6). **Deferred 2026-07-20** — a promotion + Step 1 session was opened then postponed by the owner before any doc/code change; resume order: promote → fold-in above → re-audit the RFC's "Current state" → Step 1. NB that section is stale (written 2026-06-06): "zero AI/LLM code" no longer holds (the $0-mode research pipeline / local `claude -p` + the multi-user `LLMEngine` RFC exist), the route/auth surface changed post-multi-user (critique endpoint should be owner-gated via `require_owner`, not bare `require_cognito_token`), and its Secrets Manager references must become SSM (CHORE-secrets-ssm-migration).
- **DATA-catalog-noise-and-lyrics-coverage** (**accepted 2026-07-28**; **Steps 1a + 2 + 3a + 4 SHIPPED 2026-07-28, Step 3b SHIPPED 2026-08-03, Step 1b DROPPED 2026-08-03** — **no decisions left; remaining is the two observation gates below**) — four independently-mergeable changes sharing one root cause: the catalog ingests bulk classical compilation noise and every downstream surface inherits it. **Step 1a search ranking — SHIPPED + prod-smoked** (music #63): `tracks.views` was 0 on all 26,602 rows (dead ORDER BY); now ranks on album popularity. Prod: `Butterfly` classical 20/20 → **3/20** with BTS/Travis Scott/Smashing Pumpkins in-page; `Suite` *BITTERSUITE* rank **226 → 4**. **1b (optional classifier predicate) — DROPPED 2026-08-03, owner decision, no code shipped** (RFC OQ1 carries the full re-measurement): re-measured on live prod with the shipped Step 2 predicate as the classifier (`Butterfly` 3/20 reproduces the 07-28 smoke exactly, so the instrument matches). The residue is unchanged — `Suite` 17/20, `Prelude`/`Nocturne` 17/20 each — but splitting the 50 queries by what the query word *is* relocates the problem: **ordinary English words 63/549 = 11.5% classical** (20 of 28 return 0/20; the residue is `Winter`·`Spring`·`Autumn`·`Romance`) vs **classical form-words 325/399 = 81.5%**, which is the decision-log line "classical must stay findable" working correctly. 86% of the residue carries an opus token, so 1b's key works — and that is the objection: catalog-wide, `Concerto` has **326 matches / 0 non-classical**, `Etude` 103/0, `Suite` 270/3, so the specced `WHERE` form **empties those result pages**, contradicting this RFC's own 2026-07-25 decision. Re-shaped as an `ORDER BY` demotion it is safe but buys 11.5% → ~8.7% on three queries (`Romance` 16→15, `Autumn` unchanged — no non-classical rows exist to promote), and that 63 already includes false positives (`Sun`'s row is Agust D's *Snooze*, flagged via a Sakamoto feature). Noted for any reopening: the whole residue comes from **164 albums / 5,631 tracks = 20.7% of the catalog**, an enumerable set that aims better than a title classifier. **Step 2 ingest choke point — SHIPPED** (worker #88 + backend twin #141): `_SELECT_ELIGIBLE` classifies on **artist** genres in SQL (exact JSONB element match with the `클래식 록` decoy list — AC/DC/Queen/ROSALÍA verified NOT flagged, Satie/Schubert/Chopin/Beethoven flagged; prod read-only: 129/1,745 eligible = 7.4% classical, holdout keeps 2), Python filters behind `INGEST_EXCLUDE_CLASSICAL` with `classical_excluded`/`classical_holdout` WARNING counters + allowlist. Twin sweep: `GET /api/me/release-feed` got the music-side compilation filter (byte-identical predicate; owner's live 4 feed rows = 0 false positives). Absorbs **DATA-release-noise (a)**. **Observation gates**: (i) trailing-7-day classical album share 37% → **≤8% by ~2026-08-11** (RFC success metric; query in RFC Step 2 Verification), (ii) holdout misclassification rate once a few weeks of `classical_holdout` sweeps accumulate, (iii) **Step 3b backlog drain** — ~~`bestof_superseded` > 0 on the first tick~~ **CONFIRMED 2026-08-03 by prod smoke: 102 rows superseded `best-of-*` → `exact-title` in one run** (122 evaluated, 83.6% hit rate — matching the pre-merge sample to the decimal; backlog 2,765 → 2,663). The arm had never executed once before. Remaining: `docs/sql/lyrics_pool_health.sql` query 6 trending toward ~450 unsupersedable rows over ~26 runs (~2026-08-29), at which point the queue head returns to unresolved by itself. If it does **not** return, the 30-day rest interval is not doing its job and unresolved recovery is starved — that is the one way this step can fail quietly. **Step 3a** lyrics pool hygiene — **SHIPPED 2026-07-28** (worker #84 + bind-param fix #85) as **rule F alone**, not the `E ∪ F` this row claimed: the owner chose empirical label yield (self-correcting, genre-free) over the genre+title-form rule E, which was never implemented. Prod 2026-08-03: 3,297 rows marked, holdout **5/202 = 2.48%** against a pool now yielding ~5.5% — rule F is not parking rows that would have matched. **Step 3b** re-check ordering — **SHIPPED 2026-08-03** (worker #93), but **not the recency re-order it was named for**: re-measured before implementing, yield is flat across release-recency tiers (live LRCLIB re-probe **10.95 / 10.77 / 8.70%** vs the RFC's claimed 32× signal), because recency was only ever a proxy for "not a dead-source label" and Step 3a now tests that directly. What shipped is the aside the RFC mentioned in passing: the best-of supersession arm was **structurally unreachable** (11,033 unresolved rows ahead of 2,765 best-of rows under `LIMIT 150`), and **83.6% of those rows (112/134 sampled) now resolve to a strictly stronger `exact-title` match** — 15× the unresolved pool. The queue is inverted to drain that backlog first (~19 runs, self-consuming), with `touch` + a 30-day rest interval so the ~16% that can never be superseded cannot pin the queue head forever. RFC's `net ≤ 0/day` metric **retired as unreachable by ordering** — intake ~57/day and a full rotation takes 74 days, so drain is bounded by rotation; net is +38/day, down from +180. **Step 4** album-scoped expedite — **SHIPPED + prod-smoked 2026-07-28** (worker #86: SQS `{"job":"lyrics_reassessment","album_id":…}`, cooldown-idempotent; MFF smoke = 8/8 `not_found` → `matched` in ~16 s, album 11/11, pre-existing rows untouched; route+button "former PR2" stays deferred). Every exclusion is a reversible mark plus a holdout that measures its own misclassification rate; **search is fixed by ranking, not filtering**, so classical stays findable. Disproves the catalog-cap hypothesis (never fired, 2,934/5,000, 0 log matches in 60 d). → `docs/rfcs/DATA-catalog-noise-and-lyrics-coverage.md`.
- **FEAT-genre-recommendation** (Frozen — gated on review volume) — the "**장르 기준 추천 평론**": related-review recommendation split out of FEAT-genre-subgenres (old Step 5, rescoped 2026-06-15). A "related reviews" block on `/review/{slug}` — reviews sharing a `post_genres` sub-genre + edge-linked sub-genres, ranked (shared-tag → edge-distance → recency). Substrate is LIVE + much richer post-FEAT-genre-deepen (genre_edges **2,064** + **1,230** genres / 4 tiers + `post_genres` filled by the `/write` picker); **deferred because nothing to rank until reviews are tagged (~1 today)** — same review-volume precondition as FEAT-ai-editorial-critique. Owner edge authoring (old Step 6, `POST/DELETE /api/genres/{id}/edges`) folded in here, lowest priority (edges are Claude-Code-driven). NB the genre-map *viz* (old Step 4) was revived into FEAT-genre-subgenres (review-volume-independent). RFC → `docs/rfcs/FEAT-genre-recommendation.md`.
- **FEAT-blog-to-review-migration** — Step 1 (the `/blog → /review` prefix migration) **DONE + prod-live 2026-06-14** → `docs/archive/done/2026-06.md`. Only optional **Step 2** remains: a true HTTP 301 via the console-managed CloudFront Function `handler` (owner-applied infra; the static stubs already guarantee no-404, so 301 is SEO-only — add if inbound `/blog` links accumulate). RFC stays open for that → `docs/rfcs/FEAT-blog-to-review-migration.md`.
- **CHORE-neuralwatt-credit-burn** (opt-in path SHIPPED 2026-07-20; **burn deferred — glm-5.2 parity gate FAILED**) — the genre-heal S3 LLM stage can now route to NeuralWatt glm-5.2 via `GENRE_HEAL_LLM=neuralwatt` (`scripts/neuralwatt_claude_shim.py`, claude-CLI-compatible PATH shim, stdlib urllib + explicit timeout, per-call usage/cost JSONL in `~/Library/Logs/myblog-neuralwatt-usage.jsonl`, fail-open to the real `claude` binary so the nightly can only degrade to status quo; plist untouched — `claude -p` stays default). 8-album QA sample (read-only, $0.066): run-to-run **instability on the K-Pop/idol axis** (identical input flipped idol=T→F on IU rows; that axis writes `confidence=high` DB rows) → parity gate failed, default NOT switched, 03:00 usage-limit-collision benefit unrealized. Pricing measured: $1.45/M in, $4.50/M out ≈ $0.14–0.19 per incremental nightly; **~$9.93 credit remains**. Fallback target (lyrics corpus) assessed — already complete, not worth it. OPEN: owner call on retrying with a hardened prompt (pin the idol-axis rubric) vs leaving opt-in only. Key stays in `.zshrc` (owner 2026-07-20 — don't re-raise).
- **FEAT-bucket-identity** (Direction A DONE 2026-06-16 → `docs/archive/done/2026-06.md`; B DONE 2026-07-21; C subsumed by pocket-buckit V31; **D AUDITED + GATED 2026-08-03**) — strengthen "bucket" as the core workflow unit (save → research → listen → notes → review → link back). **B SHIPPED + prod-smoked 2026-07-21 (front #303, front-only)**: name-regex `crMeta` replaced by a typed-field lifecycle (담음→조사 중→작성 중→완료, subtree-aggregated, rename-proof; urgency accent dropped — no typed deadline field), listened-state cover join client-side ("이미 들음 → 평론 가능" ring), review→bucket/research chips on ReviewsTab (`?tab=bucket`); CDP 18/18 at prod scale + 6/6 bucket-row play entry. **Owner-pending proposals**: board → `/profile` landing tab; research-note deep link (`?album=` param); in-place tab switch for the 버킷 chip. **C (deferred, L)**: merge `들을 것` (`album_to_listen_items`) into the bucket model = one save target. **D — AUDITED 2026-08-03, no code opened, now GATED**: the "next concrete step" line was stale. Infra claim holds (`/collection` + `is_public` + `GET /api/buckets/public` all live, linked from header + footer), but **4 of D's 5 collection types already shipped as dedicated non-bucket pages** (명반→`/canon`, 신보/이번 주→`/releases`+`/radar`, 아티스트→`/artist/[id]`, 추천→home 오늘의 픽 at track level); only **장르 평론 모음** is unbuilt (`/genres` is the taxonomy map only). Both halves are data-gated: review-based collections sit at **0 published posts / 0 `album_reviews` / 0 content MDX** (live `/canon`, `/reviews` both render empty states), and album-based collections are gated on owner curation — public buckets are **1 of 6**, and that one was `To Review` (owner's 47-item working queue, handle `user-0468fd3c`), which the owner judged **unintended → to be set 비공개** (owner action; empties `/collection`). Re-open gates + re-measure SQL in the RFC audit block. RFC → `docs/rfcs/FEAT-bucket-identity.md`.

---

## Later (트리거 대기)

- **SEO-review-structured-data 첫 발행 검증** (done 2026-07-19, RFC 아카이브됨 — 남은 것은 트리거 대기 1건) — `Review` + `MusicAlbum` JSON-LD가 `/review/[slug]`에 빌드 타임으로 들어가 있고(front #288), 로컬 MDX 픽스처로 validator.schema.org 0 err/0 warn 확인. prod에 발행된 평론이 0개라 **실제 URL에서 Rich Results Test를 못 돌렸다**. **트리거 = 첫 평론 발행** → 그 URL로 RRT 1회. 본문 → `docs/archive/done/rfcs/SEO-review-structured-data.md`.

- **FIX-nightly-draft-identity Phase B** (planned, trigger-gated — successor to audit D-1; **the P0 itself was RESOLVED 2026-07-27**) — the nightly job now runs as its own Cognito identity (`nightly-agent@ratemymusic.blog`), admitted draft-only on `POST /api/posts` with every editorial field coerced (backend #133/#134); `CHECKED_SQL` is owner-scoped with a counted foreign-memo PHASE-B TRIGGER (ws #716); grow-once works server-side via `POST /api/buckets/nightly-grow` with the acting user pinned to OWNER_SUB (backend #134, route ws #717, script cutover ws #718); delivery failures exit non-zero (2/3/4). **Live e2e verified 2026-07-27**: `1 draft(s) created, 1 memo(s) grown, 0 skipped/failed, 0 DENIED`, exit 0; DB shows the memo unchecked + post_id stamped + the post `status='draft'`. Phase B (multi-user: `posts` owner column, bucket-derived acting user, read-side scoping) builds when any RFC §4 trigger fires — watch `PHASE-B TRIGGER` in the nightly log. Design + amendments: `docs/rfcs/FIX-nightly-draft-identity.md` (§4, §6); agent runbook: `infra/README.md`.

---

## Frozen (아이디어만, RFC 없음)

확장 기능 후보. 의향 생기면 그때 RFC 시작. 지금 아무것도 안 함.

- **FEAT-genre-autoheal — cloud heal** (**only the cloud variant remains deferred**) — full laptop-independence for the daily genre backfill via a cloud iTunes-baseline heal, hung behind the album-ingest batch (worker EventBridge); the local launchd LLM pass stays the low→high / K-Pop-arbitration upgrade. **Blocker**: the S3 LLM step shells `claude -p` on the Max-subscription token (absent in Lambda) → a cloud pass would be iTunes-only + must use a DISTINCT partial marker so it never excludes the LLM pass. The **local** daily fix is DONE + LIVE (now **04:00**). **2026-06-21: the *on-demand* variant shipped** — a 분석 버킷 web button (folded into `분류하기`) → `genre_backfill_requests` queue (shared_db V25) → a 5-min local genre-heal poller runs the existing backfill (`claude -p` stays local; pull not push). Only the **cloud iTunes-only** pass is still open. → `docs/archive/done/2026-06.md`. Memory `reference-genre-backfill-stale-decisions-replay`, `reference-sqs-fanout-burst-pitfalls`.
- **Per-genre exemplar albums** — start inside `definition_md` markdown; structured linking only if a consumer needs it.
