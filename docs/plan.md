# Plan

Active workspace tracker for cross-repo work. Each row carries `Scope / Order (if multi-repo) / Verification (local + prod smoke) / Rollback / Status`; RFC-backed entries collapse to a one-line pointer at `docs/rfcs/<id>-<slug>.md`. When a row finishes, drop it — `git log` and `docs/archive/done/YYYY-MM.md` are authoritative for history.

---

## Active

- **FEAT-genre-autoheal** (local daily fix DONE 2026-06-15; cloud laptop-independence deferred) — the daily launchd genre backfill ran `--incremental --execute` (no `--label`), which took the cached-decisions branch: it **replayed a frozen `decisions.json`** every day (06-13 & 06-14 logs identical: `982 albums marked`) and never re-labeled albums synced since → new albums accumulated genre-less. **FIXED**: plist now runs `--incremental --label --execute` (shared_db #33) so each run rebuilds decisions from the current marker-less catalog, then executes; installed copy reloaded + kickstart-verified (re-labeled the one marker-less album → prod 1122/1122 marked, 0 bare). The local daily job now self-heals (laptop-dependent — fires 09:30 when awake, launchd catches up after sleep). **Still deferred per owner** — full laptop-independence via a cloud iTunes-baseline heal hung **behind the album-ingest batch** (worker EventBridge), keeping the local launchd LLM pass as the low→high / K-Pop-arbitration upgrade. **Blocker for full cloud**: the S3 LLM step shells `claude -p` on the Max subscription token (`~/.claude/.credentials.json`), absent in Lambda → cloud pass would be iTunes-only + must use a DISTINCT partial marker so it never excludes the LLM pass. Not scoped/accepted. See memory `reference-genre-backfill-stale-decisions-replay`.

- **FEAT-editor-buckit** (Stage 0 DONE+merged 2026-06-17; **Stage 1 Steps 2/3 merged, Steps 4+5 built 2026-06-18 — PR pending on `feat/FEAT-editor-buckit-stage1-runner-schedule`**) — pre-writing editorial **commissioning desk** (proposes *what to write next, from what angle*; opposite funnel end to FEAT-ai-editorial-critique). **Stage 0 / Step 1 DONE+MERGED 2026-06-17** (#378) = `$0` `claude -p` script `scripts/editor_buckit.py` + prompt `docs/editorial/editor-buckit-prompt.md` + gitignored `docs/editorial/ideas/`; proved out (5 bucket-anchored cards, refused to pad). **Stage 1** = a `$0` **file-only nightly** memo→skeleton job (rule docs `buckit-nightly-run.md`+`buckit-nightly.md`, inherit v2.5 ★/§6; "오늘 밤 키우기" checkbox gate; no DB write / route / API key). **Step 2 (data) DONE+MERGED+prod-smoked** — shared_db V22 `review_bucket_items.prep_tonight` (#37→`v0.21.0`) + backend set-only item-PATCH (#78) + contract (#381). **Step 3 (frontend) DONE+MERGED+prod-smoked** — `MemoWindow` 2-col memo+toggle overlay (#177). **Steps 4+5 BUILT this session (rule #4 exception, owner-OK'd 4+5 together)**: Step 4 = `scripts/buckit_nightly.py` ($0 nightly runner, read-only checked-memo export → opus → Korean skeletons in gitignored `docs/buckit/<date>/`; **audit-hardened — claude has NO tools, rule docs+context inlined, script does all I/O** after an adversarial audit found the agentic-write path broken + an exfil hole); Step 5 = `scripts/com.myblog.buckit-nightly.plist` (03:00 launchd, checkbox-gated, owner-installed). Verified by temp-inject→run→revert on a real item (*channel ORANGE*): skeleton inventory-shaped / candidates-not-chosen / blanks ✓, **zero prod residue**. **Remaining: push+merge, then owner runs `launchctl load`** → archive. RFC → `docs/rfcs/FEAT-editor-buckit.md`.

_STAB-1 stabilization program complete (STAB-2..STAB-7 all merged, prod-live, archived 2026-06-06); product-expansion freeze lifted 2026-06-06. FEAT-spotify-library-sync DONE + LIVE 2026-06-08, archived 2026-06-09 (#288–#291). FEAT-album-catalog-ingest DONE + LIVE 2026-06-10 (ws #297/#298, worker #42/#43; rule applied, one-shot verified `enqueued=0` as designed) — `git log` + `docs/archive/done/rfcs/` authoritative. **FEAT-genre-system (tier-0) DONE + LIVE 2026-06-13, archived 2026-06-14** (ws #325/#326 / be #66 / front #145) — `docs/archive/done/` authoritative; only the Outliner view productionized, ego-view relationships + sub-genre vocab = separate **FEAT-genre-subgenres**. **FEAT-genre-subgenres (tier-1 editorial) DONE + LIVE + archived 2026-06-15** — all Steps 1/1b/2/3/4: `genre_edges`+`post_genres`+211 tier-1+40 edges (shared_db V20, be #73) → `/write` `GenrePicker` (`genre_ids`→`post_genres`, front #163) → publish bakes `subgenres:[{slug,label}]` into review frontmatter + review-page chips (be #74 / front #163) → read-only A/B/C `/genres` map + per-node ego-view (front #164); ws #362/#364/#368. Prod smoke: `/genres` renders 12+211+40 live, genre_ids tagging round-trips. Steps 5–6 (related-review recommendation + edge authoring) split to **FEAT-genre-recommendation** (Frozen ↓, gated on review volume) — `docs/archive/done/rfcs/FEAT-genre-subgenres.md` authoritative. **FEAT-genre-deepen DONE + LIVE + archived 2026-06-15** — owner-directed ("촘촘하게 / 훨씬 더 깊이"), reversed the 2-tier decision → **4 tiers / 1,230 genres** (13 top-level: +Blues) **/ 2,064 edges** (874 nodes with 2+ relationships, was 0). Built by a 13-agent research workflow + cleanup; shared_db V21 (data-only, prod-applied 1230/13/2064; #36) + frontend N-tier (gm-model recurse, TreeA/B/C deep, collapsible picker; front #165). Prod smoke: live `/genres` renders "13개 최상위", Electronic→House→Deep House→Balearic House (tier-3) expands. Recommendation stays Frozen. `docs/archive/done/rfcs/FEAT-genre-deepen.md` authoritative. **FEAT-album-research-notes DONE + LIVE 2026-06-11, archived 2026-06-14** (5 steps, $0 local-poller path; #25/#304/#310/#138) — `docs/archive/done/` authoritative. **FEAT-home-redesign DONE + LIVE 2026-06-14, archived** (editorial home front #151 + the `/blog → /review` migration #153/#344; prod smoke passed, zero 404s). **FEAT-public-bucket-multiuser Scope A (public bucket viewer, A1–A5) DONE + LIVE 2026-06-15, archived** (opt-in `is_public` → public `/collection` viewer + `GET /api/buckets` JWT-gated; shared_db #30/#31, be #69–#72, front #154–#156, ws #346–#351; Scope B/accounts not started) — `docs/archive/done/` authoritative. **FEAT-track-play-history DONE + LIVE 2026-06-15** (durable TRACK-level play log `spotify_track_play_events` V19, append-only; worker writes it SAVEPOINT-isolated in the listening-sync tx; shared_db #32 / worker #46 / ws #353; V19 prod-applied + prod smoke 50/50 rows track_id+album_id resolved) — `git log` authoritative. **FEAT-artist-page hub page DONE + LIVE 2026-06-15** (public `/artist/[id]` "critic's lens" from the Claude Design handoff; new music `GET /api/music/artists/ids` build-enumeration endpoint + static getStaticPaths over 805 catalog artists + `ArtistHub` runtime island + build-time review overlay from the blog collection; music #43 / front #157 / ws #356; prod smoke — 저스디스 hub renders real hero+LIT review+discography+top-tracks, 805 pages served, non-catalog uuid 404s) — `git log` authoritative. **FEAT-artist-page link-in DONE + LIVE 2026-06-15** (review byline front #158; search→artist now live via FEAT-global-search ②). **FEAT-global-search DONE + LIVE 2026-06-15** — unified all music search under the 전역 검색 design (평론 first-class) + retired Pagefind. Step 1 public `/search` page (front #159); ②③④ (front #160): ② header instant-dropdown island (`HeaderSearch`, DB-only public + build-time 평론 index `reviewIndex.ts`) replacing Pagefind end-to-end (deps/script/CSS/`data-pagefind*`/deploy step removed, `/pagefind/*`→404 purged), ③ writer `CommandPalette` + ④ bucket `AddAlbumModal` re-skinned onto shared `ResultRow` (`action: navigate|button|static` + `is-current`/`is-present`; interactive Spotify rows no longer dimmed; bucket "담김" disabled from existing ids). prod smoke: header dropdown live with real data (평론1/아티스트3/앨범3/트랙4 for "저스디스"), artist→album recall firing. `git log` authoritative._

_**FIX-research-cover-fanout DONE + LIVE 2026-06-16** (be #75 / ws #373 / front #169) — batched `GET /api/research/status?album_ids=…` replaced the per-cover note GET fan-out that 503'd at the ~10 Lambda-concurrency ceiling; cover dots render from the bucket-payload seed + one batched call, full note GET only on open. **FEAT-ux-polish-2026-06-16 DONE + LIVE 2026-06-16** — owner-driven 16-item UX sweep (front/backend/music). **Search**: ranking relevance+popularity blend (music #45 — recall-gate validated vs prod catalog = **0 regression**) + writer-palette compact/auto-search + bucket add-modal scroll-lock/size + header search width (front #170). **Research notes**: status chips + fixed hierarchy + collapsible sections (front #171), wider `/write` reading column (#174), rich bucket reading **modal** via a shared `NoteBody` (#175). **Profile**: now-playing **album cover** + recent-tracks dedup (be #76 / ws #374 / front #172), edit-my-reviews + Library-tab removal (front #170). **Identity**: Pitchfork footer + **LOWFREQ persona removed** (`김저음`/`@lowfreq`/`혼자 쓰는…` → `buckit`; front #170) + bucket naming unified on `버킷` + `평론 완료` label (front #173 / be #77 — see **FEAT-bucket-identity** Direction A). Item 11 (recent-album accumulation) verified **already-correct** (durable `spotify_play_events`). Item 4 (full home redesign) **deferred — design pending**; bucket Directions B/C → **FEAT-bucket-identity** RFC. **Verification gap**: the auth-gated UX (palette auto-search, bucket modal, now-playing cover, review-edit, research chips) shipped + lint/build/recall-validated but not yet click-through-verified in a logged-in prod session. `git log` authoritative._

_**FEAT-home-redesign-v2 DONE + LIVE 2026-06-16** (front #176) — full home redesign (UX-sweep item 4), implemented from the Claude Design handoff as **Variant B "Refined Editorial"** (the owner's chosen direction). Reader-first + fully public: BNM **feature hero** (big cover + serif italic title + pull + stars) → **최신 평론 type-forward list** (hairline dividers) → 장르 → 숫자; only writer-facing affordance is the header 글쓰기. Removed the old dual-lane WriterStrip + drag/reorder module system; deleted orphaned BnmHero/LatestReviews. New `.bk-*` CSS in global.css. Verified via build + headless screenshot. The mock's **bucket-curation rows (이번 주 / 추천 버킷) deferred** — surfaced as flexible public collections (genre/artist/playlist 모음) on dedicated surfaces, not the home (owner: keep main clear/general). See **FEAT-bucket-identity** Direction D. `git log` authoritative._

---

## Backlog

> ✅ **Unblocked 2026-06-06** — STAB-1 P0 auth boundary + SQS fix + section model all shipped. These are scope-ready but still need owner go + RFC accept before promotion to Active.

- **FEAT-ai-editorial-critique** (draft) — **AI editorial critic, Phase 0 thin slice** (the product's defining feature — see 2026-06-06 whole-project review, memory `project-ai-editorial-greenfield`). LLM service layer + `POST /api/editorial/critique` that runs `docs/editorial/review-critique-method.md` over a draft + its already-linked album DB facts (`post_albums`, no schema change), returning grounded Korean feedback + `[ ]`/`[source?]` markers + §4 checklist — reconstruction/feedback only, never rewrites. UI-less; proven by curl + a golden-draft eval (critic must not hallucinate) before any frontend. Backend-only, 2 steps (Step 1 local + golden eval → Step 2 infra JWT route + contract). RFC → `docs/rfcs/FEAT-ai-editorial-critique.md`. **Caveats baked in: token-ceiling cost guard + manual-fire only ($0-bias owner); fact-check is narrow-not-deterministic (most honesty-line facts live outside the DB → `[source?]`); prompt single-source vs drift (OQ1).** Fact-check pass, `/write` sidebar, persistence, planner, publish-gate = later RFCs (non-goals). Status draft — needs owner accept before Active.
- **FEAT-genre-recommendation** (Frozen — gated on review volume) — the "**장르 기준 추천 평론**": related-review recommendation split out of FEAT-genre-subgenres (old Step 5, rescoped 2026-06-15). A "related reviews" block on `/review/{slug}` — reviews sharing a `post_genres` sub-genre + edge-linked sub-genres, ranked (shared-tag → edge-distance → recency). Substrate is LIVE + much richer post-FEAT-genre-deepen (genre_edges **2,064** + **1,230** genres / 4 tiers + `post_genres` filled by the `/write` picker); **deferred because nothing to rank until reviews are tagged (~1 today)** — same review-volume precondition as FEAT-ai-editorial-critique. Owner edge authoring (old Step 6, `POST/DELETE /api/genres/{id}/edges`) folded in here, lowest priority (edges are Claude-Code-driven). NB the genre-map *viz* (old Step 4) was revived into FEAT-genre-subgenres (review-volume-independent). RFC → `docs/rfcs/FEAT-genre-recommendation.md`.
- **FEAT-genre-artist-distribution** (draft) — `/profile` 통계 charts: genre + artist/listen distribution. Carved from `FEAT-member-dashboard` Step 4. RFC → `docs/rfcs/FEAT-genre-artist-distribution.md`. Depends on `FEAT-genre-system` (tier-0 genre labels — **DONE + archived 2026-06-13**, so the genre data exists) + `spotify_play_events` (shipped, listen counts) → **data blocker resolved, now scopable**. Not yet scoped/accepted.
- **FEAT-multi-user-accounts** (draft) — user/profile model, public `/members/[handle]`, social graph; un-hides social stats. Carved from `FEAT-member-dashboard` Step 6. RFC → `docs/rfcs/FEAT-multi-user-accounts.md`. Largest follow-on; needs data + auth model. Not yet scoped/accepted.
- **FEAT-blog-to-review-migration** — **Step 1 DONE + prod-live 2026-06-14** (front #153 / ws #344; prod smoke PASSED, old `/blog/*` all 200 → `/review/`, zero 404s). Only optional **Step 2** remains: a true HTTP 301 via the console-managed CloudFront Function `handler` (owner-applied infra; the static stubs already guarantee no-404, so 301 is SEO-only — add if inbound `/blog` links accumulate). RFC stays open for that → `docs/rfcs/FEAT-blog-to-review-migration.md`.
- **FEAT-bucket-identity** (Direction A DONE 2026-06-16; B not started) — strengthen "bucket" as the core workflow unit (save → research → listen → notes → review → link back). **A done** (front #173 / be #77): user-facing vocabulary unified on `버킷` (dropped the `크레이트` alias), `is_done` 409 message → `평론 완료`. **B (next, front-mostly)**: replace the name-regex `crMeta` status with one **derived from real fields** (`research_status`/`already_reviewed`/`post_id`/`is_done`) so it stops lying on rename, join listened-state onto covers ("이미 들음 → 평론 가능"), add the reverse link review→bucket/research, maybe promote the board to the `/profile` landing tab. **C (deferred, L)**: merge `들을 것` (`album_to_listen_items`) into the bucket model = one save target. RFC → `docs/rfcs/FEAT-bucket-identity.md`.

---

## Later (트리거 대기)

_(empty — FEAT-genre-taxonomy discarded 2026-06-12, superseded by FEAT-genre-system, done + archived 2026-06-13)_

---

## Frozen (아이디어만, RFC 없음)

확장 기능 후보. 의향 생기면 그때 RFC 시작. 지금 아무것도 안 함.

  - Per-genre exemplar albums: start inside `definition_md` markdown; structured linking only if a consumer needs it.
  - No genre-relations/influence edges (re-confirmed; tree only).
- **FEAT-new-release-feed** (L) — EventBridge 주기 → 본인 review artist 의 최근 앨범 fetch → `artist_new_releases` → `/api/music/feed/new-releases` → 메인 카드
