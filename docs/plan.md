# Plan

Active workspace tracker for cross-repo work. Each row carries `Scope / Order (if multi-repo) / Verification (local + prod smoke) / Rollback / Status`; RFC-backed entries collapse to a one-line pointer at `docs/rfcs/<id>-<slug>.md`. When a row finishes, drop it — `git log` and `docs/archive/done/YYYY-MM.md` are authoritative for history.

---

## Active

- **FEAT-multi-user-accounts** (**in-progress 2026-07-07** — Phase 0 Identity minimum) — multi-user platform: Google+Kakao signup, RYM-style all-public album reviews, per-user buckets, Last.fm-primary listening, model-agnostic `LLMEngine` AI. Phase 0 sub-steps: **0a** shared_db V36 `users` table → **0b** 개인정보처리방침 page → **0c** infra Cognito self-signup+IdPs (**in-progress 2026-07-08** — creds in SSM `/myblog/oauth/*`; audit found self-signup would expose 27 single-owner backend routes to members → **2 coordinated PRs: backend `require_owner` gate must deploy BEFORE the infra `apply`**; owner-apply-gated) → **0d** backend `/api/me` lazy-provisioning + account deletion → **0e** front settings page. Scope: shared_db + infra + backend + front. Verification: shared_db pytest + prod migration apply check; front lint/astro check + real-browser click-through; prod smoke per sub-step PR. Rollback: additive-only (new table/page — drop/remove). Gates G1/G2 per RFC → `docs/rfcs/FEAT-multi-user-accounts.md` (Status: in-progress).

**Last-shipped trail** (history is authoritative in `git log` + `docs/archive/done/`):
**FEAT-pocket-buckit** (all repos, 6 steps + follow-ons, **CLOSED 2026-07-06**) — the global Pocket Buckit interaction layer: user-selectable-design bottom tray (D9, `pb:design`), generalized membership (V28–V31: `item_type` album/track/review/playback/snapshot/artist + per-kind partial-uniques + snapshot side-table + `들을 것` fold), single-owner server-minted playback token (A+), sign-in resume (`pb:resume`), tray-open DnD, bucket-management UI + multi-type tile rendering (front #211/#212), and finally the **member-authoring paths for review/playback/snapshot (front #244, 2026-07-06)** — with which every planned member kind is authorable + renderable and the RFC closed (remaining "future kinds" were hypothetical, not committed). Full history → `docs/archive/done/rfcs/FEAT-pocket-buckit.md` (Status: done) + `docs/archive/done/2026-06.md`/`2026-07.md`; Design Atlas + configurator on claude.ai/design (project `cf932975`). — Same day: **FEAT-lyrics-translation-sweep** (workspace-only, single step, **CLOSED 2026-07-06**) — the lyrics poller auto-queues translation for research-note albums from state each firing; Step 1 ws #538 (2026-07-05), the 505-track backlog drained in ~1 day: candidates **0**, ledger **484 done (`claude.sonnet`/v2) + 36 `failed('korean_source')`** with the 4 transient `engine_validation` failures re-requested → all `done`; the exit gate's 2-day-stability leg **waived by the owner 2026-07-06** (backlog fully drained + fully-accounted ledger; the sweep keeps running under launchd either way — the gate only gated the RFC paperwork). Full history → `docs/archive/done/rfcs/FEAT-lyrics-translation-sweep.md` (Status: done) + `docs/archive/done/2026-07.md`. — Before it: **FEAT-mobile-web-app** (front-only, all 4 steps, 2026-07-05) — the public read path is comfortable at 390px and installable as a PWA (end state per scope decision — no Capacitor; logged-in workspace touch redesign = follow-on `FEAT-mobile-workspace-touch`, not drafted, OQ4 revisits with real mobile usage). Step 1 compact 52px header + hamburger drawer + full-width search overlay <768px — pattern (B) picked at the Step 1a mockup gate (ws #536; bottom edge stays Pocket-tray-only; front #234); Step 2 breakpoint consolidation onto the canonical set 480/640/767/**880 (`--bp-tab`, extended)**/1024/1280 + read-path clip sweep + `/genres` mobile force-layout-A (front #235); Step 3 `<ClientRouter />` view transitions, `transition:persist` on 4 header roots + the PocketBuckit island, swap side-effects (theme/accent re-apply, drawer close, `astro:page-load` inits) handled (front #236); Step 4 `@vite-pwa/astro` PWA — shell-only precache, `/review/*` NetworkFirst + cover SWR runtime caches, `/api/*` never cached, iOS install hint, **no infra change** (deploy workflow already ships `sw.js` no-cache; CloudFront `handler` passes `.`-URIs — verified live; front #237). Per-step prod smoke quoted on each front PR; SW one-cycle-update self-confirms on the next routine deploy. PRs: front #234/#235/#236/#237, ws #535/#536/#539. Full history → `docs/archive/done/rfcs/FEAT-mobile-web-app.md` (Status: done) + `docs/archive/done/2026-07.md`. — Before it: **FEAT-lyrics-engine-sonnet** (workspace `scripts/`+`tools/` only, both steps, 2026-07-05) — the lyrics-translation poller engine swapped per-line Amazon Translate → one headless `claude -p --model sonnet` call per track with the benchmark-frozen 의역 prompt (2026-07-04 LUX bench 15/15 PASS, 0 refusals — the 07-04 copyright refusal keyed on prompt shape, not headless-ness), whole-lyric context replacing context-free per-line MT; **no Amazon fallback** (validation failure retries once → fail-visible `engine_validation`; transient CLI failure leaves the claim for the 20-min stale-claim retry), boto3 translate path deleted, `model='claude.sonnet'`, `TRANSLATOR_VERSION` v1→v2, fingerprint parity untouched. Step 1 poller swap (ws #531 — offline engine-path harness 5/5, forced-failure claim-kept check, 2-pilot `--drain` re-request incl. the 75-line Porcelana → `done\|claude.sonnet\|v2` with segment/gap/fingerprint parity); Step 2 in-place backfill (ws #532, `tools/lyrics_retranslate_backfill.py` importing the poller functions verbatim; pre-state dump `tools/out/retranslate_prestate_20260705-013142.json`) — **11/11 PASS → prod 13/13 `done\|claude.sonnet\|v2`, 0 amazon rows**, authed prod GET spot check (Berghain 43/43 `text_ko`) + owner 3-track viewer spot-audit PASS. RFC accepted ws #529. Full history → `docs/archive/done/rfcs/FEAT-lyrics-engine-sonnet.md` (Status: done) + `docs/archive/done/2026-07.md`. — Before it: **FIX-lyrics-matcher-noise** (worker + workspace tools, all 3 steps, 2026-07-04) — the best-of OQ5 follow-on: the conservative lyrics matcher stops manufacturing false ambiguity from LRCLIB crowd-title noise, resolving rows as genuine `exact-title` matches at the source. `_strip_noise` (track-number prefixes, non-rendition parenthetical/bracket AND trailing dash segments, symmetric catalog+candidate), `exact_base_equal` (noise-stripped OR plain step2-v2 canonical equality — new exact set is a strict superset, no pre-fix match can be lost; fuzzy gate stays on plain canonicals), group key = base title only (artist component split alias/spelling variants into false ambiguity), version-agreeing representative with full-canonical pick-stability preference; `MATCHER_VERSION` → `step3-v1`. Step 1 branch-local + 3-round old-vs-new replay probe (`tools/lyrics_matcher_noise_probe.py` — exact regression guard 0 lost / 300/300 same pick; labelled precision 68/68 = 100% ≥90% OQ3 gate; 308 pytest); Step 2 worker #64 `c53c48a` + deploy 28692610239 + prod smoke (matched +4, deltas exact, exact-title/best-of untouched); Step 3 pool-scoped one-shot over the 41 residual (matched +20 — source exact +16, best-of +4; **ambiguous 18→0, review 26→21**; corpus matched 10,628→10,652, exact-title 8,001→8,021). Residual 21 review rows have no tier-1 candidate even post-fix. Full history → `docs/archive/done/rfcs/FIX-lyrics-matcher-noise.md` (Status: done) + `docs/archive/done/2026-07.md`. — Before it: **FEAT-lyrics-best-of-promotion** (worker + workspace tools, all steps 0–3, 2026-07-03) — auto-promotes the single best plausible candidate out of the conservative matcher's `ambiguous`/`review_required` pool as a tagged `best-of-*` `matched` row (v1 tier-1-only: exact base-title + version agreement + body precondition; no manual UI, no `exact-title` change, no schema change, no new public exposure). Step 1 `promote_best` core + read-only risk probe (worker #60; tier-1 hit 94.4% of 2,783, labelled precision 48/48); Step 2 live integration into the shared `run_eval_batch` + basis-aware consistency invariant + `_BASIS_STRENGTH` rung 2 + widened reassessment selection (worker #61; first live run 32 promotions, exact delta reconciliation); Step 3 retroactive local one-shot backfill against prod (ws #509, `tools/lyrics_bestof_backfill.py` reusing the live path verbatim) — **2,705/2,757 = 98.1% of the parked pool promoted** (best-of matched +2,580, all-instrumental no_lyrics +125, pool-drift exact-title +8), corpus `matched` share **37%→49.5%**, manual audit 40/40 = 100% (≥90% bar), zero pre-existing exact-title rows touched; residual 18 LRCLIB-transient ambiguous (reassessment drains) + 26 review parked = the OQ5 follow-on class (matcher noise source-fix RFC — natural next). Full history → `docs/archive/done/rfcs/FEAT-lyrics-best-of-promotion.md` (Status: done) + `docs/archive/done/2026-07.md`. — Before it: **FEAT-lyrics-viewer** (front-only Step 3, 2026-07-03) — the authenticated-only full-screen lyrics viewer overlay, first consumer of FEAT-lyrics-corpus, complete. **Step 1** backend JWT-gated `GET /api/lyrics/{spotify_track_id}` (server-side resolve → `Track.id`, one round trip; `require_cognito_token`, **not** edge_guard-only) + apigateway route + merged contract (backend #98 / ws #491 / front #220; apply owner-approved; prod smoke 8/8 incl. unauth-401 privacy gate). **Step 2** front overlay (front #221) — full-screen `useDismissable` + `.lf-scrim`, focus-one-segment emphasis, manual nav (prev/next + swipe/drag + tap-to-focus + arrows/wheel), availability-aware empty states, isolated debug entry `?lyrics=<id>`; click-through caught a real-pointer tap-to-focus killer (`setPointerCapture` on pointerdown retargeted click → fix = defer capture to the 8px drag threshold). **Step 3** (front #222, merge `f097356`) — dynamic entry on `NowPlaying` live branches only (idle/최근재생 branch gets no entry → no recent-history fallback); tap → 1-shot client-side `GET /v1/me/player` (token mint lazy on the explicit action; same sanctioned pattern as `requestPlayback`, rule #9 not engaged) — `playing` opens with live `item.id`+`progress_ms`, `idle` hides entry, `unavailable` keeps a retry-able button; viewer gains 1-shot position-seeded initial focus (`focusIndexForMs` skips plain rows — no `start_ms`) + manual refresh `↻` (track change swaps segments, position-only re-inits focus once, stopped → "지금 재생 중인 곡이 없어요"); no auto-advance anywhere. Front-only Step 2/3 — no backend/contract/infra change. Prod smoke: API (unauth 401 + authed 200 / 45 synced seg w/ numeric `start_ms`) + UI (idle-gate entry-hidden + unauth→Cognito + debug-overlay 45 seg incl. KR/EN + "간주" stanza gap + tap-to-focus, console 0); live dynamic-entry path deferred to owner active-session spot-check. PRs: backend #98, workspace #491, front #220/#221/#222. Full history → `docs/archive/done/rfcs/FEAT-lyrics-viewer.md` (Status: done) + `docs/archive/done/2026-07.md`. — Before it: **FEAT-my-buckit-artist** (shared_db + backend + workspace contract + front, all 6 steps, 2026-06-30) — renamed the 평론 버킷 area to **My Buckit** + a closed bucket-type set (**General** unchanged / **Artist** = artist-members-only, no-dup, featuring/compilation source **auto-expands** to credited artists, VA excluded, never silent-converts) + a single `aria-expanded` Pocket toggle (local, no API) + an in-memory drag **NEW** marker (clears ~8s / drawer-close) + **Pocket-open DnD into visible buckets**. shared_db **V32** (`review_buckets.type` + `review_bucket_items.artist_id` + `uq_..._artist` partial-unique + inverse CHECK, `v0.26.0`) → backend membership + type-gate (album→Artist=400, enforced in add **and** move) + VA-excluded source-expansion via a `Union` `POST /items` (openapi purely additive), no new route → workspace contract → front type chips (전체/General/Artist) + Artist Buckit → **Step 6 tray-open DnD** (front #217 `d2cfbeb`): a Pocket-tray drawer item drags onto a visible board bucket via a `pb:dnd-start`/`pb:dnd-end` synchronous cross-island window handoff that populates the board's module-level `dnd`, reusing `canAcceptAlbumDrag`/`acceptCol`/`ops.*` verbatim (General target = single reorder move + NEW dot; Artist target = source-expansion) — the shared `bucketStore` re-renders both islands off one optimistic update. Prod-verified: backend 7/7 (TRACK C #216 — create-artist / add / dedup-409 / album-400-gate / source-expansion / cleanup); Step 6 new bundle live on prod (`events` chunk carries `pb:dnd-start`/`pb:dnd-end`, tray chunk carries the draggable wiring) + full pre-merge headless-CDP end-to-end (handoff payload / General-move single PUT + NEW dot / Artist `POST source_album_id` expansion / no spurious drag fetches / console 0). Real mouse-drag *gesture* + no-glow gate deferred to manual owner spot-check (CDP can't synthesize native HTML5 drag; gate reused from prod-verified TRACK C). NB the front deploy gate was unblocked by a `WORKSPACE_GITHUB_TOKEN` `gho` stopgap (owner to swap for a scoped fine-grained PAT). PRs: shared_db #48, backend #97, workspace #465/#466/#467, front #214/#216/#217. Full history → `docs/archive/done/rfcs/FEAT-my-buckit-artist.md` (Status: done) + `docs/archive/done/2026-06.md`. — Before it: **FEAT-spotify-streaming-playback** (all 4 repos + contract, 4 steps, 2026-06-25) — the existing ▶ buttons (Pocket tray + review/album tracklist) now **play real audio** for the owner on Spotify Premium (previously wired but dormant). Step 1 `--streaming` bootstrap → `streaming_refresh_token` in SSM `/myblog/spotify` (`/api/playback/spotify-token` 503→200); Step 2 `GET /api/playback/resolve?type=&id=` → `spotify:album|track:<spotify_id>` (catalog direct read, **edge_guard-only** — no JWT / no apigateway.tf / no terraform apply, since `spotify_id` is a public id); Step 3 front `resolveProviderUri` calls `/resolve` (plain fetch, not apiFetch) + `getOAuthToken` re-mint + `account_error`→Premium / region-404 → notice (album=`context_uri`, track=`uris[]`). Single-owner v1 (in-app OAuth deferred D27; multi-listener a later RFC). Prod-verified: token route 200, `/resolve` 200 via CloudFront (exact `spotify:album:<id>`), deployed bundle carries the wiring, **owner-confirmed ▶ plays real audio on prod 2026-06-25**. PRs: workspace #459/#460/#462/#463, backend #96, front #213. Full history → `docs/archive/done/rfcs/FEAT-spotify-streaming-playback.md` (Status: done) + `docs/archive/done/2026-06.md`. — Before it: **FEAT-analysis-explore** (backend + workspace contract + front, single-pass, 2026-06-23) — made the 분석 버킷 **임포트(평생+라이브)** source *explorable*, all re-aggregating the existing `_unified_events` union (per-stream `event_ts`) with **no new ingestion / ML / schema**: (1) a **time-range filter** — a half-open `[from, to)` threaded into BOTH union legs (`as_of` held GLOBAL so the import/live boundary stays invariant under the range), surfaced as 전체/올해/작년/이번달 presets + data-derived 연도·시대 dropdowns (KST wall-clock → UTC instant); (2) **item drill-down** — `GET /api/library/stream-history/item?type=artist|album|track` → count/time/first-last/KST per-year (+ an artist's top tracks/albums), in a private `lf-slideover` from any chart-item click (`DistChart` gained an optional `onSelect`; the 좋아요 charts stay non-interactive); (3) a **listening clock** — `GET /api/library/stream-history/clock` → KST hour×weekday heatmap. Additive contract (+2 paths, +3 schemas, +`from`/`to` params on the 6 existing endpoints); edge_guard reads, **DB-only (rule #9)**. Prod-smoked 2026-06-23: clock self-consistent (cell-sum **3252 == total**), range 전체 **3,252→이번달 948**, drill-down 키드밀리 **274** (10 tracks / 4 albums), bad-id 404 / bad-metric 422 guards; deployed front bundle carries the new UI (`시간대`/`대표 트랙`/`data-chart-click`); read-only prod invariants (partition-sum, drill==panel, `as_of`-invariant) all hold; full real-browser CDP click-through pre-merge (console 0). NB all current import data is calendar-**2026**, so the 연도/시대 dropdowns are data-gated (they appear as more history imports). PRs: workspace #437, backend #91, front #201. Full history → `docs/archive/done/rfcs/FEAT-analysis-explore.md` (Status: done) + `docs/archive/done/2026-06.md`. — Before it: **FEAT-listening-live-merge** (backend + front, 2 steps, 2026-06-23) — folded the live recently-played poller into the 분석 버킷 import source as one continuous **lifetime + live** signal (import authoritative `ts ≤ as_of`, live poller `spotify_track_play_events` fills `as_of → now` via a safe **time-boundary union**, not per-row dedup — count exact, post-`as_of` time **estimated** `duration_sec × plays`), and removed the standalone **재생** toggle; the source toggle is now `[임포트(평생+라이브), 좋아요]` with a "추정" time marker + a live-extension caption when `live_streams > 0`. Additive contract field `live_streams`; no schema change. Prod-verified: total **3,215→3,252** (+37 live), retrospective on-this-day **0→9**. PRs: backend #90, front #199/#200, workspace #432/#433/#434. Full history → `docs/archive/done/rfcs/FEAT-listening-live-merge.md` (Status: done) + `docs/archive/done/2026-06.md`. — Before it: **FEAT-listening-history-import** (all 4 repos, 6 steps, 2026-06-23) — the owner's Spotify Extended Streaming History (GDPR export) imported to prod Postgres (4523 rows / 186h lifetime; coverage 80.6%→99.7% after the catalog wave drained), surfaced in the /profile 분석 버킷 as the new **임포트(평생)** primary source: lifetime play counts + listening time (count↔time toggle), top tracks/artists/albums, genre + decade distributions, and a per-year/"on this day" retrospective. shared_db V27 (`spotify_stream_history`, `v0.24.0`) → import script → coverage gate → ungated count/time endpoints → gated album/genre/era endpoints → front import source. PRs: shared_db #44/#45, backend #88/#89, front #196/#197/#198, workspace contracts/docs #425/#426/#428/#429/#430. Full history → `docs/archive/done/rfcs/FEAT-listening-history-import.md` (Status: done) + `docs/archive/done/2026-06.md`. — Before it: **FIX-analysis-artist-attribution** (myblog_backend #86) — the 분석 버킷 artist distribution now credits each artist individually instead of a single comma-joined `'A, B'` bucket that fragmented solo counts. **Track-primary attribution** (refined in **#87** after live review — `album_artists` alone under-counted features in a feature-heavy library): `track_artists` first (captures track-level features, never surfaces "Various Artists") → `album_artists` (non-VA) → split denormalized `artist_name`; played stays `album_artists` (album-grain). Pure helpers extracted to `distribution.py` (unit-tested); values-only, **no contract change** (openapi zero-diff). Prod-smoked 2026-06-22: per-artist de-fragmentation + features counted (식케이 23→31, 창모 13→24; distinct artists 601→760), **no "Various Artists"**, `unclassified 0`. + **FEAT-analysis-source-clarity** (myblog_front #194) — 좋아요(intent) vs 재생(behavior) made legible in the 분석 버킷: a source explainer (meaning + denominator liked-N/played-N + horizon "첫 배포일 이후"), server-whole ("전체 집계") vs client-loaded ("좋아요 N곡") population captions, and a ≤1000-row ceiling note; **#195** then raised the artist chart cap 8→15 with a "상위 N · 전체 M" caption. Front-only (`DistributionResponse` already carries `total`/`last_synced_at`); prod-smoked (new strings live in the served bundle; CDP click-through pre-merge). The lifetime-import follow-on (`FEAT-listening-history-import`) was **accepted 2026-06-22** and is now **Active** (see below — Step 1 shipped). Then **CHORE-secrets-ssm-migration** — all secrets moved Secrets Manager → SSM Parameter Store SecureString (dual-source loader, reversible env-flip cutover); **Secrets Manager fully emptied → storage bill ~$3.20/mo → ~$0/mo**. DONE + prod-validated 2026-06-22 (#85/#46/#50/#51/#414/#416/#418/#420): all Lambdas + scripts read SSM (smoke 28/0, spotify write-back to SSM confirmed, zero fallback, `terraform plan` clean); 8 SM secrets deleted (30-day recovery window); empty/dormant `myblog/anthropic` deleted too. NB `claude_aws_manager` inline policy `claude-ssm-myblog` must stay (local pollers/scripts read SSM as that user); FEAT-ai-editorial-critique should provision anthropic in SSM (RFC Step 6), not SM. Full writeup → `docs/archive/done/rfcs/CHORE-secrets-ssm-migration.md`. Then **FEAT-liked-tracks-workbench** v1 — 분석 버킷 redesigned to a "좋아요한 트랙(Liked Tracks) workbench" (hero + analysis [genre/artist + 좋아요/재생 toggle, decade, likes-flow] + facets + search/sort + list/card table + 평론 버킷에 담기); Steps 1–3 live + prod-smoked 2026-06-21 (backend #82 `album.genres` populate, front #187 workbench, **N+1 hotfix #83**); **Step 4 (duration) SHIPPED 2026-06-22** (shared_db #42 + tag v0.23.0 + V26 prod-applied/backfilled 1017 rows, worker #49, backend #84, contract #410, front #192 — 길이 column + sort, prod CDP-smoked), **Step 5 (unlike) dropped**. Then **FEAT-genre-artist-distribution** (분석 버킷) + its post-merge hardening + the **FEAT-genre-autoheal on-demand 장르 채우기** button (merged into 분류하기) + the **FIX-uiux-audit-2026-06-21** front fix batch (all DONE + archived 2026-06-21), preceded by **FEAT-editor-buckit** (2026-06-18) and the 2026-06-15/16 batch — `home-redesign-v2`, the `ux-polish` sweep, `global-search`, `artist-page`, `genre-subgenres`/`-deepen`, `track-play-history`, and the `genre-autoheal` local fix. All 2026-06 completions are written up in `docs/archive/done/2026-06.md`; `git log` is authoritative.

---

## Backlog

> ✅ **Unblocked 2026-06-06** — STAB-1 P0 auth boundary + SQS fix + section model all shipped. These are scope-ready but still need owner go + RFC accept before promotion to Active.

- **FEAT-ai-editorial-critique** (draft) — **AI editorial critic, Phase 0 thin slice** (the product's defining feature — see 2026-06-06 whole-project review, memory `project-ai-editorial-greenfield`). LLM service layer + `POST /api/editorial/critique` that runs `docs/editorial/review-critique-method.md` over a draft + its already-linked album DB facts (`post_albums`, no schema change), returning grounded Korean feedback + `[ ]`/`[source?]` markers + §4 checklist — reconstruction/feedback only, never rewrites. UI-less; proven by curl + a golden-draft eval (critic must not hallucinate) before any frontend. Backend-only, 2 steps (Step 1 local + golden eval → Step 2 infra JWT route + contract). RFC → `docs/rfcs/FEAT-ai-editorial-critique.md`. **Caveats baked in: token-ceiling cost guard + manual-fire only ($0-bias owner); fact-check is narrow-not-deterministic (most honesty-line facts live outside the DB → `[source?]`); prompt single-source vs drift (OQ1).** Fact-check pass, `/write` sidebar, persistence, planner, publish-gate = later RFCs (non-goals). Status draft — needs owner accept before Active. **Open thread (from FEAT-editor-buckit, 2026-06-19):** on promotion, fold in the explicit 2-stage editorial pipeline (nightly full-draft → critique pass) + the research-grounded, tool-agnostic critique method §3.5 (`docs/editorial/review-critique-method.md` v2.6).
- **FEAT-genre-recommendation** (Frozen — gated on review volume) — the "**장르 기준 추천 평론**": related-review recommendation split out of FEAT-genre-subgenres (old Step 5, rescoped 2026-06-15). A "related reviews" block on `/review/{slug}` — reviews sharing a `post_genres` sub-genre + edge-linked sub-genres, ranked (shared-tag → edge-distance → recency). Substrate is LIVE + much richer post-FEAT-genre-deepen (genre_edges **2,064** + **1,230** genres / 4 tiers + `post_genres` filled by the `/write` picker); **deferred because nothing to rank until reviews are tagged (~1 today)** — same review-volume precondition as FEAT-ai-editorial-critique. Owner edge authoring (old Step 6, `POST/DELETE /api/genres/{id}/edges`) folded in here, lowest priority (edges are Claude-Code-driven). NB the genre-map *viz* (old Step 4) was revived into FEAT-genre-subgenres (review-volume-independent). RFC → `docs/rfcs/FEAT-genre-recommendation.md`.
- **FEAT-blog-to-review-migration** — Step 1 (the `/blog → /review` prefix migration) **DONE + prod-live 2026-06-14** → `docs/archive/done/2026-06.md`. Only optional **Step 2** remains: a true HTTP 301 via the console-managed CloudFront Function `handler` (owner-applied infra; the static stubs already guarantee no-404, so 301 is SEO-only — add if inbound `/blog` links accumulate). RFC stays open for that → `docs/rfcs/FEAT-blog-to-review-migration.md`.
- **FEAT-bucket-identity** (Direction A DONE 2026-06-16 → `docs/archive/done/2026-06.md`; B/C remain) — strengthen "bucket" as the core workflow unit (save → research → listen → notes → review → link back). **B (next, front-mostly)**: replace the name-regex `crMeta` status with one **derived from real fields** (`research_status`/`already_reviewed`/`post_id`/`is_done`) so it stops lying on rename, join listened-state onto covers ("이미 들음 → 평론 가능"), add the reverse link review→bucket/research, maybe promote the board to the `/profile` landing tab. **C (deferred, L)**: merge `들을 것` (`album_to_listen_items`) into the bucket model = one save target. **D (not started)**: surface buckets as flexible PUBLIC collections (genre/artist/playlist 모음) on dedicated surfaces (from the home-redesign-v2 deferral). RFC → `docs/rfcs/FEAT-bucket-identity.md`.
- **FEAT-release-calendar** (draft — brainstormed 2026-07-06, absorbed the parallel feed draft 2026-07-07) — two public surfaces. **Track A: home "새 앨범" card** — albums released ≤30d by high-pop catalog artists, **DB-only off the existing daily `album_ingest`** (probed: 120 releases ≤30d in prod incl. pop-95 artists; no new table/job/schema/infra); 2 cheap steps (musicApi `GET /api/music/feed/new-releases` + contract → home card), ships first. **Track B: 신보 캘린더** — announced (예정) + released (발매) albums/EPs/singles for a catalog-derived artist watchlist (prod: 4,044 valid-MBID artists; 1,518 at popularity≥50). Discovery = per-artist **MusicBrainz future-date queries** (Spotify `/browse/new-releases` removed Feb 2026 + content stale; global MB sweep rejected — long-tail); day-0 confirmation = **extend `album_ingest`** (already diffs discographies daily, pop≥60 `include_groups="album"` — floor/type gaps are OQ5), not a new worker job; calendar read DB-only (rule #9). v1 has no user concept — 공표 = calendar visibility only; per-user subscriptions + 확인 state deferred to a follow-on RFC after `FEAT-multi-user-accounts` Phase 0. Calendar all release types / card albums-only (owner, surface-specific); singles-noise re-check gate after 14 days of live data. Absorbs Frozen `FEAT-new-release-feed` + the parallel draft RFC (6a886a8, branch discarded). RFC → `docs/rfcs/FEAT-release-calendar.md`. Needs owner accept before Active.
- **ARCH-entity-interaction-unify** (draft — 2026-07-08) — one entity → one interaction, everywhere. Extends the done `ARCH-entity-interaction-contract`: **artist** stays route `/artist/[id]`; **album** → a single **app-wide overlay window** (mounted in `layout.astro`, opened by an `ent:open-album` CustomEvent) on public + member surfaces alike, **removing the load-bearing vanilla-vs-React album-detail duplication** (`scripts/albumDetail.client.ts` + `sessionCache` render path deleted, converges on `lib/albumDetail.ts`); **track** → opens the album window for its album (play/add stay reserved). `myblog_front`-only, no backend/contract/infra. 4 steps (global host + `entityLinks` dispatch → public review rewire → track-click → component-map update). **Crux/risk (OQ1): `AlbumDetail` is member-coupled (`ProfileApp`)** — architect pass at Step 1. Consumed by FEAT-today-buckit. Scope: front. Verify: lint/astro check + real-browser album/track/artist click on public + member surfaces; prod smoke per step. Rollback: revert (host additive until a surface dispatches). RFC → `docs/rfcs/ARCH-entity-interaction-unify.md`. Needs owner accept before Active.
- **FEAT-today-buckit** (**in-progress — Step 1 shipped 2026-07-08**) — two separate **buckit**-styled home sections (`EditorialHome`): **`TodaySongBuckit` (오늘의 곡)** = a 100%-manual owner pick shown **only on posted days** (empty otherwise, no rotation) with a stored, browsable **history**, identity-only tile (**no written impression** — owner); **`TodayAlbumBuckit` (오늘, 이 앨범들)** = albums whose `release_date` month/day == today across past years ("N년 전"), DB-only. **Distinct from `FEAT-release-calendar` Track A** (that = ≤30d new releases; this = same-date anniversaries). Depends on ARCH-entity-interaction-unify for clicks. Scope: music (on-this-day GET) + shared_db (`daily_picks` table/migration) + backend (owner `require_owner` pick store: public GET + history + PUT/DELETE) + infra (apigateway routes for the writes; public GETs ride edge_guard) + front (2 tiles + owner posting control + history). 6 steps (music GET → front album tile → migration → backend store → infra apply → song tile+post UI+history); album track (Steps 1–2) ships first. Static-build ⇒ both tiles are runtime fetches (home's first). Verify: pytest per service + prod migration apply + real-browser click-through + prod smoke per step. Rollback: additive throughout (revert PRs; store rows harmless). **Step 1 DONE** (music #52 `GET /api/music/albums/on-this-day`, deploy 28880892242, prod smoke 8/8 — 07/07 slice, years_ago/dedup/no-current-year verified; contract merged ws). NEXT = Step 2 front `TodayAlbumBuckit` on the home (needs ARCH-entity-interaction-unify for album/artist click, or interim wiring). RFC → `docs/rfcs/FEAT-today-buckit.md`.
---

## Later (트리거 대기)

_(empty — FEAT-genre-taxonomy discarded 2026-06-12, superseded by FEAT-genre-system, done + archived 2026-06-13)_

---

## Frozen (아이디어만, RFC 없음)

확장 기능 후보. 의향 생기면 그때 RFC 시작. 지금 아무것도 안 함.

- **FEAT-genre-autoheal — cloud heal** (**only the cloud variant remains deferred**) — full laptop-independence for the daily genre backfill via a cloud iTunes-baseline heal, hung behind the album-ingest batch (worker EventBridge); the local launchd LLM pass stays the low→high / K-Pop-arbitration upgrade. **Blocker**: the S3 LLM step shells `claude -p` on the Max-subscription token (absent in Lambda) → a cloud pass would be iTunes-only + must use a DISTINCT partial marker so it never excludes the LLM pass. The **local** daily fix is DONE + LIVE (now **04:00**). **2026-06-21: the *on-demand* variant shipped** — a 분석 버킷 web button (folded into `분류하기`) → `genre_backfill_requests` queue (shared_db V25) → a 5-min local genre-heal poller runs the existing backfill (`claude -p` stays local; pull not push). Only the **cloud iTunes-only** pass is still open. → `docs/archive/done/2026-06.md`. Memory `reference-genre-backfill-stale-decisions-replay`, `reference-sqs-fanout-burst-pitfalls`.
- **Per-genre exemplar albums** — start inside `definition_md` markdown; structured linking only if a consumer needs it.
