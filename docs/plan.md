# Plan

Active workspace tracker for cross-repo work. Each row carries `Scope / Order (if multi-repo) / Verification (local + prod smoke) / Rollback / Status`; RFC-backed entries collapse to a one-line pointer at `docs/rfcs/<id>-<slug>.md`. When a row finishes, drop it — `git log` and `docs/archive/done/YYYY-MM.md` are authoritative for history.

---

## Active

- **FEAT-multi-user-accounts** (in-progress since 2026-07-07) — multi-user platform: Google+Kakao signup, RYM-style public album reviews, per-user buckets, Last.fm/Spotify member listening, `LLMEngine`. **Phases 0–4 + P3b/3c + library user-scope + 07-14 surface-audit remediation + profile-merge PR1–3 ALL SHIPPED & prod-verified** (shipped-phase digest → `docs/archive/done/2026-07.md`; full phase log + decisions → RFC). **Remaining (all owner-only): launch gates G1/G2** (Google brand verification + Kakao 프로덕션 심사 + OAuth secret reissue; G1 = ≥10 reviews by ≥5 non-owner users in 4wk after public launch; **owner deferred 2026-07-20, no timeline — keep listed, don't re-prompt**), owner live-login returnTo observation, and the deferred canonical-link decision (runtime `/members/?u=` vs static `/members/[handle]` — necessity-gated while prod `/api/members` is empty). → `docs/rfcs/FEAT-multi-user-accounts.md` (Status: in-progress).

- **FEAT-release-calendar** (done 2026-07-23; RFC archived) — public new-release surfaces (home 새 앨범 card + `/releases/` 신보 캘린더, multi-source MB+iTunes poller + day-0 confirm). **Steps 1–7 ALL SHIPPED + prod-smoked 2026-07-11→13** (digest → `docs/archive/done/2026-07.md`). Owner promoted accepted→done 2026-07-23; data-quality follow-on spun out to **DATA-release-noise** below. RFC → `docs/archive/done/rfcs/FEAT-release-calendar.md`.
- **DATA-release-noise** (P1 — from FEAT-release-calendar OQ4 + 2026-07-23 감사 P-7) — **Step (b) read-side filter SHIPPED + prod-smoked 2026-07-24** (myblog_music #60). Fresh 07-24 재측정으로 감사치 확인·악화 발견: 홈 "새 앨범" 30일 후보 풀 94건 중 54건(57%)·`/releases/` 7월 173건 중 ~36건(~21%)이 클래식 컴필("065 Piano Essentials"류, 죽은 작곡가 귀속); 노이즈 100% Spotify `album_ingest` 경로 유입. 오너 선택 = (b). 공용 술어 `is_compilation_noise`(아티스트 ≥10 ∪ 순수컴필 레이블 ∪ 컴필 제목패턴, `settings` 튜닝, read-side·가역) → feed_service + release_calendar_service. 오탐 안전: 진짜 클래식 연주 6건(모차르트 레퀴엠 n=8/DG, Argerich n=3, Orff n=8/Pentatone 등) 전부 통과(순진한 n≥6은 3건 오탐). **Prod 실측: 홈 컴필 7/12→0/12, `/releases/` ~36→1**(잔여 1 = MB-announced "The Art of Itzhak Perlman"/Vivaldi 귀속, 확정앨범 없어 제목신호만 적용 → 오너 A 결정으로 향후 (a)에서 흡수). `no_upc` sentinel 재점검 ✓ 건강(805건 전부 watchlist pop≥50, 매 사이클 무료 재확인, iTunes-소스 스킵은 설계대로 — 노이즈 요인 아님). **남은 것**: (a) ingest-side 필터/소스신뢰가중 + (c) exact-dup dedup — (b) prod 결과로 근거 확보됨, 착수 여부 ~07-27 오너 결정. Deps: PERF-home-feed-latency(원인 공유 — (a) 시 N+1도 흡수).
- **FEAT-personal-release-tracking** (done 2026-07-18; RFC archived 2026-07-21) — personal release feed (tracked artists + Buckit snapshot import; Upcoming/Recent 30d × Albums&EPs/Singles; 3 trust badges) on #548's `artist_release_events`. **All 5 steps SHIPPED + prod-smoked** (Steps 1–3 07-17: V47 `user_artist_tracks` shared_db #66 + backend #120/#121/#122 + ws #632 + front #282; Steps 4–5 07-18: worker #73 poller-scope + front #285 `/radar`; post-merge hardening music #56 + front #286) — digest → `docs/archive/done/2026-07.md`. **Remaining observation gate: H1 re-measure ~07-27** (does not block closeout). RFC done + archived → `docs/archive/done/rfcs/FEAT-personal-release-tracking.md`.

- **FIX-lyrics-primary-artist + FIX-isrc-backfill** (2026-07-26 — `DATA-catalog-noise-and-lyrics-coverage` "Related defects" #1·#2, 오너 지시로 병렬 처리) — RFC가 "steps 아님, plan.md 행으로" 남겨둔 선행 결함 2건. **코드는 전부 머지·배포됨; 남은 것은 오너 액션 1건 + 관찰 2건.**
  - **#1 알파벳순 검색 아티스트** (worker #80 + ws #698): LRCLIB 검색어가 `artist_names[0]`인데 `ARRAY_AGG(DISTINCT a.name)`이 알파벳 정렬이라 피처링 게스트가 검색 아티스트가 됐음. `track_artists.role`은 44,568행 전부 NULL, Spotify 크레딧 순서도 미저장 → **앨범 아티스트 우선 → `artists.popularity` → 이름** 순으로 LATERAL 유도(Postgres가 `ARRAY_AGG(DISTINCT x ORDER BY y)`를 거부해서 LATERAL이 필수). 제목의 feature 절로 만든 **1,793곡 ground truth 기준 정확도 45.34% → 96.77%** (not_found 실제 분포로 재가중 시 52.45% → 88.26%; 인기도만 쓰면 70.83% — 게스트가 더 유명한 경우가 29.17%라서 짐). `decide_match`는 `artist_names`를 순서 없는 집합으로 쓰므로 **검색 단계 전용 변경**(기존 행 재작성 없음). Twin sweep 8곳(worker 2 + `tools/` 6, 상수 1개 공유 import).
  - **#2 isrc_backfill 미스케줄** (worker #81·#82 + ws #698): 서비스·핸들러·테스트는 있는데 EventBridge 규칙이 없어 한 번도 안 돌았고(그래서 `tracks.isrc` 26,619행 전부 NULL), 게다가 미스를 문자열 `"not_found"`/`"no_isrc"`로 **컬럼에** 써서 스케줄 거는 순간 모든 `isrc IS NOT NULL`이 깨질 상태였음. 마커를 `ext_refs.isrc_status`로 이동(`ext_refs.isrc` 키는 진짜 ISRC이자 `backfill_genres.py`의 "이미 조회함" 술어라 재사용 금지). 리뷰 중 **Spotify Track Relinking** 결함 추가 발견 — market을 넘기면 응답 `id`가 요청 `id`와 달라져 살아있는 트랙을 영구 미스로 찍었을 것(마커가 풀에서 빼버리므로 복구 불가) → `linked_from.id` 색인으로 방어(**주의: `market=None`을 넘겨도 `SpotifyClient._default_market`가 `SPOTIFY_DEFAULT_MARKET="KR"`로 덮으므로 market은 항상 전송됨 — 방어는 오직 이 색인**). 함께 정렬/세션수명/예산/WARNING 로깅도 형제 잡 기준에 맞춤.
  - **#2에서 배운 것 — 테스트가 거짓 녹색이었음**: #81의 PR CI는 통과했지만 `tests/integration/test_isrc_backfill_db.py` **4개 전부 skip**(Neon 테스트 브랜치에 `tracks.isrc` 없음 → V34 가드 발동)이었고, 신규 단위 테스트 11개는 `FakeSession`이라 파라미터 바인딩을 하지 않음. 그래서 `jsonb_build_object('isrc_status', :status)`의 `IndeterminateDatatype`(첫 miss에서 잡이 죽는 버그)이 CI를 그대로 통과했음. → `CAST(:status AS text)` + **DB 없이 도는 백스톱 테스트**(방출된 SQL을 파싱해 `jsonb_build_object` 안의 미캐스팅 파라미터를 잡음, mutation-check로 옛 형태에서 실패하는 것 확인). **교훈: 이 저장소에서 "worker PR CI green"은 DB 경로를 검증했다는 뜻이 아님** — [[reference-pr-ci-matrix]].
  - **#2 후속 (#82) — ISRC 포맷 정규화**: 실제로 잡을 돌려보고 발견. Spotify가 포맷을 일관되게 안 줌 → 첫 500건 런이 `GB-SMU-41-19063`(하이픈 표기)을 컬럼에 그대로 씀. 혼재 포맷은 `WHERE isrc = :code`가 조용히 안 맞는, 방금 고친 센티널과 같은 부류의 결함. `normalize_isrc`로 구분자 제거+대문자+검증(2 alpha+3 alnum+7 digit), 실패값은 컬럼 오염 대신 `no_isrc` 미스 처리. prod 코퍼스 실측: 정상 7,799 / 소문자 32 / 하이픈 2 / 구제불가 1(문자열 `"-"`).
  - **적용 완료 (prod)**: 데이터 리페어 1트랜잭션(드라이런으로 동일 카운트 확인 후 커밋) — 컬럼 비정규값 4건 정규화, `ext_refs`에서 **7,334건 무료 시딩**(Spotify 호출 0), 구제불가 `'-'` 키 1건 제거. 배포 코드로 600건 추가 실행. **결과: `tracks.isrc` 100% NULL → 7,932건(29.4%), 비정규 0, 문자열 센티널 0, 잔여 18,685건.** prod smoke 19/0.
  - **스케줄 적용 + 백로그 소진 완료 (2026-07-26)**: 오너 승인으로 `terraform apply` 실행(3 add/0/0, 드리프트 없음) → `worker-isrc-backfill` 생성, 스케줄대로 1회 발화 확인(FailedInvocations 0, Lambda 에러 0, DLQ 0). **백로그는 주기를 올리는 대신 수동 소진** — 실측상 500건=4.9초, 3,000건=41초(90초 예산의 절반 이하)라 `limit=3000`으로 6회 호출, **약 4분에 완료**(37시간 예상이었음). 주기 상향은 소진 후 되돌려야 하는 영구 설정 변경이고 `limit`을 EventBridge 입력에 넣으면 `settings`와 이중 관리가 되므로 택하지 않음. **최종: `tracks.isrc` 0% → 26,594/26,619 = 99.9%**, 미스 25건(Spotify에 실제로 ISRC 없는 곡), 비정규 0, 문자열 센티널 0, 백로그 0. 소진 후 규칙은 **주 1회(`cron(0 21 ? * SUN *)`)로 하향** — 기존 Spotify 잡 시퀀스(18/19/20시)와 1시간 간격 확보.
  - **⚠️ ISRC가 드러낸 카탈로그 중복 (신규 근거, `DATA-catalog-noise` (c) exact-dup dedup에 직결)**: ISRC는 녹음 1개당 1개인데 **중복 코드 1,982개 / 묶인 행 4,571건 / 제거 가능 여분 2,589행(카탈로그의 9.7%)**, 최대 중복도 8. 표본을 보면 정체가 명확함 — 바흐 `Capriccio BWV 992`(DEF055909682) 한 녹음이 *Pastoral Dreams / Spring Scene / Summer Promenade / Sunrise Prelude / Classical Horizons / Summer Sunsets / Golden Melodies* **7개 컴필에 중복 적재**, 쇼팽 왈츠(FRUM72201082) 7개, 베토벤 독일무곡(NLA507712213) 8개. 전부 2026-06~07 생성된 클래식 벌크 컴필. **즉 ISRC 오류가 아니라, 이미 알고 있던 컴필 노이즈를 ISRC가 결정적 키로 증명해준 것.** 지금까지 dedup에 쓸 신뢰 키가 없었는데(제목/아티스트 휴리스틱뿐) **이제 exact-match 키가 생겼다** — (c) 착수 시 판정 기준으로 바로 사용 가능. 같은 뿌리로 대표-아티스트 규칙의 정확도 상한도 여기서 막힘(같은 곡의 두 행이 `album_artists`가 서로 달라 대표가 갈림).
  - **#1 prod 인과 검증 완료**: 배포 후 `lyrics_reassessment`(limit 40) 수동 실행 → `not_found` 14,034 → 14,029, **5건 회수(적중률 12.5%)**. RFC가 측정한 기존 적중률은 1.54%. 회수된 5건 **전부 multi-credit이고 전부 알파벳 첫 크레딧이 피처링 게스트**였음 — `No Love (with SZA)`→Summer Walker, `Bus Stop (feat. Brent Faiyaz)`→Don Toliver, `Amazing`→UNEDUCATED KID, `Cloud of Unknowing`→Gorillaz, `소문내 리믹스`→레디/오케이션. 즉 우연이 아니라 예측한 메커니즘 그대로. **단 n=40은 비율 추정치가 아님** — 실제 수치는 스케줄 로테이션에서 나옴.
  - **관찰**: (a) ~~isrc 백로그 소진~~ **DONE 2026-07-26** (위 참조). 남은 관찰은 주간 슬롯이 신규 유입분을 실제로 집어가는지 1회 확인(첫 예정 발화 = 일 21:00 UTC). (b) `not_found` 회수율 — 실제 검색어가 바뀌는 행은 prod 실측 **5,412건 / 14,034건 = 38.6%**(multi-credit not_found 8,767건 기준 절반 이상). 회수가 크면 `DATA-catalog-noise` Step 3(가사 풀 위생)의 기대값을 다시 계산해야 함.
  - **별건 리스크 (이번에 드러남)**: Neon **테스트 브랜치가 prod 스키마에 뒤처져 있음** — `track_lyrics` 없음(FEAT-lyrics-corpus 마이그레이션 미적용), `tracks.isrc`도 최근까지 없었음. 그래서 **가사 도메인 전체가 CI에서 integration 커버리지 0**이고, isrc 쓰기 경로도 이번에 거짓 녹색을 냈음. 신규 `test_lyrics_primary_artist_db.py`는 이 때문에 2단으로 나눠 1단(공유 SQL 상수 직접 검증)만 지금 돌고, 2단은 브랜치가 따라잡으면 자동으로 켜짐. **테스트 브랜치 마이그레이션 동기화는 별도 작업으로 잡을 것.**
  - **후속 후보 2건 (이번 범위 아님, 근거만 확보)**:
    - **규칙 없이 고치는 방법이 존재함.** `decide_match`는 이미 크레딧 *전체* + aliases로 후보를 검증하므로(`identity_norms`, 순서 무관), 대표를 맞히는 대신 **크레딧마다 LRCLIB를 한 번씩 조회해 합집합**을 취하면 구성상 100% 정확해지고 잔여 오차 ~3%가 사라짐. 이번에 안 한 이유는 비용: LRCLIB 실효 상한이 ~2.5 req/s인데 multi-credit이 11,016곡이라 요청량이 2배 이상 됨. 풀 위생(Step 3) 이후 재평가할 것.
    - **중복 트랙 행의 `album_artists` 불일치** — 같은 곡이 여러 앨범으로 중복 적재되면서 한쪽에만 게스트가 album_artist로 붙어 있음(`is it cool? (feat. SZA)`, `POPSTAR (feat. Drake)`, 모차르트 트랙 4중복 확인). 즉 앨범-아티스트 신호 자체가 self-consistent하지 않아 **어떤 앨범-아티스트 규칙이든 정확도 상한이 여기서 막힘**. `DATA-catalog-noise` (c) exact-dup dedup과 같은 뿌리.

_2026-07-18 UI/UX planning session (areas 1–6, 9, 10 investigated; owner decisions folded into the rows below — all since promoted to in-progress/shipped; areas 7·8·11·12 deferred to a follow-up measurement/research session):_

- **BUG-artist-image-backfill** (done 2026-07-19) — **all 3 steps SHIPPED + prod-smoked** (worker #74/#75 enrich+sentinel+one-shot: still_null 463→0, sentinel 460; ws #647 weekly EventBridge sweep `cron(0 20 ? * SUN *)`, applied + manual-invoke no-op verified). **Remaining (non-blocking): observation gate** — weekly `still_null ≈ 0` (denominator = all `artists` rows; first scheduled run Sun 20:00 UTC). OQ1 RESOLVED 2026-07-19: owner confirmed **Extended Quota Mode** (also unblocks FEAT-member-player rollout sizing). RFC done + archived → `docs/archive/done/rfcs/BUG-artist-image-backfill.md`.
- **FEAT-member-player** (in-progress — **Steps 1–4 SHIPPED + prod-smoked** (1–3: 2026-07-19, 4: 2026-07-20): Step 1 backend #124 + front #287 + ws #644; Step 2 backend #125 + #126 follow-up (per-member token mint, member-without-connection → 404 per #126); Step 3 front #293 — hairline-LCD player bar across banner/full/list: Connect remote play/pause/seek (`sendPlayerCommand`, 403-probe degrade D5), fallback clock-estimate bar (`@lib/clockEstimate` shared with lyrics), 502 inline reconnect line, owner gate removed, ui-unify artists/album id plumb via `@lib/spotifyCatalog`; CDP 33 asserts + mobile 390 + owner real-device clickthrough PASS; **Step 4 front #297 — Connect-style "Listening on <device>" bottom-edge hint, full/banner only, zero extra requests (device rides the existing one-shot; D28 by construction); CDP 18/18 + mobile 390 PASS, **owner active-device line check CONFIRMED 2026-07-20** — Step 4 fully closed) — rebuild '지금 듣는 음악' internals as a real player bar, **capability-tiered (D1, repeals D11; D28 no-polling upheld)**: remaining scope = Step 5 in-page SDK device (opt-in output, `streaming` scope) + **Step 6 candidate menu recorded 2026-07-20 on owner request** (next/prev · play specific track/album · queue · 좋아요 Liked Songs [sole re-consent item] · shuffle/repeat/volume/transfer — owner selects the subset at promotion) + **Step 6 subset (6a next/prev · 6b play album/track · 6d 좋아요) + Step 7a 연동-tab capability guide SHIPPED + prod-smoked 2026-07-21** (front #302; 6b bucket-row entry rode front #303 per the BucketBoard file-ownership split; CDP 37/37 + 6/6, deploy 29783059420, owner real-device checklist pending in #302): remaining scope = Step 5 in-page SDK device + candidates 6c/6e + 7b/7c, all rule-4 gated. → `docs/rfcs/FEAT-member-player.md`.
- **SEO-review-structured-data** (done 2026-07-19; RFC archived 2026-07-21) — area 10: `Review` + `MusicAlbum` JSON-LD on `/review/[slug]`, build-time only (OG-image autogeneration deferred). **Step 1 SHIPPED + prod-deployed 2026-07-19** (front #288: `review-jsonld.astro` behind `isReview`), verified via local MDX fixture since the content collection is empty on `main` — validator.schema.org 0 errors/0 warnings; inert in prod until the first review publish. **Remaining observation gate: OQ3 — Rich Results Test on the first live review URL after first publish** (code-snippet RRT needs Google sign-in; no live review URLs yet). RFC done + archived → `docs/archive/done/rfcs/SEO-review-structured-data.md`.

_Shipped history lives in `git log` + `docs/archive/done/` (per-month digests + archived RFCs) — plan.md carries open work only._

---

## 2026-07-23 감사 파생 작업 (P0/P1/P2)

> `docs/reviews/2026-07-23-current-state-audit.md`에서 검증·수락된 항목만. 근본 원인은 "발행 평론 0" (콘텐츠 생성이 구속 제약) — 아래는 콘텐츠와 무관하게 오늘 실害이 있거나 안전망 공백인 것 위주.
>
> **오너 결정 2026-07-23:** (1) 우선순위 = **안전망·품질 먼저** → OPS·REFACTOR RFC가 활성 우선, discovery는 P2 유지. (2) 제품 정체성 = **비평지 프레이밍 유지**(첫화면 재구성 안 함 → 감사 P-2/P-4는 오너 결정으로 종결, 액션 없음). (3) RFC 2건 **둘 다 accepted**. (4) 문서 불일치 **전체 정리 승인**(architecture.md·infra/README·RFC 상태 3건 — 이 세션에서 반영 완료).

**P0 — 현재 운영 리스크 / 무효 UX / 안전 게이트 공백 / 심각 성능**

- ~~**FIX-publish-github-timeout**~~ — **DONE + prod-smoked 2026-07-23** (backend #131, SHA 0cf1a6b): GitHub Contents API 호출 4곳(`:114,125,144,158`)에 `timeout=10` 추가(백엔드 컨벤션·Lambda 15s 아래). 크로스리포 sweep = 유일 offender 확인. 로컬 pytest 554 passed(+publish 32), prod smoke 19/0. 새 test 게이트를 처음 실제 통과한 PR.
- ~~**OPS-delivery-safety-gates Step 1**~~ — **DONE + prod-smoked 2026-07-23** (backend #130, SHA 4018f50): backend `pytest -m "not integration"`가 PR 차단 `test` 잡으로 배선(deploy: needs), `integration` 마커 pyproject.toml 등록, 6 integration 스위트 마킹, requirements-test.txt(pytest+boto3 — 게이트가 즉시 CI의 boto3 누락을 잡음). PR CI 554 passed/77 deselected, prod smoke 19/0. 전체 RFC 완료(아래 P1 행 참조). → `docs/archive/done/rfcs/OPS-delivery-safety-gates.md`.
- ~~**PERF-home-feed-latency**~~ — **DONE + prod-applied 2026-07-23** (workspace #686, SHA 67ac5c2). **감사 리드(N+1/인덱스) 반증됨**: prod EXPLAIN ANALYZE로 new-releases 쿼리 0.55ms·on-this-day 0.94ms(albums 2,884행, `artists` 이미 selectinload) → 원인은 **musicApi Lambda 콜드 스타트**(엔드포인트 cold 7.21s→warm 0.73s; backend엔 warm-ping 있으나 musicApi엔 없었음). 수정 = `music-api-warm-ping` EventBridge 규칙(5분, direct-invoke `feed/new-releases?days=1&limit=1`, ~$0). `terraform apply` = 3 added/0/0, rule ENABLED, payload 검증 invoke 200. 홈 첫 music 요청이 이제 warm(~0.7s).

**P1 — 핵심 플로우 · 점진 유지보수성**

- **OPS-delivery-safety-gates** (done 2026-07-24; RFC archived) — **Steps 1–5 ALL SHIPPED**: PR-blocking pytest/check gates (backend #130 / worker #77 / front #306), 배포후 health-only smoke (Step 3: backend #132·music #59·worker #78+#79·front #313, 4개 배포 run smoke green, worker IAM `lambda:InvokeFunction` v2 + `function-updated-v2` waiter), 비동기 크론 실패 경계 (Step 4: `worker-cron-dlq` + worker `event_invoke_config` on_failure + `FailedInvocations` 알람 2→12, `terraform apply` 14 add/0/0 AWS-verified), 롤백 런북 (Step 5: `docs/ops/rollback.md`). RFC done + archived → `docs/archive/done/rfcs/OPS-delivery-safety-gates.md`. **⚠️ Step 4's "알람 2→12" no longer holds** — the alarm list is a hand-maintained map, a 13th worker cron shipped 07-26, and coverage silently reverted to 12-of-13. Superseded by **OPS-safety-net-drift** below; the DLQ half is unaffected.
- **DOC-accuracy-reconciliation** (**DONE 2026-07-23** — 이 세션) — architecture.md 갱신(EventBridge 1→13, 워커 shared_db import 정정, 핀 3개 실제값 + 드리프트 배너), infra/README(48 routes + 13 EventBridge), RFC 상태 3건 정리(ui-surface-unification 인덱스→done 브레드크럼, FEAT-release-calendar accepted→done 승격+archive, 노이즈 작업은 DATA-release-noise로 분리). 남은 minor: FEAT-bucket-identity가 plan.md Backlog에 있으나 RFC는 in-progress(D가 오너 go 대기 중이라 placement는 방어 가능 — 그대로 둠).

**P2 — 실사용자 증거 후 (조기 착수 비권장)**

- **CHORE-dep-reproducibility** — `myblog_backend`·`myblog_music`. lockfile 도입 + `fastapi`/`boto3`/`SQLAlchemy` 핀 + backend 비태그 SHA→태그(감사 C-10). 트리거: 핀-드리프트 배포 500 재발. 현재 additive 컨벤션 하 수용. **2026-07-26 audit adds evidence, not a new row**: `SQLAlchemy==2.*` and `psycopg[binary]==3.*` are open major-range pins, so each deploy re-resolves them — and querying a CVE database with the literal string `2.*` returns three CRITICAL SQLAlchemy 0.x/1.x advisories that do not apply (real installed version 2.0.50). The wildcards actively produce false security signal, which is a second argument for this row beyond reproducibility. Dependency *notification* is separate → OPS-safety-net-drift Step 4.
- **DISCOVERY-flow (가설, 기각-현시점)** — `signup→import→Buckit→publish→discovery`를 핵심으로 투자. 감사 §3: 발행 0·타 사용자 0·공개 컬렉션은 오너 자기것 1개·`/members/` 공백 → discovery 명목상. 실사용자+콘텐츠 증거 나올 때 재평가. **핵심을 무엇으로 볼지는 오너 전략 결정**.

---

## 2026-07-26 감사 파생 작업 (P0/P1/P2)

> Source: `docs/reviews/AUDIT-2026-07-26-system-audit.md` (merged ws #704, squash `9d4711c`). 21 findings; **all evidence stays in the report** — these rows are pointers, not summaries.
> **Nothing has been implemented.** The report's §7 owner decisions are all still open, including the ordering.
> ⚠️ **Read §9 first.** The report's own re-review corrected **14 of its claims** and added 5 findings. Acting on a pre-§9 statement will mislead you — D-1's root cause and DEP-2's applicability were both overturned.

**P0**

- **FIX-nightly-draft-delivery** (P0 — audit D-1) — the Editor Buckit nightly generates review drafts and **cannot deliver them**: `scripts/buckit_nightly.py:568` `mint_smoke_token()` authenticates as the smoke user, but `POST /api/posts` is `require_owner` → permanent **403 "Owner only"**. **This is a regression, not an unfinished feature** — delivery worked 2026-06-23 and 2026-07-05 and was broken by backend `392dd50` (#107, owner-gate, 2026-07-08); the next draft only appeared 07-25, so it sat invisible for 17 days. Hidden three ways: only a `WARNING`, classified as retryable when it is permanent (`:705-711`), and the process still exits 0. The 07-25 draft is still unpublished at `docs/buckit/2026-07-25/the-strokes-is-this-it.md`. **⚠️ Urgency raised by ws #705 (2026-07-26, prompt v0.4): the hold is gone — "every checked memo now gets a complete draft, zero drafts is never a valid outcome".** That also explains the quiet logs the audit misread as "nothing produced": the hold had fired on **13 of 14 completed runs**. So from the next 03:00 cycle this 403 stops being a twice-in-history event and fires **every night there is a checked memo** — each one writing a draft to `docs/buckit/` that cannot reach the blog. #705 touched only the generation path; the delivery/auth path is unchanged (verified). **Owner decision required before the shape is known** — (a) owner credentials to the pipeline (config only, no code, but the automation gains publish/delete rights), (b) a service identity `require_owner` accepts (cleanest; touches `app/core/auth.py` ⇒ same-PR twin sweep across backend+music ⇒ **earns its own RFC**), (c) relax the gate for `status:'draft'` creation only (narrow, but opens a public path). Verification needs a full 03:00 cycle. Also regresses `FEAT-editor-buckit` Stage 2 (RFC archived). → audit §4 D-1, §7-1.

**P1**

- **OPS-safety-net-drift** (P1 — audit OPS-1/SEC-1/SEC-2 + DEP-1) — four controls recorded as protecting us that don't, plus the mechanism that let a *closed* safety net go false. Steps are independent: (1) derive the worker-cron alarm map from the rule resources instead of a hand-maintained list — `worker-isrc-backfill` currently has no `FailedInvocations` alarm (12 alarms / 13 crons); (2) the CloudFront WAF has `RuleCount: 0` + default `Allow` while `infra/README.md:52` documents it as a control — attach managed rules or delete it honestly; (3) `ENV` defaults to `"local"` in **both** services and `local|dev` disables the auth guards entirely — the only missing-config path that fails **open** (auth ⇒ twin sweep, Claude directly); (4) Dependabot alerts are disabled on all six repos and no CI audit step exists. → `docs/rfcs/OPS-safety-net-drift.md` (Status: **draft** — needs owner accept before Active).
- **FIX-publish-frontmatter-yaml** (P1 — audit A-2) — an album title containing **both** `'` and `"` (e.g. `Taylor Swift's "1989"`) makes `publish_service.py:43-47`'s `repr()` emit invalid YAML; publish returns 200, then the next Astro build fails at `getCollection` and **the whole site deploy is blocked** until someone finds the file by hand. Reproduced against PyYAML; apostrophe-only and double-quote-only titles are fine. Fix = proper YAML serialisation, not `repr()`. Single-file change.
- **PERF-home-genre-payload** (P1 — audit E-1 + E-3, shared cause) — `/` fetches `/api/genres/tree` (**292 KB gzip / 742 KB decoded, no `cache-control`, ~1.3 s**) on every visit to render six bars, while the two sibling homepage endpoints both cache correctly. The largest unused chunk is the **2,064-entry `edges` array (~246 KB)**, which `/` never reads — *not* the markdown definitions (audit §9 C-6 corrected this). Same late-arriving sections give first-visit mobile **CLS 0.14–0.32**. Cheapest first move = add the `cache-control` header (one line, matches siblings); then a slim shape for `/` (tier-0 `slug`/`label`/`album_count` only). Fix site: `BrowseGenres.tsx:191` → `lib/genres.ts:78-86`. Contract change ⇒ cross-repo order applies.
- **FIX-worker-txn-across-http** (P1, latent — audit A-1) — `handler.py:179` opens `with SessionLocal() as session, session.begin():` and `sync_service.py:252` calls `enrich_artists()` — an outbound Spotify loop — inside it, while holding `ON CONFLICT DO UPDATE` row locks on artists/albums/tracks. A 403/404/410 on the batch `GET /v1/artists` fans out to up to 50 retried single GETs → Neon drops the idle-in-txn connection → whole batch rolls back → SQS redelivers → DLQ, and 10-way concurrent invocations block on the held locks. **Has never fired** (all queues + both DLQs at 0), so this is latent. The correct pattern already exists ~10 lines below in the same file (`run_artist_photo_backfill`). Needs one real-engine test — mocks miss pool semantics. Neighbouring: `album_artists` (`:192`) and `track_artists` (`:230`) insert unsorted pair lists while their three siblings in the same function sort explicitly (audit §9 C-9).

**P2 — batch (single PR each, no design question; detail in the report)**

- **A-3** malformed id → unauthenticated **500** instead of 404 on `/api/research/*` (no internals leaked); `_parse_album_id` guard already exists at `reviews.py:54`, just never propagated · **A-4** Today's Pick uses UTC `current_date` while the codebase's written convention is a KST day boundary (`release_feed.py:36-41`) → picks vanish 00:00–09:00 KST · **A-5** `infra/lambda.tf:83-88` sets `QUEUE_NAME` but not `SQS_QUEUE_URL`, so musicApi issues a blocking `sqs:GetQueueUrl` per `/candidates` call, outside the best-effort guard · **D-2** `LLMTransientError` is never retried (`cli_engine.py:131-146` retries only `LLMValidationError`) → one blip costs a whole night, no alert · **D-3** the "Notify workspace of openapi.json update" workflow is documented as dead yet still enabled, failing every run since 07-07 → trains us to ignore red CI · **E-2** `/collection/` bylines a raw Cognito-sub fragment (`@user-0468fd3c`) as a display name on a public page · **E-4** 8 homepage `button.nrl-open` fail WCAG 2.5.3 "Label in Name" (visible text not contained in `aria-label`) · **E-5** `/write/` **and `/drafts/`** render in full for any logged-in member including 발행 buttons — no data risk (backend fails closed) but the member does the work first; `isOwnerUser()` already exists and is used in 4 other components · **E-6** `.lf-artist-link` is emitted site-wide by the `AlbumOverlay` island but styled only in `member/layout.css` (loads on `/collection/` + `/settings/` only) → no hover affordance anywhere else; **4th recurrence** of the documented `member.css` trap, live-verified · **DEP-3** `@astrojs/node` declared but never used (`output: 'static'`, no adapter) → carries 6 non-applicable advisories forever; deleting it changes no behaviour · **DEP-2** `@astrojs/rss` 4.0.13 → 4.0.19, **hygiene only** — applicability overturned in re-review (the advisory's `source.title`/`enclosure.type` are never populated and the feed emits zero items).

**Decided, no action**

- **PUB-1** — the workspace and 4 service repos are **PUBLIC**; merging the audit published reproduction detail for still-unfixed defects. Owner chose to merge as-is (2026-07-26). Severity was overstated in the report: `package.json`/`pnpm-lock.yaml` are already public, so §8's version list added nothing an attacker could not derive. Residual exposure is A-3's URL and the E-5 surfaces — both above.

**Not covered by this audit** (so the clean verdicts aren't read as broader than they are) — `infra/` beyond apigateway/eventbridge/lambda/monitoring (IAM scope, S3/CloudFront policies, KMS, Cognito), all six CI workflow files, `tools/` and the non-buckit `scripts/`, frontend server-side code, PWA/service-worker config, escaping of member-supplied content at render sites, load/concurrency testing, DB-level data quality, Terraform state, and call-path tracing for the Python CVEs (`starlette` is the one in the request path). → audit §9.4.

---

## Backlog

> Scope-ready drafts/deferred work — each still needs an owner go (+ RFC accept where draft) before promotion to Active.

- **FEAT-ai-editorial-critique** (draft) — **AI editorial critic, Phase 0 thin slice** (the product's defining feature — see 2026-06-06 whole-project review, memory `project-ai-editorial-greenfield`). LLM service layer + `POST /api/editorial/critique` that runs `docs/editorial/review-critique-method.md` over a draft + its already-linked album DB facts (`post_albums`, no schema change), returning grounded Korean feedback + `[ ]`/`[source?]` markers + §4 checklist — reconstruction/feedback only, never rewrites. UI-less; proven by curl + a golden-draft eval (critic must not hallucinate) before any frontend. Backend-only, 2 steps (Step 1 local + golden eval → Step 2 infra JWT route + contract). RFC → `docs/rfcs/FEAT-ai-editorial-critique.md`. **Caveats baked in: token-ceiling cost guard + manual-fire only ($0-bias owner); fact-check is narrow-not-deterministic (most honesty-line facts live outside the DB → `[source?]`); prompt single-source vs drift (OQ1).** Fact-check pass, `/write` sidebar, persistence, planner, publish-gate = later RFCs (non-goals). Status draft — needs owner accept before Active. **Open thread (from FEAT-editor-buckit, 2026-06-19):** on promotion, fold in the explicit 2-stage editorial pipeline (nightly full-draft → critique pass) + the research-grounded, tool-agnostic critique method §3.5 (`docs/editorial/review-critique-method.md` v2.6). **Deferred 2026-07-20** — a promotion + Step 1 session was opened then postponed by the owner before any doc/code change; resume order: promote → fold-in above → re-audit the RFC's "Current state" → Step 1. NB that section is stale (written 2026-06-06): "zero AI/LLM code" no longer holds (the $0-mode research pipeline / local `claude -p` + the multi-user `LLMEngine` RFC exist), the route/auth surface changed post-multi-user (critique endpoint should be owner-gated via `require_owner`, not bare `require_cognito_token`), and its Secrets Manager references must become SSM (CHORE-secrets-ssm-migration).
- **FEAT-lyrics-annotations** (**accepted 2026-07-26** — Thread 1 cleared to build; Thread 2 still gated on the filter-shape decision, RFC §8) — two lyrics-domain threads. **All 2026-07-25 morning numbers were re-derived and most were wrong; RFC §9 lists what is superseded so nobody re-derives it.** **Thread 2 (lyrics coverage)**: the unresolved pool is 16,808 and **diverges** — re-check rate 146/day but actual **drain 2.25/day** (hit rate 1.54%), intake ~129/day median ⇒ **+180/day**, 96-day cycle stretching 1.2 d/day, and **10,536 of 14,057 tier-0 rows never re-checked once**. Raising the batch limit cannot work (break-even ≈60×; LRCLIB already 429s at concurrency 20). Structural defect: a new track gets **one** attempt 15 min after ingest, exactly when LRCLIB is emptiest, then falls into a stalest-first queue. Recency is a **32× signal** once classical is controlled for (≤60 d non-classical flips 26.7% vs 0.8% raw). Charli XCX MFF is now **8/8 available on LRCLIB** but sits 13,698 places back = 94 days. **Thread 1 (Genius)**: token issued 2026-07-25 and live-tested. Coverage splits by language, not genre — description ≥200 chars **7/8 EN vs 1/8 KR**, ≥3 annotated lines **7/8 vs 0/8**, but **credits 7/8 in both**; 47.4% of engagement is Korean, so prose-dependent features are empty for half the library and credit-based ones are not. Justification is stronger than the viewer case: **78/78 stored notes failed to confirm samples, 75/78 credits**, with WhoSampled named fetch-blocked in the notes themselves. Anchoring measured (`tools/genius_anchor.py`, LUX, 95 lyric-bound annotations): **74.7% resolve to one span, +20.0% `repeated` = 94.7% placeable, 5.3% unmatched**, median per-track placed 75.0% — clears the in-text-layer bar. An annotation spans a **range** (median 2 lines, max 12), not a line. R0–R6 plan with file:line anchors in RFC §6.5. Six-album KR/EN regression scheduled 2026-07-29. **R0 DONE 2026-07-27 (ws #712) — the gate passes, R1 is cleared to build.** `tools/genius_probe.py --full-run` over **every** bucketed track (1,073 tracks / 87 albums, no sampling): **track match 87.4%** (52/87 albums fully matched, only 4 with zero). Fill on matched tracks vs what the notes record as unobtainable — **credits 96.9%** (closes the 75/78 failure), **performers 79.5%**, description ≥200 chars 52.5%, **relationships 48.6%** (the 78/78 failure), recording location 30.1%. Three carry-forwards: (1) 48.6% is not a shortfall — many albums simply have no sample, and the win is that a *confirmed absence* replaces "확인 불가", which the note cannot express today; (2) relationships run **both ways** and the inbound edge is new — `sampled_in` 214 / `interpolated_by` 174 / `covered_by` 177 is what the album later became material for, which no note has ever carried and which feeds R6 directly; (3) description and recording location are thin, so **size R5's prose tier as a bonus, not load-bearing**. Per-language output exists (65.4% en / 31.9% ko of matched) but is deliberately **not** a gate — owner decision 2026-07-27, build/no-build called on the overall numbers. → `docs/rfcs/FEAT-lyrics-annotations.md` §6.5.
- **DATA-catalog-noise-and-lyrics-coverage** (draft — needs owner accept) — four independently-mergeable changes sharing one root cause: the catalog ingests bulk classical compilation noise and every downstream surface inherits it. **Step 1 (parallel) search ranking** — `tracks.views` is 0 on all 26,602 rows so the ORDER BY is dead and classical wins on volume; 45.9% → **15.1%** classical in track results by ordering on album popularity, zero classifier. **Step 2 (parallel) ingest choke point** — `album_ingest_service.py:43-50` `_SELECT_ELIGIBLE` is the single query both calendar-confirm and catalog-enqueue derive from; keys on **artist** genres (tracks do not exist yet at ingest) with **exact JSONB element match, never substring** (substring would have removed AC/DC, Led Zeppelin and the Rolling Stones via `클래식 록`). Absorbs **DATA-release-noise (a)**. **Step 3** lyrics pool hygiene (rule E, 0 false positives ∪ rule F, empirical label yield — self-correcting and genre-free) + recency re-order. **Step 4** album-scoped expedite. Every exclusion is a reversible mark plus a holdout that measures its own misclassification rate; **search is fixed by ranking, not filtering**, so classical stays findable. Disproves the catalog-cap hypothesis (never fired, 2,934/5,000, 0 log matches in 60 d). → `docs/rfcs/DATA-catalog-noise-and-lyrics-coverage.md`.
- **FEAT-genre-recommendation** (Frozen — gated on review volume) — the "**장르 기준 추천 평론**": related-review recommendation split out of FEAT-genre-subgenres (old Step 5, rescoped 2026-06-15). A "related reviews" block on `/review/{slug}` — reviews sharing a `post_genres` sub-genre + edge-linked sub-genres, ranked (shared-tag → edge-distance → recency). Substrate is LIVE + much richer post-FEAT-genre-deepen (genre_edges **2,064** + **1,230** genres / 4 tiers + `post_genres` filled by the `/write` picker); **deferred because nothing to rank until reviews are tagged (~1 today)** — same review-volume precondition as FEAT-ai-editorial-critique. Owner edge authoring (old Step 6, `POST/DELETE /api/genres/{id}/edges`) folded in here, lowest priority (edges are Claude-Code-driven). NB the genre-map *viz* (old Step 4) was revived into FEAT-genre-subgenres (review-volume-independent). RFC → `docs/rfcs/FEAT-genre-recommendation.md`.
- **FEAT-blog-to-review-migration** — Step 1 (the `/blog → /review` prefix migration) **DONE + prod-live 2026-06-14** → `docs/archive/done/2026-06.md`. Only optional **Step 2** remains: a true HTTP 301 via the console-managed CloudFront Function `handler` (owner-applied infra; the static stubs already guarantee no-404, so 301 is SEO-only — add if inbound `/blog` links accumulate). RFC stays open for that → `docs/rfcs/FEAT-blog-to-review-migration.md`.
- **CHORE-neuralwatt-credit-burn** (opt-in path SHIPPED 2026-07-20; **burn deferred — glm-5.2 parity gate FAILED**) — the genre-heal S3 LLM stage can now route to NeuralWatt glm-5.2 via `GENRE_HEAL_LLM=neuralwatt` (`scripts/neuralwatt_claude_shim.py`, claude-CLI-compatible PATH shim, stdlib urllib + explicit timeout, per-call usage/cost JSONL in `~/Library/Logs/myblog-neuralwatt-usage.jsonl`, fail-open to the real `claude` binary so the nightly can only degrade to status quo; plist untouched — `claude -p` stays default). 8-album QA sample (read-only, $0.066): run-to-run **instability on the K-Pop/idol axis** (identical input flipped idol=T→F on IU rows; that axis writes `confidence=high` DB rows) → parity gate failed, default NOT switched, 03:00 usage-limit-collision benefit unrealized. Pricing measured: $1.45/M in, $4.50/M out ≈ $0.14–0.19 per incremental nightly; **~$9.93 credit remains**. Fallback target (lyrics corpus) assessed — already complete, not worth it. OPEN: owner call on retrying with a hardened prompt (pin the idol-axis rubric) vs leaving opt-in only. Key stays in `.zshrc` (owner 2026-07-20 — don't re-raise).
- **FEAT-bucket-identity** (Direction A DONE 2026-06-16 → `docs/archive/done/2026-06.md`; B DONE 2026-07-21; C subsumed by pocket-buckit V31; D remains) — strengthen "bucket" as the core workflow unit (save → research → listen → notes → review → link back). **B SHIPPED + prod-smoked 2026-07-21 (front #303, front-only)**: name-regex `crMeta` replaced by a typed-field lifecycle (담음→조사 중→작성 중→완료, subtree-aggregated, rename-proof; urgency accent dropped — no typed deadline field), listened-state cover join client-side ("이미 들음 → 평론 가능" ring), review→bucket/research chips on ReviewsTab (`?tab=bucket`); CDP 18/18 at prod scale + 6/6 bucket-row play entry. **Owner-pending proposals**: board → `/profile` landing tab; research-note deep link (`?album=` param); in-place tab switch for the 버킷 chip. **C (deferred, L)**: merge `들을 것` (`album_to_listen_items`) into the bucket model = one save target. **D (not started)**: surface buckets as flexible PUBLIC collections (genre/artist/playlist 모음) on dedicated surfaces (from the home-redesign-v2 deferral). RFC → `docs/rfcs/FEAT-bucket-identity.md`.


---

## Later (트리거 대기)

_(empty — FEAT-genre-taxonomy discarded 2026-06-12, superseded by FEAT-genre-system, done + archived 2026-06-13)_

---

## Frozen (아이디어만, RFC 없음)

확장 기능 후보. 의향 생기면 그때 RFC 시작. 지금 아무것도 안 함.

- **FEAT-genre-autoheal — cloud heal** (**only the cloud variant remains deferred**) — full laptop-independence for the daily genre backfill via a cloud iTunes-baseline heal, hung behind the album-ingest batch (worker EventBridge); the local launchd LLM pass stays the low→high / K-Pop-arbitration upgrade. **Blocker**: the S3 LLM step shells `claude -p` on the Max-subscription token (absent in Lambda) → a cloud pass would be iTunes-only + must use a DISTINCT partial marker so it never excludes the LLM pass. The **local** daily fix is DONE + LIVE (now **04:00**). **2026-06-21: the *on-demand* variant shipped** — a 분석 버킷 web button (folded into `분류하기`) → `genre_backfill_requests` queue (shared_db V25) → a 5-min local genre-heal poller runs the existing backfill (`claude -p` stays local; pull not push). Only the **cloud iTunes-only** pass is still open. → `docs/archive/done/2026-06.md`. Memory `reference-genre-backfill-stale-decisions-replay`, `reference-sqs-fanout-burst-pitfalls`.
- **Per-genre exemplar albums** — start inside `definition_md` markdown; structured linking only if a consumer needs it.
