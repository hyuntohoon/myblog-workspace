# Plan

Active workspace tracker for cross-repo work. Each row carries `Scope / Order (if multi-repo) / Verification (local + prod smoke) / Rollback / Status`; RFC-backed entries collapse to a one-line pointer at `docs/rfcs/<id>-<slug>.md`. When a row finishes, drop it — `git log` and `docs/archive/done/YYYY-MM.md` are authoritative for history.

---

## Active

- **FEAT-member-dashboard** (accepted) — 회원 대시보드 (`/profile`). Auth-gated member page (개요·평론·평론 버킷·라이브러리·통계). RFC → `docs/rfcs/FEAT-member-dashboard.md`. Built in vertical slices. **Step 1 = frontend shell** shipped (#72): real reviews + derived stats, sample-labeled placeholders behind one adapter (`src/lib/member.ts`), nested bucket board on localStorage. **Step 2 = library tab data (to-listen + reviewed)** shipped — three-source design per RFC Decisions D18 (new `album_to_listen_items` table [shared_db v0.7.0, Neon V8] + reviewed derived view from `post_albums`; the 3rd source "최근 들은 앨범" comes in Step 3). Backend `GET/POST/DELETE/PUT /api/library/to-listen` + `GET /api/library/reviewed`, JWT routes applied, front 라이브러리 tab real. prod e2e verified (to-listen CRUD+reorder via smoke JWT; reviewed GET real data). **Step 3 = listening history + now-playing + Spotify 연동** shipped (cross-repo: shared_db #17/v0.8.0/Neon V9, worker #33, backend #48, front #76, workspace #208): 3rd library source "최근 들은 앨범" (D25/D26) + now-playing, worker/EventBridge-fed (rule #9). `spotify_recent_albums` + `spotify_now_playing`; worker **1h cron** (`{job:"spotify_listening"}`) + manual "지금 새로고침" SQS (`{job:"spotify_refresh"}`); backend `GET /api/library/recently-listened|now-playing|spotify-connection` + `POST /api/library/refresh-recent` (JWT→SQS); front real recent + 새로고침 + honest read-only now-playing + **연동 tab**. Refresh token bootstrapped **out-of-band** (`scripts/spotify_bootstrap_token.py`, phone OAuth → manual code paste) → Secrets `myblog/spotify`; in-app PKCE OAuth deferred (D27). prod smoke green (worker invoke → `spotify_recent_albums` 5 real rows incl. The Bends; `/api/library/recently-listened` 200 real data; `connected:true`). **Post-ship follow-ups** (RFC D28–D31 + review fixes from the 2026-06-04 multi-agent review, workspace PR #210): **D28 shipped 2026-06-04** (workspace #211 / front #77 / backend #49) — now-playing drops `progress_ms`/`duration_ms` (intentionally-public vanity read) + idle response now carries `updated_at`; prod smoke green (idle `{is_playing:false, …, updated_at:…}`, no progress/duration keys; deployed ProfileApp chunk has no `progress_ms`/`duration_ms`). **D31 shipped 2026-06-04** (worker #34 / workspace #213 / backend #50 / front #78) — manual refresh debounced server-side (worker skips Spotify reads when cache `<60s` old, measured DB-side via `GREATEST(synced_at, updated_at)`; 1h cron never debounced) + `last_synced_at` added to recently-listened; front polls it until it advances (replaces blind 4s setTimeout), neutral "이미 최신 상태" on the debounced-timeout path. prod smoke green (debounce e2e: refresh ×2 → only the 1st advanced `synced_at` 06:57→07:21, 2nd skipped). **D29 shipped 2026-06-04** (shared_db #18/v0.9.0/Neon **V10**, worker #35) — append-only `spotify_play_events` table (durable play history for Step 4 listen-counts; the V9 snapshot prunes its rolling window). Worker appends every play in the snapshot tx with `ON CONFLICT (album_id, played_at) DO NOTHING`; worker pin **not** bumped (raw SQL). V10 applied to prod Neon (psql, rule #3). prod smoke green (append SQL ROLLBACK-validated on prod `INSERT 0 1`/`INSERT 0 0`; manual refresh → table 0→50 rows). **D30 shipped 2026-06-04** (worker #36 / workspace #216 contract + #217 infra / backend #51 / front #79) — Spotify token write-back: the worker persists a rotated `refresh_token` + `last_successful_refresh_at` to Secrets `myblog/spotify` and flips `needs_reauth` on a 400 `invalid_grant`, so `spotify-connection` reflects token **validity** (연동 tab shows "재인증 필요" after a revoke), not mere presence. Worker-only `secretsmanager:PutSecretValue` added (`terraform apply`: 2 add/0 change/0 destroy, no drift). prod smoke green (worker cron invoke → secret gained `last_successful_refresh_at`; `/api/library/spotify-connection` → `{connected:true, needs_reauth:false, last_successful_refresh_at:2026-06-04T09:09:33Z}`; deployed ProfileApp chunk ships 연결됨/재인증 필요/연결 안 됨 + last-refresh stamp; `needs_reauth` path unit+browser-verified, live token not revoked). **Follow-up rollout complete 2026-06-04**: retry/backoff + symmetric isolation (worker #37), real-album panel (front #80), cron `FailedInvocations` alarm (workspace #220, `terraform apply`-ed; cache-staleness deferred) — all prod smoke green. **Step 5 in progress**: **5.1 shipped** — `review_buckets.parent_id` (V11) applied to prod Neon + shared_db **v0.10.0** (#19). **5.2–5.5 remain** (backend nested tree + `PUT /buckets/{id}/move` + D22 hard-delete cleanup + pin v0.10.0 → workspace contract → infra apigateway `move` route → front DnD board + retire `/reviews/queue`) — one coupled landing (contract↔front api.gen gate) for a focused next pass. **Step 4** (genre/artist agg) + **Step 6** (multi-user) carved to Backlog RFCs `FEAT-genre-artist-distribution` + `FEAT-multi-user-accounts`. Verify: `pnpm lint` + `astro check` + browser click-through; prod smoke = DB-row evidence for the cache (LOG_LEVEL=WARNING).

---

## Backlog

- **FEAT-genre-taxonomy** (draft) — genre branch-authoring v1 (2-tier single-parent tree, create + assign-parent only; seeded from prod `artists.genres`). RFC → `docs/rfcs/FEAT-genre-taxonomy.md`. v2+ (album↔genre linkage, multi-genre on write, cross-cutting filter, browse-by-genre view, rename/merge/delete) tracked in that RFC's Non-goals. Cross-repo (shared_db V7 → seed → backend API → contract → front). Not yet accepted.
- **FEAT-genre-artist-distribution** (draft) — `/profile` 통계 charts: genre + artist/listen distribution. Carved from `FEAT-member-dashboard` Step 4. RFC → `docs/rfcs/FEAT-genre-artist-distribution.md`. Depends on `FEAT-genre-taxonomy` (genre) + `spotify_play_events` (shipped, listen counts). Not yet scoped/accepted.
- **FEAT-multi-user-accounts** (draft) — user/profile model, public `/members/[handle]`, social graph; un-hides social stats. Carved from `FEAT-member-dashboard` Step 6. RFC → `docs/rfcs/FEAT-multi-user-accounts.md`. Largest follow-on; needs data + auth model. Not yet scoped/accepted.
- **FEAT-header-scroll-prefs** (draft) — site-wide header scroll behavior with three user-selectable modes (hide-on-scroll-down / compact-on-scroll / threshold-hide), default = hide-on-scroll-down. RFC → `docs/rfcs/FEAT-header-scroll-prefs.md`. Frontend-only single repo. Not yet accepted.
- **FEAT-music-search-recall** (draft) — writer-facing music search recall re-assessment; goal Hit@5 ≥ 0.9 / Hit@1 ≥ 0.6 on a 30-query fixture covering multi-token / KO / common-word / typo / alias cases. RFC → `docs/rfcs/FEAT-music-search-recall.md`. Cross-repo (front IME → music fixture+gate → shared_db V11 pg_bigm PoC → music repos + flag → shared_db V12 album/track aliases + worker MB extend → music query decomposition → music ?explain=1). Brainstorm went through 3-agent critique (architect/reviewer/general-purpose) before draft — pg_trgm KO claim rejected, A3 promoted ahead of A2, §4 debug-storage reframed to V10 precedent (`spotify_play_events`). Not yet accepted.

---

## Later (트리거 대기)

_(none — FEAT-view-redesign trigger fired 2026-05-31, promoted to Active.)_

---

## Frozen (아이디어만, RFC 없음)

확장 기능 후보. 의향 생기면 그때 RFC 시작. 지금 아무것도 안 함.

- **FEAT-new-release-feed** (L) — EventBridge 주기 → 본인 review artist 의 최근 앨범 fetch → `artist_new_releases` → `/api/music/feed/new-releases` → 메인 카드
